#include "aion/gameserver/services/abyss/AbyssService.h"

#include "aion/gameserver/runtime/base/Unported.h"

namespace aion::gameserver::services::abyss {

bool AbyssService::shouldAnnounceHighRankedDeath(model::gameobjects::player::Player& victim) {
	AION_UNPORTED();
}

void AbyssService::announceHighRankedDeath(model::gameobjects::player::Player& victim) {
	AION_UNPORTED();
}

void AbyssService::announceAbyssSkillUsage(model::gameobjects::player::Player& player, std::string_view skillL10n) {
	AION_UNPORTED();
}

} // namespace aion::gameserver::services::abyss
