#include "aion/gameserver/model/templates/zone/Sphere.h"

namespace aion::gameserver::model::templates::zone {

Sphere::Sphere(float xValue, float yValue, float zValue, float radius) : x(xValue), y(yValue), z(zValue), r(radius) {
}

} // namespace aion::gameserver::model::templates::zone
