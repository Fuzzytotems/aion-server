#pragma once

// Category prelude of the quest handlers: the only file where a using-directive is allowed (Java: import static DialogAction.*).
#include "regscan_fixture/FakeCore.h"

namespace aion::gameserver::handlers::quest {
using namespace aion::gameserver::model::DialogAction;
} // namespace aion::gameserver::handlers::quest
