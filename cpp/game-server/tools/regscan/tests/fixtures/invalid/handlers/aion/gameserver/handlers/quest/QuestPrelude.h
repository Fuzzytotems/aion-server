#pragma once

// invalid: the quest prelude may only hold the DialogAction using-directive
namespace aion::gameserver::handlers::quest {
using namespace std;
} // namespace aion::gameserver::handlers::quest
