// GENERATED by tools/xmlgen from model/templates/zone/ZoneTemplate.java - do not edit (regenerate with: python cpp/tools/xmlgen/xmlgen.py generate)

// XmlBinding definitions of com.aionemu.gameserver.model.templates.zone.ZoneTemplate; compiled through the package translation unit zone.bind.cpp.

#include <memory>
#include <string_view>

#include "aion/gameserver/model/templates/zone/Cylinder.bind.h"
#include "aion/gameserver/model/templates/zone/Points.bind.h"
#include "aion/gameserver/model/templates/zone/Semisphere.bind.h"
#include "aion/gameserver/model/templates/zone/Sphere.bind.h"
#include "aion/gameserver/model/templates/zone/ZoneTemplate.bind.h"

namespace aion::gameserver::xml {

bool XmlBinding<::aion::gameserver::model::templates::zone::ZoneTemplate>::attribute(::aion::gameserver::model::templates::zone::ZoneTemplate& o, BindContext& c, std::string_view name, std::string_view value) {
	switch (nameHash(name)) {
		case "flags"_xh:
			if (name == "flags") {
				c.assign(o.flags, value);
				return true;
			}
			break;
		case "priority"_xh:
			if (name == "priority") {
				c.assign(o.priority, value);
				return true;
			}
			break;
		case "mapid"_xh:
			if (name == "mapid") {
				c.assign(o.mapid, value);
				return true;
			}
			break;
		case "siege_id"_xh:
			if (name == "siege_id") {
				c.assignList(o.siegeId, value);
				return true;
			}
			break;
		case "town_id"_xh:
			if (name == "town_id") {
				c.assign(o.townId, value);
				return true;
			}
			break;
		case "area_type"_xh:
			if (name == "area_type") {
				c.assign(o.areaType, value);
				return true;
			}
			break;
		case "zone_type"_xh:
			if (name == "zone_type") {
				c.assign(o.zoneType, value);
				return true;
			}
			break;
		case "name"_xh:
			if (name == "name") {
				o.setXmlName(value);
				return true;
			}
			break;
	}
	return false;
}

bool XmlBinding<::aion::gameserver::model::templates::zone::ZoneTemplate>::element(::aion::gameserver::model::templates::zone::ZoneTemplate& o, BindContext& c, pugi::xml_node e, std::string_view name) {
	switch (nameHash(name)) {
		case "points"_xh:
			if (name == "points") {
				c.bindSingle(o.points, e);
				return true;
			}
			break;
		case "cylinder"_xh:
			if (name == "cylinder") {
				c.bindSingle(o.cylinder, e);
				return true;
			}
			break;
		case "sphere"_xh:
			if (name == "sphere") {
				c.bindSingle(o.sphere, e);
				return true;
			}
			break;
		case "semisphere"_xh:
			if (name == "semisphere") {
				c.bindSingle(o.semisphere, e);
				return true;
			}
			break;
	}
	return false;
}

} // namespace aion::gameserver::xml
