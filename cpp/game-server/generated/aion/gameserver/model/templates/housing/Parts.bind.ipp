// GENERATED by tools/xmlgen from model/templates/housing/Parts.java - do not edit (regenerate with: python cpp/tools/xmlgen/xmlgen.py generate)

// XmlBinding definitions of com.aionemu.gameserver.model.templates.housing.Parts; compiled through the package translation unit housing.bind.cpp.

#include <memory>
#include <string_view>

#include "aion/gameserver/model/templates/housing/Parts.bind.h"

namespace aion::gameserver::xml {

bool XmlBinding<::aion::gameserver::model::templates::housing::Parts>::element(::aion::gameserver::model::templates::housing::Parts& o, BindContext& c, pugi::xml_node e, std::string_view name) {
	switch (nameHash(name)) {
		case "fence"_xh:
			if (name == "fence") {
				c.bindText(o.fence, e);
				return true;
			}
			break;
		case "garden"_xh:
			if (name == "garden") {
				c.bindText(o.garden, e);
				return true;
			}
			break;
		case "frame"_xh:
			if (name == "frame") {
				c.bindText(o.frame, e);
				return true;
			}
			break;
		case "outwall"_xh:
			if (name == "outwall") {
				c.bindText(o.outwall, e);
				return true;
			}
			break;
		case "roof"_xh:
			if (name == "roof") {
				c.bindText(o.roof, e);
				return true;
			}
			break;
		case "infloor"_xh:
			if (name == "infloor") {
				c.bindText(o.infloor, e);
				return true;
			}
			break;
		case "inwall"_xh:
			if (name == "inwall") {
				c.bindText(o.inwall, e);
				return true;
			}
			break;
		case "door"_xh:
			if (name == "door") {
				c.bindText(o.door, e);
				return true;
			}
			break;
	}
	return false;
}

} // namespace aion::gameserver::xml
