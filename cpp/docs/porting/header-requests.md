# Header requests after the spine freeze

The integrator's log of header requests (docs/design/hub-headers.md §14): every request a chunk filed against a frozen header, a header of
another chunk, the chunk manifest or the concurrency decisions, with the decision and its reason. Approved changes are applied by the integrator
in all affected files (headers, stubs, the requesting lanes' workarounds, call sites, `fieldmap.toml`, `chunks.cmake`) in one batch.

Decisions: **approved** (applied as requested), **approved (modified)** (applied in a different or smaller form, the reason says how),
**rejected** (not applied; the reason says what happens instead).

## Wave 3a-1 (2026-09-14)

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| base-1 | `model/geometry/Area.h` (P4-05) | `getClosestPoint(const Point2D*)` and `getClosestPoint(float, float)` return `std::optional<templates::zone::Point2D>` instead of `const Point2D*` | approved | Java returns a new point per call (AbstractArea.java:81-83, CylinderArea.java:98/105, RectangleArea.java:156/186, PolyArea.java:92-121) and null for SphereArea (SphereArea.java:93-101); a `const Point2D*` cannot own a new point. Point2D is a copyable value, so hub-headers.md §5 (K5 row: `std::optional<X>` where Java may return null) applies. No caller outside model.geometry in Java or C++. Applied to AbstractArea, CylinderArea, RectangleArea, PolyArea (the overrides return the C++-only `getClosestPoint2D`) and SphereArea (`std::nullopt`); the four `AION_UNPORTED` overrides are ported; GeometryTest covers them |
| base-2 | `model/geometry/Area.h` (P4-05) | `getClosestPoint(Point3D&)` and `getClosestPoint(float, float, float)` return `runtime::Ref<Point3D>` instead of `runtime::Ptr<Point3D>` | approved | AbstractArea.java returns `new Point3D(...)`: a factory result, and hub-headers.md §5 forbids a `Ptr` to an object nobody holds. Applied to AbstractArea and SphereArea; the RectangleArea/PolyArea `getDistance3D` locals and GeometryTest hold the `Ref` |
| base-3 | `model/templates/zone/Point2D.h` (P4-07a) | add `Point2D()` and `Point2D(float x, float y)` | rejected | Already done: P4-07a added both constructors in its own chunk this wave (`Point2D.h`, `Point2D.cpp`). The follow-up is applied: `model/geometry/Point2DFactory.h` `makePoint2D` returns `templates::zone::Point2D(x, y)` instead of the local subclass workaround |
| templates-1 | `model/templates/stats/StatsTemplate.h` (P4-07a) | `getWalkSpeed`, `getRunSpeed`, `getFlySpeed`, `getPower`, `getHealth`, `getAgility`, `getBaseAccuracy`, `getKnowledge`, `getWill` become `virtual`; add `virtual ~StatsTemplate() = default;` | approved | Layout change (the class gains a vtable). Java PlayerClass.PlayerStatsTemplate (PlayerClass.java:200-245) overrides exactly these getters, and PlayerGameStats reads them through a `const StatsTemplate*`; non-virtual getters would give players 100 for every attribute and 0 speeds. The destructor is virtual unconditionally: the per-level PlayerStatsTemplate will be owned through a StatsTemplate handle. The other getters are not overridden in Java and stay non-virtual. The template is held by `std::unique_ptr` in NpcTemplate, so no binder copies or moves it |
| objects-1 | `model/gameobjects/Trap.h` (P4-11a) | add `std::optional<std::string> getMasterName() override;` (C++ only) | approved | Java Trap.java:18 stores `""`, so SummonedObject.getMasterName (SummonedObject.java:47-49) returns `""`; Npc's `Field<std::string>` reads `""` as null, and SummonedObject::getMasterName would substitute the creator's name, which SM_NPC_INFO then sends. Same override as Homing, Servant and SummonedHouseNpc; body `Npc::getMasterName().value_or(std::string())` |
| player-1 | `world/knownlist/KnownList.h` (P4-10) | add `bool clearWithoutNotify();` (C++-only zombie breaker, returns true if the list was not empty) | approved | `KnownList.knownObjects` is a zombie-safe edge (cycles.toml, runtime-architecture.md §5.3) and the header has only `clear(ObjectDeleteAnimation)`, which sends packets. Additive; `AION_UNPORTED` stub for P4-10. `LogoutBreakers::breakZombieEdges` calls it (skipping objects without a known list) and reports `knownObjects` |
| player-2 | `controllers/ObserveController.h` (P4-11b) | add `bool hasObservers();` (C++ only: observer or attack-calc observer list not empty) | approved (modified) | Needed so the zombie breaker reports only cut edges. Declared non-const (hub-headers.md §9.1: methods of runtime-based classes are non-const). Additive; `AION_UNPORTED` stub for P4-11b. `breakZombieEdges` clears and reports `observers` (both lists) only when it returns true. P4-11b should take the query and the clear under the same monitor when it ports both bodies |
| player-3 | `model/items/storage/PlayerStorage.h` (P4-13) | add `runtime::Ptr<gameobjects::player::Player> getActor() const { return actor.get(); }` (C++ only) | approved | Inline `SelfOrRef` getter (hub-headers.md §3.3). `breakZombieEdges` now resets the inventory and warehouse actors silently (the owning player acts for them) and resets and reports `storageActor` for the shared account warehouse only while the zombie is its actor, so another player of the account keeps its actor |
| player-4 | `dataholders/PlayerExperienceTable.h` (P4-09) | add `int64_t getStartExpForLevel(int32_t) const; int32_t getLevelForExp(int64_t) const; int32_t getMaxLevel() const;` | approved | Java PlayerExperienceTable.java:28-58; every exp and level body of PlayerCommonData uses them and P4-09 is a later wave. Additive; `AION_UNPORTED` stubs in the new `PlayerExperienceTable.cpp`. PlayerCommonData's three local stand-ins are replaced by calls through `DataManager::PLAYER_EXPERIENCE_TABLE` |
| player-5 | `dataholders/{TitleData,NpcFactionsData,InstanceCooltimeData,PetData,PetFeedData,AtreianPassportData,TribeRelationsData}.h` (P4-09) | add `TitleData::getTitleTemplate`, `NpcFactionsData::getNpcFactionById`/`getNpcFactionByNpcId`, `InstanceCooltimeData::getInstanceMaxCountByWorldId`/`calculateInstanceEntranceCooltime`, `PetData::getPetTemplate`, `PetFeedData::getFlavourById`, `AtreianPassportData::getAtreianPassportId`, `TribeRelationsData::getBaseTribe` (const, template pointers and values as requested) | approved | Public Java lookups (TitleData.java:32, NpcFactionsData.java:40/44, InstanceCooltimeData.java:57/70, PetData.java:46, PetFeedData.java:43, AtreianPassportData.java:44, TribeRelationsData.java:37) missing from the shells. Additive; `AION_UNPORTED` stubs. Callers updated: Player::getBaseTribe (local stand-in removed); ported because the lookup was their only blocker: TitleList::addEntry, NpcFaction's constructor, PortalCooldownList::isPortalUseDisabled and getOrCreatePortalCooldown(worldId), Passport::getTemplate. PetCommonData's constructor, the NpcFactions dialog bodies and TitleList::addTitle stay unported (they need more than the lookups) |
| player-6 | `model/templates/factions/NpcFactionTemplate.h`, `model/templates/QuestTemplate.h` (P4-07b) | add `NpcFactionTemplate::isMentor() const`, `getMinLevel() const`; `QuestTemplate::isMentor() const` | approved | Java NpcFactionTemplate.java:56/68, QuestTemplate.java:321; hub-headers.md §9.1 declares every Java method. Trivial bodies inline; `getMinLevel` throws NullPointerException for a null `min_level` like Java's unboxing. NpcFactions::startQuest, completeQuest and canStartQuest are ported (their only blocker) |
| player-7 | `skillengine/effect/WeaponDualEffect.h` (P5-04), `model/templates/item/actions/EmotionLearnAction.h` (P5-07) | add `static bool WeaponDualEffect::hasDualWieldEffect(Player&)` and `static bool EmotionLearnAction::isLearnable(int32_t)` | approved | Public static Java helpers (WeaponDualEffect.java:36, EmotionLearnAction.java:73) that read other chunks' state. Additive; `AION_UNPORTED` stubs (new `WeaponDualEffect.cpp`). Equipment's local stand-in is removed; EmotionList::canUse is ported |
| network-1 | `GameServer.h` (P5-14, new) | new declaration header with `static bool isShutdownScheduled()` and `isShuttingDownSoon()` | rejected | No frozen header changes: GameServer.h is a new file of P5-14 (`aion_gs_app`, not part of `aion_gs_core`, so the network library and its tests could not link it), and its bodies need the ShutdownHook of P5-14. Until then no shutdown can be scheduled in the C++ server, so the local stand-ins returning false are exact. Markers retagged `TODO(P5-14)` in AionConnection.cpp, LoginServerConnection.cpp and ChatServerConnection.cpp |
| network-2 | `network/aion/clientpackets/CM_PING.h` (P5-00, new) | new declaration header with `static constexpr int32_t CLIENT_PING_INTERVAL = 180 * 1000;` | rejected | A new file of P5-00, which writes the CM_PING declaration header with its port (a header holding only the constant would not be the class's declaration header). The local constant in AionConnection.cpp has Java's value; marker retagged `TODO(P5-00)` |
| network-3 | `dataholders/ItemRestrictionCleanupData.h` (P4-09) | add `int32_t size() const; const std::vector<ItemCleanupTemplate>& getList() const; bool hasAccountOrLegionWhStorabilityDisabled(int32_t) const;` | approved | Java ItemRestrictionCleanupData.java:23-33; GeneralInfoBlobEntry.writeThisBlob calls the last one for every item info blob. Additive; `AION_UNPORTED` stubs in the new `ItemRestrictionCleanupData.cpp`. The local stand-in in GeneralInfoBlobEntry.cpp is replaced by the call through `DataManager::ITEM_CLEAN_UP` |
| network-4 | `game-server/chunks.cmake` (P4-15) | add `TEST_SUPPORT support` to P4-15 and move `FakeGameClient.h` and `NetworkTestSupport.h` from `tests/network/support` to `tests/support` | approved (modified) | handlers-and-porting-plan.md §2.6 lists `tests/support/FakeGameClient` as a P4-15 deliverable and §2.2 requires an owner through `TEST_SUPPORT`. A test include directory cannot name the `tests` root, so P4-15 also gets `TEST_INCLUDES support` and the headers are included by name (`"FakeGameClient.h"`, `"NetworkTestSupport.h"`); later chunks' tests add the same `TEST_INCLUDES support`. `GameServerTestServer.h` stays in `tests/network/support` |
| network-5 | `game-server` CMake: `aion_gs_network_tests` | link `aion_loginserver_lib` and add the login server test includes | rejected | Not needed by any test yet, and not safe as requested: commons `DatabaseFactory` is one process-wide pool, so an in-process login server (`aion_ls` schema) and game server (`aion_gs` schema) cannot share a test process as the libraries stand. The in-process LS link belongs with the M5a scenario harness (P5-00), which has to solve the two pools (a second process or per-server pools); LoginServerLinkTest keeps its byte-protocol fake LS |
| network-6 | `game-server/generated/concurrency/fieldmap.toml` | turn the network lane's `// fieldmap:` waivers into decisions | approved (modified) | hub-headers.md §4: waivers are the interim form. Added `[kinds]` K5 for the three `PacketInfo` table entries (so their `// confined:` waivers go too), `[fields]` `BannedMacEntry.timeEnd` `Field<std::optional<Timestamp>>`, `LoginServer.nioServer`/`ChatServer.nioServer` `Field<NioServer*>`, `LoginServerConnection.sendMsgQueue`/`ChatServerConnection.sendMsgQueue` dropped, `EnchantInfoBlobEntry.SIZE` `static constexpr int32_t`, `ItemBlobEntry.type` `const ItemInfoBlob_ItemBlobType`, the `PacketInfo.packetConstructor` fields dropped and their `validStates` spelled `StateSet`/`uint32_t`, and `[cpp_members]` `LoginServerConnection.packetExecutor`/`ChatServerConnection.packetExecutor` `SerialExecutor`. Lint L1 did not accept `[cpp_members]` spellings as decisions (only `[fields]` overrides), although hub-headers.md §4 says it does; `lint_concurrency.py` now accepts an exactly spelled `[cpp_members]` entry for L1 (test in `tools/porting/tests/test_lint_rules.py`). The headers carry `// fieldmap.toml:` notes; `fieldmap.json` and `escape_report.md` are regenerated |

## Wave 3a-2, pre-stage (2026-09-14)

The four requests wave 3a-1 left open (`build/workflows/w3a1-followups.json`, all from the base/templates fixer).

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| pre-1 | `dataholders/SkillData.h` (P4-09) | add `std::vector<const skillengine::model::SkillTemplate*> getSkillTemplates() const;` and `const std::vector<const skillengine::model::SkillTemplate*>* getSkillTemplatesByGroup(std::string_view skillGroup) const;` (nullptr for Java null) | approved | Public Java lookups (SkillData.java:55/72). Additive; the GMService constructor is the M4 login blocker (every `getInstance()` threw). No lane works on P4-09 in this wave, so the bodies are ported instead of stubbed: `afterUnmarshal` builds the id, group and stack indexes into the bound storage (private members, as `QuestsData`) and the values in Java's `HashMap<Integer, ...>` iteration order (a model of putVal/resize/treeifyBin, docs/deviations/P4-09.md); tests in `tests/dataholders`. Callers ported: `GMService::GMService` (the stand-in comment and the `AION_UNPORTED` are gone; `getInstance()` now throws only while `SKILL_DATA` is unpublished) and `Stigma::getGainSkillsByGroup`. Staff logins still throw in `GMService::onPlayerLogin` while `LOGIN_EXECUTE_COMMANDS` is non-empty (P5-14 `ChatProcessor`) |
| pre-2 | `dataholders/{NpcData,ItemSetData,ItemGroupsData,WorldMapsData}.h` (P4-09) | add `NpcData::getNpcTemplate(int32_t) const`, `ItemSetData::getItemSetTemplateByItemId(int32_t) const`, `ItemGroupsData::isFood(int32_t, model::templates::pet::FoodType) const`, `WorldMapsData::getTemplate(int32_t) const` (template pointers, nullptr for Java null) | approved | Public Java lookups (NpcData.java:99, ItemSetData.java:55, ItemGroupsData.java:246, WorldMapsData.java:50). Additive. Bodies ported with private index members: `ItemSetData` and `WorldMapsData` including their `afterUnmarshal` (index-only hooks); `NpcData::getNpcTemplate` and `ItemGroupsData::isFood` over indexes whose hooks stay unported (NpcData.init needs NpcStatCalculation of P5-01 and mutates templates; ItemGroupsData's hook builds the bonus group lists and clears the feed groups), so their lookups see empty maps like Java before loading (`isFood` throws NullPointerException). `ItemGroupsData.h` includes the generated `pet/FoodType.h` (hub-headers.md §3.1 allows generated enum headers). Callers ported: `ChatUtil::path(int, bool)`, `ItemTemplate::getItemSet`, `PetFlavour::getFoodType`, `WorldZoneTemplate(size, mapId)` (delegates to the three-argument constructor; an unknown map throws NullPointerException). `MaterialZoneTemplate`'s constructor stays unported (BoundingBox, P4-04, this wave's geo lane) |
| pre-3 | `game-server/generated/concurrency/fieldmap.toml` | `[fields]` `SpawnSearchResult.spot` = `SpawnSpotTemplate` (owned value instead of the proposed `const SpawnSpotTemplate*`) | approved | Java's only construction site (SpawnsData.toSpawnSearchResult) wraps a new SpawnSpotTemplate built from a run-time SpawnTemplate's current coordinates; a template pointer would dangle or carry the load-time coordinates. The class is K5, so no cycle edge. Applied: the decision, `SpawnSearchResult.h`'s `// fieldmap:` waiver turned into a `// fieldmap.toml:` note, `fieldmap.json` regenerated (`escape_report.md` unchanged) |
| pre-4 | `commons/src/aion/commons/utils/StringUtils.h` (commons); `network/PacketWriteHelper.cpp`, `network/aion/AionServerPacket.cpp` (P4-15) | add `std::string toWtf8(std::u16string_view)` and `std::u16string wtf8ToUtf16(std::string_view)`; the server packet string writers decode with `wtf8ToUtf16` | approved (modified) | Java `ChatUtil.l10n` puts two raw UTF-16 units into the String and `writeS` writes them unchanged; for ids with `id % 32768` in 27648..28671 the UTF-8 path sent U+FFFD. `toUtf16`/`toUtf8` stay Java-strict; the pair is additive (a `decodeUtf8` flag keeps one decoder). Wider than requested: `AionServerPacket::writeS(text)` and `writeS(text, fixedLength)` decode themselves (the first called commons `BaseServerPacket::writeS`, which stays strict, so the login server is unchanged), and the producers and re-encoders of l10n strings use WTF-8: `ChatUtil::l10n`, `ChatUtil::split` (decode and parts), the stand-in `network::detail::l10n` and `SM_SYSTEM_MESSAGE::toJavaString(char16_t)`. `byteLengthForString` needed no change (`utf16Length` counts an encoded surrogate as one unit either way). Tests: commons `StringUtilsTest.Wtf8KeepsLoneSurrogates`, `AionServerPacketTest.LoneSurrogatesAreWrittenUnchanged`, `PacketWriteHelperTest`, `ChatUtilTest` (l10n, split), `SystemMessageL10nTest`; the P4-05 and P4-06 deviation rows are rewritten. Not changed: client packet `readS` still replaces lone surrogates from the client with U+FFFD (open issue) |

## Wave 3a-2 (2026-09-15)

The 17 requests of the four lanes (geo P4-04, templates-b P4-07b, shells P4-08, items P4-13). No lane works on P4-09 in this wave, so the approved
holder lookups are ported, not stubbed (as in the pre-stage): each holder's `afterUnmarshal` builds the index the lookup needs into the bound
storage; the other indexes of those hooks come with the P4-09 port. Tests: `tests/dataholders/HolderLookupsTest.cpp` (8 new tests).

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| geo-1 | `dataholders/MaterialData.h` (P4-09) | add `const model::templates::materials::MaterialTemplate* getTemplate(int32_t materialId) const;` (nullptr for Java null) | approved | Public Java lookup (MaterialData.java:50) used by `GeoWorldLoader.loadMeshes` (GeoWorldLoader.java:146) and later `ZoneService.createMaterialZoneTemplate`. Additive. `afterUnmarshal` builds `materialsById` (Java's `skillIds`, `isMaterialSkill` and `size` are left to P4-09). Caller: `GeoWorldLoader.cpp hasMaterialTemplate` calls `DataManager::MATERIAL_DATA->getTemplate` (NullPointerException while the material data is unpublished, like Java); `GeoWorldLoaderFilesTest` and `GeoRealDataTest` publish `MATERIAL_DATA` (the real test binds `mesh_materials/material_templates.xml`); the P4-04 deviation row "Model node names" is removed |
| geo-2 | `generated/concurrency/fieldmap.toml` `[fields]` | `IgnoreProperties.ELYOS`/`ASMODIANS`/`BALAUR`/`ANY_RACE` = `static const Ref<IgnoreProperties>&`; `IgnoreProperties.race` = `const std::optional<Race>` | approved | hub-headers.md §11.1 (a static final RefCounted constant is a never-released `Ref` bound in the `.cpp`, precedent `PanesterraTeam.ELYOS_ORIGIN_POS`; `DespawnableNode.collideWith` compares identities) and §6 (Java stores null for `ANY_RACE` and `of(staticId)`, precedent `Announcement.faction`). The `// fieldmap:` waivers in `IgnoreProperties.h` became `// fieldmap.toml:` notes; `fieldmap.json` and `escape_report.md` regenerated |
| geo-3 | `generated/concurrency/fieldmap.toml` `[kinds]`, `[fields]` | `[kinds]` K5 for `TempVars`, `TempVars.TempVarsStack`, `BIHNode.BIHStackData`; `[fields]` spellings `TempVars.varsLocal` `static thread_local TempVarsStack`, `TempVarsStack.tempVars` `std::array<std::unique_ptr<TempVars>, STACK_SIZE>`, the five float arrays `std::array<float, 3>`, `bihSwapTmp`/`bihSwapTmpShort` `std::array<int8_t, 3>`/`<int16_t, 3>`, `BIHStackData.node` `const Ref<BIHNode>`, `min`/`max` `const float` | approved | A Java `ThreadLocal` pool (runtime-architecture.md §3.2 `ThreadLocal<T>` → `thread_local T`): the thread-local stack is the only holder, which the escape analysis does not treat as confinement, and a RefCounted stack entry per BIH push would put every ray query on the Reclaimer. The confined members fieldmap then prints (`Vector3f`, `bool`, `std::vector<BIHNode::BIHStackData>`) match `TempVars.h`; the overrides record the other spellings. `BIHStackData.node` stays a `Ref` because the thread-local stack outlives a task (a `Ptr` must not). The 19 `// fieldmap:` waivers in `TempVars.h` and the `// confined:` comment in `BIHNode.h` became notes; `BIHNode` stays K4 (member of `BIHTree.root`) |
| templates-b-1 | `dataholders/ItemData.h` (P4-09) | add `const model::templates::item::ItemTemplate* getItemTemplate(int32_t itemId) const;` | approved | Public Java lookup (ItemData.java:88, `items.get(itemId)`); the same declaration as items-1 (one change). Additive. `afterUnmarshal` builds `items` (the manastone lists, `cleanup`, `getItemTemplates`, `size` are left to P4-09). Callers: `ItemRaceEntry::matches` (`publishedItemTemplate`: NullPointerException for an unknown id), `XMLStartCondition::checkEquippedItems` ported (the l10n of the missing item in `STR_QUEST_ACQUIRE_ERROR_EQUIP_ITEM`, returns false). The `GlobalDropItem`, `ItemRaceEntry` and `ResultedItem` hooks keep their XmlID lookup (in a full load the same templates; the published fallback only matters for //reload, D3); their comments and the P4-07a/P4-07b deviation reasons are updated |
| templates-b-2 | `dataholders/HouseBuildingData.h` (P4-09) | add `const model::templates::housing::Building* getBuilding(int32_t buildingId) const;` | approved | Public Java lookup (HouseBuildingData.java:40). Additive. `afterUnmarshal` builds `buildingById`; a duplicate id fails the load with Java's message (`LoadContext::fail`, P4-09 deviation row). Caller: `Building.cpp holderBuilding` (NullPointerException for an id without a building, as Java's field access on null) |
| templates-b-3 | `dataholders/WalkerVersionsData.h` (P4-09) | add `std::optional<std::string> getRouteVersionId(std::string_view routeId) const;` | approved | Public Java lookup (WalkerVersionsData.java:44); `InstanceWalkerFormations` compares the result with null, so `std::optional<std::string>`. The parameter stays `std::string_view` although Java null-checks it (hub-headers.md §6 would make it optional): the only caller passes the required `route_id` (class comment). `isRouteVersioned` is not added (no caller yet). `afterUnmarshal` fills `walkParents` and clears the bound list like Java. Caller: `WalkerTemplate::getVersionId` ported |
| shells-1 | `dataholders/SkillData.h` (P4-09) | add `const skillengine::model::SkillTemplate* getSkillTemplate(int32_t) const;`, `const std::vector<const skillengine::model::SkillTemplate*>* getSkillTemplatesByStack(std::string_view) const;`, `int32_t size() const;` | approved | Public Java lookups (SkillData.java:48/64/68) over the indexes pre-1 already builds. Additive, bodies ported. The linear searches in `SkillTemplate.cpp` (`skillTemplateById`) and `SkillLearnTemplate::getSkillLevel` are replaced and their P4-08 deviation row removed. `Creature::canUseSkillInMove` keeps reading the casting skill's own template (the same object; the objects tests publish no `SKILL_DATA`); its P4-11a deviation reason is updated |
| shells-2 | `skillengine/effect/{DamageEffect,BufEffect,AbstractOverTimeEffect,ShieldEffect,AbstractHealEffect}.h` (P5-03/P5-04 shells, lease P4-08), their overriders, new `HealEffectTemplate.h` (P5-03) | the effect behaviour virtuals overridden across the A-L/M-Z split | approved (modified) | Layout change (new vtable entries) that spine-status.md "Freeze exceptions" assigns to P4-08. Applied: `DamageEffect` `resolveMagicalCritical`/`calculateDamage` overrides and the five `virtual` `should*` hooks (bodies ported: `true`, `shouldApplyMagicalSkillBoostBonus` reads the skill template), with `false` overrides in `DispelBuffCounterAtkEffect` (4), `NoReduceSpellATKInstantEffect` (5), `ProcAtkInstantEffect` (3), `SignetBurstEffect` (2); `BufEffect::startEffect` override, `protected virtual std::vector<runtime::Ref<IStatFunction>> getModifiers(model::Effect&) const` (namespace `model::stats::calc::functions`, not `stats::functions` as requested) with `using EffectTemplate::getModifiers;` for the generated getter, overridden by `AbstractAbsoluteStatEffect`; `ShieldEffect::startEffect` override and `virtual model::ShieldType getType() const` (`NORMAL`), overridden with Java's constants in `ConvertHealEffect`, `MPShieldEffect`, `ProtectEffect`, `ReflectorEffect`. Modified: `AbstractOverTimeEffect::startEffect(model::Effect&, std::optional<AbnormalState>)` and `endEffect(model::Effect&, std::optional<AbnormalState>)` are non-virtual (no Java class overrides them: `HealOverTimeEffect.startEffect(Effect, HealType)` is another overload), with `using EffectTemplate::endEffect;`; `startEffect(Effect&)` delegates with `std::nullopt` (ported). `HealEffectTemplate` is a new interface (hub-headers.md §9.2: const pure `isPercent`, `allowHpHealBoost`, `allowHpHealSkillDeboost`, `getCurrentStatValue`, `getMaxStatValue`, `calculateBaseHealValue`; virtual defaults `calculateSnapshotHealValue`, `applyHealDeboost`, `calculateHealValue` stubbed in `HealEffectTemplate.cpp`) implemented by `AbstractHealEffect` and, wider than requested, by `HealOverTimeEffect` (Java implements it there too; adding the base later would be a second layout change). Their Java overrides are declared (`isPercent`, `allowHp*`, `calculateBaseHealValue` ported as the Java one-liners; `AbstractHealEffect::calculateHealValue` stubbed), and all 13 concrete heal shells declare `getCurrentStatValue`/`getMaxStatValue` (stubs), `CaseHealEffect` and `ProcHealInstantEffect` their `allowHp*` `false` overrides. Not in this batch: override declarations of the virtuals `EffectTemplate` already declares (`startEffect`, `endEffect`, `calculate`, `onPeriodicAction`, ...) in the other effect shells (open issue) |
| shells-3 | `skillengine/{condition/Condition,action/Action,periodicaction/PeriodicAction,effect/modifier/ActionModifier}.h` and subclasses (P5-02 shells, lease P4-08) | the Java abstract/virtual behaviour methods | approved (modified) | Layout change: Java abstract methods (Condition.java:26-44, Action.java:22-29, PeriodicAction.java:10, ActionModifier.java:32-40) that no shell declared, called across chunks (Skill/SkillEngine P5-02, `EffectTemplate::getActionModifiers` P5-03). Applied: `Condition` `validate(model::Skill&) const = 0`, `canValidate`, `validate(Stat2&, IStatFunction&)`, `validate(model::Effect&)` (the defaults `return true` ported); `Action` `act = 0`, `canAct` (`true`); `PeriodicAction` `act(model::Effect&) = 0`; `ActionModifier` `analyze`/`check = 0`. Override declarations with stubs in the 24 concrete conditions (in Java order, each with `using <Base>::validate;` against name hiding; `ItemChargeCondition::validate(Skill)` `false` and `SkillChargeCondition::validate(Skill)` `true` ported), 4 actions, 2 periodic actions and 5 modifiers (new `.cpp` files). Modified: no `StatCondition` base, because no `StatCondition.h` exists (P5-01) and `Condition` is its only implementor, which nobody holds by the interface (P4-08 deviation row); `IStatFunction` is `model::stats::calc::functions::IStatFunction` |
| shells-4 | `questEngine/handlers/models/{XMLQuest,MonsterHuntData,xmlQuest/conditions/QuestCondition,xmlQuest/events/QuestEvent,xmlQuest/operations/QuestOperation}.h` and subclasses (P5-06 shells, lease P4-08) | the XML quest behaviour virtuals | approved (modified) | Layout change: Java abstract `XMLQuest.register`/`getAlternativeNpcs`, `QuestCondition.doCheck`, `QuestOperation.doOperate` and the virtual `QuestEvent.operate`; handlers-and-porting-plan.md §1 names `XMLQuest::register_(QuestEngine&)` as the registration of the 4,184 XML quests. Applied: `XMLQuest` `register_(QuestEngine&) const = 0` and `std::optional<std::unordered_set<int32_t>> getAlternativeNpcs(int32_t) const = 0` (`QuestSpawnAnalyzer` only tests membership, so the unordered set's order is not observable), overridden in the 15 concrete XML quest shells and `MonsterHuntData` (`MentorMonsterHuntData` overrides only `register_`, like Java; `CraftingRewardsData`/`ItemOrdersData::getAlternativeNpcs` return `std::nullopt`, ported); `QuestCondition::doCheck = 0` (5 overriders), `QuestEvent::operate` (`false`, ported; overridden by `OnKillEvent`, `OnTalkEvent`), `QuestOperation::doOperate = 0`. Modified: 8 operation overriders, not 9: Java's `KillOperation` has no shell (it is not in `QuestOperation`'s `@XmlSeeAlso`, so the binder never creates it) |
| shells-5 | `game-server/chunks.cmake` | a test directory P4-08 may write during phase 4 | approved | The shell tests must link the libraries the shells compile into (`aion_gs_skills_tests`, `aion_gs_quest_tests`). Added a second lease part `aion_gs_chunk(P4-08 LEASE PHASE 4 TEST_SUPPORT skills quest)` (the pattern of the P4-02b lease): `chunks.py owner` names P5-02/P5-06 "leased to P4-08", and `check-ownership P4-08` accepts the four test files. 75 parts |
| items-1 | `dataholders/ItemData.h` (P4-09) | add `getItemTemplate(int32_t) const` | approved | See templates-b-1 (the same declaration). Callers: `model/items/detail/StaticDataLookups.cpp getItemTemplate` evaluates `DataManager::ITEM_DATA->getItemTemplate` when no test lookup is installed (was `AION_UNPORTED`), and the P4-11a stand-ins `findItemTemplate` in `Item.cpp` (the DAO constructor, `addGodStone`; the C4702 pragma is gone) and `UseableItemObject.cpp` |
| items-2 | `dataholders/ItemRandomBonusData.h` (P4-09) | add `const model::templates::stats::ModifiersTemplate* getTemplate(StatBonusType, int32_t statBonusSetId, int32_t statBonusId) const;` | approved | Public Java lookup (ItemRandomBonusData.java:82). Additive. `afterUnmarshal` builds `bonusData` (P4-09 deviation row: a `std::map` without Java's pre-created empty maps); `statBonusId` is 1-based and throws `IndexOutOfBoundsException` like `List.get`. Caller: `StaticDataLookups.cpp getRandomBonusTemplate` |
| items-3 | `model/templates/item/actions/ItemActions.h` (P5-07) | add `const CraftLearnAction* getCraftLearnAction() const;` and `const PolishAction* getPolishAction() const;` | approved | Public Java lookups (ItemActions.java:62/112: the first action of that type). Additive, bodies ported in the new `ItemActions.cpp` (`dynamic_cast` for `instanceof`); the header includes `item/actions/fwd.h`. The other typed lookups stay with P5-07. Callers: `StaticDataLookups.cpp getPolishAction`/`getCraftLearnAction` |
| items-4 | `dataholders/TemperingData.h` (P4-09) | add `const std::unordered_map<int32_t, const std::vector<model::enchants::TemperingStat>*>* getTemplates(const model::templates::item::ItemTemplate*) const;` | approved | Public Java lookup (TemperingData.java:36): by tempering name, else by `ItemGroup.toString()` (the constant name); nullptr for Java null, NullPointerException for a null template. Additive. `afterUnmarshal` builds the maps (the stat lists point into the bound storage); the header includes `templates/item/fwd.h`. Caller: `StaticDataLookups.cpp getTemperingTemplates` |
| items-5 | `dataholders/RecipeData.h` (P4-09) | add `const model::templates::recipe::RecipeTemplate* getRecipeTemplateById(int32_t id) const;` | approved | Public Java lookup (RecipeData.java:47). Additive. `afterUnmarshal` builds `recipeData` (the auto-learn list is left to P4-09). Caller: `StaticDataLookups.cpp getRecipeTemplateById` |
| items-6 | `model/templates/recipe/RecipeTemplate.h` (P4-07b) | add `int32_t getSkillId() const { return skillid; }` | rejected | Already done: P4-07b declared `getSkillId()` in its own chunk this wave (`RecipeTemplate.h:35`). The follow-up is applied: `StaticDataLookups.cpp getRecipeSkillId` returns `recipeTemplate.getSkillId()` |
| items-7 | `generated/concurrency/fieldmap.toml` `[captures]` | a decision for the captured stone of `IdianStone$1` (requested: `OwnerRef<IdianStone>`) | approved (modified) | `[captures]` `"com.aionemu.gameserver.model.items.IdianStone$1#this"` = `IdianStone* const` (the capture of an anonymous class is `#this`, not `#this$0`). `OwnerRef` stays reserved for parts (hub-headers.md §10.3, lint L3): the observer is RefCounted and also held by the player's `ObserveController`, so it is no part; `X*` is the documented non-retaining capture spelling (fieldmap.toml header), so no lint change is needed. Lifetime argument in the reason: the stone (a RECLAIMER part of its Item) outlives every notification, `IdianStone::onUnEquip` removes the observer before a stone is replaced or unequipped, and `LogoutBreakers` cuts `actionListener` (`breakActionListener`). The member in `IdianStone.cpp` keeps `IdianStone* const idianStone` with a `// fieldmap.toml:` note instead of the waiver; `fieldmap.json` regenerated |

## Wave 3b-1 (2026-09-15)

The 12 requests of the four lanes (holders P4-09, world P4-10, controllers P4-11b, dao P4-14) and the request wave 3a-2 left open
(`build/workflows/w3a2-followups.json`, prev-1). The integrator-owned change requests of the lanes that were applied are listed after the table.

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| prev-1 | `game-server/generated/concurrency/fieldmap.toml` `[fields]` `BIHNode.BIHStackData.node` | restate the reason of the geo-3 decision with the invariant the code now keeps | approved | No declaration changes. The reason now says that `BIHNode::intersectWhere` empties `TempVars.bihStack` before every return and on the exception path (`BihStackClearGuard`), so no `Ref` stays in thread-local storage beyond the call (L15), instead of the untrue "cleared by every query". The spelling stays `const Ref<BIHNode>`: the optional `BIHNode* const` would save a retain/release per push but needs a new lifetime argument that no lane asked for. `fieldmap.json` regenerated |
| holders-1 | `model/autogroup/AutoGroup.h` (P5-10) | add `const std::vector<int32_t>& getNpcIds() const;` and `bool isRecruitableInstance() const;` | approved | Additive; public Java methods (AutoGroup.java:78-84) that `AutoGroupData.afterUnmarshal` needs, and the generated member block keeps `npcIds` private (`std::optional<std::vector<int32_t>>`). Inline in the shell: `getNpcIds` returns a function-local static empty vector for Java's `Collections.emptyList()`, `isRecruitableInstance` keeps Java's precedence. `AutoGroupData::afterUnmarshal` now builds `recruitableInstanceMaskIdByPortalNpc` and `portalNpcsInHashOrder` (`JavaHashMapOrder` of the `computeIfAbsent` keys); its P4-09 deviation row is removed |
| holders-2 | `model/templates/item/actions/DecomposeAction.h` (P5-07) | add `static void validateRandomItemIds(const dataholders::ItemData& itemData);` | approved | Additive; the last DataManager post-processing step (DataManager.java:234), with the holder passed because `ITEM_DATA` is not published during C++ post-processing (static-data.md §3.3). The header includes only `dataholders/fwd.h`. The body and the Java id arrays (file-local `constexpr` arrays; the static members `fieldmap.py` lists and `premiumOphidanRecipe` come with the port of `act()`) move into the new `DecomposeAction.cpp`; the stand-in `dataholders/detail/DecomposeActionIds.{h,cpp}` is deleted. Callers: `DataManager::postProcess`, `HolderHooksTest` (`DecomposeActionIdsTest`); the P4-09 deviation row is updated |
| world-1 | `world/zone/ZoneName.h` (P4-10 hub) | `int32_t id();` becomes `int32_t id() const;`; also `std::string toString();` becomes `std::string toString() const;` | approved | Interned names are handled only as `const ZoneName*` and `id()` only hashes `_name`, so the non-const declaration forced `const_cast`s in `MapRegion`'s zone comparator, `ZoneName::getId` and `WorldBasicsTest`; all three are removed. `toString()` is included as the reviewer asked: the hub-headers.md §9.1 rule for a non-const `toString()` on Immortal classes exists because such bodies call virtual getters, while the final `ZoneName` returns its immutable name |
| world-2 | `services/WeatherService.h` (P4-10) | `setNextWeather` takes `runtime::Array<const WeatherEntry*>&` instead of a read-only span | rejected | Not needed: `setNextWeather` is private, every caller passes `worldZoneWeathers.get(mapId)`, and the body writes to exactly that array under its monitor, so the behaviour equals Java. The `TODO(header-request)` comments in `WeatherService.cpp` become plain notes and the P4-10 deviation row says why the span stays |
| world-3 | `world/knownlist/KnownList.h` (P4-10) | add `protected: static void delPair(VisibleObject& a, VisibleObject& b);` | approved | Additive, in the lane's own chunk (already declared): `FlagKnownList` removes both edges (P4-10 deviation row `FlagKnownList.update`) and cannot call the protected `del` of another object's list through a `KnownList&`. A static next to `addPair` is the smallest form |
| world-4 | `taskmanager/AbstractPeriodicTaskManager.h` (P4-10) | add the two-argument constructor `(int32_t period, std::string_view simpleClassName)` and the static `fifo*` helpers | approved | Additive, in the lane's own chunk (already declared): a C++ base constructor cannot see `getClass().getSimpleName()`, and the helpers keep the logger, config and `RunnableStatsManager` out of the template header. The one-argument constructor logs the base class name, so its remaining user `BrokerService::BrokerPeriodicTaskManager` (P5-09, `BrokerService.cpp`) now passes `"BrokerPeriodicTaskManager"` |
| world-5 | `taskmanager/AbstractFIFOPeriodicTaskManager.h` (P4-10) | add the member `const std::string simpleClassName`, the two-argument constructor and the private `taskClassOf`/`describeTask` helpers | approved | Additive, in the lane's own chunk (already declared) and required by the `run()` port. The member is now a `fieldmap.toml [cpp_members]` decision (its `// fieldmap:` waiver became a `// fieldmap.toml:` note). Not changed here: `run()` stays inline in the template header, so `ZoneUpdateService.h` and `MovementNotifyTask.h` include `Creature.h`; moving the body to an `.inl` is left to the world lane's review fixes |
| controllers-1 | `model/templates/item/actions/ItemActions.h` (P5-07) | add `const SkillUseAction* getSkillUseAction() const;` | approved | Additive; public Java lookup (ItemActions.java:132-140), same pattern as items-3 (`firstActionOf<SkillUseAction>`, `dynamic_cast` for `instanceof`); `SkillUseAction` is in `item/actions/fwd.h`. `StanceObserver::itemused` is ported (StanceObserver.java:34-37) and its `AION_UNPORTED` removed |
| dao-1 | `dao/{PlayerTitleListDAO,FriendListDAO,MailDAO}.h` (P4-14) | `loadTitleList`, `load`, `loadPlayerMailbox` return `std::unique_ptr<TitleList/FriendList/Mailbox>` instead of `runtime::Ref<...>` | approved | Signature change without C++ callers. The three classes derive `runtime::OwnedPart`, `Player::setTitleList/setFriendList/setMailbox` take `std::unique_ptr`, and hub-headers.md §5 spells a newly created part `std::unique_ptr<X>`; a `Ref` to an unbound part would retain a missing owner. The public functions return the file-local bodies (the `AION_UNPORTED` stubs are gone); the P4-14 deviation row is updated |
| dao-2 | `dao/PlayerDAO.h` `PlayerAndLegionInfo` (P4-14) | the `legionRank` component (member, constructor parameter, accessor) becomes `std::optional<model::team::legion::LegionRank>` | approved | Signature change of a K5 record without C++ callers. Java passes null directly (`legionRank == null ? null : LegionRank.valueOf(...)`, PlayerDAO.java:456), so hub-headers.md §5.1/§6 make it `std::optional`; the value-initialized stand-in was `BRIGADE_GENERAL`, a trap for the port of `DatabaseCleaningService.maintainBrigadeGenerals` (P5) even though Java reads it only for legion members. `hashCode` hashes a null rank as 0 (`Objects.hashCode`). Tests: `PlayerDaoTest` (no legion: no rank), `RecordHashTest` (null rank); the P4-14 deviation row is removed |
| dao-3 | `model/gameobjects/BrokerItem.h` (P4-11a) | the loading constructor and its `create` take `runtime::Ptr<Item>` instead of `Item&` | approved | Signature change; Java's only caller `BrokerDAO.loadBroker` passes a null item for sold entries and for entries without an item row (hub-headers.md §5.1: a parameter receiving a variable Java holds as null). The member stays `const runtime::Ref<Item>` and may hold null, as `getItem()` (a `Ptr`) already allows. `BrokerDAO::loadBroker` passes the item directly (its `AION_UNPORTED` branch is gone), so sellers keep their sold entries after a restart. Existing callers pass `*item`, which binds to `Ptr<Item>`; `GameObjectsModelTest.BrokerItemCopiesTheItemAndTruncatesTheExpireTime` covers a null item |
| dao-4 | `dao/HouseBidsDAO.h` (P4-14) | `loadBids` takes `runtime::ConcurrentHashMap<int32_t, runtime::Ref<model::house::HouseBids>>& bidsById` | approved | Signature change without C++ callers: Java puts a new `HouseBids` into the caller's map for the first bid of each house (HouseBidsDAO.java:40-42), and hub-headers.md §7.1 spells a parameter the callee fills for a shared field as the shim by non-const reference; the map is `HousingBidService.bids` (the same shim type). The header includes `runtime/collections/ConcurrentHashMap.h` (a lean runtime header) instead of `<unordered_map>`; the file-local body became the public body |

Change requests applied by the integrator:
- `game-server/CMakeLists.txt`: realdata labels for `StaticDataRealDataTest`/`ConfigRootsRealDataTest` (with `AION_TEST_PYTHON` set through `ENVIRONMENT_MODIFICATION` when Python 3.12 is found, so the test also runs `oracle.py compare-counts/compare-totals`), `WorldRealDataTest` and the DAO database/parity tests (`ServerDataDaoTest`, `PlayerDaoTest`, `LegionHouseItemDaoTest`, `DaoSqlParityTest`).
- `gs.smoke.startup` (`game-server/cmake/RunStartupSmoke.cmake`, milestone M4): passes when the startup completes (exit code 0, "Static Data loaded in N seconds", "S0a startup sequence complete", "Runtime shut down:"); a stop at an unported function fails and names it, and only real database errors print the database message. The stale comments at the top of `src/main.cpp` and above the test in `CMakeLists.txt` are updated.
- `fieldmap.toml`: `[fields]` `ZoneService.zoneHandlers` (`HashMap<const ZoneName*, const ZoneHandlerEntry*>`) and `AttackShieldObserver.healType` (`const std::optional<HealType>`), `[cpp_members]` `ZoneService.worldZoneTemplates` and `AbstractFIFOPeriodicTaskManager.simpleClassName`; their `// fieldmap:` waivers became `// fieldmap.toml:` notes. `ZoneService.worldZoneTemplates` keeps its `// confined:` waiver: lint L19 accepts `[fields]` spellings but not `[cpp_members]` ones. `cycles.toml` needs no change (`lint_concurrency.py --cycles=core` is clean with the regenerated `fieldmap.json`).
- Test expectations made stale by the lanes' ports (P4-12/P4-13 test owners' requests): `PlayerCreationTest.WithoutThePetLoaderTheDaoStopsCreation` is now `WithoutThePetLoaderTheDaoLogsAndCreationContinues` (the ported `PlayerPetsDAO.getPlayerPets` logs the missing DatabaseFactory and returns no pets, so the controller owner is bound before the unported `PlayerGameStats`); `ItemStonesTest.IdianStoneReadsItsTemplatesAndBurnsPolishCharge` expects the `ClassCastException` of `RandomBonusEffect.applyEffect` on the test stats after the ported `addObserver` (and removes the observer again), and no exception from the ported `ItemStoneListDAO.storeIdianStones`, which marks the stone `UPDATED` even when saving fails, as Java does.

Not applied: the `main.cpp` used-id sources and `PlayerDAO::setAllPlayersOffline()` (P5-14), because Java's `IDFactory` fails the start when a `getUsedIDs` query fails and `gs.smoke.startup` runs against the schema-less `aion_cpp_test` database, so the smoke test first needs a database with `sql/aion_gs.sql`; the optional `noexcept` on `ObserveController::clearWithoutNotify`/`PlayerController::breakStanceObserver` (a layout change that nothing needs: `LogoutBreakers` already wraps them); the documentation merges (P4-09 into DEVIATIONS.md, phase4-status.md, static-data.md §3.3), which belong to the wave's documentation stage.

## Wave 3b-1, M4 gate (2026-09-15)

The M4 gate integrator's own changes to frozen or other chunks' headers and to the chunk manifest, logged by the header-request process (no lane
asked for them). All are additive; no existing declaration changed.

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| m4-1 | `geoEngine/GeoWorldLoader.h` (P4-04) | add `static Statistics getLastLoadStatistics() noexcept;` and the private `static inline runtime::Field<Statistics> lastLoadStatistics{};` | approved | M4 item 5: `aion_game_server --check-static-data` reports the entity counts of the real startup path, where `GeoService.init` calls the Java overload `load(maps)` that discards the statistics. `load(maps, geoDir)` stores them before it returns (written once during the startup, read afterwards; `Field` for the concurrency rules). Lint and header check clean |
| m4-2 | `geoEngine/GeoCallbacks.h` (P4-04) | add `static void setMaterialZoneListener(MaterialZoneSink listener) noexcept;` and the private `materialZoneListener` field; `createMaterialZone` notifies it before the sink | approved | M4 item 5: the check mode records the 7,961 material zone names while `GeoService.init` loads with its own sink (`createMaterialZone` of `GeoService.cpp`). A listener instead of a second sink keeps the Java path unchanged; `nullptr` removes it. Also ported in `GeoCallbacks.cpp` under the M4 path exception: the default sink calls `ZoneService::createMaterialZoneTemplate` with `ZoneName::createOrGet` (its `AION_UNPORTED` is gone, P4-04 has none left) |
| m4-3 | `world/WorldMapInstance.h` (P4-10 hub) | add `std::vector<const zone::ZoneName*> getZoneNames();` (a snapshot of `zones.keySet()`) | approved | M4 item 6: the check mode reports the zone names of every map instance, which `python -m geo m4-compare` compares with a prediction from the zone XML and the geo oracle; `zones` is private and Java has no accessor |
| m4-4 | `game-server/chunks.cmake` | P5-14 `aion_gs_app` part: `TEST_SUPPORT m4` | approved | `tests/m4/M4Database.cpp` is the database helper `aion_gs_m4_database` of `gs.m4.check_static_data` and `gs.smoke.startup` (creates a schema from `sql/aion_gs.sql`, inserts the IDFactory fixture rows, counts online players, drops the schema), built by `CMakeLists.txt` itself and not a GoogleTest executable |

Also applied by the gate (bodies, CMake, tests): `main.cpp` (the Java startup order through `World`, the used-id sources and
`setAllPlayersOffline` that the header-request stage left open, the check options; docs/deviations/P5-14.md), `ForkJoinPool::runIndexed` (P4-02b: the
elements of a STARTUP or SHUTDOWN caller keep that task kind, so the watchdog's stall exemption covers them; test
`ForkJoinPoolTest.ElementsOfStartupAndShutdownPhasesKeepThePhaseKind`), `gs.m4.check_static_data` (`cmake/RunM4Check.cmake`), `gs.smoke.startup`
(its own `aion_gs_test_smoke` schema, geo data off, expects "World: 161 world maps created." and "M4 startup sequence complete"), and
`tools/oracle/geo/m4.py` (`python -m geo m4-probes|m4-compare`, tests in `tests/test_geo_oracle.py`; `loader.py` records the material id of each
material zone).

## Wave 3b-2 (2026-09-15)

The two requests of the packet lanes (sm-a P4-16 filed none, sm-b P4-17 two), the independent review of the M4 gate's accessors m4-1..m4-3
(approved by the gate itself in wave 3b-1), and the integrator-owned change requests of the three lanes (listed after the table). No frozen
declaration changed in this wave.

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| sm-b-1 | `network/aion/serverpackets/SM_SIEGE_LOCATION_INFO.h` (P4-17) | `locations` becomes `std::vector<std::pair<int32_t, runtime::Ref<model::siege::SiegeLocation>>>` (Java `LinkedHashMap`/`HashMap` order) | rejected | Not needed. Java's no-argument constructor stores a reference to the service's live map (`SiegeService.getSiegeLocations()`, SM_SIEGE_LOCATION_INFO.java:27-30), and `writeImpl` iterates that map when the packet is written; the C++ constructor leaves `locations` empty and `writeImpl` reads the service's map (`detail::getSiegeLocations()`) when the packet is serialized, which is what the stored reference observes. The one-location constructor has a single entry, so no order exists. A vector member gains nothing. The order of the type-0 packet is decided by the service's container, not by this header: `SiegeService.locations` (Java `Map`, assigned from `DataManager.SIEGE_LOCATION_DATA.getSiegeLocations()`, SiegeService.java:83) is a `runtime::HashMap`, whose shim happens to iterate in insertion order without guaranteeing it (`MapOrder::HASH`); if the holder's XML order has to be kept, the SiegeService owner (P5-12a) requests `runtime::LinkedHashMap` for that member. The lane's workaround and its P4-17 deviation row stay |
| sm-b-2 | `serverpackets/SM_NEARBY_QUESTS.h`, `SM_RECIPE_LIST.h`, `SM_RECIPE_COOLDOWN.h`, `SM_TOWNS_LIST.h` (P4-17) | keep the `std::unordered_map`/`unordered_set` members and constructor parameters, or take the caller's insertion order (a vector of pairs) so Java's `HashMap` bucket order is reproduced for colliding keys | rejected (keep the declarations) | A vector member alone would not reproduce Java's order: the callers would also have to hand over Java's bucket order, which depends on the table capacity after removals (`RecipeList.removeRecipe`) and on the per-bucket insertion history, and the C++ caller containers (`runtime::HashMap`, `std::unordered_set`) do not keep it. The order of recipe ids, nearby quest markers, craft cooldowns and towns changes nothing in the client. Four frozen headers stay unchanged; `writeImpl` keeps `JavaHashMapOrder` over ascending keys (Java's order whenever no two keys share a bucket), and the P4-17 deviation row states it as an approximation (the reviewer's deviation-text finding goes to the packets fixer). hub-headers.md §14 still lists these packets as layout candidates, should a client-visible order ever turn up |
| m4-1 | `geoEngine/GeoWorldLoader.h` (P4-04) | review of `static Statistics getLastLoadStatistics() noexcept;` and `static inline runtime::Field<Statistics> lastLoadStatistics{};` | approved (kept) | Needed: `GeoService.init` calls the Java overload `load(maps)`, which discards the statistics, and the M4 check reports the counts of that real path (M4 item 5). Additive, C++-only and documented in the class comment; `GeoWorldLoader` is a K5 static utility class, so the static member needs no `fieldmap.toml` decision (lint clean). `Field<Statistics>` is valid (`Statistics` is 12 `int32_t`, trivially copyable; `Field::get` is `noexcept`, so the `noexcept` accessor cannot throw), and the one write at the end of the load and later reads cannot tear. An out parameter or a second `GeoService` entry point would change the Java path instead. Added a test: `GeoWorldLoaderFilesTest.LoadsTerrainsMeshesAndPlacements` checks `getLastLoadStatistics()` against the returned statistics |
| m4-2 | `geoEngine/GeoCallbacks.h` (P4-04) | review of `static void setMaterialZoneListener(MaterialZoneSink listener) noexcept;`, the private `materialZoneListener` field, and `createMaterialZone` notifying it before the sink | approved (kept) | Needed: the check mode must record the material zone names while `GeoService.init` installs its own sink (`GeoService.cpp:90`); replacing that sink in `main.cpp` would change the zones the server creates. A listener next to the sink keeps the Java path unchanged, reuses the `MaterialZoneSink` function-pointer type, and follows the class's thread-safety rule (captureless pointers in `Field`s, set before the load). `nullptr` removes it; `main.cpp` sets it only with `--check-static-data`. Added a test: the same geo test installs a listener and checks that it sees exactly the zones the sink creates (`TearDown` also resets it) |
| m4-3 | `world/WorldMapInstance.h` (P4-10 hub) | review of `std::vector<const zone::ZoneName*> getZoneNames();` | approved (kept) | Needed: `zones` is private and Java has no accessor, and the M4 check compares the zone names of every map instance with the oracle's prediction (M4 item 6). Additive and hub-headers.md-conform: non-const like the other methods of a runtime-based class (§9.1), a newly built collection returned as `std::vector` (§7.1), interned names as `const ZoneName*` (§5), body a snapshot of `zones.keySet()` taken under the map's Monitor. Added a test: `WorldZonesTest.WorldCreatesTheMapsAndTheirInstances` checks the five zone names of Poeta's main instance |

Change requests applied by the integrator:
- `network/aion/AionConnection.cpp` (P4-15, from sm-a): `initialized()` serializes `serverpackets::SM_KEY` itself now that P4-16 ported its
  `writeImpl` (`writeD(con->enableCryptKey())`, the same bytes); the private `ConnectionKeyPacket` stand-in and its `TODO(P4-16)` are gone (the
  default packet name is also "SM_KEY"). The P4-15 deviation row `SM_KEY` is updated. `AionConnectionTest` (key exchange bytes) passes.
- `tests/controllers/CreatureControllerTest.cpp` `OnDespawnCancelsTheDecayTaskAndStopsMovement` (P4-11b, from sm-a and sm-b): the stale
  expectation. With `SM_MOVE` ported (P4-17), `abortMove` → `setAndSendStopMove` broadcasts to the sighted players and threw
  `NullPointerException` on the test npc's empty known list part slot. The test now gives the npc a `NpcKnownList`, as Java's `SpawnEngine` does,
  so `onDespawn` again ends with the `UnportedException` of `AggroList.clear` (P5-01) after the controller's own steps; all other assertions are
  unchanged.
- `model/geometry/RectangleArea.cpp`, `PolyArea.cpp` `getDistance3D(x, y, z)` (P4-05, from the runtime lane): outside the z range the closest
  point is computed as values (`getClosestPoint2D(x, y)` and the nearer z bound, exactly the floats `AbstractArea::getClosestPoint(x, y, z)`
  stores) instead of creating a `Ref<Point3D>` per call, which World creation did about 11.1 million times for Reshanta's 3D region and zone
  tests. `getClosestPoint(x, y, z)` itself still returns the new `Point3D` (Java API). Tests: `AreaTest.RectangleArea` and `AreaTest.PolyArea`
  compare `getDistance3D` below and above the z range with the distance to `getClosestPoint(x, y, z)`; `gs.m4.check_static_data` checks the
  world zones against the oracle. No timing was measured here (the runtime fixer measures RelWithDebInfo).
- `docs/design/runtime-architecture.md` §4.3 and the DEVIATIONS.md row "Deadlock detection" (from the runtime lane): the watchdog's snapshot
  minidumps from a helper process with a timeout, the one-dump-per-check STALL report and the rate limit.
- `gs.chunks.consistency` and the L14 warning in `world/WorldMap3DInstance.cpp` (from sm-a): the fresh configure of this stage records every test
  file the lanes added (75 parts, 6,455 C++ files, 194 test files); `lint_concurrency.py --werror --cycles=core game-server/src` reports 3,394
  files, 0 errors, 0 warnings (the runtime lane's own fix had already removed the warning).

Not applied:
- Merging `docs/deviations/P4-17.md` and `P4-02.md` into DEVIATIONS.md: the wave's close-out documentation stage merges all fragments
  (as in wave 3b-1); only the "Deadlock detection" row the runtime lane named is updated here.
- `RuntimeConfig::watchdogConfig` keys for `minidumpHelperExecutable`, `minidumpTimeout`, `minidumpMinInterval`, `maxMinidumps` (P4-01,
  optional): the defaults are the design values (D5), the helper path is only a test seam, and a string key needs a `ConfigValue` plus the
  runtime-architecture.md §10 table; left to P4-01 when an operator needs them.
- The 61 C4702 (unreachable code after `AION_UNPORTED`) warnings of the RelWithDebInfo build in stub files of other chunks (`BaseService.cpp`,
  `PeriodicInstanceManager.cpp`, `SM_LEGION_HISTORY.cpp`, `SM_MESSAGE.cpp`, `SM_PET.cpp`, ...): Debug, the gate configuration, is warning-free,
  and the stubs disappear as their owners port the bodies (the packet stubs with the packets fixer, the rest in phase 5).
- The local enum tables of the P4-16/P4-17 packets: replaced by the companion owners (P5-01, P5-02, P5-07, P5-10, P5-11, P5-12a, P5-13) when
  the companion headers exist; nothing to apply before.

## Wave 5a, pre-stage (2026-09-15)

The header requests that docs/design/m5a-plan.md §6 expects, decided before the stage-1 lanes start (plan items I-01 and I-03), and the manifest
rows of I-01. Lane ownership in stage 1 follows plan §4: a lane adds declarations to its own chunks without a request.

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| 5a-pre-1 | `services/AccountService.h` (P5-00) | `static std::unique_ptr<PlayerAccountData> loadPlayerAccountData(int32_t playerId);` becomes `loadPlayerAccountData(model::account::Account& account, int32_t playerId);` | approved | Signature change without C++ callers. The result is a part (`Account.players` is a `PartMap`), and `PlayerAccountData`'s constructors take the owning `Account&` (PlayerAccountData.h), so the body cannot create it from the player id alone. Both Java callers add the result to a known account right away (AccountService.java:79 `loadAccount`, PlayerEnterWorldService.java:119-122), so the account is passed first. Applied to the header (class comment) and the `AION_UNPORTED` stub; the slice lane (S-02, S-05) ports the body |
| 5a-pre-2 | `configs/main/AIConfig.h` (P4-01) | C++-only key `gameserver.dev.missing_ai_handlers = fail\|warn` (default `fail`) | rejected (no request needed) | Additive, and P4-01 belongs to the world-engines lane in stage 1 (plan §4), the lane of the only user (W-01 `AIEngine::newAI`). The lane adds the field and its `AION_BIND` in its own chunk and records the C++-only key in `docs/deviations/P4-01.md` (merged into DEVIATIONS.md at the wave's close-out) |
| 5a-pre-3 | `runtime/base/Unported.h` (P4-02a) | `AION_PARTIAL(reason)`, `partialHits()`, `writePartialTrace()` | approved | Additive; plan D3, used by every lane from the first day, and no lane owns the runtime. Applied as requested plus the siblings of the unported trace API: `struct PartialSite` (per-site state; the reason is a constant expression stored with `constinit`, so the argument must be a string literal), `struct PartialHit {function, file, line, reason, hits}`, `partialReached(site, location) noexcept`, `partialHitCount() noexcept`, `resetPartialHitsForTests()`. The macro counts the hit, logs one warning per site ("AION_PARTIAL reached: <function> at <file>:<line>: <reason>", logger `com.aionemu.gameserver.Unported`, no stack trace) and returns; it never throws, so it is usable in noexcept functions and in the spawn and logout paths of plan §7. Trace format: "# AION_PARTIAL trace v1", then `hits file:line function reason` tab separated. Not counted as unported. Tests: `PartialTest` in `tests/runtime/base/UnportedTest.cpp` (return value, counts, once-per-site log, noexcept, concurrent hits, trace format). Writing `partial_trace.txt` in the check-output mode is F-02 (P5-14) |
| 5a-pre-4 | `utils/audit/AuditLogger.h` (P4-05) | a `log` that accepts a null player | approved (modified) | CM_PING passes `getConnection().getActivePlayer()` (CM_PING.java:36-41), a `Ptr<Player>` that is null before enter world, and P4-05 belongs to another lane (stats-skills) than the requester (slice). Instead of changing `log(Player&, std::string_view)` (a signature change for its 11 callers), an additive overload `static void log(runtime::Ptr<Player> player, std::string_view message);`: non-null delegates to the reference overload (which a `Player&` argument still selects exactly); null does what the Java body does with null: `NullPointerException` when punishments are enabled (AutoBan.punishment), the audit line "null " + message, `NullPointerException` once an online staff member has AUDIT_INFO (ChatUtil.charName). So it is not "null-safe" beyond Java: the M5a profile (punishment off, no staff online) logs and continues like Java. The header includes `runtime/lifetime/Ref.h`. Tests: `tests/base/AuditLoggerTest.cpp` |
| 5a-pre-5 | `model/town/Town.h` (P5-11) | the DAO constructor with `levelUpDate` | rejected (not needed) | The declarations already allow the port: the member is `const commons::database::Timestamp levelUpDate` and the constructor takes `std::optional<Timestamp>`; `towns.level_up_date` is `NOT NULL` (sql/aion_gs.sql:1007), so Java never stores null there and SM_TOWNS_LIST.java:26 dereferences it unconditionally. Note for the economy-legion lane (E2-05): initialize `levelUpDate(levelUpDateValue.value_or(Timestamp{}))` with a comment naming the column, and let `Town(int32_t id, Race race)` delegate to it with `Timestamp(std::chrono::milliseconds(60000))` (Java `this(id, 1, 0, race, new Timestamp(60000))`), then set `PersistentState::NEW`; the stub's member initializer lists are replaced by the port |
| 5a-pre-6 | `runtime/lifetime/LiveInstanceCounters.h` (new), `runtime/lifetime/Ref.h` `makeRef`, `Reclaimer.cpp` `destroyChunk` (P4-02a) | debug live-instance counters by dynamic type (`liveCounts()`); `LeakCensus::censusNow()` only if needed | approved (modified) | Plan D8/I-03/F-07. Additive; placed in `aion_gs_runtime_lifetime` (P4-02a, not P4-02b as the plan names it) because `makeRef` and the Reclaimer are there. New header: `struct LiveCount {className, live, created}`, `LIVE_COUNTS_ENABLED`, `liveCounts()` (every type created since start, sorted by class name), `liveCountOf(const std::type_info&)`, `writeLiveCounts(std::ostream&)` ("# live instance counts v1", `live created className`). Checked builds only (`AION_CHECKED`, like C1-C16; release builds count nothing): `makeRef<T>` counts T after its constructor returned (so `VisibleObject::create<T>` and every `X::create` count, a throwing constructor counts nothing); `destroyChunk` counts the object's dynamic type next to the destroy observer call, independent of the single observer slot that LeakCensus owns. Parts (storages) are not RefCounted and not counted: a Ref to a part retains its owner and a retired part holds its owner, so a leaked PlayerStorage shows as a live Player or Account (header comment). `censusNow()` is **not** added: `configure` with `censusAfter` and `checkInterval` 0, then `Reclaimer::reclaimNow()` twice, reports every still-referenced removed object in `getLeaks()` at once; `LeakCensusTest.ZeroThresholdsAndReclaimNowReportOnDemand` proves it. Tests: `tests/runtime/lifetime/LiveInstanceCountersTest.cpp` (dynamic type through a base Ref, reclamation, throwing constructor, concurrent registration, format) |
| 5a-pre-7 | `network/aion/AionClientPacketFactory.h` (P4-15) | `unportedPacketClassesSeen()` | approved | Additive; plan D7 (m5a_summary.txt lists the client packets without a C++ port that a client sent), requested for P5-14 while P4-15 belongs to the slice lane. Applied as `static std::vector<std::string> unportedPacketClassesSeen();` (sorted), the set behind the once-per-class "which is not ported yet" warning of `tryCreatePacket` (moved from function-local statics into one leaked holder in the `.cpp`). Test: `AionClientPacketFactoryTest.UnknownOpcodesAndUnportedClasses` |
| 5a-pre-8 | `configs/main/RuntimeConfig.h` (P4-01) | C++-only test keys `gameserver.debug.leak_census_seconds`, `gameserver.runtime.zombie_break_seconds` overriding the minute keys when set | deferred to stage 3 (G-01) | No consumer before the nightly stress run: the final census of F-07 configures `LeakCensus` directly (5a-pre-6), and the scenario gate runs well below the minute thresholds. Additive; decided with G-01 so the keys, their `RuntimeLifecycle` wiring and the DEVIATIONS row land with their test. The world-engines lane (owner of P4-01 in stage 1) does not add them |
| 5a-pre-9 | `game-server/chunks.cmake`, `cmake/AionChunks.cmake`, `tools/porting/chunks.py`, `game-server/CMakeLists.txt` | I-01 manifest rows: `tests/scenario` → new chunk P5-SC with `aion_gs_scenario_tests`; `game-server/cmake` → P5-14; the login-server test support include path; stage-2 leases of the seven C-01 files | approved (modified) | Applied: (1) `aion_gs_chunk(P5-SC TARGET aion_gs_scenario PHASE 5 GLOBS "aion/gameserver/scenario/**" TESTS scenario TEST_INCLUDES support)`: a test-only chunk whose library has no sources (placeholder), so the chunk machinery builds `aion_gs_scenario_tests` from `tests/scenario` linking `aion_gs_core` and the empty registries (not a leaf: the harness may need server code, and a later change of DEPENDS would block the lane on the integrator). (2) The manifest could not own files outside `src`/`handlers`/`generated`/`tests`: new keyword `OTHER_FILES <glob>...` (paths below cpp/game-server outside the ROOTS and tests/, also in a LEASE), validated and written to chunks.json (`otherFiles`) by CMake and used by `chunks.py owner`/`check-ownership`/`verify-json` (tests in `tools/porting/tests/test_chunks.py`). Modified: P5-14 owns `cmake/{AppTests,RunStartupSmoke,RunM4Check}.cmake`, not all of `cmake/` (`AionChunks.cmake`, `CheckChunks.cmake`, `CompareEmptyRegistries.cmake` are the integrator's); the registrations of `aion_gs_m4_database`, `gs.smoke.startup` and `gs.m4.check_static_data` moved unchanged from CMakeLists.txt into the new `cmake/AppTests.cmake`, so F-02 changes their expected lines, TIMEOUT and the new `gs.smoke.startup_progress` in its own file. (3) CMakeLists.txt wires `aion_gs_scenario_tests`: include directory `login-server/tests/support` (headers by name, like network-4), link `aion_loginserver_crypto`, `add_dependencies` on `aion_game_server` and `aion_login_server`, compile definitions `AION_GAME_SERVER_EXECUTABLE`, `AION_LOGIN_SERVER_EXECUTABLE`, `AION_LOGINSERVER_JAVA_DIR`, `AION_SCENARIO_OUTPUT_DIR` (`AION_GAMESERVER_JAVA_DIR` comes from the build options), and includes `tests/scenario/ScenarioTests.cmake` (owned by P5-SC through its test directory) for the CTest registrations of F-06/G-01; `tests/scenario/ScenarioWiringTest.cpp` checks the wiring. (4) Stage-2 leases (C-01, lane packets-dao): `aion_gs_chunk(P4-16 LEASE ...)` for CM_CHECK_MAIL_UNK, CM_CUSTOM_SETTINGS, CM_CHAT_AUTH with `TEST_SUPPORT cm_ak`, `aion_gs_chunk(P4-17 LEASE ...)` for CM_MAY_QUIT, CM_PING_REQUEST, CM_SHOW_FRIENDLIST, CM_SUBZONE_CHANGE with `TEST_SUPPORT cm_lz`, released when stage 2 is merged. (5) The M5a profile of plan I-01: `game-server/config/m5a.properties.example` (D1 keys plus `connect_address`, the scenario-only keys commented out; not read by the servers). 78 parts |
| 5a-pre-10 | `services/SiegeService.h` (P5-12a) | `locations` as `runtime::LinkedHashMap` | rejected | Conditional on D1 enabling siege; D1 keeps `gameserver.siege.enable=false`, so no siege location packet is on the M5a path (O-05, see also sm-b-1) |

## Wave 5b-1, stage 1 (2026-09-22)

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| m5b-1 | `controllers/attack/AttackUtil.h` (P5-01, the requesting chunk's own frozen header) | the four result-list parameters change from `const std::vector<runtime::Ptr<AttackResult>>&` to `runtime::Ref` vectors: `adjustDamageByStatModifiers(..., const std::vector<Ref<AttackResult>>&, ...)`, `calculateAdditionalHitCount(..., const std::vector<Ref<AttackResult>>&)`, `modifyDamageByNpcAi(..., const std::vector<Ref<AttackResult>>&)` and, non-const, `amplifyDamageByAdditionalHitCount(..., std::vector<Ref<AttackResult>>&)` | **applied on the lane's branch, pending the integrator's decision** | Two reasons, both blocking. (1) `calculatePhysAttackResult`/`calculateMagAttackResult` and `StatFunctions::calculateAttackDamage` return `std::vector<Ref<AttackResult>>` (the list holds the only references, hub-headers.md §7.1), and a `vector<Ref<T>>` does not convert to a `const vector<Ptr<T>>&`, so the ported body of AttackUtil.java:45-53 could not pass its own list to the next step. (2) `amplifyDamageByAdditionalHitCount` **appends** to the list (AttackUtil.java:151-162, `attackList.add(new AttackResult(...))`), which a const vector cannot do and which a `vector<Ptr<>>` must not do: the appended result would be owned by nobody. Scope: all four are unreachable from outside the class today - three are `private`, `adjustDamageByStatModifiers` is public in Java but has no caller outside `AttackUtil` in either tree (checked with grep over `game-server/src`, both languages), so no other chunk recompiles. `ObserveController::checkShieldStatus` keeps its `const std::vector<Ptr<AttackResult>>&` parameter; `calculatePhysAttackResult` builds the borrowed view for it exactly as `CreatureController::attackTarget` already does (`CreatureController.cpp:380`). The call sites carry `// header-request: m5b-1` |
| m5b-2 | `controllers/ControllerStandIns.h` (P4-11b, the requesting chunk's own frozen header) | **delete** five declarations and their `AION_UNPORTED` bodies: `aiLoggerMoveinfo`, `targetEventHandlerOnTargetReached`, `walkManagerStopWalking`, `shoutEventHandlerOnAttack`, `shoutEventHandlerOnEnemyAttack`. With them go the now unused `#include <string_view>` and `#include "aion/gameserver/ai/fwd.h"` | **applied on the lane's branch, pending the integrator's decision** | m5b-plan.md §7 and item E-01a: the classes they stood in for now have headers - `ai/AILogger.h` had one all along, and A-01/A-02 added `ai/handler/TargetEventHandler.h`, `ai/handler/ShoutEventHandler.h` and `ai/manager/WalkManager.h` - so `NpcMoveController.cpp` (14 `AILogger::moveinfo`, 1 `TargetEventHandler::onTargetReached`, 2 `WalkManager::stopWalking`), `NpcController.cpp` (1 `ShoutEventHandler::onEnemyAttack`) and `PlayerController.cpp` (1 `ShoutEventHandler::onAttack`) call them the way Java writes them. A deletion, so it recompiles every includer: the eight `.cpp` of P4-11b and `services/AccountService.cpp` (P5-00), which uses only `gameServerUpdateRatio`; all nine compile unchanged (`aion_game_server` builds and links). The two `runtime::cast<ai::NpcAI>` expressions stay exactly as Java writes them (D15/A-00), and `tests/controllers/AiSeamTest.cpp` asserts both halves of that invariant. The remaining P5-05 stand-in, `followStartServiceNewFollowingToTargetCheckTask`, is **not** deleted here: it is outside E-01a and `ai/follow/FollowStartService.h` landing makes it a stage-2 item (see the lane report) |
| m5b-3 | `controllers/ControllerStandIns.h` (P4-11b, the requesting chunk's own frozen header) | **delete** five more declarations and their `AION_UNPORTED` bodies, the second batch after m5b-2: `attackUtilCalculatePhysAttackResult`, `statFunctionsCalculateExperienceReward`, `statFunctionsCalculateDPReward`, `statFunctionsCalculatePvEApGained`, `playerRestrictionsCanAttack`. **No include changes**: every header the five needed is still needed by a stand-in that stays (`attack/fwd.h` and `utils/stats/CalculationType.h` by `attackUtilCalculateMagAttackResult`, `model/gameobjects/fwd.h` by five others, `model/gameobjects/player/fwd.h` by four), and `ControllerStandIns.cpp` keeps `attack/AttackResult.h` for the magical result vector | **applied on the lane's branch, pending the integrator's decision** | m5b-plan.md item **E-01b, moved from stage 2 into stage 1**. The plan deferred it on the reasoning that "none of these is reachable before its owner lands"; that is true of the owners (B-01, B-02/B-03 and C-01 all landed in stage 1) and false of the *caller*: A-06 registered `AggressiveNpcAI` in the same stage, so a Poeta monster aggroes and swings, `SimpleAttackManager` → `CreatureController::attackTarget` reaches the physical stand-in, and it threw 12 times in `gs.scenario.m5a`. The call sites now read `AttackUtil::calculatePhysAttackResult(self, *target, calculationTypes)` (`CreatureController.cpp:356`, Java CreatureController.java:323), `StatFunctions::calculateExperienceReward` / `calculateDPReward` / `calculatePvEApGained` (`NpcController.cpp:251,252,270`, Java NpcController.java:219, 220, 236) and `restrictions::PlayerRestrictions::canAttack(player, *target)` (`PlayerController.cpp:490`, Java PlayerController.java:397), each behind a `using` in its own translation unit, as E-01a spelled `ShoutEventHandler`. A deletion, so it recompiles the same nine includers as m5b-2; all nine compile unchanged and `aion_game_server` builds and links. The four stand-ins that **stay** now each carry a comment naming the milestone that deletes them: `attackUtilCalculateMagAttackResult` and `chargeSkillGetAndUse` → M5b-2 (O-01/O-02), `playerRestrictionsCanUseSkill` → M5b-2 (its real body exists since C-01 but is an `AION_PARTIAL` that refuses every skill, and swapping a throwing stand-in for a silently-refusing partial would hide the gap), `playerTeamDistributionServiceDoReward` → P5-10 |

## Wave 5b-2, stage 0 (2026-09-23)

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| m5b2-1 | `controllers/ControllerStandIns.h` (P4-11b, the requesting chunk's own frozen header) | **delete** the declaration `attackUtilCalculateMagAttackResult` and its `AION_UNPORTED` body in `ControllerStandIns.cpp`. **Include changes, unlike m5b-3's batch**: with it go `<unordered_set>`, `<vector>`, `model/SkillElement.h` and `utils/stats/CalculationType.h` from the header (no remaining declaration uses them) and `controllers/attack/AttackResult.h` from the `.cpp`. `controllers/attack/fwd.h` **stays**: `playerTeamDistributionServiceDoReward` still takes an `attack::TeamDamageList&` | **applied on the lane's branch, pending the integrator's decision** | m5b2-plan.md item **M-03**, the third and last batch of the stand-in deletions m5b-2 and m5b-3 began. Its owner landed in the same stage: `AttackUtil::calculateMagAttackResult` is ported (M-02) together with `calculateMagicalStatus` and `StatFunctions::calculateMagicalResistRate`/`calculateMagicalCriticalRate` (M-01), and the call site now reads `AttackUtil::calculateMagAttackResult(self, *target, model::templates::item::getMagicalElement(self.getAttackType()), calculationTypes)` (`CreatureController.cpp:358`, Java CreatureController.java:325) through the `using attack::AttackUtil;` the physical sibling already has (`CreatureController.cpp:81`) - so the PHYSICAL and the else arm of `attackTarget` are now symmetric. **This is what closes m5b-client-session.md S-3**: a Mage, Priest or Spiritmaster whose main-hand weapon is magical could not swing at all, because `Creature::getAttackType()` is not PHYSICAL for them (Player.java:937-941) and every swing reached the stub. A deletion, so it recompiles the same nine includers as m5b-2/m5b-3 plus `ControllerStandIns.cpp`; all ten compile unchanged and `aion_game_server` builds and links with no new warning. **Of the 13 declarations at HEAD `23c4e6485`, 1 goes and 12 remain** (m5b2-plan.md §7 predicted this one plus P-04's two in stage 1), none of them on the auto-attack path. Tests: `tests/controllers/AttackSeamTest.AttackTargetOfAMagicalAttackTypeTakesItsDamageFromTheRealAttackUtil` and `.AMageWhoseMainHandWeaponIsMagicalCanAutoAttack` - both fail with an `UnportedException` if the stand-in comes back (watched) |

## Wave 5b-2, stage 1 part 1: the declaration batch (2026-09-23)

m5b2-plan.md items **I-02** and **F-04** plus the declaration halves of **S-03** and **S-07**, filed by the declarations lane for the integrator
to decide when the wave lands. Everything below is in the lane's working tree and nothing else: **declarations and `AION_UNPORTED` stubs, no
behaviour**, so the engine lanes of part 2 and the effect lanes after them can override and call what Java declares. The only bodies that are
not stubs are hub-headers.md §2 plumbing: the `ValidationResult` constructor (stores its two fields) and its four accessors, the three
`create` factories (`makeRef`), the defaulted destructors and the inline getters `getMotionId`, `getMainHand`, `getOffHand`.

**F-04 re-derived from the Java classes, and the plan's count was a third of the gap.** m5b2-plan.md §2.2 counted **29 header-only shells with
45 undeclared Java methods**; that number is exact (28 of the 29 carry them, `SkillAttackInstantEffect`'s three Java methods are generated
getters). But the same gap exists in the shells that **have** a `.cpp`: `RootEffect.h` declared only `applyEffect`, while Java overrides
`calculate`, `startEffect` and `endEffect` too. Parsing every method at class depth of the 184 Java effect classes and matching name and arity
against each shell header and its generated `.xml.inc` finds **208 undeclared Java methods in 115 shells** (45 in 28 header-only shells, 163 in
87 shells with a `.cpp`): **192 overrides of `EffectTemplate` virtuals** (`calculate` 66, `startEffect` 59, `endEffect` 43, `onPeriodicAction`
11, `resolveMagicalCritical` 6, `calculateDamage` 4, `isDodgedOrResisted` 2, `calculateBaseValue` 1), **7 non-virtual public/protected
methods** (the `HealType`/dispel overloads of `AbstractHealEffect`, `HealOverTimeEffect` and `AbstractDispelEffect`,
`AbstractAbsoluteStatEffect::getModifiersSet`, `SummonServantEffect::spawnServant`) and **9 private helpers** (the plan's `removeEffect` is
`OneTimeBoostSkillAttackEffect`'s private helper, not an `EffectTemplate` virtual). **§2.4's subset alone has 54 of them in 27 of its 34
classes, not the plan's 15.** Every one is now declared, in Java order and with Java's access, each with an `AION_UNPORTED` stub (28 new `.cpp`
files for the header-only shells), so the site count is honest: **`AION_UNPORTED(` sites in `game-server/src` went from 1,942 to 2,181
(+239 = 208 effect + 21 properties + 10 skill model)**; the effect directory from 174 to 382 (A-L 100 → 190, M-Z 74 → 192).

**Name hiding (hub-headers.md §9.1): 99 using-declarations in 80 shells.** An override of `calculate(Effect&)` hides `EffectTemplate`'s
three-argument and four-argument `calculate`; an override of `startEffect`/`endEffect` in an `AbstractOverTimeEffect` subclass hides the
`(Effect&, std::optional<AbnormalState>)` overloads; the new `HealType` and dispel overloads hide their bases' one-argument virtuals and are
hidden in turn by the subclasses' overrides. Each shell that declares a name whose base overloads it would hide now says
`using <DirectBase>::<name>;` right before the declaration (computed to a fixpoint over the class tree, so `BuffBindEffect` uses
`BindEffect::calculate` once `BindEffect` has its own using-declaration). Six shells (`DispelBuffEffect`, `DispelDebuffEffect`,
`DispelDebuffMentalEffect`, `DispelDebuffPhysicalEffect`, `DispelNpcBuffEffect`, `DispelNpcDebuffEffect`) gain only the using-declaration.

**All additive.** Every override names a virtual that `EffectTemplate` (or `BufEffect`, `ShieldEffect`, `AbstractOverTimeEffect`, `DamageEffect`)
already declares, so no vtable slot is added and `override` makes the compiler check the signature; the other additions are non-virtual
members and using-declarations; the property and skill model headers are new files. No member of any existing class changes. Verified in the
lane's own build directory (Debug): `aion_gs_skills`, `aion_gs_effects_al`, `aion_gs_effects_mz`, `aion_game_server`, `aion_gs_header_check`
and every test executable build with no warning; `lint_concurrency.py --werror --cycles=core game-server/src` reports 3,653 files, 0 findings;
`skeleton.py --fwd --check` reports 252 files, 0 problems (the three `fwd.h` already listed every new class); `skeleton.py --definitions` and
`chunks.py check` are clean; `aion_gs_skills_tests` (51), `aion_gs_objects_tests` (46), `aion_gs_controllers_tests` (43),
`aion_gs_stats_tests` (60) and `aion_gs_cm_ak_tests` (47) pass. No test was added: the lane owns no test directory, and a stub has no behaviour
to assert. The two compiler checks the batch relies on were watched failing: dropping `const` from `RootEffect::calculate` gives C3668 ("method
with override specifier 'override' did not override any base class methods"), and without `HealOverTimeEffect`'s
`using AbstractOverTimeEffect::startEffect;` a pointer to its one-argument `startEffect` gives C2440; both pass once restored.

**Nothing becomes reachable and no reachable behaviour changes.** Every call that now dispatches to a new override previously reached an
`AION_UNPORTED` body too: all 26 `EffectTemplate` bodies, `BufEffect::startEffect`, `ShieldEffect::startEffect`,
`DamageEffect::resolveMagicalCritical`/`calculateDamage`, and `AbstractOverTimeEffect::startEffect(Effect&)`, whose ported one-liner delegates to
the unported two-argument overload. What changes is the site `unported_trace.txt` names: the leaf class (`RootEffect::calculate`) instead of
`EffectTemplate::calculate`. **For the part-2 lanes this is the point of the batch**: porting F-01 (`EffectTemplate`) makes no leaf work, because
Mage's *Root* now dispatches `calculate`, `startEffect` and `endEffect` to `RootEffect`'s own stubs, *Healing Light* and *Bandage Heal* to
`HealInstantEffect::calculate` → `AbstractHealEffect::calculate(Effect&, HealType)`/`applyEffect(Effect&, HealType)`, *Flame Bolt* and
*Ferocious Strike* through `DamageEffect::applyEffect` to the private `DamageEffect::onAttack`, and Soul Sickness to `StatdownEffect::startEffect`.

Two `fieldmap.toml` decisions are requested (m5b2-fm-1, m5b2-fm-2); both members carried a `// fieldmap:` waiver until then.

**DECIDED 2026-09-23 by the integrator: the whole batch is approved as filed** - every row m5b2-f04-1..121, m5b2-s03-1..8 and m5b2-s07-1..3 is additive (no vtable slot is added and no existing member changes), and each carries its Java evidence. **m5b2-fm-1 and m5b2-fm-2 are approved** and written to `fieldmap.toml` `[fields]`; both are faithful to Java (the caller's list filtered in place; two enums Java initializes to null), and `fieldmap.py` now prints `const std::optional<ItemGroup>` for the wrapper. The two waivers are now plain comments that point at the decision.

**Pre-approved for stage 1 part 3 (the effect lanes), from part 1's review:** the observer structs of three §2.4 classes read non-public members of their enclosing class - `RootEffect$1` reads the protected `resistchance` (Mage Root 1328, one of the gate's four skills), `HideEffect$1`/`$3` the protected `buffCount`, `ProvokerEffect$1` the private `shouldApply` and `getProvokeTarget` and the protected `skillId`. Each class may add ONE `friend struct <Class>_<Observer>;` line for exactly the struct it writes, on the precedent of `friend struct IdianStone_ActionObserver;` (IdianStone.h:30). No other change to those headers is covered by this pre-approval.

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| m5b2-s03-1 | `skillengine/properties/Properties.h` (P5-02a, xmlgen shell), new `Properties.cpp` | add `bool validate(model::Skill& skill, CastState castState) const;`, `bool endCastValidate(model::Skill& skill) const;`, private `bool validateEffectedList(model::Skill& skill) const;`, `ValidationResult validateEffectedList(runtime::ArrayList<runtime::Ref<Creature>>& targets, runtime::Ptr<Creature> firstTarget, Creature& effector, const model::SkillTemplate* skillTemplate, float x, float y, float z) const;` and the nested `class ValidationResult` (public members `targets` - a reference, m5b2-fm-1 -, `firstTarget` (`Ptr`, nullable), `valid = false`; the constructor and `getTargets`/`getFirstTarget`/`setFirstTarget`/`isValid`). Includes `runtime/collections/ArrayList.h`, `runtime/lifetime/Ref.h`, `model/gameobjects/fwd.h`, `skillengine/model/fwd.h`. 4 `AION_UNPORTED` stubs | pending (additive) | Java Properties.java:70 `validate`, :84 `endCastValidate`, :97/:103 `validateEffectedList`, :189-217 `ValidationResult`. The list parameter is the shim by reference because one caller passes the skill's `effectedList` field (`Skill.h`, `runtime::ArrayList<Ref<Creature>>`) and the property setters filter it in place (hub-headers.md §7.1); `SkillEngine::applyEffectsDirectly` (S-01) must build a local list of the same type. `isAddWeaponRange`, which m5b2-plan.md §7 lists, is **already a generated getter** (`Properties.xml.inc`) and is not added. Properties.h is included by `SkillTemplate.xml.h`, so every includer of `SkillTemplate.h` recompiles; all compile |
| m5b2-s03-2 | `skillengine/properties/FirstTargetProperty.{h,cpp}` (P5-02a, new) | static-only class: `static bool set(model::Skill& skill, const Properties* properties);`, private `static bool isTargetTeamMember(model::Skill& skill, bool onlyGroup);`, `static bool isTargetAllowed(model::Skill& skill, runtime::Ptr<Creature> target);`. 3 stubs | pending (new file) | Java FirstTargetProperty.java:19, :151, :168 (173 lines). `set` takes `Skill&` although skeleton.py's draft says `Ptr<Skill>` (§5.1 rule 1 matched some other two-argument `set` passing null): its only caller passes the casting skill. `isTargetAllowed`'s `target` is nullable: the TARGET arm passes `skill.getFirstTarget()` unchecked for relation ALL |
| m5b2-s03-3 | `skillengine/properties/FirstTargetRangeProperty.{h,cpp}` (P5-02a, new) | `static bool set(model::Skill& skill, const Properties* properties, Properties_CastState castState);`. 1 stub | pending (new file) | Java FirstTargetRangeProperty.java:19 (76 lines). **Reaches `GeoService.canSee` twice** (the POINT arm and the final check), which answers m5b2-plan.md §13 question 2: geo *is* on the cast path, so G-04 has something to assert |
| m5b2-s03-4 | `skillengine/properties/MaxCountProperty.{h,cpp}` (P5-02a, new) | `static bool set(const Properties* properties, Properties::ValidationResult& result);`. 1 stub | pending (new file) | Java MaxCountProperty.java:16 (46 lines) |
| m5b2-s03-5 | `skillengine/properties/TargetRangeProperty.{h,cpp}` (P5-02a, new) | `static bool set(const Properties*, Properties::ValidationResult& result, Creature& skillEffector, const model::SkillTemplate*, float x, float y, float z);` and the private `checkCommonRequirements(Creature&, const SkillTemplate*)`, `isInsideDisablePvpZone(Creature&)`, `checkRange(const Properties*, Creature& skillEffector, float x, float y, float z, Creature& creature, int32_t effectiveRange, Creature& firstTarget)`, `checkGeo(VisibleObject& object, runtime::Ptr<Creature> firstTarget, const SkillTemplate*)`, `tryAddSummon(runtime::Ptr<Summon> summon, Properties::ValidationResult&, const SkillTemplate*, runtime::ArrayList<runtime::Ref<Creature>>& effectedList)`. 6 stubs | pending (new file) | Java TargetRangeProperty.java:25, :100, :117, :127, :155, :169 (173 lines). `checkGeo` calls `GeoService.getZ`/`canSee` when geodata is on (AREA, PARTY and POINT arms). `checkGeo`'s first target is `result.getFirstTarget()` (nullable); `tryAddSummon`'s summon is null-checked in Java. `isInsideDisablePvpZone` is unused in Java (`@SuppressWarnings("unused")`) and declared all the same |
| m5b2-s03-6 | `skillengine/properties/TargetRelationProperty.{h,cpp}` (P5-02a, new) | `static bool set(const Properties*, Properties::ValidationResult& result, Creature& effector, const model::SkillTemplate*);`, `static bool isBuffAllowed(runtime::Ptr<Creature> source, runtime::Ptr<Creature> target);`, `static bool isSameAreaType(Creature& source, Creature& target);`. 3 stubs | pending (new file) | Java TargetRelationProperty.java:16, :63, :81 (85 lines). `isBuffAllowed` checks both parameters for null (§5.1 rule 2) |
| m5b2-s03-7 | `skillengine/properties/TargetSpeciesProperty.{h,cpp}` (P5-02a, new) | `static bool set(const Properties* properties, Properties::ValidationResult& result);`. 1 stub | pending (new file) | Java TargetSpeciesProperty.java:11 (18 lines) |
| m5b2-s03-8 | `skillengine/properties/TargetStatusProperty.{h,cpp}` (P5-02a, new) | `static bool set(const Properties*, Properties::ValidationResult& result, const model::SkillTemplate*);`, private `static bool hasAnyAbnormalState(Creature& creature, const std::vector<effect::AbnormalState>& states);`. 2 stubs | pending (new file) | Java TargetStatusProperty.java:20, :31 (34 lines). Its JAXB annotations bind no member, so xmlgen writes no shell and the class is a plain static-only class |
| m5b2-s07-1 | `skillengine/model/ChargeSkill.{h,cpp}` (P5-02a, new) | `class ChargeSkill : public Skill` (RefCounted like Skill, fieldmap K4): member `const int32_t motionId;`, protected constructor `(const SkillTemplate*, Creature& effector, int32_t skillLevel, int32_t motionId, Skill& startSkill)` (its initializer list is Java's `super(...)` and field store, the three setter calls are `AION_UNPORTED`), `static Ref<ChargeSkill> create(...)`, `getMotionId()`, `bool useSkill() override;`. 2 stubs | pending (new file) | Java ChargeSkill.java:15, :23, :28 (44 lines). `SkillEngine::getChargeSkill` already returns `Ref<model::ChargeSkill>` (SkillEngine.h). Needed by `CM_USE_CHARGE_SKILL` only (**W**, plan O-03) |
| m5b2-s07-2 | `skillengine/model/PenaltySkill.{h,cpp}` (P5-02a, new) | `class PenaltySkill : public Skill`: protected constructor `(const SkillTemplate*, Creature& effector, int32_t skillLevel)` (initializer list `Skill(..., effector, nullptr)`, body `AION_UNPORTED`), `static Ref<PenaltySkill> create(...)`, `bool useSkill() override;`, public `void initializeSkillMethod() override;` (Java widens it to public). 3 stubs | pending (new file) | Java PenaltySkill.java:7, :12, :18 (21 lines). The port must call `PenaltySkill::initializeSkillMethod()` again in the constructor body: a C++ base constructor cannot dispatch to the override (Skill.h, Skill.cpp say so; the class comment repeats it) |
| m5b2-s07-3 | `skillengine/model/WeaponTypeWrapper.{h,cpp}` (P5-02a, new) | `class WeaponTypeWrapper : public runtime::RefCounted` (fieldmap K3): members `const std::optional<ItemGroup> mainHand{}, offHand{}` (m5b2-fm-2), protected constructor `(std::optional<ItemGroup> mainHand, std::optional<ItemGroup> offHand)`, `create`, `bool equals(const WeaponTypeWrapper&) const;`, `std::string toString();`, `int32_t hashCode() const;`, `int32_t compareTo(const WeaponTypeWrapper&) const;`, inline `getMainHand()`/`getOffHand()`. 5 stubs | pending (new file) | Java WeaponTypeWrapper.java:13, :47, :63, :68, :77, :90, :94 (97 lines). **m5b2-plan.md S-07 says the weapon conditions need it; they do not**: `grep WeaponTypeWrapper` over the Java tree finds only MotionTime, and C++ MotionTime already keys its maps with its own normalized pair (`MotionTime::WeaponKey`, `MotionTime::weaponTypeWrapper`, P4-08). So this class is **W**, not **R**. `hashCode` hashes Java enum identities (`ItemGroup.hashCode()`), which no port can reproduce: a deviation row if it is ever ported |
| m5b2-fm-1 | `generated/concurrency/fieldmap.toml` `[fields]` `Properties.ValidationResult.targets` | `runtime::ArrayList<runtime::Ref<Creature>>&` instead of the printed `std::vector<Ptr<Creature>>` | pending (decision) | Java's `private final List<Creature> targets` holds the caller's list object (Properties.java:191, the constructor stores the argument), the setters filter it in place, and `Skill.endCast` reads the skill's `effectedList` afterwards; a copy would lose every filter. The class is K5 confined and lives for one validation, so the reference never outlives its list. Lint does not check this nested member either way (verified); the waiver documents the deviation until the decision |
| m5b2-fm-2 | `generated/concurrency/fieldmap.toml` `[fields]` `WeaponTypeWrapper.mainHand`, `WeaponTypeWrapper.offHand` | `const std::optional<ItemGroup>` instead of `const ItemGroup` | pending (decision) | Java initializes both to `null` (WeaponTypeWrapper.java:10-11), keeps a null argument (`new WeaponTypeWrapper(null, null)`, MotionTime.java:138) and `compareTo`/`hashCode` test for null: hub-headers.md §6 (a nullable enum is `std::optional`), precedent `IgnoreProperties.race` (geo-2). Without the waiver lint L2 reports both members (watched: 2 errors; with it, 0) |
| m5b2-f04-1 | `skillengine/effect/AbstractAbsoluteStatEffect.h` (P5-03) | `const model::templates::stats::ModifiersTemplate* getModifiersSet() const;` (public). 1 `AION_UNPORTED` stub | pending (additive) | Java AbstractAbsoluteStatEffect.java:41 `getModifiersSet`. Additive: non-virtual members Java declares (hub-headers.md §9.1 declares every Java method, private ones included). |
| m5b2-f04-2 | `skillengine/effect/AbstractDispelEffect.h` (P5-03), new `AbstractDispelEffect.cpp` | `void applyEffect(model::Effect&, model::DispelCategoryType type, model::SkillTargetSlot slot) const;` (public overload); `using EffectTemplate::applyEffect;`. 1 `AION_UNPORTED` stub | pending (additive) | Java AbstractDispelEffect.java:26 `applyEffect`. Additive: non-virtual members Java declares (hub-headers.md §9.1 declares every Java method, private ones included). The using-declaration keeps the base overloads of `applyEffect` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-3 | `skillengine/effect/AbstractHealEffect.h` (P5-03) | `void calculate(model::Effect&, model::HealType healType) const;` (public overload); `void applyEffect(model::Effect&, model::HealType healType) const;` (public overload); `using EffectTemplate::calculate;`; `using EffectTemplate::applyEffect;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java AbstractHealEffect.java:27 `calculate`, :33 `applyEffect`. Additive: non-virtual members Java declares (hub-headers.md §9.1 declares every Java method, private ones included). The using-declarations keep the base overloads of `calculate`, `applyEffect` visible that the declarations would hide (hub-headers.md §9.1). **In §2.4's subset.** |
| m5b2-f04-4 | `skillengine/effect/AlwaysBlockEffect.h` (P5-03) | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java AlwaysBlockEffect.java:24 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-5 | `skillengine/effect/AlwaysDodgeEffect.h` (P5-03) | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java AlwaysDodgeEffect.java:24 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. **In §2.4's subset.** |
| m5b2-f04-6 | `skillengine/effect/AlwaysParryEffect.h` (P5-03) | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java AlwaysParryEffect.java:24 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-7 | `skillengine/effect/AlwaysResistEffect.h` (P5-03) | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java AlwaysResistEffect.java:24 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. **In §2.4's subset.** |
| m5b2-f04-8 | `skillengine/effect/ArmorMasteryEffect.h` (P5-03), new `ArmorMasteryEffect.cpp` | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java ArmorMasteryEffect.java:29 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. **In §2.4's subset.** |
| m5b2-f04-9 | `skillengine/effect/AuraEffect.h` (P5-03) | override declarations `onPeriodicAction`, `startEffect`, `endEffect`; private `void applyAuraTo(Creature& effected) const;`. 4 `AION_UNPORTED` stubs | pending (additive) | Java AuraEffect.java:45 `onPeriodicAction`, :70 `applyAuraTo`, :75 `startEffect`, :99 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. Additive: non-virtual members Java declares (hub-headers.md §9.1 declares every Java method, private ones included). |
| m5b2-f04-10 | `skillengine/effect/BackDashEffect.h` (P5-03), new `BackDashEffect.cpp` | override declarations `calculate`; `using DamageEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java BackDashEffect.java:27 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-11 | `skillengine/effect/BindEffect.h` (P5-03) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java BindEffect.java:25 `calculate`, :30 `startEffect`, :39 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-12 | `skillengine/effect/BleedEffect.h` (P5-03), new `BleedEffect.cpp` | override declarations `resolveMagicalCritical` (protected), `calculate`, `startEffect`, `endEffect`, `onPeriodicAction`; `using AbstractOverTimeEffect::calculate;`; `using AbstractOverTimeEffect::startEffect;`; `using AbstractOverTimeEffect::endEffect;`. 5 `AION_UNPORTED` stubs | pending (additive) | Java BleedEffect.java:24 `resolveMagicalCritical`, :29 `calculate`, :34 `startEffect`, :42 `endEffect`, :47 `onPeriodicAction`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `calculate`, `startEffect`, `endEffect` visible that the declarations would hide (hub-headers.md §9.1). **In §2.4's subset.** |
| m5b2-f04-13 | `skillengine/effect/BlindEffect.h` (P5-03) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java BlindEffect.java:30 `calculate`, :35 `startEffect`, :49 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-14 | `skillengine/effect/BoostSkillCastingTimeEffect.h` (P5-03), new `BoostSkillCastingTimeEffect.cpp` | override declarations `calculate`; `using BufEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java BoostSkillCastingTimeEffect.java:19 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-15 | `skillengine/effect/BoostSkillCostEffect.h` (P5-03), new `BoostSkillCostEffect.cpp` | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java BoostSkillCostEffect.java:24 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-16 | `skillengine/effect/BuffBindEffect.h` (P5-03), new `BuffBindEffect.cpp` | override declarations `calculate`; `using BindEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java BuffBindEffect.java:17 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-17 | `skillengine/effect/BuffSilenceEffect.h` (P5-03), new `BuffSilenceEffect.cpp` | override declarations `calculate`; `using SilenceEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java BuffSilenceEffect.java:17 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-18 | `skillengine/effect/BuffSleepEffect.h` (P5-03), new `BuffSleepEffect.cpp` | override declarations `calculate`, `startEffect`; `using SleepEffect::calculate;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java BuffSleepEffect.java:18 `calculate`, :23 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-19 | `skillengine/effect/BuffStunEffect.h` (P5-03), new `BuffStunEffect.cpp` | override declarations `calculate`; `using StunEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java BuffStunEffect.java:17 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-20 | `skillengine/effect/CaseHealEffect.h` (P5-03) | override declarations `startEffect`; private `bool tryHeal(model::Effect&) const;`; `using AbstractHealEffect::applyEffect;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java CaseHealEffect.java:46 `startEffect`, :59 `tryHeal`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. Additive: non-virtual members Java declares (hub-headers.md §9.1 declares every Java method, private ones included). The using-declaration keeps the base overloads of `applyEffect` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-21 | `skillengine/effect/ChangeHateOnAttackedEffect.h` (P5-03) | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java ChangeHateOnAttackedEffect.java:32 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-22 | `skillengine/effect/CloseAerialEffect.h` (P5-03) | override declarations `calculate`; `using EffectTemplate::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java CloseAerialEffect.java:23 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-23 | `skillengine/effect/CondSkillLauncherEffect.h` (P5-03) | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java CondSkillLauncherEffect.java:34 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-24 | `skillengine/effect/ConfuseEffect.h` (P5-03) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java ConfuseEffect.java:48 `calculate`, :53 `startEffect`, :74 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-25 | `skillengine/effect/ConvertHealEffect.h` (P5-03) | override declarations `startEffect`, `endEffect`. 2 `AION_UNPORTED` stubs | pending (additive) | Java ConvertHealEffect.java:21 `startEffect`, :32 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-26 | `skillengine/effect/CurseEffect.h` (P5-03), new `CurseEffect.cpp` | override declarations `calculate`, `startEffect`, `endEffect`; `using BufEffect::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java CurseEffect.java:18 `calculate`, :23 `startEffect`, :30 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-27 | `skillengine/effect/DPHealEffect.h` (P5-03) | override declarations `startEffect`, `onPeriodicAction`; `using HealOverTimeEffect::startEffect;`; `using HealOverTimeEffect::onPeriodicAction;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java DPHealEffect.java:19 `startEffect`, :24 `onPeriodicAction`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `startEffect`, `onPeriodicAction` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-28 | `skillengine/effect/DPHealInstantEffect.h` (P5-03) | override declarations `calculate`; `using AbstractHealEffect::applyEffect;`; `using AbstractHealEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java DPHealInstantEffect.java:19 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `applyEffect`, `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-29 | `skillengine/effect/DPTransferEffect.h` (P5-03) | override declarations `calculate`; private `int32_t getCurrentStatValue(model::Effect&) const;` (a helper: DPTransferEffect is no HealEffectTemplate); `using EffectTemplate::calculate;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java DPTransferEffect.java:27 `calculate`, :33 `getCurrentStatValue`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. Additive: non-virtual members Java declares (hub-headers.md §9.1 declares every Java method, private ones included). The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-30 | `skillengine/effect/DamageEffect.h` (P5-03) | private `void onAttack(model::Effect&, SM_ATTACK_STATUS_TYPE type, SM_ATTACK_STATUS_LOG log) const;` (includes the two generated enum headers). 1 `AION_UNPORTED` stub | pending (additive) | Java DamageEffect.java:38 `onAttack`. Additive: non-virtual members Java declares (hub-headers.md §9.1 declares every Java method, private ones included). **In §2.4's subset.** |
| m5b2-f04-31 | `skillengine/effect/DashEffect.h` (P5-03), new `DashEffect.cpp` | override declarations `calculate`; `using DamageEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java DashEffect.java:23 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-32 | `skillengine/effect/DeformEffect.h` (P5-03) | override declarations `calculate`, `startEffect`, `endEffect`; `using TransformEffect::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java DeformEffect.java:23 `calculate`, :28 `startEffect`, :35 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-33 | `skillengine/effect/DelayedFpAtkInstantEffect.h` (P5-03) | override declarations `calculate`; private `void calculateAndApplyDamage(model::Effect&) const;`; `using EffectTemplate::calculate;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java DelayedFpAtkInstantEffect.java:22 `calculate`, :39 `calculateAndApplyDamage`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. Additive: non-virtual members Java declares (hub-headers.md §9.1 declares every Java method, private ones included). The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-34 | `skillengine/effect/DelayedSkillEffect.h` (P5-03) | override declarations `endEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java DelayedSkillEffect.java:22 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-35 | `skillengine/effect/DelayedSpellAttackInstantEffect.h` (P5-03) | override declarations `calculateDamage`. 1 `AION_UNPORTED` stub | pending (additive) | Java DelayedSpellAttackInstantEffect.java:38 `calculateDamage`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-36 | `skillengine/effect/DiseaseEffect.h` (P5-03) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java DiseaseEffect.java:19 `calculate`, :30 `startEffect`, :37 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-37 | `skillengine/effect/DispelBuffCounterAtkEffect.h` (P5-03) | override declarations `resolveMagicalCritical` (protected), `calculateDamage`, `endEffect`. 3 `AION_UNPORTED` stubs | pending (additive) | Java DispelBuffCounterAtkEffect.java:28 `resolveMagicalCritical`, :39 `calculateDamage`, :55 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-38 | `skillengine/effect/DispelBuffEffect.h` (P5-03) | `using AbstractDispelEffect::applyEffect;`. No stub | pending (additive) | The using-declaration keeps the new overload `AbstractDispelEffect::applyEffect(Effect&, DispelCategoryType, SkillTargetSlot)` (m5b2-f04-2) visible, which this shell's existing `applyEffect` override would hide (hub-headers.md §9.1). |
| m5b2-f04-39 | `skillengine/effect/DispelDebuffEffect.h` (P5-03) | `using AbstractDispelEffect::applyEffect;`. No stub | pending (additive) | The using-declaration keeps the new overload `AbstractDispelEffect::applyEffect(Effect&, DispelCategoryType, SkillTargetSlot)` (m5b2-f04-2) visible, which this shell's existing `applyEffect` override would hide (hub-headers.md §9.1). |
| m5b2-f04-40 | `skillengine/effect/DispelDebuffMentalEffect.h` (P5-03) | `using AbstractDispelEffect::applyEffect;`. No stub | pending (additive) | The using-declaration keeps the new overload `AbstractDispelEffect::applyEffect(Effect&, DispelCategoryType, SkillTargetSlot)` (m5b2-f04-2) visible, which this shell's existing `applyEffect` override would hide (hub-headers.md §9.1). |
| m5b2-f04-41 | `skillengine/effect/DispelDebuffPhysicalEffect.h` (P5-03) | `using AbstractDispelEffect::applyEffect;`. No stub | pending (additive) | The using-declaration keeps the new overload `AbstractDispelEffect::applyEffect(Effect&, DispelCategoryType, SkillTargetSlot)` (m5b2-f04-2) visible, which this shell's existing `applyEffect` override would hide (hub-headers.md §9.1). |
| m5b2-f04-42 | `skillengine/effect/DispelNpcBuffEffect.h` (P5-03) | `using AbstractDispelEffect::applyEffect;`. No stub | pending (additive) | The using-declaration keeps the new overload `AbstractDispelEffect::applyEffect(Effect&, DispelCategoryType, SkillTargetSlot)` (m5b2-f04-2) visible, which this shell's existing `applyEffect` override would hide (hub-headers.md §9.1). |
| m5b2-f04-43 | `skillengine/effect/DispelNpcDebuffEffect.h` (P5-03) | `using AbstractDispelEffect::applyEffect;`. No stub | pending (additive) | The using-declaration keeps the new overload `AbstractDispelEffect::applyEffect(Effect&, DispelCategoryType, SkillTargetSlot)` (m5b2-f04-2) visible, which this shell's existing `applyEffect` override would hide (hub-headers.md §9.1). |
| m5b2-f04-44 | `skillengine/effect/EscapeEffect.h` (P5-03) | override declarations `calculate`; `using EffectTemplate::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java EscapeEffect.java:24 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). **In §2.4's subset.** |
| m5b2-f04-45 | `skillengine/effect/FPHealEffect.h` (P5-03) | override declarations `startEffect`, `onPeriodicAction`; `using HealOverTimeEffect::startEffect;`; `using HealOverTimeEffect::onPeriodicAction;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java FPHealEffect.java:18 `startEffect`, :23 `onPeriodicAction`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `startEffect`, `onPeriodicAction` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-46 | `skillengine/effect/FPHealInstantEffect.h` (P5-03) | override declarations `calculate`; `using AbstractHealEffect::applyEffect;`; `using AbstractHealEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java FPHealInstantEffect.java:18 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `applyEffect`, `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-47 | `skillengine/effect/FallEffect.h` (P5-03) | override declarations `isDodgedOrResisted` (protected). 1 `AION_UNPORTED` stub | pending (additive) | Java FallEffect.java:19 `isDodgedOrResisted`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-48 | `skillengine/effect/FearEffect.h` (P5-03) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java FearEffect.java:54 `calculate`, :59 `startEffect`, :92 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-49 | `skillengine/effect/FpAttackEffect.h` (P5-03), new `FpAttackEffect.cpp` | override declarations `calculate`, `onPeriodicAction`; `using AbstractOverTimeEffect::calculate;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java FpAttackEffect.java:20 `calculate`, :27 `onPeriodicAction`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-50 | `skillengine/effect/FpAttackInstantEffect.h` (P5-03) | override declarations `calculate`; `using EffectTemplate::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java FpAttackInstantEffect.java:26 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-51 | `skillengine/effect/HealCastorOnAttackedEffect.h` (P5-03) | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java HealCastorOnAttackedEffect.java:33 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-52 | `skillengine/effect/HealCastorOnTargetDeadEffect.h` (P5-03) | override declarations `endEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java HealCastorOnTargetDeadEffect.java:36 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-53 | `skillengine/effect/HealEffect.h` (P5-03) | override declarations `startEffect`, `onPeriodicAction`; `using HealOverTimeEffect::startEffect;`; `using HealOverTimeEffect::onPeriodicAction;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java HealEffect.java:18 `startEffect`, :23 `onPeriodicAction`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `startEffect`, `onPeriodicAction` visible that the declarations would hide (hub-headers.md §9.1). **In §2.4's subset.** |
| m5b2-f04-54 | `skillengine/effect/HealInstantEffect.h` (P5-03) | override declarations `calculate`; `using AbstractHealEffect::applyEffect;`; `using AbstractHealEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java HealInstantEffect.java:23 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `applyEffect`, `calculate` visible that the declaration would hide (hub-headers.md §9.1). **In §2.4's subset.** |
| m5b2-f04-55 | `skillengine/effect/HealOverTimeEffect.h` (P5-03) | override declarations `calculate`; `void startEffect(model::Effect&, model::HealType healType) const;` (public overload); `void onPeriodicAction(model::Effect&, model::HealType healType) const;` (public overload); `using AbstractOverTimeEffect::calculate;`; `using AbstractOverTimeEffect::startEffect;`; `using AbstractOverTimeEffect::onPeriodicAction;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java HealOverTimeEffect.java:25 `calculate`, :32 `startEffect`, :37 `onPeriodicAction`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. Additive: non-virtual members Java declares (hub-headers.md §9.1 declares every Java method, private ones included). The using-declarations keep the base overloads of `calculate`, `startEffect`, `onPeriodicAction` visible that the declarations would hide (hub-headers.md §9.1). **In §2.4's subset.** |
| m5b2-f04-56 | `skillengine/effect/HiPassEffect.h` (P5-03), new `HiPassEffect.cpp` | override declarations `calculate`; `using BufEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java HiPassEffect.java:8 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-57 | `skillengine/effect/HideEffect.h` (P5-03) | override declarations `endEffect`, `startEffect`. 2 `AION_UNPORTED` stubs | pending (additive) | Java HideEffect.java:43 `endEffect`, :54 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. **In §2.4's subset.** |
| m5b2-f04-58 | `skillengine/effect/HostileUpEffect.h` (P5-03) | override declarations `calculate`; `using EffectTemplate::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java HostileUpEffect.java:57 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-59 | `skillengine/effect/InvulnerableWingEffect.h` (P5-03) | override declarations `calculate`, `endEffect`; `using EffectTemplate::calculate;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java InvulnerableWingEffect.java:18 `calculate`, :31 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-60 | `skillengine/effect/MPHealEffect.h` (P5-04) | override declarations `startEffect`, `onPeriodicAction`; `using HealOverTimeEffect::startEffect;`; `using HealOverTimeEffect::onPeriodicAction;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java MPHealEffect.java:18 `startEffect`, :23 `onPeriodicAction`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `startEffect`, `onPeriodicAction` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-61 | `skillengine/effect/MPHealInstantEffect.h` (P5-04) | override declarations `calculate`; `using AbstractHealEffect::applyEffect;`; `using AbstractHealEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java MPHealInstantEffect.java:18 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `applyEffect`, `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-62 | `skillengine/effect/MPShieldEffect.h` (P5-04) | override declarations `startEffect`, `endEffect`. 2 `AION_UNPORTED` stubs | pending (additive) | Java MPShieldEffect.java:23 `startEffect`, :32 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-63 | `skillengine/effect/MagicCounterAtkEffect.h` (P5-04) | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java MagicCounterAtkEffect.java:35 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-64 | `skillengine/effect/MoveBehindEffect.h` (P5-04), new `MoveBehindEffect.cpp` | override declarations `calculate`; `using DamageEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java MoveBehindEffect.java:23 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-65 | `skillengine/effect/MpAttackEffect.h` (P5-04), new `MpAttackEffect.cpp` | override declarations `onPeriodicAction`. 1 `AION_UNPORTED` stub | pending (additive) | Java MpAttackEffect.java:20 `onPeriodicAction`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-66 | `skillengine/effect/MpAttackInstantEffect.h` (P5-04) | override declarations `calculate`; `using EffectTemplate::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java MpAttackInstantEffect.java:24 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-67 | `skillengine/effect/NoDeathPenaltyEffect.h` (P5-04), new `NoDeathPenaltyEffect.cpp` | override declarations `calculate`; `using BufEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java NoDeathPenaltyEffect.java:8 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-68 | `skillengine/effect/NoFlyEffect.h` (P5-04) | override declarations `calculate`, `isDodgedOrResisted` (protected), `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 4 `AION_UNPORTED` stubs | pending (additive) | Java NoFlyEffect.java:19 `calculate`, :29 `isDodgedOrResisted`, :37 `startEffect`, :46 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-69 | `skillengine/effect/NoReduceSpellATKInstantEffect.h` (P5-04) | override declarations `resolveMagicalCritical` (protected), `calculateDamage`. 2 `AION_UNPORTED` stubs | pending (additive) | Java NoReduceSpellATKInstantEffect.java:24 `resolveMagicalCritical`, :29 `calculateDamage`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-70 | `skillengine/effect/NoResurrectPenaltyEffect.h` (P5-04), new `NoResurrectPenaltyEffect.cpp` | override declarations `calculate`; `using BufEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java NoResurrectPenaltyEffect.java:8 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-71 | `skillengine/effect/OneTimeBoostSkillAttackEffect.h` (P5-04), new `OneTimeBoostSkillAttackEffect.cpp` | override declarations `startEffect`; private `void removeEffect(model::Effect&) const;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java OneTimeBoostSkillAttackEffect.java:27 `startEffect`, :60 `removeEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. Additive: non-virtual members Java declares (hub-headers.md §9.1 declares every Java method, private ones included). |
| m5b2-f04-72 | `skillengine/effect/OneTimeBoostSkillCriticalEffect.h` (P5-04) | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java OneTimeBoostSkillCriticalEffect.java:31 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-73 | `skillengine/effect/OpenAerialEffect.h` (P5-04) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java OpenAerialEffect.java:31 `calculate`, :54 `startEffect`, :71 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-74 | `skillengine/effect/ParalyzeEffect.h` (P5-04) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java ParalyzeEffect.java:25 `calculate`, :30 `startEffect`, :43 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-75 | `skillengine/effect/PetOrderUseUltraSkillEffect.h` (P5-04) | override declarations `calculate`; `using EffectTemplate::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java PetOrderUseUltraSkillEffect.java:55 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-76 | `skillengine/effect/PetrificationEffect.h` (P5-04) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java PetrificationEffect.java:25 `calculate`, :30 `startEffect`, :43 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-77 | `skillengine/effect/PoisonEffect.h` (P5-04), new `PoisonEffect.cpp` | override declarations `resolveMagicalCritical` (protected), `calculate`, `startEffect`, `endEffect`, `onPeriodicAction`; `using AbstractOverTimeEffect::calculate;`; `using AbstractOverTimeEffect::startEffect;`; `using AbstractOverTimeEffect::endEffect;`. 5 `AION_UNPORTED` stubs | pending (additive) | Java PoisonEffect.java:24 `resolveMagicalCritical`, :29 `calculate`, :34 `startEffect`, :42 `endEffect`, :47 `onPeriodicAction`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `calculate`, `startEffect`, `endEffect` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-78 | `skillengine/effect/PolymorphEffect.h` (P5-04), new `PolymorphEffect.cpp` | override declarations `startEffect`, `endEffect`. 2 `AION_UNPORTED` stubs | pending (additive) | Java PolymorphEffect.java:20 `startEffect`, :31 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. **In §2.4's subset.** |
| m5b2-f04-79 | `skillengine/effect/ProcAtkInstantEffect.h` (P5-04) | override declarations `calculateBaseValue` (protected). 1 `AION_UNPORTED` stub | pending (additive) | Java ProcAtkInstantEffect.java:30 `calculateBaseValue`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-80 | `skillengine/effect/ProcDPHealInstantEffect.h` (P5-04) | override declarations `calculate`; `using AbstractHealEffect::applyEffect;`; `using AbstractHealEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java ProcDPHealInstantEffect.java:19 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `applyEffect`, `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-81 | `skillengine/effect/ProcFPHealInstantEffect.h` (P5-04) | override declarations `calculate`; `using AbstractHealEffect::applyEffect;`; `using AbstractHealEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java ProcFPHealInstantEffect.java:18 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `applyEffect`, `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-82 | `skillengine/effect/ProcHealInstantEffect.h` (P5-04) | override declarations `calculate`; `using AbstractHealEffect::applyEffect;`; `using AbstractHealEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java ProcHealInstantEffect.java:18 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `applyEffect`, `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-83 | `skillengine/effect/ProcMPHealInstantEffect.h` (P5-04) | override declarations `calculate`; `using AbstractHealEffect::applyEffect;`; `using AbstractHealEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java ProcMPHealInstantEffect.java:18 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declarations keep the base overloads of `applyEffect`, `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-84 | `skillengine/effect/ProtectEffect.h` (P5-04) | override declarations `startEffect`, `endEffect`. 2 `AION_UNPORTED` stubs | pending (additive) | Java ProtectEffect.java:23 `startEffect`, :43 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-85 | `skillengine/effect/ProvokerEffect.h` (P5-04) | override declarations `startEffect`, `endEffect`; private `bool shouldApply(Creature& effector, Creature& target, int32_t attackSkillId) const;`; private `Ptr<Creature> getProvokeTarget(Creature& effector, Creature& target) const;`. 4 `AION_UNPORTED` stubs | pending (additive) | Java ProvokerEffect.java:41 `startEffect`, :68 `shouldApply`, :83 `getProvokeTarget`, :91 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. Additive: non-virtual members Java declares (hub-headers.md §9.1 declares every Java method, private ones included). **In §2.4's subset.** |
| m5b2-f04-86 | `skillengine/effect/PulledEffect.h` (P5-04) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java PulledEffect.java:33 `calculate`, :56 `startEffect`, :75 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-87 | `skillengine/effect/RandomMoveLocEffect.h` (P5-04) | override declarations `calculate`; `using EffectTemplate::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java RandomMoveLocEffect.java:41 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-88 | `skillengine/effect/RecallInstantEffect.h` (P5-04) | override declarations `calculate`; `using EffectTemplate::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java RecallInstantEffect.java:26 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-89 | `skillengine/effect/ReflectorEffect.h` (P5-04) | override declarations `startEffect`, `endEffect`. 2 `AION_UNPORTED` stubs | pending (additive) | Java ReflectorEffect.java:23 `startEffect`, :33 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. **In §2.4's subset.** |
| m5b2-f04-90 | `skillengine/effect/ResurrectBaseEffect.h` (P5-04) | override declarations `calculate`, `endEffect`; `using ResurrectEffect::calculate;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java ResurrectBaseEffect.java:17 `calculate`, :27 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-91 | `skillengine/effect/ResurrectEffect.h` (P5-04) | override declarations `calculate`; `using EffectTemplate::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java ResurrectEffect.java:34 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-92 | `skillengine/effect/ResurrectPositionalEffect.h` (P5-04) | override declarations `calculate`; `using ResurrectEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java ResurrectPositionalEffect.java:34 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-93 | `skillengine/effect/ReturnEffect.h` (P5-04) | override declarations `calculate`; `using EffectTemplate::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java ReturnEffect.java:24 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). **In §2.4's subset.** |
| m5b2-f04-94 | `skillengine/effect/ReturnPointEffect.h` (P5-04) | override declarations `calculate`; private `int32_t getTargetObjectId(Player& player) const;` (Java private final); `using EffectTemplate::calculate;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java ReturnPointEffect.java:39 `calculate`, :45 `getTargetObjectId`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. Additive: non-virtual members Java declares (hub-headers.md §9.1 declares every Java method, private ones included). The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-95 | `skillengine/effect/RideRobotEffect.h` (P5-04) | override declarations `startEffect`, `endEffect`. 2 `AION_UNPORTED` stubs | pending (additive) | Java RideRobotEffect.java:23 `startEffect`, :40 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-96 | `skillengine/effect/RootEffect.h` (P5-04) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java RootEffect.java:32 `calculate`, :37 `startEffect`, :58 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). **In §2.4's subset.** |
| m5b2-f04-97 | `skillengine/effect/SanctuaryEffect.h` (P5-04) | override declarations `startEffect`, `endEffect`. 2 `AION_UNPORTED` stubs | pending (additive) | Java SanctuaryEffect.java:18 `startEffect`, :24 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. **In §2.4's subset.** |
| m5b2-f04-98 | `skillengine/effect/SearchEffect.h` (P5-04) | override declarations `endEffect`, `startEffect`. 2 `AION_UNPORTED` stubs | pending (additive) | Java SearchEffect.java:30 `endEffect`, :40 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-99 | `skillengine/effect/ShieldMasteryEffect.h` (P5-04), new `ShieldMasteryEffect.cpp` | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java ShieldMasteryEffect.java:22 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. **In §2.4's subset.** |
| m5b2-f04-100 | `skillengine/effect/SignetBurstEffect.h` (P5-04) | override declarations `calculateDamage`, `calculate`; `using DamageEffect::calculate;`. 2 `AION_UNPORTED` stubs | pending (additive) | Java SignetBurstEffect.java:31 `calculateDamage`, :50 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-101 | `skillengine/effect/SignetEffect.h` (P5-04) | override declarations `calculate`; `using EffectTemplate::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java SignetEffect.java:22 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-102 | `skillengine/effect/SilenceEffect.h` (P5-04) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java SilenceEffect.java:25 `calculate`, :30 `startEffect`, :39 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-103 | `skillengine/effect/SimpleRootEffect.h` (P5-04) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java SimpleRootEffect.java:33 `calculate`, :49 `startEffect`, :64 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-104 | `skillengine/effect/SkillLauncherEffect.h` (P5-04) | override declarations `calculate`; `using EffectTemplate::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java SkillLauncherEffect.java:27 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-105 | `skillengine/effect/SleepEffect.h` (P5-04) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java SleepEffect.java:25 `calculate`, :31 `startEffect`, :44 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-106 | `skillengine/effect/SlowEffect.h` (P5-04) | override declarations `calculate`, `startEffect`, `endEffect`; `using BufEffect::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java SlowEffect.java:23 `calculate`, :28 `startEffect`, :35 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). **In §2.4's subset.** |
| m5b2-f04-107 | `skillengine/effect/SnareEffect.h` (P5-04) | override declarations `calculate`, `endEffect`, `startEffect`; `using BufEffect::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java SnareEffect.java:23 `calculate`, :28 `endEffect`, :34 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). **In §2.4's subset.** |
| m5b2-f04-108 | `skillengine/effect/SpellAtkDrainEffect.h` (P5-04), new `SpellAtkDrainEffect.cpp` | override declarations `resolveMagicalCritical` (protected), `onPeriodicAction`. 2 `AION_UNPORTED` stubs | pending (additive) | Java SpellAtkDrainEffect.java:26 `resolveMagicalCritical`, :31 `onPeriodicAction`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. |
| m5b2-f04-109 | `skillengine/effect/SpellAttackEffect.h` (P5-04), new `SpellAttackEffect.cpp` | override declarations `resolveMagicalCritical` (protected), `startEffect`, `onPeriodicAction`; `using AbstractOverTimeEffect::startEffect;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java SpellAttackEffect.java:23 `resolveMagicalCritical`, :28 `startEffect`, :36 `onPeriodicAction`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `startEffect` visible that the declarations would hide (hub-headers.md §9.1). **In §2.4's subset.** |
| m5b2-f04-110 | `skillengine/effect/SpinEffect.h` (P5-04) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java SpinEffect.java:26 `calculate`, :37 `startEffect`, :50 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-111 | `skillengine/effect/StaggerEffect.h` (P5-04) | override declarations `startEffect`, `calculate`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java StaggerEffect.java:33 `startEffect`, :50 `calculate`, :72 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-112 | `skillengine/effect/StatdownEffect.h` (P5-04), new `StatdownEffect.cpp` | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java StatdownEffect.java:17 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. **In §2.4's subset.** |
| m5b2-f04-113 | `skillengine/effect/StatupEffect.h` (P5-04), new `StatupEffect.cpp` | override declarations `endEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java StatupEffect.java:17 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. **In §2.4's subset.** |
| m5b2-f04-114 | `skillengine/effect/StumbleEffect.h` (P5-04) | override declarations `startEffect`, `calculate`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java StumbleEffect.java:33 `startEffect`, :52 `calculate`, :74 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). |
| m5b2-f04-115 | `skillengine/effect/StunEffect.h` (P5-04) | override declarations `calculate`, `startEffect`, `endEffect`; `using EffectTemplate::calculate;`. 3 `AION_UNPORTED` stubs | pending (additive) | Java StunEffect.java:25 `calculate`, :30 `startEffect`, :42 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declarations would hide (hub-headers.md §9.1). **In §2.4's subset.** |
| m5b2-f04-116 | `skillengine/effect/SummonEffect.h` (P5-04) | override declarations `calculate`; `using EffectTemplate::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java SummonEffect.java:42 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |
| m5b2-f04-117 | `skillengine/effect/SummonServantEffect.h` (P5-04) | protected `Ref<Servant> spawnServant(model::Effect&, int32_t spawnDuration, NpcObjectType npcObjectType, float x, float y, float z) const;` (also called by SummonSkillAreaEffect and SummonTotemEffect; a new object, hub-headers.md §5). 1 `AION_UNPORTED` stub | pending (additive) | Java SummonServantEffect.java:47 `spawnServant`. Additive: non-virtual members Java declares (hub-headers.md §9.1 declares every Java method, private ones included). |
| m5b2-f04-118 | `skillengine/effect/TransformEffect.h` (P5-04) | override declarations `endEffect`, `startEffect`. 2 `AION_UNPORTED` stubs | pending (additive) | Java TransformEffect.java:59 `endEffect`, :79 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. **In §2.4's subset.** |
| m5b2-f04-119 | `skillengine/effect/WeaponDualEffect.h` (P5-04) | override declarations `startEffect`, `endEffect`. 2 `AION_UNPORTED` stubs | pending (additive) | Java WeaponDualEffect.java:16 `startEffect`, :26 `endEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. **In §2.4's subset.** |
| m5b2-f04-120 | `skillengine/effect/WeaponMasteryEffect.h` (P5-04), new `WeaponMasteryEffect.cpp` | override declarations `startEffect`. 1 `AION_UNPORTED` stub | pending (additive) | Java WeaponMasteryEffect.java:29 `startEffect`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. **In §2.4's subset.** |
| m5b2-f04-121 | `skillengine/effect/XPBoostEffect.h` (P5-04), new `XPBoostEffect.cpp` | override declarations `calculate`; `using BufEffect::calculate;`. 1 `AION_UNPORTED` stub | pending (additive) | Java XPBoostEffect.java:17 `calculate`. Additive: the virtuals already exist on `EffectTemplate` (or its shells), so no vtable slot is added and `override` is compiler-checked. The using-declaration keeps the base overloads of `calculate` visible that the declaration would hide (hub-headers.md §9.1). |

## Wave 5b-2, stage 1 part 2: the cast engine and the effect core (2026-09-23)

The requests of m5b2-plan.md's part 2 lanes, the cast engine (P5-02a, items S-01..S-08) and the effect core (P5-02b, K-01..K-07), filed by
the integrator's fix-cross lane from the working tree. **m5b2-p2-1..4 are applied on the lanes' branch** (read off the diff against
`29009d778`); **m5b2-p2-5..10 are not applied**: their callers carry a file-local stand-in, a null guard or Java's exact arithmetic under UB
meanwhile, and each row names that workaround so the change can remove it.

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| m5b2-p2-1 | `skillengine/condition/Conditions.h` (P5-02a, the requesting chunk's own frozen header), new `Conditions.cpp` | declare Java's four methods, non-virtual: `bool validate(model::Skill&) const`, `bool canValidate(model::Skill&) const`, `bool validate(gameserver::model::stats::calc::Stat2&, gameserver::model::stats::calc::functions::IStatFunction&) const`, `bool validate(model::Effect&) const`; with them the includes `model/stats/calc/functions/fwd.h`, `model/stats/calc/fwd.h`, `skillengine/model/fwd.h`; the bodies in the new `Conditions.cpp` | **approved** (2026-09-23, applied) | Conditions.java:41-75. Every cast asks them: Skill.preCastCheck/preUsageCheck/endCondCheck `validate(this)` (Skill.java:817-838) and canPayCastCosts `canValidate(this)` (:188-189); EffectTemplate's effect and sub-effect conditions `validate(effect)` (EffectTemplate.java:332, :440) and StatFunction's `validate(stat, this)` (StatFunction.java:69, :73). Additive: `Conditions` is a K1 `StaticTemplate` without virtuals, so no layout change; the shell's comment said the families were P5-02's behaviour |
| m5b2-p2-2 | `skillengine/condition/ChainCondition.h` (P5-02a, own frozen header) | private `bool shouldReset(model::ChainSkills& chain, model::Skill& env) const;` | **approved** (2026-09-23, applied) | Java private ChainCondition.shouldReset (ChainCondition.java:55), called by `validate(Skill)`. Additive, non-virtual, private (hub-headers.md §9.1 declares every Java method) |
| m5b2-p2-3 | `skillengine/condition/{HpCondition,MpCondition,WeaponCondition}.h`, `skillengine/action/{HpUseAction,MpUseAction}.h` (P5-02a, own frozen headers) | private `int32_t getCost(model::Skill& skill) const;` in `HpCondition`, `MpCondition`, `HpUseAction`, `MpUseAction`; private `bool isValidWeapon(runtime::Ptr<gameserver::model::gameobjects::Creature> creature) const;` in `WeaponCondition`, with the includes `model/gameobjects/fwd.h` and `runtime/lifetime/Ref.h` | **approved** (2026-09-23, applied) | Java private helpers: HpCondition.java:49, MpCondition.java:48, HpUseAction.java:49, MpUseAction.java:48 `getCost(Skill)` (value + delta * skill level, or the ratio of the max) and WeaponCondition.java:45 `isValidWeapon(Creature)`. Additive, non-virtual. `isValidWeapon` takes a `Ptr`, not a reference: `validate(Stat2, ...)` passes `stat.getOwner()`, and Java's `instanceof Player` answers false for null (hub-headers.md §5.1) |
| m5b2-p2-4 | `controllers/ControllerStandIns.h` (P4-11b, the requesting chunk's own frozen header) | **delete** the declaration `chargeSkillGetAndUse` and its `AION_UNPORTED` body in `ControllerStandIns.cpp`, and with it the header's now unused `#include "aion/gameserver/skillengine/model/fwd.h"` | **approved** (2026-09-23, applied) | m5b2-plan.md P-04's second half, the last stand-in of the P5-02 section: `ChargeSkill` has a header since S-07 and `SkillEngine::getChargeSkill`/`ChargeSkill::useSkill` are ported (S-01, S-07), so `CreatureController::useChargeSkill` calls them the way CreatureController.java:468-497 writes them. The same shape as m5b-2/m5b-3/m5b2-1 |
| m5b2-p2-5 | new `skillengine/model/SubEffectTypeInfo.h` (P5-02a, the enum's chunk) | the companion of the generated enum `SubEffectType`: `constexpr int8_t getId(SubEffectType)` from Java's constructor data (NONE 0, SPIN 0, PULL 2, PULL_NPC 6, STUMBLE 4, STAGGER 4, OPENAERIAL 4, SIMPLE_MOVE_BACK 12) | **approved** (2026-09-23): additive new file; applied by the first lane that touches `Effect.cpp`'s stand-in (part 3 or stage 2) | Java SubEffectType.java:8-15, `getId()` :27. `Effect.cpp` (P5-02b) needs it for `getSuccessfulEffectsAsByte` (Effect.java:975 `sucEffects += subEffectType.getId()`) and carries the file-local stand-in `subEffectTypeId` (Effect.cpp:93) until the companion exists; the enum's chunk owns its companion (hub-headers.md §13 `TODO(enum)`), so P5-02b cannot add it itself |
| m5b2-p2-6 | new `skillengine/effect/AbnormalStateInfo.h` (P5-03), new `skillengine/model/ShieldTypeInfo.h` (P5-02a) | the companions of the generated enums: `constexpr int32_t getId(AbnormalState)` (the bit of a single state, the union of a compound one) and `constexpr int32_t getId(ShieldType)` | **approved** (2026-09-23): additive new files; stage 1 part 3's effects-al lane applies `AbnormalStateInfo.h`, the P5-02a half follows with it | Java AbnormalState.java:60 and ShieldType.java:21 `getId()`. `Effect.cpp` and `EffectController.cpp` (P5-02b) read them through the controllers' stand-ins `detail::getAbnormalStateId` and `detail::shieldTypeId` in `controllers/ControllerSupport.h` (Effect.cpp:281, :456, :463; EffectController.cpp:59-61, :351); the stand-ins are removed when the companions land |
| m5b2-p2-7 | `model/skill/NpcSkillTemplateEntry.h` (P5-02b, own frozen header) | private `bool hasCarvedSignet(runtime::Ptr<gameobjects::VisibleObject> curTarget, const skillengine::model::SkillTemplate* skillTemp, int32_t signetLvl) const;` and private `void spawnNpc(gameobjects::Npc& npc, const templates::npcskill::NpcSkillSpawn& spawn) const;` | **approved** (2026-09-23), **applied** (2026-09-24) by stage 2's npc-abilities lane under its P5-02b lease: both bodies are members now, unchanged except two locals renamed against C4458 (`template_` -> `spawnTemplate`, `count` -> `spawnCount`) | Java private NpcSkillTemplateEntry.hasCarvedSignet(VisibleObject, SkillTemplate, int) (NpcSkillTemplateEntry.java:133) and spawnNpc(Npc, NpcSkillSpawn) (:164), called by conditionReady and fireOnEndCastEvents. hub-headers.md §9.1 declares every Java method; meanwhile both are file-local functions of `NpcSkillTemplateEntry.cpp` (:52, :80), which read nothing of the entry, and the class comment says the header does not declare them yet |
| m5b2-p2-8 | `ai/handler/ShoutEventHandler.h` (P5-05) | `static void onCast(NpcAI& npcAI, runtime::Ptr<model::gameobjects::Creature> firstTarget);` instead of `model::gameobjects::Creature& firstTarget` | **approved** (2026-09-23): Java's parameter is nullable; **applied** (2026-09-24) by stage 2's npc-abilities lane (P5-05), and the stage-2 join dropped `Skill.cpp`'s `if (target)` guard, so `Skill::startCast` passes the nullable first target as Java does | Java ShoutEventHandler.java:123-126 `onCast(NpcAI, Creature firstTarget)` is a no-op for null (`firstTarget instanceof Player`), and Skill.startCast passes the nullable first target of every npc cast of target type 0 (Skill.java:504-506). With the reference signature `Skill.cpp:611` skips the call for a null target, which is exact today but moves Java's null handling out of the handler (hub-headers.md §5.1: a Java parameter that may be null is a `Ptr`) |
| m5b2-p2-9 | `services/RecallService.h` (P5-08) | `static bool validateCast(model::gameobjects::player::Player& caster, runtime::Ptr<model::gameobjects::VisibleObject> target);` instead of `model::gameobjects::VisibleObject& target` | **approved** (2026-09-23): Java's parameter is nullable; applied by the next lane that owns P5-08 (M5e/M5f at the latest). Until then a recall skill cast without a target throws NPE (Skill.cpp:858-860) | Java RecallService.java:115-139: the flying and position checks (:116-123, each with its own message) come BEFORE `target instanceof Player` (:124), which answers STR_SKILL_TARGET_IS_NOT_VALID for null; Skill.isInvalidRecall passes the nullable first target (Skill.java:742-744). With the reference signature `Skill.cpp:858-860` dereferences a null first target - a NullPointerException where Java sends the flying, position or invalid-target message. The divergence is live for a recall skill cast without a target; the Ptr parameter removes it |
| m5b2-p2-10 | `skillengine/effect/AbstractOverTimeEffect.h` (P5-03) | `getDuration2()`: `return static_cast<int32_t>(static_cast<uint32_t>(duration2) + 1000u);` instead of `return duration2 + 1000;` (AbstractOverTimeEffect.h:21) | **approved** (2026-09-23): applied by stage 1 part 3's effects-al lane | Java AbstractOverTimeEffect.java:64-67 is int arithmetic and wraps; in C++ `duration2 + 1000` is signed overflow, UB, for a `duration2` above 2147482647 (docs/CONVENTIONS.md "Signed overflow is UB in C++ ... use unsigned arithmetic and cast back"). The m5b2 skills oracle models Java's wrap (`effectiveDuration2`, tools/oracle/m5b2/skills.py) and its fixture pins it (skill 1017, `duration2="2147483000"`), so the C++ body must wrap the same way. An inline body in a frozen header, hence the request; part 3's effects-al lane ports the class |
| m5b2-p2-11 | `skillengine/SkillEngine.h` (P5-02a, hub header) | `applyEffectDirectly(int32_t skillId, runtime::Ptr<Creature> effector, Creature& effected)`: the effector nullable | **deferred** (2026-09-23) | Filed from the fix-cast lane: `Skill::startPenaltySkill` dereferences a null first target where Java passes null on into `new Effect(null, ...)` (docs/deviations/P5-02a.md). A signature change on a hub header with many callers, for a case no gate, no live path and no penalty-skill template is known to reach; revisit when one does |

## Wave 5b-2, stage 1 part 3: the effect classes and the attack effect-engine half (2026-09-23)

Filed by the integrator from the part 3 lanes' reports (effects-al P5-03, effects-mz P5-04, attack-effects P5-01). None is applied; each
caller carries a file-local stand-in or Java's exact behaviour under a narrower type meanwhile, and each row names that workaround.

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| m5b2-p3-1 | new `model/stats/calc/functions/StatWeaponMasteryFunction.{h,cpp}` and `StatShieldMasteryFunction.{h,cpp}` (P5-01) | declaration headers shaped like `StatArmorMasteryFunction.h` (RefCounted plus `StatRateFunction`, `create()`, `apply`, member `const ItemGroup itemGroup`); the file-local classes in `WeaponMasteryEffect.cpp` and `ShieldMasteryEffect.cpp` move there unchanged | **approved** (2026-09-23): new files, no signature changes; applied by the next lane that owns P5-01 | `fwd.h` declares both and nothing defines them (docs/deviations/P5-01.md B-06) |
| m5b2-p3-2 | `controllers/CreatureController.h` (P4-11b) | `std::optional<skillengine::model::HopType>` in both `Effect&` overloads of `onAttack` | **approved** (2026-09-23): applied by the next lane that owns P4-11b | Java passes the nullable template `hopType`; 260 of the 601 `<spellatk>` have none. The C++ passes `SKILLLV` meanwhile, which `AggroList.addDamage` treats the same. Filed by both effect lanes |
| m5b2-p3-3 | new `skillengine/model/EffectReserved_ResourceTypeInfo.h` (P5-02b) | the companion of the generated enum: `of(HealType)` | **approved** (2026-09-23): additive new file | A file-local helper in `AbstractHealEffect.cpp` and `HealOverTimeEffect.cpp` meanwhile |
| m5b2-p3-4 | `utils/stats/StatArmorMasteryFunction.h` (P5-01) | `create` takes `std::optional<ItemSubType>` | **deferred** (2026-09-23) | Java allows a null `armorType`; the C++ throws Java's NullPointerException instead, and no shipped template reaches it |
| m5b2-p3-5 | `skillengine/effect/HideEffect.h` (P5-03) | a second friend line, `friend struct HideEffect_ActionObserver_3;` | **approved** (2026-09-23): the same precedent as the part 3 pre-approval, which allowed one line per class | `HideEffect$3` reads `buffCount` through the static `HideEffect_ActionObserver::buffCountOf` meanwhile |

## Wave 5b-2, stage 2: npc abilities and the gate (2026-09-24)

Filed by the stage-2 join from the npc-abilities lane's and the gate lane's reports. None is applied; the workaround of each is named.

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| m5b2-s2-1 | `skillengine/model/Effect.h` (P5-02b) | a const accessor for the private `reservedEffects` (a count, or a `forEach` over the stored `EffectReserved`s) | pending (additive) | CheckOutput.cpp heldEffects (G-07) can only bound live `EffectReserved` by the held Effects' templates whose class stores one (`storesReserved`); with the accessor X13 and M5b's Q2b assert `live EffectReserved == what the held Effects store` (m5b2-plan.md §10.7) |
| m5b2-s2-2 | `ai/manager/SkillAttackManager.h` (P5-05) | private `static runtime::Ptr<NpcSkillEntry> getNpcSkillEntryIfNotTooFarAway(Npc& owner, runtime::Ptr<NpcSkillEntry> entry);`, `static bool isReady(Npc& owner, NpcSkillEntry& entry);`, `static bool targetTooFar(Npc& owner, NpcSkillEntry& entry);` | pending (additive, private) | Java's three private statics (SkillAttackManager.java:176-213); hub-headers.md §9.1 declares every Java method. File-local functions of `SkillAttackManager.cpp` meanwhile |
| m5b2-s2-3 | `ai/manager/SkillAttackManager.h` (P5-05), `handlers/ai/GeneralNpcAI.h` (P5-05), `model/skill/NpcSkillList.h` (P5-02b) | comments only: SkillAttackManager.h's class comment still says "M5b-1 stub ... every body is an AION_PARTIAL"; GeneralNpcAI.h's `chooseSkillAttack` doc still says "an AION_PARTIAL that answers false"; NpcSkillList.h's class comment still says the entries of an npc with skills "wait for NpcSkillTemplateEntry, P5-02" | pending (comment only) | N-01/N-02 ported all of it in stage 2; the comments describe M5b-1 |

## Wave 5b-3 stage 0: the item-action API and the quest-drop signature (2026-09-24)

m5b3-plan.md item **I-01**, the header batch of its §7 (`m5b3-h01`..`h03`), applied by the integrator's stage-0 lane on HEAD `27726d32c`.
**Declarations and `AION_UNPORTED` stubs only, no behaviour**: the three typed lookups that were ported before (`getCraftLearnAction`,
`getPolishAction`, `getSkillUseAction`) keep their bodies and only move into Java order.

**The class list, re-derived from the Java.** `ItemActions`' `@XmlElements` (ItemActions.java:15-31) binds 32 classes, and they are exactly the
32 classes of the package that `extends AbstractItemAction` (a grep of the package; none outside it in `src` or `data/handlers`). The two other
files of the package get nothing: `CompositionAction` does not extend `AbstractItemAction` (its own `canAct`/`act` take two stones and are
called only by CM_COMPOSITE_STONES.java:71-74, as its javadoc says, CompositionAction.java:10-15) and `UseTarget` is an enum. So the plan's count holds:
**32 classes, 30 new `.cpp` files** (`DecomposeAction.cpp` and `EmotionLearnAction.cpp` existed). **74 new `AION_UNPORTED(` sites**
(`game-server/src`: 1,744 → 1,818): 64 `canAct`/`act` overrides, `EnchantItemAction`'s five-argument `act`, 9 `ItemActions` lookups.
`census.py --chunks P5-07`: the chunk's unported bodies 100 → 174, undeclared 130 → 56, shells 30 (95 bodies) → 0; the action package keeps
45 undeclared bodies (39 private helpers and non-generated getters of the 32 classes, `CompositionAction` 4, `UseTarget` 2), none of them on
M5b-3's path except `SkillUseAction::isIneffectiveHealSkill` (m5b3-h04 below).

**One change against the plan's text: `parentItem` is a `runtime::Ptr`, not an `Item&`** (m5b3-plan.md §7 h01 and D6 wrote
`gameobjects::Item& parentItem`). hub-headers.md §5.1 makes it nullable twice over: rule 1, CM_APPEARANCE.java:112-113 passes `null`
directly (`action.canAct(player, null, null)`, `action.act(player, null, item)`); rule 2, five bodies of the family compare it with `null`
(AnimationAddAction.java:38, EmotionLearnAction.java:40, EnchantItemAction.java:48, RideAction.java:48, TitleAddAction.java:29). With `Item&`
the M5c port of CM_APPEARANCE could not make its call and those five checks could not be ported, and fixing it later is the same layout change
over 32 classes again. `targetItem` is a `Ptr` as the plan says (CM_MEGAPHONE.java:54, IceChunkAI.java:33 pass `null`; six bodies compare it).
In `EnchantItemAction`'s non-virtual five-argument `act` only `supplementItem` is a `Ptr` (the varargs override passes `null` there,
EnchantItemAction.java:82; nothing passes or compares `null` for the parent or the target at that arity), as the plan wrote it.

**Nothing becomes reachable and no reachable behaviour changes.** No C++ code calls `canAct`, `act` or the new lookups yet: their Java callers
(CM_USE_ITEM, CM_MANASTONE, CM_APPEARANCE, CM_MEGAPHONE, CM_TUNE, CM_COMPOSITE_STONES, IceChunkAI, the Decompose player command) have no C++
file. The binders (`actions.bind.cpp`) still instantiate 32 concrete classes, which is the compiler's check that every class declares both
overrides with the base's exact signature (`override` on each).

**Verified** in the shared `build/msvc` (Debug): `aion_game_server`, `aion_gs_header_check` and the 48 game-server test targets (every
`aion_gs_*_tests` target of the build dir but the three `aion_gs_regscan_*` tool tests, which link none of the changed libraries) build with no
error and no warning; `aion_gs_items_tests` (32), `aion_gs_itemsvc_tests` (21), `aion_gs_cm_ak_tests` (66), `aion_gs_cm_lz_tests` (36),
`aion_gs_quest_tests` (15), `aion_gs_templates_tests` (78, the real item templates through the binders) and `aion_gs_dataholders_tests` (48)
pass; `tools.gen` and `tools.porting` pass; `lint_concurrency.py --werror --cycles=core game-server/src` 3,689 files, 0 findings;
`chunks.py check` 0 problems; `skeleton.py --fwd --check`, `--definitions` and `--guards` clean. The two compiler checks the batch relies on
were watched failing and restored byte for byte (sha256): removing `TitleAddAction`'s two overrides gives C2259 "cannot instantiate abstract
class" in the binder (`aion_gs_staticdata`), and the plan's `Item& parentItem` in `SkillUseAction::canAct` gives C3668 "did not override any
base class methods". `census.py`'s two `LIVE_CHECKS` rows for `SkillUseAction.canAct`/`act` move from `undeclared` to `unported` (the port
moved); its one remaining live failure, `NpcSkillTemplateEntry.hasCarvedSignet` (m5b2-p2-7 made it a member in stage 2), predates this batch.
**Reviewed 2026-09-24: approve.** After the review's fixes (decision cells, m5b3-h04's read-only list, a comment in `EnchantItemAction.h`)
the same 50 targets were rebuilt with no error and no warning (only `EnchantItemAction.cpp` and `actions.bind.cpp` recompiled), and the seven
test binaries above, `tools.gen`, `tools.porting`, the lint, `chunks.py check` and the three `skeleton.py` checks were run again and pass.

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| m5b3-h01 | `model/templates/item/actions/AbstractItemAction.h` (P5-07) | `virtual bool canAct(gameobjects::player::Player& player, runtime::Ptr<gameobjects::Item> parentItem, runtime::Ptr<gameobjects::Item> targetItem, std::initializer_list<std::any> params = {}) const = 0;` and `virtual void act(` the same `) const = 0;`, public, Java order; includes `<any>`, `<initializer_list>`, `model/gameobjects/fwd.h`, `model/gameobjects/player/fwd.h`, `runtime/lifetime/Ref.h`; the class comment names what `params.begin()[0]` holds for the four receivers that read it (DyeAction a `Ref<HouseObject>`, null for Java null; MultiReturnAction and InstanceTimeClear an `int32_t`; MegaphoneAction a `std::string`) | **reviewed 2026-09-24: approve** (the stage-0 review; a layout change needs the reviewer, hub-headers.md §14), applied in stage 0 by the integrator | **Layout** (two new pure virtuals on the base every action shell derives from; the precedent is shells-3). Java AbstractItemAction.java:26 `canAct`, :34 `act`, both abstract. `const` because action templates are `const X*` (hub-headers.md §9.1, static data); `Object... params` is `std::initializer_list<std::any>` with `= {}` (§7.4, overrides repeat the default; no overload with one parameter less exists); `parentItem`/`targetItem` nullable as above. `= 0` is allowed because every shell the binders instantiate declares the overrides in the same change (§9.1) |
| m5b3-h01-1 | `.../actions/AdoptPetAction.h` (P5-07), new `AdoptPetAction.cpp` | override declarations `canAct`, `act` (with the includes of m5b3-h01). 2 `AION_UNPORTED` stubs | **reviewed 2026-09-24: approve** (with m5b3-h01, whose layout change these overrides complete), applied in stage 0 by the integrator | Java AdoptPetAction.java:23 `canAct`, :28 `act` (`<adoptpet>`) |
| m5b3-h01-2 | `.../actions/AnimationAddAction.h`, new `.cpp` | the same | the same | AnimationAddAction.java:37 `canAct`, :46 `act` (`<animation>`) |
| m5b3-h01-3 | `.../actions/ApExtractAction.h`, new `.cpp` | the same | the same | ApExtractAction.java:31, :128 (`<apextract>`) |
| m5b3-h01-4 | `.../actions/AssemblyItemAction.h`, new `.cpp` | the same | the same | AssemblyItemAction.java:34, :48 (`<assemble>`) |
| m5b3-h01-5 | `.../actions/ChargeAction.h`, new `.cpp` | the same | the same | ChargeAction.java:33, :68 (`<charge>`) |
| m5b3-h01-6 | `.../actions/CosmeticItemAction.h`, new `.cpp` | the same | the same | CosmeticItemAction.java:31, :54 (`<cosmetic>`) |
| m5b3-h01-7 | `.../actions/CraftLearnAction.h`, new `.cpp` | the same, **`act` before `canAct`** (Java order) | the same | CraftLearnAction.java:26 `act`, :38 `canAct` (`<craftlearn>`) |
| m5b3-h01-8 | `.../actions/DecomposeAction.h`, existing `DecomposeAction.cpp` | the same, before the existing `validateRandomItemIds` (Java order); the header's `dataholders/fwd.h` include moved below the generated `.xml.h` like the other shells | the same | DecomposeAction.java:100, :118, `validateRandomItemIds` :404 (`<decompose>`) |
| m5b3-h01-9 | `.../actions/DecorateAction.h`, new `.cpp` | the same | the same | DecorateAction.java:17, :23 (`<housedeco>`) |
| m5b3-h01-10 | `.../actions/DyeAction.h`, new `.cpp` | the same; the class comment names `params.begin()[0]` | the same | DyeAction.java:31, :51, reading `params[0]` at :32, :52 (`<dye>`) |
| m5b3-h01-11 | `.../actions/EmotionLearnAction.h`, existing `EmotionLearnAction.cpp` | the same, before the existing `isLearnable` (Java order) | the same | EmotionLearnAction.java:39, :52, `isLearnable` :73 (`<learnemotion>`) |
| m5b3-h01-12 | `.../actions/EnchantItemAction.h`, new `.cpp` | the same **plus** `using AbstractItemAction::act;` and the public non-virtual overload `void act(gameobjects::player::Player& player, gameobjects::Item& parentItem, gameobjects::Item& targetItem, runtime::Ptr<gameobjects::Item> supplementItem, int32_t targetWeapon) const;` (include `<cstdint>`). 3 `AION_UNPORTED` stubs | the same | EnchantItemAction.java:45 `canAct`, :81 `act` (calls the overload with `(null, 1)`), :86 the overload ("necessary overloading to not change AbstractItemAction"); CM_MANASTONE.java:79-86 constructs the action (`new EnchantItemAction()`) and calls `canAct` and the overload (m5b3-plan.md D8). The using-declaration is D8's; it is redundant while the class declares the override itself (a using-declaration and an override of the same signature coexist, [namespace.udecl]), and harmless (`<enchant>`) |
| m5b3-h01-13 | `.../actions/ExpExtractAction.h`, new `.cpp` | the same as m5b3-h01-1 | the same | ExpExtractAction.java:30, :49 (`<expextract>`) |
| m5b3-h01-14 | `.../actions/ExpandInventoryAction.h`, new `.cpp` | the same | the same | ExpandInventoryAction.java:30, :38 (`<expandinventory>`) |
| m5b3-h01-15 | `.../actions/ExtractAction.h`, new `.cpp` | the same | the same | ExtractAction.java:25, :43 (`<extract>`) |
| m5b3-h01-16 | `.../actions/FireworksUseAction.h`, new `.cpp` | the same | the same | FireworksUseAction.java:21, :26 (`<fireworkact>`) |
| m5b3-h01-17 | `.../actions/InstanceTimeClear.h`, new `.cpp` | the same; the class comment names `params.begin()[0]` | the same | InstanceTimeClear.java:34, :44, reading `params[0]` at :35, :46 (`<instancetimeclear>`) |
| m5b3-h01-18 | `.../actions/MegaphoneAction.h`, new `.cpp` | the same, the parent parameter named `item` as in Java; the class comment names `params.begin()[0]` | the same | MegaphoneAction.java:32, :37, reading `params[0]` at :38 (`<megaphone>`) |
| m5b3-h01-19 | `.../actions/MultiReturnAction.h`, new `.cpp` | the same, the parent parameter named `item` as in Java; the class comment names `params.begin()[0]` | the same | MultiReturnAction.java:31, :36, reading `params[0]` at :38 (`<multireturn>`) |
| m5b3-h01-20 | `.../actions/PackAction.h`, new `.cpp` | the same as m5b3-h01-1 | the same | PackAction.java:23, :92 (`<pack>`) |
| m5b3-h01-21 | `.../actions/PolishAction.h`, new `.cpp` | the same | the same | PolishAction.java:34, :47 (`<polish>`) |
| m5b3-h01-22 | `.../actions/QuestStartAction.h`, new `.cpp` | the same | the same | QuestStartAction.java:35, :41 (`<queststart>`) |
| m5b3-h01-23 | `.../actions/ReadAction.h`, new `.cpp` | the same | the same | ReadAction.java:21, :26 (`<read>`) |
| m5b3-h01-24 | `.../actions/RemodelAction.h`, new `.cpp` | the same | the same | RemodelAction.java:20, :25 (`<remodel>`) |
| m5b3-h01-25 | `.../actions/RideAction.h`, new `.cpp` | the same | the same | RideAction.java:46, :72 (`<ride>`) |
| m5b3-h01-26 | `.../actions/SkillLearnAction.h`, new `.cpp` | the same | the same | SkillLearnAction.java:32, :53 (`<skilllearn>`) |
| m5b3-h01-27 | `.../actions/SkillUseAction.h`, new `.cpp` | the same (the one class M5b-3 ports, T-04) | the same | SkillUseAction.java:52, :100 (`<skilluse>`); its private `isIneffectiveHealSkill` (:82) is m5b3-h04 |
| m5b3-h01-28 | `.../actions/SummonHouseObjectAction.h`, new `.cpp` | the same | the same | SummonHouseObjectAction.java:22, :28 (`<houseobject>`) |
| m5b3-h01-29 | `.../actions/TamperingAction.h`, new `.cpp` | the same | the same | TamperingAction.java:33, :42 (`<tampering>`) |
| m5b3-h01-30 | `.../actions/TitleAddAction.h`, new `.cpp` | the same | the same | TitleAddAction.java:28, :41 (`<titleadd>`) |
| m5b3-h01-31 | `.../actions/ToyPetSpawnAction.h`, new `.cpp` | the same | the same | ToyPetSpawnAction.java:49, :70 (`<toypetspawn>`) |
| m5b3-h01-32 | `.../actions/TuningAction.h`, new `.cpp` | the same | the same | TuningAction.java:37, :62 (`<tuning>`) |
| m5b3-h02 | `model/templates/item/actions/ItemActions.h` (P5-07) | `const std::vector<std::unique_ptr<AbstractItemAction>>& getItemActions() const;` and `getEnchantAction`, `getHouseObjectAction`, `getDecorateAction`, `getDyeAction`, `getAdoptPetAction`, `getRemodelAction`, `getTuningAction`, `getRideAction`, each `const X* name() const` like the three existing lookups; all twelve now in Java order; includes `<memory>`, `<vector>`. 9 `AION_UNPORTED` stubs (T-04 ports them with the file's `firstActionOf` helper; `getItemActions` returns the bound vector, empty where Java's list is null) | approved by the integrator under the standing instruction (phase5-roadmap.md), applied in stage 0 (additive, batched without review, hub-headers.md §14; the stage-0 review of 2026-09-24 also approved it) | Additive (the items-3 / controllers-1 pattern). Java ItemActions.java:38 `getItemActions`, :42, :52, :72, :82, :92, :102, :122, :142; CM_USE_ITEM.java:85-86 needs `getItemActions`, EnchantItemAction.java:205 `getEnchantAction` |
| m5b3-h03 | `services/QuestService.h` (P5-06), `QuestService.cpp` | `getQuestDrop(runtime::RcHashSet<runtime::Ref<model::drop::DropItem>>& dropItems, int32_t index, Npc& npc, const std::vector<Ptr<Player>>& players, Player& player)` instead of `const std::unordered_set<runtime::Ptr<model::drop::DropItem>>& dropItems`; the definition's signature follows (still `AION_UNPORTED`, L-04); `#include <unordered_set>` removed (nothing else in the header used it); a comment on the declaration | **reviewed 2026-09-24: approve** (the stage-0 review; a signature change needs the reviewer, hub-headers.md §14), applied in stage 0 by the integrator | **Signature.** Java adds to the caller's set (QuestService.java:682-728 `dropItems.add(...)`), which a const set of borrowed pointers cannot do; the caller's set is DropRegistrationService's live `RcHashSet<Ref<DropItem>>` (`DropRegistrationService.h:30`, the type `addGlobalDrops`/`addDropItems` already take). No C++ caller existed, so nothing else recompiles beyond `QuestService.h`'s includers |
| m5b3-h04 | `model/templates/item/actions/SkillUseAction.h` (P5-07) | private `static bool isIneffectiveHealSkill(const std::vector<std::unique_ptr<skillengine::effect::EffectTemplate>>& effects, const std::vector<runtime::Ptr<gameobjects::Creature>>& effectedList);` (includes `<memory>`, `<vector>`, `skillengine/effect/fwd.h`; `Ptr` and `Creature` come from m5b3-h01's `runtime/lifetime/Ref.h` and `model/gameobjects/fwd.h`), between `canAct` and `act` (Java order). `canAct` passes `skill.getEffectedList().snapshot()` | **approved** (2026-09-24, integrator): additive private static; applied by the next lane that owns P5-07 (M5c), which moves the file-local isIneffectiveHealSkill into the class | Java private static SkillUseAction.isIneffectiveHealSkill(List<EffectTemplate>, List<Creature>) (SkillUseAction.java:82-97), called by `canAct` (:75) when `gameserver.items.ignore_potions_at_full_health` is true; the plan counts it among T-04's three bodies (§2.3, §3) but §7 declares only the API, so T-04 would otherwise need a mid-wave request or a file-local function (the m5b2-s2-2 workaround). Types: `SkillTemplate::getEffects()->getEffects()` (the generated `Effects.xml.inc:10`) and `Skill::getEffectedList()` (`Skill.h:252`, an `ArrayList<Ref<Creature>>`, whose `snapshot()` is a `std::vector<Ptr<Creature>>`). The body only streams the list (`allMatch`, :94-95), so the parameter is hub-headers.md §7.1's read-only kind, `const std::vector<Ptr<X>>&`, not the shim by non-const reference that §7.1 keeps for a list the callee fills (as `Properties::validateEffectedList` fills the effected list); `canAct` has just filled the list on this thread (`skill.canUseSkill(CAST_START)`, :73), so the snapshot holds what Java streams. Non-virtual, private (hub-headers.md §9.1) |

## Wave 5b-3 stage 1: loot, items, player side, effects, gate harness (2026-09-24)

Filed by the stage-1 integration step from the five lanes' reports (m5b3-plan.md §16). **None is applied**: stage 1 ran with the headers frozen
at stage 0, and each lane wrote the workaround named in the Reason cell. m5b3-h04 above is still pending: T-04 wrote `isIneffectiveHealSkill`
as a file-local function of `SkillUseAction.cpp` (docs/deviations/P5-07.md), which moves into the class unchanged when h04 lands. m5b2-p3-2
(the optional `HopType` of the two `CreatureController::onAttack` Effect overloads, approved 2026-09-23 for the next lane that owns P4-11b)
now also covers `ProcAtkInstantEffect` and `PoisonEffect` (docs/deviations/P5-04.md).

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| m5b3-loot-h01 | `model/templates/quest/QuestDrop.h` (P4-07b) | a virtual destructor on `QuestDrop` | **deferred** (2026-09-24, integrator): a layout change (a vtable on a generated static-data class) for an answer the membership workaround already gives now that QuestsData sets every quest id; revisit if a caller ever needs a polymorphic delete or a real HandlerSideDrop type test | Java `drop instanceof HandlerSideDrop` (QuestService.java `isQuestDrop`) becomes a `dynamic_cast`. Meanwhile `QuestService.cpp`'s `asHandlerSideDrop` answers by membership in the drop's own quest template's `<quest_drop>` list, which gives the same answer for both sources of drops now that `QuestsData::afterUnmarshal` sets every quest id (docs/deviations/P5-06.md, P4-09.md) |
| m5b3-p04-1 | `model/gameobjects/player/Equipment.h` (P4-12) | `friend struct Equipment_Runnable;` (the `IdianStone_ActionObserver` precedent) | **approved** (2026-09-24, integrator): additive, the IdianStone_ActionObserver precedent; applied by the next lane that owns P4-12, which removes the member-function-pointer stand-in | `Equipment$3.run` calls the private `equip` and reads `owner`. Meanwhile `soulBindItem` hands `&Equipment::equip` to the request handler as a C++-only member-function-pointer field (`// fieldmap:` waiver) and the task passes the responder, the same player (docs/deviations/P4-12.md) |
| m5b3-p04-2 | `model/gameobjects/player/Equipment.h` (P4-12) | comment only: the class comment says the soul-bind callback structs are "defined in Equipment.cpp when soulBindItem is ported" | **approved** (2026-09-24, integrator): comment only; applied with m5b3-p04-1 | P-04 ported them in stage 1 |
| m5b3-p01-1 | `restrictions/PlayerRestrictions.h` (P5-13) | comment only: the class comment says "The remaining bodies are AION_UNPORTED" after listing M5b-1's and M5b-2's | **approved** (2026-09-24, integrator): comment only; applied by the next lane that owns P5-13 | P-01 ported `canUseItem`, `canChangeEquip` and `canTrade`; `canChat` and the three `canInviteTo*` remain |
| m5b3-e-1 | `skillengine/effect/FearEffect.h` (P5-03) | `friend struct FearEffect_ActionObserver;` (the `RootEffect.h` precedent) | **approved** (2026-09-24, integrator): additive, the RootEffect.h precedent; applied by the next lane that owns P5-03 | `FearEffect$1.attacked` reads the protected `resistchance`; meanwhile `startEffect` passes a copy into the observer (the template is immutable static data, so the value is the one Java reads; docs/deviations/P5-03.md) |
| m5b3-i-1 | `CheckOutput.h` (P5-14) | comment only: `runFinalCensus`'s doc says it "runs Reclaimer::reclaimNow() twice and writes census.txt"; since the stage-1 integration it keeps scanning while a reported count is 0 or still changing, and writes a leak only when two checks in a row agree (docs/deviations/P5-14.md) | **approved** (2026-09-24, integrator): comment only; applied by the next lane that owns P5-14 | The integration fixed the census race `gs.scenario.m5b` hit twice in a row in `CheckOutput.cpp` alone; the header was left frozen |
| m5b3-e-2 | `skillengine/effect/FearEffect.h` (P5-03) | a nested `struct FearTask;` (fieldmap.json's `cppName` is `FearEffect::FearTask`) | **approved** (2026-09-24, integrator): additive nested declaration; applied with m5b3-e-1, moving FearEffect_FearTask in unchanged | Java's nested `record FearTask`; meanwhile a file-local `FearEffect_FearTask` in `FearEffect.cpp` with a `// fieldmap-class:` marker, which moves there unchanged (docs/deviations/P5-03.md) |

## Wave 5c stage 0: the P5-09 split and the item-action, Profession and CraftingTask declarations (2026-09-24)

m5c-plan.md items **I-01** (decision D1, the manifest split) and **I-02** (the header batch of its §7 and the `CraftingTask` shell), applied by
the integrator's stage-0 lane on HEAD `5fbb03a08` (M5b-3 complete). **Declarations and `AION_UNPORTED` stubs only, no behaviour**: the one
ported body the batch touches, `DecomposeAction::validateRandomItemIds`, reads the same ids from the new members in the same order (below).
Not applied, because §7 does not list them for M5c: m5b3-h04 (`SkillUseAction::isIneffectiveHealSkill`), m5b3-p04-1/-2, m5b3-p01-1, m5b3-e-1/-2,
m5b3-i-1, m5b3-loot-h01 and the approved-but-unapplied rows of the earlier waves; they stay with the next lane that owns their chunk.

**What §7 lists and what it does not.** §7's stage-0/1 rows are m5c-h01 to m5c-h04 below (E-03, E-04, P-07, C-01) and the new `CraftingTask`
files; `CraftLearnAction.h` needs nothing beyond m5b3-h01 (census: only `canAct`/`act`, both declared); `StatEnumInfo.h` (C-03),
`TemporaryTradeTimeTask` (P-04), `PostboxAI` (D-05) and the 23 client packets are new files their lanes create; `CheckOutput` is G-06's, stage 2.
The census of the four classes at HEAD matches §7 exactly: `EnchantItemAction` 5 undeclared (`isSuccess`, `getMaxLevel`, `getMinLevel`,
`isSupplementAction`, `checkSupplementLevel`), `DecomposeAction` 4 (`postValidate`, `finishUse`, `filterItemsByLevel`, `containsSpecialCubeItems`;
`validateItemIds` is credited file-local, `isValidItemId` inlined), `TuningAction` 1, `Profession` 6, `CraftingTask` 9 with no C++ class.

**Two corrections to §7's wording, both from the Java.** `getMaxLevel` and `getMinLevel` are `public` in Java (EnchantItemAction.java:178, 182;
today only `checkSupplementLevel` calls them, on the supplement's action) and `isSupplementAction` is package-private, so the three are `public`
(Java access, hub-headers.md §4: package-private is `public`); §7 wrote "(private)" for all five. And `CraftingTask`'s `responder` is
a nullable `runtime::Ptr<StaticObject>`, not the `StaticObject&` skeleton.py drafts from the call sites alone: `startCrafting` casts
`player.getKnownList().getObject(targetObjId)` and a morph (skill 40009) skips the target check (CraftService.java:101, 123, 149-157), so the
responder can be null, which AbstractInteractionTask.java:29-32 replaces with the requester (hub-headers.md §5.1, "review the parameters that
receive ... null without a literal").

**Verified** in the private build directory `build/c0-int` (Debug, configured from the msvc preset with `-DAION_BUILD_CHAT_SERVER=OFF`):
`aion_game_server`, `aion_gs_header_check`, `aion_gs_m4_database` and all 51 `aion_gs_*_tests` targets build with no error; the only warnings
are C4390 in the dialog lane's in-progress `CM_SHOW_DIALOG.cpp:35` and `CM_DIALOG_SELECT.cpp:64` (two earlier attempts stopped at the npc-leak
lane's in-progress `LeakCensus.h`/`.cpp`, which that lane then finished). The binders (`actions.bind.cpp`) compiled against the three changed
action headers. Passing, each case in its own process (ctest): `aion_gs_economy_tests` 50/50 (the moved and split cases, under their new
names), `aion_gs_itemsvc_tests` 69/69, `aion_gs_items_tests` 32/32, `aion_gs_dataholders_tests` 48/48 (`DecomposeActionIdsTest` on the
members), `aion_gs_templates_tests` 78/78, `aion_gs_skills_tests` 203/203, the `CM_USE_ITEM`/`CM_MANASTONE` suites of `aion_gs_cm_lz_tests`
26/26; `tools.porting` 71, the `tools.gen` fieldmap and skeleton-tree tests 48; `census.py --self-check` 0 synthetic failures (the 4 live
ones predate the batch); `chunks.py check`, `lint_concurrency.py --werror --cycles=core game-server/src` (3,726 files, 0 findings),
`fieldmap.py --check`, `skeleton.py --fwd --check`, `--definitions`, `--guards` clean. **Watched failing and restored byte for byte
(sha256):** renaming P5-09b to P5-09 in the manifest fails test_chunks.py's design set; `'P5-09c': 'M5j'` in census.py fails its milestone
self-check; removing `CraftingTask::onInteractionFinish` gives C2259 "cannot instantiate abstract class" in `CraftingTask::create`.

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| m5c-m01 | `game-server/chunks.cmake` (integrator), `tools/porting/tests/test_chunks.py`, `tools/porting/census.py`, handlers-and-porting-plan.md's chunk table | P5-09 becomes **P5-09a** (`services/{drop,reward}/**`, `model/guide/**`, `AtreianPassportService`, `BonusPackService`, `FactionPackService`), **P5-09b** (`services/trade/**`, `BrokerService`, `ExchangeService`, `PrivateStoreService`, `TradeService`) and **P5-09c** (`services/{mail,craft}/**`, `model/craft/**`, `RecipeService`), three parts of `aion_gs_economy` with the same Java globs; the tests move into `tests/economy/P5-09a`, `P5-09b`, `P5-09c` (m5c-plan.md §17 lists every file); census.py maps P5-09a to M5b-3 and P5-09b/c to M5c instead of its P5-09 path split | **approved by the integrator under the standing instruction (phase5-roadmap.md), applied in stage 0** (m5c-plan.md D1, an integrator decision) | A chunk is the unit of ownership: unsplit, the trade services and mail would be one serial lane through M5c's stages 1 and 2. The P5-02a/b split (`2e47bbb64`) is the precedent, test directories and the test_chunks.py/handlers-and-porting-plan.md pair included. `chunks.py check`: 69 chunks, 81 parts, 0 problems; the census totals of M5b-3 and M5c are unchanged by the split (94 open P5-09 bodies = 18 + 38 + 38) |
| m5c-h01 | `model/templates/item/actions/EnchantItemAction.h` (P5-07), `EnchantItemAction.cpp` | after the five-argument `act`, in Java order: private `bool isSuccess(gameobjects::player::Player& player, gameobjects::Item& parentItem, gameobjects::Item& targetItem, runtime::Ptr<gameobjects::Item> supplementItem, int32_t targetWeapon) const;`; public `int32_t getMaxLevel() const;`, `int32_t getMinLevel() const;`, `bool isSupplementAction() const;`; private `bool checkSupplementLevel(gameobjects::player::Player& player, const ItemTemplate* supplementTemplate, const ItemTemplate* targetItemTemplate) const;`; include `model/templates/item/fwd.h`. 5 `AION_UNPORTED` stubs | **approved by the integrator under the standing instruction (phase5-roadmap.md), applied in stage 0** (additive, hub-headers.md §14) | EnchantItemAction.java:160 `isSuccess`, :178 `getMaxLevel`, :182 `getMinLevel`, :194 `isSupplementAction`, :198 `checkSupplementLevel`; E-03 ports them. `supplementItem` is a `Ptr` because the five-argument act hands on its own nullable one (the varargs act passes null, :82). `const` like every hand-written method of a static-data class (hub-headers.md §9.1). The anonymous ItemUseObserver of act (:93) reads only captured locals, so it needs no friend declaration: it is the callback struct fieldmap.py prints, defined in the `.cpp` |
| m5c-h02 | `model/templates/item/actions/DecomposeAction.h` (P5-07), `DecomposeAction.cpp`, `generated/concurrency/fieldmap.toml` (+ `fieldmap.json` regenerated) | private `bool postValidate(Player& player, Item& parentItem, runtime::Ptr<Item> targetItem) const;`, `void finishUse(Player& player, Item& parentItem, runtime::Ptr<Item> targetItem, const ExtractedItemsCollection* selectedCollection) const;`, `std::optional<std::vector<const ExtractedItemsCollection*>> filterItemsByLevel(Player& player, const std::vector<ExtractedItemsCollection>* itemsCollections) const;`, `bool containsSpecialCubeItems(const std::vector<ExtractedItemsCollection>& itemGroups, Player& player) const;` (4 `AION_UNPORTED` stubs); and the static reward tables the `.cpp` comment deferred: `static const std::map<Race, std::vector<int32_t>> chunkEarth, chunkSand, premiumOphidanRecipe;` (defined in the `.cpp`) and `static constexpr std::array<int32_t, N>` `chunkRock` (15), `chunkGemstone` (8), `scrolls` (7), `potion` (6), `lesser_potions` (7), `potion_50` (7), `illusion_godstones` (17) with their literal contents; includes `<array>`, `<cstdint>`, `<map>`, `<optional>`, `<vector>`, `model/Race.h`, `model/templates/item/fwd.h`. Ten `fieldmap.toml` `[fields]` decisions spell the tables | **approved by the integrator under the standing instruction (phase5-roadmap.md), applied in stage 0** (additive: static members add nothing to an object's layout) | DecomposeAction.java:161 `postValidate`, :172 `finishUse`, :372 `filterItemsByLevel`, :387 `containsSpecialCubeItems`; E-04 ports them. `targetItem` is nullable (CM_USE_ITEM.java:64-72 passes null unless the client names a target) and `parentItem` is not (act dereferences it first, :119). `filterItemsByLevel` takes the holder's nullable list (`DecomposableItemsData::getInfoByItemId` returns `nullptr` for Java null) and returns `std::nullopt` for Java's null, the `getSelectableItems` shape. The tables: Java fills the maps only in its static initializer (:60-97) and never writes the arrays (:44-58), so hub-headers.md §11.1 makes them constants, the AbyssService.killAnnounceMaps decision, instead of fieldmap's `static inline HashMap<Race, Ref<Array<int32_t>>>` and `static inline const Ref<Array<int32_t>>` (a shim Monitor and RefCounted arrays at static initialization). `premiumOphidanRecipe` (:85-96) is new to C++; its 124 ids and the 271 of the other tables were compared with the Java file in order (395 of 395 equal). `validateRandomItemIds` now reads the members: Java iterates `chunkEarth.values()`, whose order a HashMap of enum keys (identity hash) leaves open, and the C++ keeps the put order it had, ASMODIANS first, so `DecomposeActionIdsTest.EveryRandomRewardItemMustExist` still sees 152000051 first |
| m5c-h03 | `model/templates/item/actions/TuningAction.h` (P5-07), `TuningAction.cpp` | public `static int32_t getRandomStatBonusIdFor(gameobjects::Item& item);` after `act`; include `<cstdint>`. 1 `AION_UNPORTED` stub | **approved by the integrator under the standing instruction (phase5-roadmap.md), applied in stage 0** (additive) | TuningAction.java:111; P-07 ports it. Callers: act's task (:103), ItemActionService.identifyItem (ItemActionService.java:45), ItemPurificationService.java:130; each passes an item it has dereferenced, so `Item&` |
| m5c-h04 | `model/craft/ProfessionInfo.h` (P5-09c), new `model/craft/ProfessionInfo.cpp` | free functions after `isCrafting`: `std::optional<int32_t> getUpgradeCost(Profession profession, int32_t skillLevel);`, `int32_t getMaxUpgradableLevel(Profession profession);`, `std::string getClientName(Profession profession);`, `std::string getClientName(Profession profession, int32_t skillLevel);`, `std::string getSkillGrade(Profession profession, int32_t skillLevel);`, `std::optional<Profession> getBySkillId(int32_t skillId);`; includes `<optional>`, `<string>`. 6 `AION_UNPORTED` stubs in the new `.cpp` | **approved by the integrator under the standing instruction (phase5-roadmap.md), applied in stage 0** (additive) | Profession.java:37 `getUpgradeCost` (returns `Integer`, null at other levels), :53, :57, :61, :65 (private: a free function of the companion with the Java note, the DropRewardEnumInfo.h `levelDifference` pattern), :81 `getBySkillId` (null if none: `std::optional`, the FriendList_StatusInfo.h `getByValue` pattern); C-01 ports them. **§7 asked whether the companion is frozen: it is** — hub-headers.md §14 makes a hand-written companion a new file only while it does not exist, and ProfessionInfo.h exists since wave 5a, so this is a request. The bodies go into a `.cpp` rather than inline, so that C-01 ports them without touching the header again |
| m5c-n01 | new `skillengine/task/CraftingTask.{h,cpp}` (P5-02a) | the shell: fieldmap.json's members verbatim (`const RecipeTemplate* recipeTemplate`, `const int32_t maxCritCount`, `const int32_t bonus`, `Field<const ItemTemplate*> itemTemplate`, `Field<int32_t> critCount`, `showBarDelay`, `executionSpeed`), the protected constructor and `static Ref<CraftingTask> create(Player& requester, Ptr<StaticObject> responder, const RecipeTemplate* recipeTemplate, int32_t skillLvlDiff, int32_t bonus)`, the eight methods in Java order (`onFailureFinish`, `onSuccessFinish`, private `calculateCrit`, `sendInteractionUpdate`, `onInteractionAbort`, `onInteractionFinish`, `onInteractionStart`, `analyzeInteraction() override final`), all 9 bodies `AION_UNPORTED` (the constructor constructs its ported base and throws, the League.cpp shape) | **approved by the integrator under the standing instruction (phase5-roadmap.md), applied in stage 0** (new files, which hub-headers.md §14 lets through without a request; recorded here because I-02 applies it) | Drafted with `skeleton.py --draft com.aionemu.gameserver.skillengine.task.CraftingTask` and reviewed against GatheringTask.h: the draft's class-local `unportedArgument` helper and its brace initializers on the `const` members are left out (hub-headers.md §4), `responder` is a `Ptr` (above), and `onInteraction{Abort,Finish,Start}` say `override`, since AbstractInteractionTask.h declares them (the draft only looked at AbstractCraftTask.h). `skeleton.py --fwd` changed nothing: `skillengine/task/fwd.h` already declares `CraftingTask` |
| m5c-h05 | `services/EnchantService.h` (P5-07), `EnchantService.cpp` | `enchantItemAct(Player& player, Item& parentItem, Item& targetItem, Item& supplementItem, int32_t currentEnchant, bool success)` and `socketManastoneAct(Player& player, Item& parentItem, Item& targetItem, Item& supplementItem, int32_t targetWeapon, bool result)`: `model::gameobjects::Item& supplementItem` → `runtime::Ptr<model::gameobjects::Item> supplementItem`; the two stubs' parameter follows (their bodies are E-01's). Static functions: no layout change; no C++ caller today (only the two stub definitions, `EnchantService.cpp:23, 39`) | **pending: the integrator's decision (a signature change of a frozen header, which needs the reviewer); not applied.** Found by the stage-0 integrator lane (m5c-plan.md §17.2), filed here by its review fix (§17.5); needed before E-03. **Stage-0 integration (2026-09-25, m5c-plan.md §18): still pending, carried to stage 1's header batch.** E-03 landed without it: the task hands a null supplement on as the parent item (`EnchantItemAction.cpp:152-158`), which neither callee reads, so nothing observable differs (docs/deviations/P5-07.md). Once applied, change `EnchantService.cpp:499, :760` and `EnchantItemAction.cpp:152-158` to pass the nullable `Ptr` | E-03's task passes the five-argument act's `supplementItem` on to both (EnchantItemAction.java:125, 127), and that argument is null whenever CM_USE_ITEM calls the varargs act, which calls `act(player, parentItem, targetItem, null, 1)` (:82). With `Item&`, E-03 cannot make the call faithfully. The Java bodies never read the parameter (EnchantService.java:173-231, 404-424: only the declaration and a "Decrease required supplements" comment), so a nullable `Ptr` loses nothing. `enchantItem` and `socketManastone` already take a `Ptr` supplement (`EnchantService.h:26, 30`), and m5c-h01's `isSuccess` does too |

## M5b-3 client session leak S-1: the holder probe and the silent zone removal (2026-09-25)

The leak change of m5b3-client-session.md S-1 (docs/deviations/P4-10.md, "M5b-3 client session leak"). Its fix of the leak itself,
`InstanceWalkerFormations::organizeAndSpawn`, is a body and needs no request. m5b3-leak-h01 is applied with the leak change's commit: the lane
kept the header change and every file that needs it out of the working tree as a patch, built and tested in a private build, until the
integrator approved it (the review of the leak change: a frozen header must not change while its request is pending). m5b3-leak-h02 and
m5b3-leak-h03 are deferred.

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| m5b3-leak-h01 | `runtime/services/LeakCensus.h` (P4-02b) | public, after `zombieCutCount`: `struct ProbedLeak { RefCounted* object = nullptr; const char* className = nullptr; int32_t objectId = 0; };`, `static constexpr size_t MAX_PROBED_LEAKS_PER_PASS = 4;`, `using HolderProbe = void (*)(std::span<const ProbedLeak> leaks);`, `void setHolderProbe(HolderProbe probe) noexcept;`; includes `<cstddef>`, `<span>`; a "Holder probe" bullet in the class comment | **approved** (2026-09-25, integrator): additive diagnostic API, capped at 4 probed leaks per pass and never posted for the final census; applied with its tests in the leak change's commit | **Additive**, C++ only, no Java counterpart, no layout change of an existing member (the 5a-pre-6 precedent for a runtime-services addition). The census counts the references of a leaked object but cannot say whose they are: S-1's `Npc 25582` (refcount 1, no task) could not be attributed from the log. A check that reports new leaks posts the game layer's probe once for at most four of them, oldest removal first (4 = `Pin::MAX_OWNERS`: one task pins them all, so the probe makes one pass over the world), at most once a minute, and skips the others, the checks within the minute and every new leak while the previous probe has not finished, logging how many (`LeakCensusHolderProbeTest`). A check with `censusAfter` 0 posts no probe: CheckOutput's final census re-reads the counts until two checks agree, and the probe's pin turned a logout whose references were still falling into a census.txt leak in the review's run (`CheckOutputTest.FinalCensusWithAHolderProbeStillWaitsForAFallingCount`) |
| m5b3-leak-h02 | `world/zone/ZoneInstance.h` (P4-10) | public, after `onLeave`: `bool removeWithoutNotify(model::gameobjects::Creature& creature) noexcept;` - under the zone's monitor, removes `creatures[creature.getObjectId()]` if and only if it is this very creature (identity compare-and-remove), calls no `onLeave`, no zone handler and no controller callback; returns whether it removed the entry | **deferred** (2026-09-25, integrator): hardening for a stale zone entry that no known path produces at the session's hunting spot; the sweep logs such entries meanwhile - revisit if the probe or a WARN ever names a zone | **Additive**, C++ only. `World::removeObject`'s `dropStaleRegionEntries` (World.cpp) now only logs a zone of the final region's neighbourhood that still holds the deleted creature, because Java's only removal, `onLeave`, notifies the controller and the zone handlers of a creature that left the world. `MapRegion::revalidateZones` closes the two races that left such an entry (a revalidation against a despawn or a region crossing, `WorldContainerLifetimeTest`), but two concurrent `World.updatePosition` calls (the `MoveTaskManager` tick and a `StaggerEffect`/`StumbleEffect` move) can still leave one; with this method the delete path would drop it silently like a stale region entry. The review of the request has to confirm that no reader of `ZoneInstance.creatures` (`isInsideCreature`, `onDie`, the `forEach` callers) acts on a creature that left the world, as the P4-10.md row of `dropStaleRegionEntries` does for `MapRegion.objects` |
| m5b3-leak-h03 | `spawnengine/InstanceWalkerFormations.h`, `spawnengine/TemporarySpawnEngine.h`, `taskmanager/tasks/MoveTaskManager.h`, `taskmanager/tasks/PlayerMoveTaskManager.h` (P4-10), `model/town/Town.h` (P5-11), `model/base/Base.h`, `services/worldraid/WorldRaid.h`, `services/WorldRaidService.h` (P5-12b) | `friend class ::aion::gameserver::world::WorldLeakProbe;` in each class (with a forward declaration of `world::WorldLeakProbe` where the header has none) | **deferred** (2026-09-25, integrator): probe accessors only; the probe names the structures it cannot read, so a report that points at one of them decides it | **Additive** (a friend declaration changes no layout and no signature). The adversarial review of the leak change listed these holders of a removed npc; each keeps its container private with no reader (`groupedSpawnObjects`, `formationVariants`, `walkerVariants`, `movingCreatures`, `movingPlayers`, `spawnedObjects`, `spawnedNpcs`, `flag`/`assaulter`, a raid's `boss`/`flag`/`vortex`/`locationMarkers` and `activeRaids`), so `WorldLeakProbe::findHolders` cannot search them and its "no holder found" warning names them as not searched (`WorldLeakProbe::NOT_SEARCHED`). With the friends the probe reads them by identity under their own locks, read only. Not requested: `ObserveController` - an observer's captures cannot be searched by identity whatever the access |

## Wave 5e stage 0: the effects overlap, E-01 and E-02 (2026-09-25)

The two effect lanes of m5e-plan.md §15.4, run ahead of M5e in a worktree while M5c stage 0 held the main tree (m5e-plan.md §16). Headers
were frozen for both lanes, so each request below has a `.cpp`-local workaround that is recorded in docs/deviations/P5-03.md and P5-04.md.
The three additive requests are the ones §7 and §15.5 expected. They are approved but not applied here: the next lane that owns P5-03 or
P5-04 (M5e stage 1) applies each one and, in the same commit, drops its workaround and the workaround's `// fieldmap:` waiver. The approved
rows m5b3-e-1, m5b3-e-2 (`FearEffect.h`) and m5b2-p3-5 (`HideEffect.h`) stay unapplied: neither lane touched those classes.

| Id | File | Change | Decision | Reason |
|---|---|---|---|---|
| m5e-h01 | `skillengine/effect/AuraEffect.h` (P5-03) | a nested `struct AuraTask;` | **approved by the integrator under the standing instruction (phase5-roadmap.md)**: additive; applied by the next P5-03 lane | `fieldmap.json`'s `cppName` is `AuraEffect::AuraTask`, the same case as the approved m5b3-e-2 (`FearEffect::FearTask`). Until then the task is the file-local `AuraEffect_AuraTask` in `AuraEffect.cpp`, marked `// fieldmap-class: com.aionemu.gameserver.skillengine.effect.AuraEffect.AuraTask`; it moves into the header unchanged |
| m5e-h02 | `skillengine/effect/CondSkillLauncherEffect.h` (P5-03) | `friend struct CondSkillLauncherEffect_ActionObserver;` | **approved by the integrator under the standing instruction**: additive, the `RootEffect.h` and `ProvokerEffect.h` precedent; applied by the next P5-03 lane | CondSkillLauncherEffect.java:35-57's `$1` reads the effect's protected `skillId`. Until then the observer carries a C++-only `const int32_t skillId; // fieldmap: C++ only` that `startEffect` passes in (`CondSkillLauncherEffect.cpp:51`) |
| m5e-h03 | `skillengine/effect/OneTimeBoostSkillAttackEffect.h` (P5-04) | `friend struct OneTimeBoostSkillAttackEffect_AttackCalcObserver;` | **approved by the integrator under the standing instruction**: additive; applied by the next P5-04 lane | OneTimeBoostSkillAttackEffect.java:33-55's `$1` reads the private `count` and `type` and calls the private `removeEffect`. Until then the observer carries three C++-only members, `count`, `type` and `removeEffect` (a pointer to the private member function), each marked `// fieldmap: C++ only` (`OneTimeBoostSkillAttackEffect.cpp:42-44`) |
| m5e-h04 | `world/geo/GeoService.h` (not P5-03 or P5-04) | a test-only hook that installs a terrain or `GeoMap` for a map id | **deferred** (2026-09-25, integrator): a test seam on a service header outside both lanes, and no workaround is needed for the port itself; revisit with G-04's geo cases | BackDash's direction and distance cannot be tested on the empty geo map, the one review low of E-01 left open; the same hook would give Dash, Pulled and SimpleRoot G-04 cases on real terrain. E-02's `GeoMovementEffectsTest` gets real walls another way, by generating geo files for `GeoService.init` |
