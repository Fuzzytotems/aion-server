# P5-04 effects M-Z: deviations

## Wave 5b-2, stage 1 part 3 (effects-mz lane, m5b2-plan.md F-03, F-05 and D13)

Ported: every `AION_UNPORTED` body of `PolymorphEffect`, `ProvokerEffect`, `ReflectorEffect`, `ReturnEffect`, `RootEffect`, `SanctuaryEffect`,
`ShieldEffect`, `ShieldMasteryEffect`, `SlowEffect`, `SnareEffect`, `SpellAttackEffect`, `StatdownEffect`, `StatupEffect`, `StunEffect`,
`TransformEffect`, `WeaponDualEffect` and `WeaponMasteryEffect` (F-03), and of the four classes D13 added because part 2 made them reachable:
`StumbleEffect` and `StaggerEffect` (the critical proc of POLEARM, STAFF, GREATSWORD and BOW attacks) and `MPHealEffect` and
`ProcVPHealInstantEffect` (8751, CuringZoneService). `SkillAttackInstantEffect` (its three Java methods are generated or inline in the shell),
`SpellAttackInstantEffect` and `StatboostEffect` (data-only) had nothing to write. `RootEffect.h` and `ProvokerEffect.h` gained the pre-approved
friend line for their observer structs (`RootEffect_ActionObserver`, `ProvokerEffect_ActionObserver`, the fieldmap names of `RootEffect$1` and
`ProvokerEffect$1`); both are removed with the effect by `Effect.endEffect -> removeObservers` (cycles.toml `RootEffect$1#effect`,
`#effected`, `ProvokerEffect$1#effector`: java-hook), because both bodies register them through `Effect.addObserver`. Tests:
`tests/effects_mz/` (fixture `EffectsMzTestSupport.h`).

| Area | Java | C++ | Reason |
|---|---|---|---|
| `WeaponMasteryEffect.startEffect`, `ShieldMasteryEffect.startEffect`: the mastery stat functions | `new StatWeaponMasteryFunction(itemGroup, stat, value, bonus)`, `new StatShieldMasteryFunction(stat, value, bonus)` (model.stats.calc.functions, P5-01) | the two classes ported statement by statement as file-local classes of `WeaponMasteryEffect.cpp` and `ShieldMasteryEffect.cpp` (anonymous namespace, `// fieldmap-class:` mapped to their Java names, members as fieldmap.json has them) | Neither class has a C++ file: `model/stats/calc/functions/fwd.h` declares both and nothing defines them (docs/deviations/P5-01.md, "B-06 is deferred": their only constructors are these two effects). Header request (P5-01): `StatWeaponMasteryFunction.{h,cpp}` and `StatShieldMasteryFunction.{h,cpp}` shaped like `StatArmorMasteryFunction.h`; the local classes then move there unchanged and the effects include the headers. The behaviour is Java's, including the random rate of a dual-wielded skill hit. Tests: `StatEffectsTest.SwordTrainingAddsSixteenPercentToTheSwordHand`, `ADualWieldedSkillHitDrawsTheMasteryRate`, `ShieldTrainingAppliesOnlyWithAShield` |
| `SpellAttackEffect.onPeriodicAction`: a template without `hoptype` | passes the null `hopType` field to `CreatureController.onAttack(Effect, TYPE, int, boolean, LOG, HopType, boolean)` | passes `hopType.value_or(HopType::SKILLLV)` | The same header gap and the same answer as `DamageEffect.onAttack` and `BleedEffect.onPeriodicAction` (docs/deviations/P5-03.md): the Effect overloads of `CreatureController.h` (P4-11b) take a non-nullable `HopType`; the private overload hands the value only to `AggroList.addDamage`, which reads it behind `notifyAttack && hopType == HopType.DAMAGE`, and a periodic spell attack passes `notifyAttack` false. 260 of the 601 `<spellatk>` of skill_templates.xml have no `hoptype`. Header request: `std::optional<skillengine::model::HopType>` in both overloads (filed by the effects-al lane as well) |
| `ProvokerEffect.getProvokeTarget`: `provoke_target` absent, or a constant the switch does not know | the switch expression over the null field throws `NullPointerException`; an unknown constant throws `MatchException` | `runtime::NullPointerException` for the absent attribute; `commons::utils::IllegalStateException` after the switch | C++ has no exhaustive switch expression. The second throw is unreachable for the two constants of `ProvokeTarget`; the data has no provoker without `provoke_target` |
| `ProvokerEffect.shouldApply`, `ProvokerEffect$1.tryApplyEffect`, `WeaponDualEffect.hasDualWieldEffect`: a skill id missing from `SKILL_DATA` | `getSkillTemplate(id).getType()` / `.getL10n()` / `skillEntry.getSkillTemplate().getEffects()` throw `NullPointerException` | a file-local `nonNull` throws `runtime::NullPointerException` naming the expression | Java's implicit null check; a plain C++ dereference of the null template pointer would be undefined behaviour (the helper of `EffectTemplate.cpp`) |
| `WeaponMasteryEffect.startEffect`: no `weapon` attribute | `itemGroup.getItemSubType()` throws `NullPointerException` at the first change | `NullPointerException` from a file-local unbox of the `std::optional<ItemGroup>` at the same point | The generated member is optional (hub-headers.md §6). Unreachable with the data (all 123 `<wpnmastery>` carry `weapon=`). Test: `StatEffectsTest.AMasteryWithoutChangesStartsNothingAndOneWithoutAWeaponThrows` |
| `StumbleEffect.calculate`, `StaggerEffect.calculate`: the knock-back vector | `Math.toRadians`, `Math.cos`, `Math.sin` | `angdeg * 0.017453292519943295` (JDK 9+ `Math.toRadians`), `std::cos`, `std::sin` | Java's `Math.cos`/`sin` may differ from the C runtime in the last bit of the double; both are narrowed to float at once, as `NpcMoveController` and `GeoService.findMovementCollision` already do. For the axis-aligned headings of the tests both give the exact (2, 0) and (0, 2) (to float precision). Tests: `ControlEffectsTest.AStumbleKnocksTheMonsterTwoMetresAwayFromTheEffector`, `AStumbledPlayerIsForceMovedAndLosesItsCast` |

**No deviation elsewhere.** Every other body is its Java method statement by statement: the int arithmetic of `ShieldEffect`,
`ReflectorEffect` and `WeaponDualEffect` (`hitvalue + hitdelta * level`, `maxDamageChance + level * maxDamageDelta`, `value + level * delta`) wraps
through `uint32_t`, `ProcVPHealInstantEffect`'s `long` products through `uint64_t`, and its `(int) (long * int * 0.001)` uses the saturating
`JavaFloat::doubleToInt`. `WeaponMasteryEffect`'s `change == null` is `change.empty()` (JAXB leaves an absent list null, the bound list is empty:
the precedent of `Effect.endEffects`); the mastery lists hold the `Ref`s until `CreatureGameStats.addEffect` has taken its own.

### Reachable after part 3 (unported bodies these classes reach)

- `ReturnEffect.applyEffect` -> `TeleportService.moveToBindLocation`: a bind point on another map (or another instance) takes the cross-map arm
  of `TeleportService.teleportTo`, which reaches `InstanceService::onLeaveInstance` (`services/instance/InstanceService.cpp:170`, P5-13,
  `AION_UNPORTED`). Return to a bind point on the caster's own map is ported end to end (`OtherEffectsTest.ReturnTakesTheCasterToItsBindPoint`).
- `ProvokerEffect$1` applies its `skill_id` through `SkillEngine.applyEffectDirectly`, so a provoker reaches whatever effect class the provoked
  skill has: 620 Armor of Attrition's 8929 is a `<prochealinstant>` (`ProcHealInstantEffect::calculate`, `ProcHealInstantEffect.cpp:7`,
  P5-04, outside §2.4's subset and D13: `AION_UNPORTED`), so an attack on a Gladiator under 620 throws from the observer. The npc provokers of
  the start maps (§2.4(b)) were not traced here.
- The post-spawn provokers of the D11 holdback (`NpcSkillList::getPostSpawnSkills`, `model/skill/NpcSkillList.cpp:91`): two of the 33
  post-spawn skill ids are provokers, both `is_post_spawn` on npcs of `npc_skills/instances/301390000_Drakenspire_Depths.xml` (the `IDSEAL_TWIN`
  pair). The part 3 Regate removed that partial, so these npcs cast them at spawn, and an attack on them runs `ProvokerEffect$1` ->
  `SkillEngine.applyEffectDirectly` into an unported effect class, whose exception comes out of the attack's `notifyAttackedObservers`:
  - 21641 Binding Curse (`<provoker hittype="PHHIT" provoke_target="OPPONENT">`) provokes 20771 Bind, a `<bind>`: `BindEffect::calculate`
    (`skillengine/effect/BindEffect.cpp:11`, P5-04, `AION_UNPORTED`), for every auto attack or PHYSICAL skill on the npc.
  - 21642 Silence Curse (`<provoker hittype="MAHIT" provoke_target="OPPONENT">`) provokes 20772 Silence, a `<silence>`:
    `SilenceEffect::calculate` (`skillengine/effect/SilenceEffect.cpp:11`, P5-04, `AION_UNPORTED`), for every MAGICAL skill on the npc.
  Only inside Drakenspire Depths; the other post-spawn effect positions (statup, hide, shield, sanctuary, reflector, alwaysresist, heal, statdown,
  spellatkinstant) are all in the ported subset.
- `WeaponDualEffect.hasDualWieldEffect` is now answered for `Equipment.equipItem` and `Equipment.checkDualWieldRestriction`, which reached its
  `AION_UNPORTED` before for every one-handed weapon.
- The test paths of every class (including the gate's 1328, 37, 39, 43, 140, 8291 and D13's 8217, 8218, 8751) reach no `AION_UNPORTED` site:
  the fixture's `TearDown` asserts `runtime::unportedHitCount() == 0` for every case.

## Wave 5b-3, stage 1 (effects lane, m5b3-plan.md E-01..E-04)

Ported: every `AION_UNPORTED` body of `MpAttackInstantEffect` (2), `NoDeathPenaltyEffect` (1), `NoResurrectPenaltyEffect` (1), `ParalyzeEffect`
(4), `PoisonEffect` (5), `ProcAtkInstantEffect` (2), `ProcHealInstantEffect` (4), `ProcMPHealInstantEffect` (4), `SilenceEffect` (4) and
`XPBoostEffect` (1) - the P5-04 part of the 14 classes of m5b3-plan.md §2.5/§2.6. `census.py --chunks P5-04`: 134 -> 106 `AION_UNPORTED` sites,
no undeclared body. No header changed. Tests: `tests/effects_mz/ItemEffectsTest.cpp` (the potions 9889 and 9894, the amulet's 10249, the
boon 10350, 8683, 620's 8929) and `tests/effects_mz/ProcEffectsTest.cpp` (the godstone procs 8267, 8428, 8542, 8538, 8536, the camp fire's
8302 through `SkillEngine.applyEffectDirectly(..., MATERIAL_SKILL)`, the material 8682), each driven calculate -> applyEffect -> startEffect ->
endEffect with the fixture's no-`AION_UNPORTED` check.

| Area | Java | C++ | Reason |
|---|---|---|---|
| `ProcAtkInstantEffect.applyEffect`, `PoisonEffect.onPeriodicAction`: a template without `hoptype` | passes the null `hopType` field to `CreatureController.onAttack(Effect, TYPE, int, boolean, LOG, HopType[, boolean])` | passes `hopType.value_or(HopType::SKILLLV)` | The header gap and the answer of `DamageEffect.onAttack` / `BleedEffect.onPeriodicAction` (docs/deviations/P5-03.md) and `SpellAttackEffect.onPeriodicAction` above: the only reader, `AggroList.addDamage`, compares the value with `DAMAGE`, where null and `SKILLLV` answer the same. None of the 70 godstone `<procatk_instant>`, the 13 material ones and the 6 godstone `<poison>` carries a `hoptype`. Tests: `ProcEffectsTest.AGodstoneProcHitsWithDeltaTimesTheLevel` (8267 leaves the monster the hate floor of 1, `AggroInfo.addHate`, where `DAMAGE` would add 1155), `.PoisonSlashPoisonsForTwentyOneSecondsTickingItsReserve` |

**No deviation elsewhere.** `MpAttackInstantEffect`'s percent reserve is Java's int `(maxMP * value) / 100` (the product wraps through `uint32_t`)
and is set before `EffectTemplate.calculate(effect, null, null, element)`, whose answer is not read (a resisted MP attack keeps its reserve:
`ProcEffectsTest.AResistedMpAttackKeepsTheReserveItSetFirst`). Its `element` argument has no observable effect: the four-argument
`EffectTemplate.calculate` never reads its `element` parameter (it shadows the field, and `checkDodgeOrResistRate` reads the field,
EffectTemplate.java:298-360; EffectTemplate.cpp keeps it unnamed), so no test can tell `element` from any other value - an equivalent mutant.
`SilenceEffect.startEffect` reads `getCastingSkill()` twice, as Java does (`// java-race`). `XPBoostEffect`, `NoDeathPenaltyEffect` and
`NoResurrectPenaltyEffect` only add their success, without `super.calculate`
(`ItemEffectsTest.TheFlagAndBoostEffectsLandWithoutEffectTemplateCalculatesChecks`). The review fixes (same wave) pin the rest of what the
first set left open: an item heal ignores the drinker's HEAL_BOOST where 1838 Healing Light takes it
(`ItemEffectsTest.AnItemHealTakesNoHealBoostWhereASkillHealDoes`); a poison's reserve is not sent (`getReservedEffectsToSend`); Silence and
Paralyze tell their player effector STR_SKILL_TARGET_SKILL_CANCELED (cancelCurrentSkill's lastAttacker); the camp fire's 8302 deals more than 0.

### Closed by M5b-3 (the "Reachable after part 3" notes above)

- 620 Armor of Attrition's provoked 8929 (`<prochealinstant>`) now heals the effector: `ItemEffectsTest.ArmorOfAttritionHealsItsEffectorThroughTheProvokedItemHeal`.
- 21642 Silence Curse's provoked 20772 Silence (`<silence>`, Drakenspire Depths) reaches the ported `SilenceEffect`; 21641 Binding Curse's 20771
  Bind still reaches `BindEffect::calculate` (`AION_UNPORTED`).
- The skill templates the plan names hold one fact the plan got wrong: skill_templates.xml has **one** 8267 template (:80570-80582, `EARTH`,
  with properties and a weapon start condition), not two - the `WIND` `<procatk_instant>` at :80566 belongs to 8266 (m5b3-plan.md §2.6 (b),
  risk 11, §12). The oracle's last-wins rule changes nothing for 8267.
- Poeta's and Ishalgen's npc skills reach none of the 14 classes, directly or through a `<subeffect skill_id>` or a provoker's `skill_id` (the
  closure over the 68 skill lists of their 311 spawned npc ids adds only 8217, 8218 and 16393, as the m5b2 review note says), so the
  `oracle.py m5b2-skills --npc` gap does not hide any of these classes.

## Wave 5e, stage 1 (effects-mz lane, m5e-plan.md E-02, §2.4 and §15.4)

Ported: every `AION_UNPORTED` body of the 15 classes of E-02 - `MPHealInstantEffect` (4), `MPShieldEffect` (2), `OneTimeBoostSkillAttackEffect`
(2 + the inner `$1` AttackCalcObserver's two overrides and the 100 ms removal lambda `@L61:44`), `PulledEffect` (4), `RandomMoveLocEffect` (2),
`ResurrectEffect` (2), `RideRobotEffect` (3 + the inner `$1` ActionObserver(UNEQUIP)), `SignetBurstEffect` (2), `SignetEffect` (2),
`SimpleRootEffect` (4), `SkillAtkDrainInstantEffect` (1 + the 1,000 ms drain lambda `@L28:44`), `SkillCooltimeResetEffect` (1), `SleepEffect`
(4), `SpinEffect` (4) and `TargetChangeEffect` (1): P5-04's `AION_UNPORTED` sites 106 -> 68 (38), no undeclared body. The two observers are
`.cpp`-local callback structs named as fieldmap.json names them (`OneTimeBoostSkillAttackEffect_AttackCalcObserver`,
`RideRobotEffect_ActionObserver`) and are registered through `Effect.addObserver`, so `Effect.endEffect -> removeObservers` takes them away
(cycles.toml `OneTimeBoostSkillAttackEffect$1#effect`, `RideRobotEffect$1#effect`: java-hook); the two lambdas are pinned tasks
(`schedule({&effect}, ...)`, `schedule({this, &effect}, ...)`, fieldmap.json's pins) that nothing stores. No `cycles.toml` or `fieldmap` row
was needed (`gs.lint.concurrency` passes with `--werror --cycles=core`). No header changed. Tests: `tests/effects_mz/MovementEffectsTest.cpp`
(Pulled, SimpleRoot, Spin from an Npc effector, Sleep, RandomMoveLoc), `GeoMovementEffectsTest.cpp` (the tests/geo-fixture cases of E-02 and
G-04: walls loaded through GeoService.init from generated models.mesh / .geo files), `RecoveryEffectsTest.cpp` (MPHealInstant, SkillAtkDrainInstant,
MPShield, Resurrect), `CombatBuffEffectsTest.cpp` (OneTimeBoostSkillAttack, SkillCooltimeReset, RideRobot, TargetChange) and
`SignetEffectsTest.cpp` (Signet, SignetBurst), each driven calculate -> applyEffect -> startEffect -> endEffect on the verbatim
skill_templates.xml `<effects>` with the fixture's no-`AION_UNPORTED` check.

| Area | Java | C++ | Reason |
|---|---|---|---|
| `OneTimeBoostSkillAttackEffect$1`: the template's `count`, `type` and `removeEffect` | the anonymous class reads the private `count` / `type` and calls the private `removeEffect` through the captured `this` | `startEffect` hands the two values and `&OneTimeBoostSkillAttackEffect::removeEffect` (a pointer to the private member function, taken inside the member) to the observer's constructor; the observer calls `(template->*removeEffect)(effect)` | `OneTimeBoostSkillAttackEffect.h` has no friend line for the struct (m5e-plan.md §7 expected the request). The values are the ones Java reads (the template is immutable static data after load) and the call is the same member function. Header request (in the lane's report): `friend struct OneTimeBoostSkillAttackEffect_AttackCalcObserver;` - the three members then go and the struct reads the template, like `RootEffect_ActionObserver`. The three C++-only members carry `// fieldmap:` waivers (none retains). Tests: `CombatBuffEffectsTest.KillersEyeBoostsTheNextPhysicalSkillByHalfOnce`, `FocusedShotsBoostsFivePhysicalSkillsAndGoesAfterTheFifth`, `StoppingPowerBoostsMagicalSkillsOnly`, `AnAllTypeBoostCountsPhysicalAndMagicalSkillsTogether` |
| `OneTimeBoostSkillAttackEffect.startEffect`: no `type` attribute | `switch (type)` unboxes null: `NullPointerException` after `super.startEffect` | `runtime::NullPointerException` at the same point | The generated member is optional. Unreachable with the data (10 of 10 `<onetimeboostskillattack>` carry `type=`). Test: `CombatBuffEffectsTest.ABoostWithoutTypeThrowsInStartEffect` |
| `SignetBurstEffect.calculateDamage`: `SignetEnum.valueOf(signet)` | `IllegalArgumentException` for a name that is no constant, `NullPointerException` for a missing attribute | `runtime::IllegalArgumentException` for both | The generated `std::string` member cannot tell a missing attribute from an empty one. Unreachable with the data: 68 of 68 `<signetburst>` carry SIGNET1..SIGNET4 |

**No deviation elsewhere.** Every other body is its Java method statement by statement: `MPShieldEffect`'s `hitvalue + hitdelta * level` wraps
through `uint32_t` (as `ShieldEffect`'s); `SkillAtkDrainInstantEffect`'s `reserve * percent / 100` is int arithmetic (the product wraps, the
division truncates); `SignetBurstEffect`'s `valueWithDelta *= damageMultiplier` is a float product narrowed by the saturating
`templates::detail::floatToInt`, and `add_effect_prob * multi` wraps; `SkillCooltimeResetEffect`'s long arithmetic wraps through `uint64_t`, its
`delta / 100` keeps Java's int division (a delta below 100 takes nothing off), its loop's `i++` wraps, and it reads the wall clock three times
as Java calls `System.currentTimeMillis()` three times; `PulledEffect`'s `(float) Math.cos(radian) * 1.5f` casts before the float product and
`SimpleRootEffect`'s `(float) (Math.cos(radian) * 0.7f)` after the double product, as written (bit-exact at 9 degrees:
`MovementEffectsTest.ANineDegreePullAdds...`, `ANineDegreeSetBackAdds...`); `RideRobotEffect`'s `(Player)` casts are
`runtime::cast` (ClassCastException for another creature, `CombatBuffEffectsTest.OnlyAPlayerRides`).

**Findings the tests pin (Java's behaviour, kept).**
- 8441 Capture (skillsubtype NONE) can never be reflected: `Effect.setShieldDefense` drops the SKILL_REFLECTOR bit for any skill that is not
  ATTACK or DEBUFF, so `PulledEffect`'s reflected arms are reached only by a DEBUFF pull (`MovementEffectsTest.AReflectedPullDraws...` uses one).
- 2440 Kinetic Battery's MP cost of an absorbed 30 is `(int) (30 * 0.01f * 50)` = 14, not 15: `30 * 0.01f` rounds to 0.29999998f
  (AttackShieldObserver's float arithmetic, left to right); 60 absorbed cost 29 (`RecoveryEffectsTest.KineticBatteryAbsorbs...`).
- On the empty GeoMaps of geo data off, `GeoService.findMovementCollision` finds no ground and leaves a grounded leaper where it stands (a flying
  one gets the whole distance), and `canSee` refuses only beyond 80 m.
- 1699 Light of Resurrection's offer now reaches the client; accepting it (`CM_REVIVE` SKILL_REVIVE) reaches `PlayerReviveService::skillRevive`
  (`services/player/PlayerReviveService.cpp:46-48`, P5-08, `AION_UNPORTED`, m5e-plan.md C-02).

**Review follow-up (tests only, no body changed).** 8 more cases kill the mutants that survived the review (63 cases in the lane, 151 in
`aion_gs_effects_mz_tests`):
- The heights: a pull takes the effector's z (`PulledEffect.java:48`) and a set-back keeps the effected's (`SimpleRootEffect.java:43`). The two
  creatures stand 3 m apart in height (`MovementEffectsTest.APullDrawsTheCreatureDownToTheEffectorsHeight`, `ASetBackKeepsTheCreaturesOwnHeight`).
- The cast order: at heading 3 (9 degrees) the two orders differ by one float ulp on each axis. The creatures stand near (0, 0) so that the sums
  stay exact. The goldens are derived by hand from `Math.toRadians(9.0)`; a cosine or sine one double ulp away gives the same floats
  (`ANineDegreePullAddsTheFloatProductsOfTheNarrowedCosineAndSine`, `ANineDegreeSetBackAddsTheNarrowedDoubleProducts`).
- The boost's effected: 8406 Inspiring Mantra I Effect, cast on another player, boosts the target and not the caster, and the removal task takes
  it off the target's EffectController (`CombatBuffEffectsTest.InspiringMantraBoostsTheTargetsMagicalSkillsNotTheCasters`). 8406's position 1
  (`<boostskillcastingtime>`) goes through `BoostSkillCastingTimeEffect.calculate`, which the effects-al lane ported in the same worktree.
- `BufEffect.startEffect` under the boost: 64505 has a `<change>`, which no template in the data has (`TheBoostAddsItsChangesAsABufEffect`).
- The ride's robot comes from the weapon's skin (`AReskinnedKeybladeRidesTheRobotOfItsSkin`: 102100181 reskinned as 102100017).
- The drain's `hpPercent != 0` guard: 865 Manaleech Shot (mp_percent only) sends one TYPE.MP status and no ABSORBED_HP 0.
  `CreatureLifeStats.increaseHp` would send that status for any skill id (`RecoveryEffectsTest.ManaleechShotDrainsHalfAsMpAndSendsNoHpStatus`).
