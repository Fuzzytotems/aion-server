Merged into [../DEVIATIONS.md](../DEVIATIONS.md), section "game-server / server packets SM_L* to SM_Z* and the Abstract* bases" (phase 4 close-out, 2026-09-15).

# P4-17 server packets SM_L* to SM_Z*: deviations

## Wave 5a, stage 1 (app-harness lane, m5a-plan.md F-01a "the P4-17 SM_VERSION_CHECK stand-ins")

| Area | Change |
|---|---|
| `SM_VERSION_CHECK` server ratios and start time | The file-local stand-ins are gone: `writeImpl` calls `GameServer::getRatiosFor`, `GameServer::getCountFor` and `GameServer::START_TIME_SECONDS` like Java (GameServer.h, P5-14). Delete the DEVIATIONS.md row "`SM_VERSION_CHECK` server ratios and start time" when the wave is merged. The bytes are unchanged until `GameServer.updateRatio` has callers: the ratios and counts start at Java's initial 0 |
