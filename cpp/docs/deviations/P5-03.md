# P5-03 effects A-L: deviations

## Wave 5b-2, stage 1 part 1 (effects-al lane, m5b2-plan.md F-01: `EffectTemplate`)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `EffectTemplate.validateEffectConditions` / `effectSubConditionsCheck` | `effectConditions.validate(effect)` / `effectSubConditions.validate(effect)`, i.e. `Conditions.validate(Effect)` (Conditions.java:66-73) | The same loop (every condition's `validate(Effect)`, the first `false` ends it) in a file-local `validateConditions` of `EffectTemplate.cpp` | `condition/Conditions.h` (P5-02a, xmlgen shell) declares no `validate` overload at all, although its class comment says the family is behaviour. `StatFunction.cpp` (P5-01) ports `Conditions.validate(Stat2, IStatFunction)` the same way. Header request: declare `validate(model::Skill&)`, `canValidate(model::Skill&)`, `validate(Stat2&, IStatFunction&)` and `validate(model::Effect&)` on `Conditions`; both local loops then become the member call. The behaviour is Java's. Test: `EffectTemplateTest.EffectConditionsAndSubConditionsPassOnlyWhenEveryConditionPasses` |
| `EffectTemplate.checkEffectResistRate`, cumulative resist | `player.getEffectController().getCumulativeResistance(CumulativeResistType.get(statEnum))` for every stat of a player effected by a player; `get` answers null except for FEAR/PARALYZE/SLEEP_RESISTANCE, and `getCumulativeResistance(null)` answers 0 (`EnumMap.get(null)`, PlayerEffectController.java:149-153) | `CumulativeResistType.get` is a file-local `cumulativeResistTypeOf` answering `std::optional`; for nullopt the body subtracts 0 without calling `getCumulativeResistance` | `PlayerEffectController::getCumulativeResistance(CumulativeResistType)` (P5-02b) takes a non-optional enum, and no companion header declares `CumulativeResistType.get`. Header requests: a nullable parameter (hub-headers.md §6) and a `CumulativeResistTypeInfo.h` companion. The result is Java's; the skipped call only takes the `cumulativeResistInfo` monitor and reads nothing. Not reachable before `Effect::getEffected` is ported (P5-02b, K-01), so it has no test yet |
| `EffectTemplate.calculateSubEffect`, a sub skill missing from `SKILL_DATA` | `new Effect(..., null, ...)` throws `NullPointerException` inside the Effect constructor (`skillTemplate.getReqDispelCount()`) | **Resolved in stage 1 part 3**: the lookup goes on to `Effect::create` unchecked, as in Java, and the constructor's `requireTemplate` (Effect.cpp, P5-02b part 2) throws the `NullPointerException` | Part 1 raised the exception one frame earlier, because the C++ constructor then dereferenced the raw template pointer. Test: `EffectTemplateTest.APassingChanceBuildsTheSubEffectFromSkillData` |
| `EffectTemplate.calculateHate`, the `default` arm | `"Unhandled effect type " + hopType` | The constant's name through `xml::enumName`, empty for a value outside `HopType` | Java cannot reach the arm (`HopType` has two constants and both have a case); C++ reaches it only with a value outside the enum. Test: `EffectTemplateTest.DamageHateReadsTheReservedDamageAndAnUnknownHopTypeThrows` |

## Wave 5b-2, stage 1 part 3 (effects-al lane, m5b2-plan.md F-02 and F-05: the 13 classes of §2.4)

Ported: `AbstractHealEffect`, `AbstractOverTimeEffect` (with the approved header change m5b2-p2-10: `getDuration2` wraps like Java's int sum),
`AlwaysDodgeEffect`, `AlwaysResistEffect`, `ArmorMasteryEffect`, `BleedEffect`, `BufEffect`, `DamageEffect`, `EscapeEffect`, `HealEffect`,
`HealInstantEffect`, `HealOverTimeEffect`, `HideEffect`, and the three default methods of the interface `HealEffectTemplate` (P5-03 as well), which
`AbstractHealEffect.calculateHealValue` and `HealOverTimeEffect` call and without which neither heal base could run. `HideEffect.h` gained the
pre-approved friend line for `HideEffect_ActionObserver`. Tests: `tests/effects_al/BuffEffectsTest.cpp`, `DamageEffectsTest.cpp`,
`HealEffectsTest.cpp`, `EscapeEffectTest.cpp` (fixture `EffectClassTestSupport.h`).

| Area | Java | C++ | Reason |
|---|---|---|---|
| `DamageEffect.onAttack`, `BleedEffect.onPeriodicAction`: a template without `hoptype` | passes the null `hopType` field to `CreatureController.onAttack(Effect, TYPE, int, boolean, LOG, HopType[, boolean])` | passes `hopType.value_or(HopType::SKILLLV)` | The two Effect overloads of `CreatureController.h` (P4-11b, hub header) take a non-nullable `HopType`, and the private overload they forward to (which takes `std::optional`) is not reachable from an effect. The only reader of the value on that path is `AggroList.addDamage` (`notifyAttack && hopType == HopType.DAMAGE`), where null and `SKILLLV` answer the same, so the behaviour is Java's. 149 of the 2,413 `<skillatk>`, 255 of the 3,278 `<spellatkinstant>` and 37 of the 124 `<bleed>` templates have no `hoptype`. Header request: `std::optional<skillengine::model::HopType>` in both overloads. Test: `DamageEffectsTest.DamageEffectHitsWithTheReservedDamageOfItsPosition` (a template without hoptype adds no hate) |
| `BufEffect.getModifiers`: a change whose stat JAXB could not bind | `changeItem.getStat() == null`: logs "Skill stat has wrong name for skillid: ..." and skips the change | no such arm | JAXB binds an unknown enum constant as null; the C++ binder rejects it when the data is loaded (`XmlValues.h`: "an unknown enum constant is an error"), so a bound `Change` always has its stat and `Change::stat` is not optional. The arm is unreachable, and so is the `effect.getSkillId()` Java reads only for its message |
| `BufEffect.getModifiers`, `ArmorMasteryEffect.startEffect`: no `<change>` | `if (change == null)` | `change.empty()` | JAXB leaves a list without elements null, and the bound C++ list is empty then (the precedent of `Effect.endEffects`). JAXB never produces an empty non-null list, so the two tests agree on every template. Test: `BuffEffectsTest.ArmorMasteryReturnsWithoutChangesBeforeItCastsTheEffectedToAPlayer` |
| `ArmorMasteryEffect.startEffect`: no `armor` attribute | `new StatArmorMasteryFunction(null, ...)`: the equipment factor stays 0 (no item has a null subtype), so the functions add nothing | `NullPointerException("ArmorMasteryEffect.armorType is null")` | `StatArmorMasteryFunction::create` (P4-06) takes a non-optional `ItemSubType`. Unreachable with the data: all 38 `<armormastery>` of skill_templates.xml carry `armor=`. Header request: `std::optional<ItemSubType>` there |
| `AbstractHealEffect.calculate`, `HealOverTimeEffect.startEffect`: `EffectReserved.ResourceType.of(healType)` | `valueOf(healType.name())` | a file-local `resourceTypeOf` in each of the two .cpp files, `enumFromName(enumName(healType))` | No companion header of the generated `EffectReserved_ResourceType` declares `of` (P5-02b). The same lookup by name as Java; every `HealType` name is a `ResourceType` constant. Header request: `EffectReserved_ResourceTypeInfo.h` with `of(HealType)` |
| `HideEffect$3` (the ITEMUSE observer) reads `buffCount` | the anonymous class reads the enclosing template's protected field | `HideEffect_ActionObserver_3` reads it through the static `HideEffect_ActionObserver::buffCountOf` | The pre-approval of stage 1 part 3 allows `HideEffect.h` one friend line, for `HideEffect_ActionObserver` (`HideEffect$1`); `HideEffect$3` reads the same field. Same value, same moment. Header request: a second friend line, `friend struct HideEffect_ActionObserver_3;` |

### Reached next (unported bodies the ported classes now reach)

- **A player's hide stops in `PlayerController.onHide`**: it calls `DuelService.fixTeamVisibility`, `AION_UNPORTED` at `services/DuelService.cpp:45`
  (P5-08). `HideEffect.startEffect` has by then added the SPEED function, set the HIDE abnormal state and the visual state, sent SM_PLAYER_STATE and
  scheduled `removeTargetFrom`; the player's three observers (`HideEffect$1` to `$3`) and the cancel on damage come after `onHide` and are not
  reached, and the exception leaves the effect in the controller without an end task. An npc's hide (the post-spawn hides of D7) does not call
  it and runs to the end. Test: `BuffEffectsTest.HideOfAPlayerSetsItsStatesUntilPlayerControllerOnHideStops`.
  **Regate blocker.** This is worse than the throw it replaces: before this part `HideEffect::applyEffect` threw before it changed anything, and
  now a player's hide (3222 *Stealth*, a Scout's level-5 skill of §2.4) leaves the player slowed by 40 %, invisible and in the HIDE state with
  nothing that can end it - no end task, no observer, no cancel on damage - until the effect is dispelled or the player logs out. It cannot be
  closed inside P5-03: Java calls `onHide` before it adds the observers (HideEffect.java:74-118), and the port keeps that order. It closes when
  P5-08 ports `DuelService.fixTeamVisibility` (DuelService.java:176-184, eight lines; under an I-03 lease if P5-08 has no lane open) before the
  regate; the test above then has to cover `HideEffect$1` to `$3` and the cancel on damage.
  **Closed (2026-09-23, after HEAD 760e8ab5c):** P5-08 ported `DuelService.fixTeamVisibility` (docs/deviations/P5-08.md). A player's hide now
  runs `startEffect` to its end in Java order and ends at its duration, at the hider's attack (`HideEffect$2`), at a skill as `HideEffect$1`
  decides, at an item use as `HideEffect$3` decides, and for type 0 at the first hit or damage over time; the end removes the HIDE state, the
  visual state and the SPEED function. The stop-point test is replaced by `BuffEffectsTest.HideOfAPlayerLastsItsDurationAndEndsWithItsStatesAndStatFunction`,
  `.HideOfAPlayerEndsAtItsAttackAndAtDamageUnlessItsTypeKeepsItThroughDamage`, `.HideOfAPlayerEndsAtASkillUnlessItIsASelfBuffWithinItsBuffCount`
  and `.HideOfAPlayerEndsAtAnItemUseUnlessItIsASkillItemItsBuffCountAllows`. No `HideEffect.cpp` change was needed. After every end these four
  also check that no observer of the hide stays on the player (`ObserveController::hasObservers`, so an observer added past
  `Effect.addObserver` would be caught). `.HideOfAPlayerTakesItOutOfTheSightOfTheCreaturesThatKnowItUntilItEnds` covers `onHide` and
  `onHideEnd` of a player: a monster that knows the hider stops seeing it, still knows it, and sees it again when the hide ends. The observer
  is an npc because this fixture cannot build the SM_PLAYER_INFO that a player observer would be sent.
- **Escape with a bind point on another map** (`EscapeEffect.applyEffect` -> `TeleportService.moveToBindLocation` -> the cross-map arm of
  `SpawnTask`) reaches `InstanceService::onLeaveInstance`, `AION_UNPORTED` at `services/instance/InstanceService.cpp:170` (P5-13); the same-map arm
  runs (`EscapeEffectTest.EscapeTeleportsTheCasterToItsBindPoint`).
- **`DamageEffect.calculateDamage` and `BleedEffect.startEffect`** reach `AttackUtil.calculateSkillResult` and
  `AttackUtil.calculateMagicalOverTimeSkillResult` (P5-01, item F-06), which the attack-effects lane ported in the same tree during this lane
  (uncommitted at HEAD c1edb0afb); the golden values of `DamageEffectsTest` ran through them.
- **The MP arm of `HealOverTimeEffect.onPeriodicAction` is live**: `MPHealEffect` (P5-04, ported in this part under D13) reaches it, and
  `CuringZoneService` casts 8751 *Light of Repose*, whose third position is an `<mpheal>`, once a second on every player near a curing object
  (maps 700010000 and 710010000). Test: `HealEffectsTest.AnMpHealOverTimeTicksAsTypeMpWithItsSkillId`. The FP and DP arms of both heal bases,
  and the MP arm of `AbstractHealEffect.applyEffect`, are reached only through subclasses that stay `AION_UNPORTED`: `DPHealInstantEffect`,
  `FPHealInstantEffect`, `DPHealEffect`, `FPHealEffect`, `CaseHealEffect` in this chunk, `MPHealInstantEffect`, `ProcMPHealInstantEffect`,
  `ProcHealInstantEffect`, `ProcFPHealInstantEffect`, `ProcDPHealInstantEffect` in P5-04. The arms themselves are ported and call ported bodies.
  **M5b-3:** `ProcHealInstantEffect` and `ProcMPHealInstantEffect` are ported (docs/deviations/P5-04.md, wave 5b-3), so the item-heal arms of
  `AbstractHealEffect.applyEffect` (TYPE.HP, TYPE.MP) and its MP arm are live; `ItemEffectsTest` (tests/effects_mz) covers both.

## Wave 5b-3, stage 1 (effects lane, m5b3-plan.md E-01..E-04)

Ported: `BlindEffect` (4 bodies and its anonymous `AttackStatusObserver`, `BlindEffect$1`), `DispelEffect` (1), `FearEffect` (4 bodies, its
anonymous `ActionObserver` `FearEffect$1` and the nested record `FearEffect.FearTask`) and `HiPassEffect` (1) - the P5-03 part of the 14 classes
of m5b3-plan.md §2.6. `census.py --chunks P5-03`: 122 -> 112 `AION_UNPORTED` sites, undeclared bodies 4 -> 3 (`FearTask.run`), anonymous bodies
in open members 11 -> 9. No header changed. Tests: `tests/effects_al/StateEffectsTest.cpp` (22 cases on the data's 10350, 8539, 8542, 262,
518, 283, 9928, 16447, 20355, 16600, 8252, 18803, 17573, 8361, 18464, 18892, 19110, 304, 16704, 2918 and 3222, plus test templates for the arms
the data does not isolate); its fixture fails a case that reaches an `AION_UNPORTED` site. The review fixes (same wave) pin what the first set
left open: each dispel arm with a dispel level apart from its power and a count below its matches (16600 Antidote: EFFECTTYPE, level 3, power
50, count 3 on four poisons; 18464 Happy Memory: EFFECTID, level 5, power 100, count 1; 283's `<evade>`: level 1, power 100; a SLOTTYPE test
skill of count 1, level 5, power 10, since the data's four SLOTTYPE dispels are all 255/100/100), and each statement of `FearEffect`: the hide
it ends, the abortMove of a walking npc and player, a fear reflected by 2918 Shield of Vengeance (the original effected is the effector), the
`isUnderFear()` guard of `FearTask`, and the ATTACK event of the end (an `NpcAI` that records it).

| Area | Java | C++ | Reason |
|---|---|---|---|
| `FearEffect$1.attacked` reads `resistchance` | the anonymous class reads the enclosing template's protected field through the captured `this` | `FearEffect_ActionObserver` keeps the fieldmap's captured `const FearEffect* fearEffect` and a C++-only `const int32_t resistchance`, which `startEffect` (a member, allowed to read the field) passes in | `FearEffect.h` has no friend line for the struct (the part-3 pre-approval covered `RootEffect`, `HideEffect` and `ProvokerEffect` only; headers are frozen). The template is immutable static data, so the value read at construction is the one Java reads at each attack. Header request: `friend struct FearEffect_ActionObserver;` in `FearEffect.h` (the `RootEffect.h` precedent); the copy then goes. Test: `StateEffectsTest.AFearOfResistChanceBelowAHundredBreaksOnAnAttack` |
| `FearEffect.FearTask` | a nested `record FearTask(Creature effector, Creature effected) implements Runnable` | a file-local `FearEffect_FearTask` in `FearEffect.cpp`: an aggregate `runtime::TaskStruct` with `const Ref<Creature> effector, effected`, marked `// fieldmap-class: ...FearEffect.FearTask` so the concurrency lint checks its members against fieldmap.json | `FearEffect.h` declares no nested `FearTask` (fieldmap.json's `cppName` is `FearEffect::FearTask`). The two `Ref`s retain what the record's final fields retain; the Future `Effect.setPeriodicTask` keeps is cancelled by `Effect.endEffect -> stopTasks`, which releases the task (cycles.toml `FearEffect.FearTask.effector/.effected`: java-hook). The record's `equals`/`hashCode` have no caller. Header request: `struct FearTask;` (or the whole record) nested in `FearEffect`, after which the struct moves there unchanged. Tests: `MaterialTestFearMakesAMonsterFleeForTenSeconds`, `AFearedPlayerStopsGlidingLeavesWalkModeAndRunsAway` |
| `DispelEffect.applyEffect`: absent lists | `effectids == null`, `effecttype == null`, `slottype == null` | `.empty()` | JAXB leaves a list without elements null and the bound C++ list is empty then; JAXB never produces an empty non-null list (the `BufEffect` row above). Test: `ADispelWithoutItsTypeOrItsListDispelsNothingAndAOneIdRangeThrows` |
| `DispelEffect.applyEffect`, EFFECTIDRANGE | `effectids.get(0)`, `effectids.get(1)` (re-read at every test of the loop), `effectId++` on an int | a file-local `listGet` throwing `runtime::IndexOutOfBoundsException` with the JDK's message, re-read at every test; the increment wraps through `uint32_t` | Java's exception for a range of one id (no data template has one: the only EFFECTIDRANGE, 9928, has two); C++ signed overflow is undefined. Same test |

`BlindEffect$1` needs no row: its `value` is the observer's own inherited field (`AttackStatusObserver.value`, set from the template's value by the
constructor), the `AlwaysDodgeEffect$1` precedent. `HiPassEffect.calculate` only adds its success, as Java does (no `super.calculate`).

### Reached next

- **A feared creature's flee point needs geo terrain.** `FearTask` asks `GeoService.findMovementCollision`; on a map without terrain (geo data off,
  as in every non-geo gate) `GeoMap.findMovementCollision` finds no ground 1 m ahead and answers the start position, so the creature "flees" to
  where it stands - Java's behaviour with an empty `GeoMap` as well, not a deviation. The direction is the geo gate's to see (G-04).
- `FearTask` moves an npc through `NpcMoveController.moveToPoint` -> `MoveTaskManager`, and a player through
  `PlayableMoveController.startMovingToDestination` -> `PlayerMoveTaskManager` (the controlled move, SM_MOVE); both ported, reached by the tests.
- `FearEffect.endEffect` hands an npc's AI `onCreatureEvent(ATTACK, <the npc itself>)` after `setStateIfNot(IDLE)` (FearEffect.java:96-99):
  Java's call, ported as is. The tests' `DummyAI` ignores it; `TheEndOfAFearHandsTheNpcsAiAnAttackEventOnTheNpcItself` gives the npc an `NpcAI`
  that records it. A monster's real AI then runs `AttackEventHandler.onAttack` (ported).
- **`EvadeEffect` is live through `DispelEffect`.** Java's `class EvadeEffect extends DispelEffect {}` is a data-only generated struct deriving
  `DispelEffect`, so the ported `applyEffect` runs the `<evade>` of the 10 templates that carry one - e.g. 283 *Remove Shock I* (an EFFECTTYPE
  dispel of STUN, STAGGER, STUMBLE, SPIN and OPENAERIAL, dispel level 1, power 100), which every class learns at level 40. Before this wave
  they reached `AION_UNPORTED`. No player skill of level 10 or below on the start maps reaches one. Test:
  `RemoveShockEvadesAStunWithDispelLevelOneAndPowerAHundred` (283 verbatim against 8361's stun).
- **`FearTask`'s `isUnderFear()` guard.** With the data, single-threaded, a `FearTask` never runs after its fear's FEAR state is gone:
  `Effect.endEffect` stops the task before `FearEffect.endEffect` unsets FEAR, and every data fear either has an effect id (`searchConflict`
  ends the older one) or a cooldown id of its own (`checkEffectCooldownId` ends the older one). In Java the guard also covers a run racing the
  end. The one state `EffectController` allows is a fear of no effect id in a skill of cooldown id 1, cast twice: `addEffect`'s `put` replaces
  the first without ending it, the first's end clears FEAR (`unsetAbnormal` counts only the second, which is in the map), and the second's
  `FearTask` stops moving the creature though the second fear lasts on. Ported as Java does it; test (a test skill, 21345 *Fascination*'s
  `<fear>` under cooldown id 1): `TheFearTaskMovesNoOneWhoIsNoLongerUnderFear`.
- `DispelEffect` does not reach `AbstractDispelEffect.applyEffect` (still `AION_UNPORTED`, `AbstractDispelEffect.cpp`): `DispelEffect` extends
  `EffectTemplate` directly; the `DispelBuff`/`DispelDebuff*`/`DispelNpc*` classes that extend `AbstractDispelEffect` stay unported.
  **M5e:** `AbstractDispelEffect.applyEffect`, `DispelDebuffEffect` and `DispelDebuffPhysicalEffect` are ported (below).

## M5e, stage 1 (effects-al lane, m5e-plan.md E-01, §15.4; worktree `wip/m5e-effects`)

Ported: the 20 classes of E-01, 43 `AION_UNPORTED` sites and the 5 inner bodies - `CondSkillLauncherEffect` (2 bodies and its anonymous
`ActionObserver`, `CondSkillLauncherEffect$1`: `hpChanged`, `onRemoved`; first, D7: the Gladiator's level-15 passive 563 *Determination* no
longer throws in `activatePassiveSkillEffects`, W-07), `AbstractDispelEffect` (1), `AlwaysBlockEffect` (2 and `$1`), `AlwaysParryEffect` (2 and
`$1`), `AuraEffect` (5 and the inner `AuraTask`), `BackDashEffect` (1), `BindEffect` (4), `BoostSkillCastingTimeEffect` (1),
`CarveSignetEffect` (1), `CurseEffect` (3), `DashEffect` (1), `DeformEffect` (4), `DelayedSkillEffect` (2), `DelayedSpellAttackInstantEffect`
(2 and its scheduled hit, fieldmap `@L30:44`), `DispelBuffCounterAtkEffect` (4), `DispelDebuffEffect` (1), `DispelDebuffPhysicalEffect` (1),
`FallEffect` (2), `FpAttackInstantEffect` (2) and `HostileUpEffect` (2, its temporary hate task `@L46:72` and `DeathObserver` `@L50:39`).
`census.py --chunks P5-03`: 112 -> 69 `AION_UNPORTED` sites, undeclared bodies 3 -> 1 (`ConfuseTask.run`, not M5e's). No header changed.
Tests: `tests/effects_al/DaevaEffectsTest.cpp` (22 cases) and `DispelAndMonsterEffectsTest.cpp` (17 cases) on the fixture of
`DaevaEffectsTestSupport.h`, over the data's 563, 8930, 1809, 8998, 2945, 2981, 517, 1574, 1350, 873, 1417, 4285, 8905, 2529, 1759, 3455,
2350, 3385, 3365, 3935, 4443, 9909, 8539, 8361, 8542, 16704, 18892, 16423, 2974, 16702, 3570, 1421, 16436, 16905, 17235, 8217 and 1995 (positions of
another lane's classes left out and named per template); the fixture fails a case that reaches an `AION_UNPORTED` site. The monsters-only
classes (`AlwaysBlockEffect`, `CurseEffect`, `FallEffect`, `FpAttackInstantEffect`) are cast from an `Npc` effector.

| Area | Java | C++ | Reason |
|---|---|---|---|
| `CondSkillLauncherEffect$1.hpChanged` reads `skillId` | the anonymous class reads the enclosing template's protected field through the captured `this` | `CondSkillLauncherEffect_ActionObserver` keeps the fieldmap's captured `const CondSkillLauncherEffect* condSkillLauncherEffect` and a C++-only `const int32_t skillId`, which `startEffect` passes in; `value` is read through the template's public generated `getValue()`, which no subclass overrides | `CondSkillLauncherEffect.h` has no friend line for the struct (headers are frozen; the `FearEffect$1` precedent above). The template is immutable static data, so the copy is the value Java reads at each report. Header request: `friend struct CondSkillLauncherEffect_ActionObserver;` (m5e-plan.md §7); the copy then goes. Tests: `DeterminationLaunchesAPermanentBuffAtTenPercentHpAndEndsItAbove`, `ANonPassiveLauncherAppliesTheBuffForItsOwnDuration` |
| `CondSkillLauncherEffect$1.onRemoved` | `if (conditionalEffect != null) conditionalEffect.endEffect();` - two reads of the field | one read into a local | Equivalent single-threaded; a concurrent `hpChanged` resetting the field between Java's two reads would be a `NullPointerException` there. `hpChanged` keeps Java's `synchronized (this)` as `SYNCHRONIZED(*this)` (lint L7) |
| `AuraEffect.AuraTask` | the inner class `AuraTask implements Runnable` (constructor, `run` -> `onPeriodicAction`, `Thread.yield()`) | a file-local `AuraEffect_AuraTask` in `AuraEffect.cpp`: an aggregate `runtime::TaskStruct` with `const Ref<Effect> effect` and the captured `const AuraEffect* auraEffect`, marked `// fieldmap-class: ...AuraEffect.AuraTask`; `Thread.yield()` is `std::this_thread::yield()` | `AuraEffect.h` declares no nested `AuraTask` (fieldmap.json's `cppName` is `AuraEffect::AuraTask`, K4). The `Ref` retains the effect as the inner class's field does; the Future `Effect.setPeriodicTask` keeps is cancelled by `Effect.endEffect -> stopTasks`, which releases the task (cycles.toml `AuraEffect.AuraTask.effect`: java-hook) - the `FearEffect.FearTask` precedent above. Header request: `struct AuraTask;` nested in `AuraEffect` (m5e-plan.md §7), after which the struct moves there unchanged. Tests: `CelerityMantraReappliesItsEffectEverySixAndAHalfSeconds`, `AnNpcsAuraAppliesItsEffectToTheNpc` |
| `DelayedSpellAttackInstantEffect`'s delayed hit: a template without `hoptype` | passes the null `hopType` field to `CreatureController.onAttack(Effect, TYPE, int, boolean, LOG, HopType)` | passes `hopType.value_or(HopType::SKILLLV)` | The `DamageEffect.onAttack` row above: the Effect overloads take a non-nullable `HopType`, and their only reader, `AggroList.addDamage`, compares it with DAMAGE, so `SKILLLV` answers exactly what Java's null does there; the `NpcController`, `PlayerController` and `SummonController` overrides pass the value on unchanged. 20 of the data's 25 `<delaydamage>` templates have a hoptype; the other 5 (skill_templates.xml:112737, 122334, 123479, 123679, 181472) take this path. Test: `TheDelayedHitAddsTenTimesItsDamageAsHate` (hoptype DAMAGE: the hit's hate) |
| `HostileUpEffect.applyEffect`'s `AtomicReference<DeathObserver> observerRef` | a local the lambda captures and the method sets after scheduling | `Ref<Rc<AtomicReference<Ref<DeathObserver>>>>`: the shim made a heap object, so the task's closure and the method share it (fieldmap.json's capture type is `AtomicReference<Ref<DeathObserver>>`); the task skips `removeObserver` when it reads null | Representation only. Java's `removeObserver(null)` removes nothing (`observers.remove(null)` is false, so no `onRemoved`); the C++ `removeObserver` takes a reference. The closure -> observerRef -> DeathObserver -> its consumer's `FutureRef` -> the task -> the closure cycle ends when `temp_duration` runs the task or the effector's death cancels it (`Future` releases its callable, `runtime/sched/Future.h`); cycles.toml has no row for either callback and `lint_concurrency --cycles` asks for none. Java's per-template `tempHate` is `Effect::hostileUpTempHate` (the existing deviation of `HostileUpEffect.h`), read by the task when it runs as Java reads the field. Tests: `InciteRageAddsTemporaryHateForFiveSeconds`, `TheEffectorsDeathCancelsTheTemporaryHatesRemoval` |

### Reached next

- **`BoostSkillCastingTimeEffect`'s SLOW_RESISTANCE arm resists nothing.** `Effects.normalizeNoResist` (Effects.java:20-29, 176-179) makes every
  `boostskillcastingtime` noresist at load, so `super.calculate(effect, SLOW_RESISTANCE, null)` differs from `super.calculate(effect)` only in the
  altered-state immunity check (`isImmuneToAbnormal`: a boss or a static npc refuses an enemy's negative boost). Java's behaviour, ported as is;
  test: `ANegativeCastingBoostOnAnEnemyPassesSlowResistanceToCalculate` (an `NpcAI` that answers `IS_IMMUNE_TO_ABNORMAL_STATES`).
- **The dashes need geo terrain.** On the empty `GeoMap` of a map without geo data, `DashEffect`'s `getClosestCollision` answers the target
  point (tested: 3455 moves the Assassin to (508.75, 500), and on a diagonal to (508.85806, 504.49158)), and `BackDashEffect`'s `findMovementCollision` finds no ground 1 m behind and
  answers the start position, so a back dash only turns the effector - Java's behaviour with an empty `GeoMap` as well. A `tests/geo`-fixture
  case (m5e-plan.md G-04) needs a way to put a terrain into `GeoService`'s map of the test Poeta, which `GeoService` does not offer a test; the
  geo gate sees the direction.
- `AuraEffect.onPeriodicAction`'s team arm reaches `TemporaryPlayerTeam.getOnlineMembers` (`AION_UNPORTED`, M5g; W-23): dormant for a player
  without a group or an alliance, which is every player until M5g.
- `CarveSignetEffect` launches the carved signet through `SkillEngine.applyEffect(signet_id + level - 1)`: the data's 8303-8307 are `<signet>`
  (`SignetEffect`, P5-04, the parallel effects-mz lane); the tests stand in a statdown of the same stack and duration. The data's signets
  have no effectid, so `EffectController.addEffect`'s `put` replaces the stack's entry without ending the previous signet: only
  `activeSignet.endEffect()` does, which the stand-ins' statdown shows (one PHYSICAL_DEFENSE - 1 at a time).
- The data's deform skills (1417, 17, 18, 4406) have a `<sleep>` (`SleepEffect`, P5-04) at position 2, and the taunts (2981-2983, 2945) a
  `<targetchange>` (P5-04) at position 2; both are the effects-mz lane's. `DelayedSkillEffect` launches 8905 (`ProcAtkInstantEffect`, ported),
  `CondSkillLauncherEffect` 8930 (`ProvokerEffect`, ported; its 8931 stun is ported too), `AuraEffect` 8998 (`StatupEffect`, ported).
- `tests/ai/NpcSkillTestSupport.h:33-34` (P5-05's test support) still says `CarveSignetEffect` is `AION_UNPORTED`; stale after this wave.
