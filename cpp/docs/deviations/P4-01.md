Earlier configs deviations are in [../DEVIATIONS.md](../DEVIATIONS.md), section "game-server / configs".

## Wave 5a (world-engines lane, m5a-plan.md W-01)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `gameserver.dev.missing_ai_handlers` (`AIConfig::MISSING_AI_HANDLERS`) | No such key: `AIEngine.validateScripts` throws `GameServerError` for npc template AI names without a handler, `newAI` throws "No AI found for name" | C++-only optional key, default `fail` (Java behaviour). `warn` (compared ignoring case; any other value counts as `fail`): `validateScripts` logs the same message as a warning with the suffix "(gameserver.dev.missing_ai_handlers=warn: these NPCs get a DummyAI)", and `newAI` creates the DummyAI for such a name and logs "No AI found for name X, creating a DummyAI instead (gameserver.dev.missing_ai_handlers=warn)" once per name (`AIEngine::missingAiNamesSeen()`). The M5a profile sets `warn` (m5a-plan.md D1) | The AI handlers are ported in wave 5b (D2); NPCs must spawn without them. Tests: `AiTest.MissingHandlerFailsByDefaultLikeJava`, `WarnModeCreatesADummyAiAndWarnsOncePerName`, `InitValidatesTheNpcTemplateAiNames`, `AIConfigDevKeyTest.MissingAiHandlersBindsTheCppOnlyKey` |
| Substitute AI type in `warn` mode for an `Npc` owner (m5b-plan.md D15, item A-00) | Not reachable: `newAI` throws `IllegalArgumentException("No AI found for name " + name)` (AIEngine.java:70-71) for an unregistered name, so an `Npc` whose template names an AI always has an `NpcAI` and the `(NpcAI) creature.getAi()` casts of `NpcController.onAttack` (NpcController.java:303), `PlayerController.onAttack` (PlayerController.java:451) and `NpcMoveController` cannot fail | In `warn` mode `AIEngine::newAI` substitutes `AIEngine::DummyNpcAI` (a private class in `AIEngine.cpp`) instead of `DummyAI<Creature>` when the owner is an `Npc`, so `runtime::cast<ai::NpcAI>` keeps working. `DummyNpcAI` derives `NpcAI` and overrides every hook `NpcAI` overrides over `AITemplate` back to the empty body, `ask` back to `return false` and `isDestinationReached` back to `return false`: the behaviour is a `DummyAI`'s, only the static type differs. The other `DummyAI` arm — a **null** AI name (an `npc_template` without `ai`, or a spawn with `SpawnTemplate::NO_AI`) — is **not** redirected: Java's `new DummyAI<>(owner)` is not an `NpcAI` there either, so a cast failure on that path is Java's own behaviour and stays | The five `runtime::cast<ai::NpcAI>` sites are faithful ports of Java casts that cannot fail; what makes them fail is the C++-only `warn` deviation, so the fix belongs in `AIEngine` and not as a C++-only `runtime::as` branch inside five Java-faithful controller bodies. Poeta alone leaves 19 npc ids on a substitute AI after the three root handlers of A-06 are registered, and the whole tree 429 ai names; no scenario gate can catch this, because every NPC a gate script touches has a registered handler (m5b-plan.md §8 risk 19). Tests: `DummyNpcAITest.AnNpcWithAnUnregisteredAiNameStillCastsToNpcAI`, `TheSubstituteAnswersEveryQuestionFalseLikeADummyAi`, `TheSubstitutesHooksAreNoOps` |
| Substitute `DummyAI` name in `warn` mode | Not reachable: every NPC gets the handler its `npc_template` `ai_name` selects, and `AbstractAI.getName()` returns that name | The substitute `DummyAI` of the `warn` branch is created **without** `setRegistryEntry`, so `AbstractAI::getName()` falls through to the Java default `"noname"` (`AbstractAI.cpp`). At M5a no AI handler is compiled in, so every NPC takes this path and every `AILogger` line (and anything else reading `AI::getName()`) shows `noname` instead of the `npc_template` `ai_name` | There is no registry entry to point at: the name is the entry's. Only debug logging reads it; no packet, no Java-visible behaviour depends on it. It disappears in wave 5b when the handlers are registered (D2) |
