Earlier configs deviations are in [../DEVIATIONS.md](../DEVIATIONS.md), section "game-server / configs".

## Wave 5a (world-engines lane, m5a-plan.md W-01)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `gameserver.dev.missing_ai_handlers` (`AIConfig::MISSING_AI_HANDLERS`) | No such key: `AIEngine.validateScripts` throws `GameServerError` for npc template AI names without a handler, `newAI` throws "No AI found for name" | C++-only optional key, default `fail` (Java behaviour). `warn` (compared ignoring case; any other value counts as `fail`): `validateScripts` logs the same message as a warning with the suffix "(gameserver.dev.missing_ai_handlers=warn: these NPCs get a DummyAI)", and `newAI` creates the DummyAI for such a name and logs "No AI found for name X, creating a DummyAI instead (gameserver.dev.missing_ai_handlers=warn)" once per name (`AIEngine::missingAiNamesSeen()`). The M5a profile sets `warn` (m5a-plan.md D1) | The AI handlers are ported in wave 5b (D2); NPCs must spawn without them. Tests: `AiTest.MissingHandlerFailsByDefaultLikeJava`, `WarnModeCreatesADummyAiAndWarnsOncePerName`, `InitValidatesTheNpcTemplateAiNames`, `AIConfigDevKeyTest.MissingAiHandlersBindsTheCppOnlyKey` |
| Substitute `DummyAI` name in `warn` mode | Not reachable: every NPC gets the handler its `npc_template` `ai_name` selects, and `AbstractAI.getName()` returns that name | The substitute `DummyAI` of the `warn` branch is created **without** `setRegistryEntry`, so `AbstractAI::getName()` falls through to the Java default `"noname"` (`AbstractAI.cpp`). At M5a no AI handler is compiled in, so every NPC takes this path and every `AILogger` line (and anything else reading `AI::getName()`) shows `noname` instead of the `npc_template` `ai_name` | There is no registry entry to point at: the name is the entry's. Only debug logging reads it; no packet, no Java-visible behaviour depends on it. It disappears in wave 5b when the handlers are registered (D2) |
