Merged into [../DEVIATIONS.md](../DEVIATIONS.md), section "game-server / world" (phase 4 close-out, 2026-09-15).

## Wave 5a (world-engines lane, m5a-plan.md W-04, W-05)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `World.storeObject` / `World.removeObject` | No census (the garbage collector) | A successful `storeObject` calls `LeakCensus::onAddedToWorld(object)`; a successful `removeObject` calls `LeakCensus::onRemovedFromWorld(object, typeid(object).name(), objectId)` | Feeds the leak census and the zombie breaker (runtime-architecture.md §5.4, m5a-plan.md D8); the events are no-ops while the census is not installed. Test: `WorldM5aTest.RemoveObjectFeedsTheLeakCensusAndStoreObjectTakesTheObjectOutAgain` |
| `VisibleObjectSpawner.spawnGatherable` | `new Gatherable(spawn, new GatherableController())`, then `bringIntoWorld` | `AION_PARTIAL`, returns null (`spawnObject` handles null like a missing npc template): no gatherable is spawned | The `GatherableController` constructor and destructor destroy its `Field<Ref<GatheringTask>>`, which needs `skillengine/task/GatheringTask.h` (P5-02, not written yet). **Header request `world-engines-1`** asks for that declaration header; `TODO(header-request)` at `VisibleObjectSpawner.cpp` and `GatherableController.h`. Replace with the Java body once it exists |
| Consequences of the `spawnGatherable` stub (same row, M5a scope) | `SpawnEngine.spawnAll` logs "Loaded N gatherable spawns" (SpawnEngine.java:202-203) and the client gets `SM_GATHERABLE_INFO` for every gather spot it comes near | The count is **0**: `StatsCollector` counts objects that entered the world, and none does. No `SM_GATHERABLE_INFO` is ever sent. The plan's real-client checklist item 7 ("Gather nodes are visible", m5a-plan.md:386) cannot pass, and scenario check V4 ("`SM_GATHERABLE_INFO` ids ⊆ the gather spots within 100 m") is satisfied by the empty set | Not silently invisible: D3 makes the gate require every `AION_PARTIAL` site hit to be listed in `tests/scenario/m5a_partial_allowlist.txt`, and `spawnGatherable` is deliberately **not** listed, so the gate fails on it until the header request lands. Stage 2 should additionally make V4 require a non-empty set for the Poeta/Ishalgen start area |
