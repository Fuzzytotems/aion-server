## Wave 5a (player-events lane, m5a-plan.md E1-05)

Ported M5a subset: FindGroupService.onLogout, AutoGroupService (getInstance, onEnterInstance, onPlayerLogin, isSearching, getSearchEntry x2,
getSearchEntries, onLogout), and the new declaration headers `model/team/group/PlayerGroupService.{h,cpp}` and
`model/team/alliance/PlayerAllianceService.{h,cpp}` with onPlayerLogin, onPlayerLogout (no-team paths) and searchGroup/searchAlliance ported.
Tests: `tests/team`.

| Area | Java | C++ | Reason |
|---|---|---|---|
| `AutoGroupService.getSearchEntry(int, List)` | Iterates the live list inside `synchronized (parties)` | The frozen signature takes a `std::vector`: callers pass a snapshot taken under the list's Monitor (the same lock), then the vector is searched | Same result; a concurrent change after the snapshot is not seen (Java would see it after acquiring the lock). Lint L7 waiver in the `.cpp` |
| `AutoGroupService.onLogout` / `onEnterInstance` | `autoInstances.get(player.getWorldMapInstance())` | `NullPointerException` when the player has no map instance | Java's `ConcurrentHashMap.get(null)` throws the same |
| `PlayerGroupService`, `PlayerAllianceService` | Static-only classes with static maps | `static inline` shims in the header; the `OfflinePlayer*Checker` Runnables are declared in the class and defined in the `.cpp` (hub-headers.md §9.3) | New headers written for M5a; members follow `fieldmap.py --class` |

### AION_PARTIAL / unported on purpose

No `AION_PARTIAL` site in P5-10. Inside the ported bodies, the branches that need classes without a header stay `AION_UNPORTED`: the team events
(`PlayerConnectedEvent`, `PlayerDisconnectedEvent`, `PlayerGroupMember.updateLastOnlineTime`) and `AutoGroupService.onLogout`'s
`cancelEnter(..., autoInstance.getAutoGroupType().getTemplate().getMaskId())` (the AutoGroupType companion). A player without a team or search
entry never reaches them.
