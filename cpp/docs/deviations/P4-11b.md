Merged into [../DEVIATIONS.md](../DEVIATIONS.md), section "game-server / controllers" (phase 4 close-out, 2026-09-15).

## Wave 5a, stage 2 (fix-world lane, m5a-plan.md W-04 / W-07)

No deviation. `GatherableController` is fully defined now that P5-02 wrote `skillengine/task/GatheringTask.h` (header request
`world-engines-1` / `hr-2`): the constructor, the destructor, `startGathering`'s task branch, `completeInteraction`, `cancelGathering` and
`getGatheringPlayerId` are the Java bodies and no `AION_UNPORTED` is left in the file. Until that header existed the whole class was
undefined, which is why the world held no gatherable at all (see docs/deviations/P4-10.md).
