# P5-02 skills: deviations

## Wave 5a (stats-skills lane, m5a-plan.md B-05, B-06)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `SkillEngine.applyEffectDirectly(SkillTemplate, int, Creature, Creature)` | Creates the effect, initializes and applies it, returns it | `AION_PARTIAL("passive skill effects are not applied yet (M5a O-09)")`, returns null | Effect application is M5b work (m5a-plan.md D3, O-09); the only M5a caller (`PlayerEnterWorldService.activatePassiveSkillEffects`, `SkillLearnService.onLearnSkill` for passives) ignores the result. The other overloads stay `AION_UNPORTED`. **Status (M5b-2): ported in part 2, held back until part 3.** The body and every other overload are ported (P5-02a.md); this overload is kept behind the same partial until part 3 ports the effect leaves the passives reach (m5b2-plan.md F-02/F-03). **Closed by the part 3 Regate (m5b2-plan.md G-05, D11): Java's body, no partial.** Test: `SkillCastTest.ApplyEffectDirectlyOfATemplateAndTheIdOverloadsCreateARealEffect` |
| `PlayerSkillEntry(Player, int, int, PersistentState)` | Delegates with skillType 0, then assigns `skillType` in the body | Computes the skill type from the learn templates first and delegates with it | `skillType` is a `const` member (PlayerSkillEntry.h); the value and the lookups are Java's |
| `NpcSkillList.initSkillList`, a template whose skill is missing from `SKILL_DATA` | Logs `Missing skill <id> for npc <npcId>` and removes the template from the shared `NpcSkillTemplates` list of the static data, so the next npc of that id neither warns nor sees it | Logs the same line and skips the template; the static data is not changed | The bound static data is immutable in C++. The skill list of every npc is the same as Java's; only the warning is logged once per npc instance instead of once per npc id. It also removes Java's race: `spawnAll` builds these lists on the ForkJoin pool, where Java mutates one shared `ArrayList` from several threads (`// java-race` would not be fixable in place). Test: `NpcSkillListTest.SkillsAndPrioritiesOfAnNpcWithSkillTemplates` |
| `NpcSkillTemplateEntry.canUseNextChain(Npc owner)` | `owner != null && ...` | The parameter is a reference, so only the time comparison is left | The frozen `NpcSkillEntry` header declares `Npc&`; a null owner is the caller's error |
| `PlayerEffectController.addSavedEffect` | Creates the `Effect`, `put`s it into the effect map, starts it and sends `SM_ABNORMAL_STATE` | Java's checks are ported; the restore itself is `AION_PARTIAL("a saved effect is not restored: ...")` | `EffectController.put`, `Effect.addAllEffectToSucess` and `Effect.startEffect` are M5b work (O-09). `PlayerEffectsDAO` calls this once per stored row while the character loads, so an unported body would abort enter-world (plan D3 forbids that); nothing writes `player_effects` at M5a, so the D1 profile never reaches the stub. Test: `PlayerSkillLearnTest.SavedEffectsAreSkippedWithAWarningInsteadOfAbortingTheLogin` **Status (M5b-2): closed in part 2 by the effect lane (P5-02b.md, "AION_PARTIAL sites this lane closes").** |

### AION_PARTIAL sites (for tests/scenario/m5a_partial_allowlist.txt)

As of M5b-2 stage 1 part 2 this M5a list is history; the current state of each site:

- `skillengine/SkillEngine.cpp` `SkillEngine::applyEffectDirectly(const SkillTemplate*, int32_t, Creature&, Creature&)` (every passive skill of
  enter-world and of a learned passive skill) - ported in part 2 and held back until part 3 (P5-02a.md); **closed by the part 3 Regate**, and its
  allow-list rows are gone
- `controllers/effect/PlayerEffectController.cpp` `PlayerEffectController::addSavedEffect` (only for a stored effect row, i.e. never for a fresh
  D1 character) - **closed in part 2** (P5-02b.md)

### Ported outside the planned subset

`NpcSkillTemplateEntry` (new class) and `NpcSkillList::initSkillList` for npcs that have `npc_skills` rows: without them every spawn of such an
npc threw `UnportedException` from the `Npc` constructor, which ended `SpawnEngine.spawnAll` and with it the whole startup. `conditionReady` and
`fireOnEndCastEvents` of the new class stay `AION_UNPORTED` (they need the effect controller, `TribeRelationService` and `SpawnEngine`); D2 gives
every npc `DummyAI` at M5a, so no npc casts. `Event.isEventEffectForceType` (P5-12b) had to be ported for `addSavedEffect`'s first line.

## Wave 5a, stage 2 (fix-svc-a lane, item X-01a; header request world-engines-1 / hr-2)

`skillengine/task/GatheringTask.h` and `AbstractCraftTask.h` are the answer to the open header request of the world-visibility lane: without the
complete `GatheringTask` the `GatherableController` constructor and destructor could not be defined, which kept every `Gatherable` out of the
world. `AbstractInteractionTask::start/stop/abort/isInProgress` were ported with them, so a started gathering really runs.

| Area | Java | C++ | Reason |
|---|---|---|---|
| `GatheringTask.gathererObserver` | `private final ActionObserver` | `runtime::Field<runtime::Ref<ActionObserver>>`, cleared by `onInteractionFinish` right after the observer is detached and recreated by `onInteractionStart` when it is empty | `cpp-breaker` of `generated/concurrency/cycles.toml` and the `fieldmap.toml` override: Java's final field makes the task and its observer keep each other. A second `onInteractionFinish` (aborting an already stopped task) therefore finds the field empty and skips the removal, where Java would remove the same observer twice. Java's field is never null, so a restarted task re-attaches the same observer; the stage-2 fixer run added the recreation in `onInteractionStart`, which otherwise dereferenced an empty `Ref` (`GatherableController` creates a fresh task per gather today, so it was latent). Test: `InteractionTaskTest.ARestartedGatheringTaskGetsItsObserverBack` |
| `GatheringTask` constructor | `this.gathererObserver = createGathererObserver()` (captures `this`) | `create()` sets the observer immediately after construction, before the task is visible to anyone | A `Ref` to a `RefCounted` cannot be taken inside its own constructor (two-phase construction, handlers-and-porting-plan.md §1.7). Test: `InteractionTaskTest.TheGathererObserverAbortsTheGathering` |
| `AbstractCraftTask.CraftType.getProgressId()` | Enum constructor data | The free function of `skillengine/task/AbstractCraftTask_CraftTypeInfo.h` | Companion of a generated nested enum (docs/design/static-data.md §2.5) |
| `AbstractInteractionTask.start` | Schedules an anonymous `Runnable` that captures the task | Schedules a closure holding a `Ref` to the task, pinned to the requester (`Pin`) | The `Ref` is the retention Java's inner class has (cycles.toml: `stop`/`abort` cancel the task and release it); the pin adds no retention but makes the task visible to the leak census and the zombie breaker (runtime-architecture.md §5.4). The Java body is the private `runInteraction()`. Tests: `InteractionTaskTest.StartSchedulesTheTaskAndAbortStopsIt`, `AnOfflineRequesterStopsTheTaskOnItsFirstRun`, `StartingASecondTaskAbortsTheFirst` |
| `Skill(SkillTemplate, Creature, int, Creature, ItemTemplate)` | Ends with the overridable `initializeSkillMethod()` | Calls `Skill::initializeSkillMethod()` explicitly | A C++ constructor cannot dispatch to a subclass override; `PenaltySkill`'s constructor must call its own `initializeSkillMethod()` after the base constructor (already noted in `Skill.h`). No subclass exists yet. Tests: `SkillConstructionTest.*` |
| `Skill(SkillTemplate, Player, Creature)` | `this(..., effector.getSkillList().getSkillLevel(skillTemplate.getSkillId()), ...)` | The same, so an unknown skill throws `NullPointerException` out of `PlayerSkillList::getSkillLevel` | Java's `skills.get(skillId).getSkillLevel()` throws the same way |

### Ported outside the planned subset (stage 2)

`AbstractCraftTask` (new), `GatheringTask` (new), `AbstractCraftTask_CraftTypeInfo.h` (new), `AbstractInteractionTask::start/stop/abort/
isInProgress`, `Skill`'s three constructors, `Skill::initializeSkillMethod`, `Skill::getSkillId` and `Skill::isPassive`. The last three were on
the list because the constructor and `PvpService` were the two `C4702` "unreachable code" warnings of the checked RelWithDebInfo gate build that
belong to this lane (scenario-lane change request, m5a-plan.md §5.10); porting the bodies removes them without a pragma.

### Not a deviation: a throwing interaction body (stage-2 fixer run)

A review finding proposed a C++-only breaker for `AbstractInteractionTask`, on the assumption that a throwing `onInteraction` ends the periodic
`Future` as FAILED and leaves `task` -> `Future` -> body -> task alive because `stop()` never runs. It does not: `ThreadPoolManager.scheduleAtFixedRate`
wraps the runnable in `RunnableWrapper(..., catchAndLogThrowables = true)` (ThreadPoolManager.java:60-62), so in Java the exception is logged
inside the body and the periodic task is re-armed, and `Future` does exactly the same here (`logExceptions`, Future.h:89, `invokeBody` reports
`failed = false`). A throw therefore never bypasses a later `stop()`, and cancelling releases the captures (`Future::cancel` ->
`releaseCaptures`), so the handle left in the field after an external cancellation retains nothing. `runInteraction` and `stop` stay Java's;
both facts are now recorded as comments there.

`GatheringTask` calls `GatherableController::completeInteraction` (from `onInteractionFinish`, GatheringTask.java:50) and `rewardPlayer` (from
`onSuccessFinish`, :83), which P4-11b ported; the two remaining `AION_UNPORTED` bodies of that controller (`startGathering`'s tail and
`getGatheringPlayerId`) were the P4-11b owner's to close now that this header exists. **Both are closed**: the stage-3 fixer run drives the real
`startGathering` and reads `getGatheringPlayerId` in `InteractionTaskTest.TheControllerStartsAGatheringThatRunsToItsFailureFinish` (the call
chain changed only in this file's tests, not in the controller).

## Wave 5a, stage 3 (code-findings lane)

### Kept Java races (`// java-race`, design decision D6)

`AbstractInteractionTask::start` and `AbstractInteractionTask::stop` carry a `// java-race` marker (runtime-architecture.md §3.7,
docs/design/conventions-game-server.md): both guards are Java's unsynchronized check-then-act on `Player.interactionTask`
(`AbstractInteractionTask.java:61-64` and `:84-85`), and the lock-per-operation `Field` makes each read and each write safe without making the
pair atomic. `PlayerLeaveWorldService.java:125-126` (`if (player.getInteractionTask() != null) player.getInteractionTask().abort()`) is the same
pattern in another chunk and is not marked yet; it is listed in the stage-3 report for the P5-07 owner.

**What the cycle is, and what is not claimed about it** (rewritten in the stage-3 fixer run). `Player.interactionTask` is a
`Field<Ref<AbstractInteractionTask>>` and `AbstractInteractionTask.requester` is a `const Ref<Player>`, so the two form a reference cycle that
Java's garbage collector reclaims and refcounting does not. The only hook that cuts it is the guarded write above:
`generated/concurrency/cycles.toml` resolves `com.aionemu.gameserver.model.gameobjects.player.Player.interactionTask` as
`java-hook: AbstractInteractionTask finish / abort call requester.setInteractionTask(null) (AbstractInteractionTask.java:85; leaveWorld aborts
it)`. When the guard loses its race — a concurrent `start()` replaced the field between this task's read and its write — the losing task skips
the clearing and the field keeps naming the task that replaced it.

An earlier version of this section went on to state, as fact, that the cycle therefore survives `leaveWorld` and leaves **a stale `Player` across
a relogin**, visible as a non-zero `Player` row of `live_counts.txt` (§5.7 Q8). That consequence was never reproduced, the stage-3 review could
not construct the interleaving, and the code points the other way, so it is **withdrawn**: every write that stores a task comes from a `start()`,
which schedules the periodic run immediately afterwards; every run begins with `!requester->isOnline() || onInteraction()`
(`AbstractInteractionTask.java:69`); and `PlayerLeaveWorldService.java:65` (`PlayerLeaveWorldService.cpp:87`) nulls the client connection —
"this sets the player semi-offline" — before `:125-126` aborts the interaction task. Whichever task the field names after a logout therefore
stops itself on its next run, and that `stop()` then does find itself in the field and clears it. The race is real and kept; the consequence
claimed for it was not.

Fixing the race (an atomic compare-and-set of `Player.interactionTask`) would need a signature change on the frozen `Player.h` and would change
the behaviour of concurrent `start()` calls, so it is kept and documented per D6.

### The shape that does keep the cycle: a throwing `onInteractionStart`

`start()` writes `requester.setInteractionTask(this)`, then calls `RecallService.cancel` and `onInteractionStart()`, and only then schedules the
periodic run (`AbstractInteractionTask.java:64-77`). A throw in that window leaves the field naming a task that will never run, so nothing but an
explicit `stop()` cuts the cycle — the sibling of the throwing periodic body of m5a-plan.md §10.5, and, like it, exactly what Java does (its
garbage collector reclaims the cycle afterwards, which refcounting cannot). The port is faithful, so this is a fact, not a deviation. Test:
`InteractionTaskTest.AThrowingOnInteractionStartLeavesTheTaskInTheField` (the field still names the task, no run ever stops it, `stop()` cuts it).
No M5a path throws there: `GatheringTask::onInteractionStart` sends packets and attaches the gatherer observer, and `CheckOutput::zeroLiveClasses()`
lists `Player`, `AbstractInteractionTask` and `GatheringTask`, so a run in which it happened would fail the gate through its own ERROR line.

### Gathering runs to completion in a test

Two tests drive a real `GatheringTask` through `ThreadPoolManager` until it ends. Both give the player one accepted, real `AionConnection` from a
loopback `NioServer` (`TestClientLink` in the test file; `tests/network/support/GameServerTestServer.h` is the test support of P4-15 and not on
this chunk's include path), because the scheduled body is `!requester.isOnline() || onInteraction()` (`AbstractInteractionTask.java:69`) and
`Player.isOnline()` is `getClientConnection() != null` (`Player.java:348`), so every earlier test stopped the task on its first run without
reaching a single `GatheringTask` body.

- `InteractionTaskTest.AGatheringTaskRunsToItsFailureFinish` builds the task by hand with `skillLvlDiff = -1`, so `analyzeInteraction` fills the
  failure bar on the first run and the second run takes `onFailureFinish`. Only a hand-built task reaches that branch:
  `GatherableController::checkPlayerSkill` refuses a player below the template's skill level, so a gathering started through the controller never
  has a negative difference.
- `InteractionTaskTest.TheControllerStartsAGatheringThatRunsToItsFailureFinish` (stage-3 fixer run) takes the real entry point,
  `GatherableController::startGathering`, with the harvest skill at exactly the template's level (difference 0) and
  `CraftConfig::MAX_GATHER_FAILURE_CHANCE` at 100 for the scope of the test, which makes every `analyzeInteraction` fail (`Rnd::chance()` is
  `[0, 100)`), so the failure bar fills in eight runs and the ninth finishes the interaction. **This is the test that covers
  `GatheringTask::onInteractionFinish`**: with the task registered in the controller, `getGatheringPlayerId()` is the gatherer's object id before
  the finish and 0 after it (`completeInteraction`), and `ObserveController::hasObservers()` is true before and false after (the observer
  removal). The hand-built test asserted neither: its task is not the one the controller holds, so `getGatheringPlayerId()` was 0 either way, and
  `notifyMoveObservers()` on an already stopped task changes nothing whether the observer is still attached or not.

The success branch still needs `ItemService.addItem` and a `WorldMapInstance` with an instance handler and stays out of a unit test.

## Wave 5b-1, stage 1 (npc-skills-shim lane, m5b-plan.md E-02 and E-05)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `SkillEngine.createCriticalProcEffect`, the `isUnderNormalShield` guard | First statement: `if (target.getEffectController().isUnderNormalShield()) return null;` (SkillEngine.java:194-195) | **Skipped**; the body starts with the weapon-group switch | **Closed in M5b-2 part 2: the body is SkillEngine.java:193-225 in Java's order again (P5-02a.md); test `SkillRulesTest.ATargetUnderANormalShieldIsNeverStumbled`.** m5b-plan.md **D14**. `EffectController::isUnderNormalShield` is itself `AION_UNPORTED` (`controllers/effect/EffectController.cpp:122-124`), so keeping the guard would make every critical hit of a `Player` throw — and the guard can only change the answer while a normal-shield effect is up, which cannot happen before the effect engine lands (M5b-2, work item O-01). **M5b-2 closes this row together with the `id != 0` partial below.** Tests: `CriticalProcEffectTest.*` (they call the body with a target that has no effects, which is the only state M5b-1 can reach) |
| `SkillEngine.createCriticalProcEffect`, the `skillId != 0` filter | `if (skillId != 0) { … }` over the skill template's `SkillType` and its effect types, which can `return null` (SkillEngine.java:196-204) | **Skipped**, by evaluating the weapon-group switch first | **Closed in M5b-2 part 2: the filter runs before the switch again, and an unknown skill id throws `NullPointerException` as in Java; test `CriticalProcEffectTest.ASkillIdFiltersMagicalStunningAndNonAttackSkills`.** Same decision (D14). The filter can only *reduce* the answer to null, and the reordered body already answers null for every main-hand group outside `{POLEARM, STAFF, GREATSWORD, BOW}` — which is Java's own answer — while the four that remain answer null through the partial. The only call site passes a literal 0 (`controllers/CreatureController.cpp:375`, Java CreatureController.java:341-345), so no M5b-1 path can tell the difference. One Java behaviour is dropped with it: Java dereferences `DataManager.SKILL_DATA.getSkillTemplate(skillId)` without a null check (SkillEngine.java:197-198) and throws `NullPointerException` for an unknown skill id; the port returns null instead. M5b-2 restores both the guard and the filter |
| `SkillEngine.createCriticalProcEffect`, the `id != 0` arm | `checkAndGetSkillTemplate(id)`, then `new Effect(attacker, target, …, true, null)` and `initialize()` (SkillEngine.java:218-224) | `AION_PARTIAL("critical proc stumble/stun needs the effect engine (M5b-2)")`, returns null | **Closed in M5b-2 part 2: the partial is gone and the Effect is created and initialized as in Java; tests `CriticalProcEffectTest.PolearmStaffAndGreatswordCreateTheStumble`, `BowCreatesTheStagger` (8217's only effect is a `<stagger>`, not the stun Java's comment names, P5-02a.md). The effects themselves (`StumbleEffect`, `StaggerEffect`) are outside part 3's subset (P5-02a.md "Reachable after part 2").** The `Effect` class is 83 of 95 bodies unported (m5b-plan.md §2.2, O-01). The M5b-1 gate's Elyos Warrior carries item 100000094 "Training Sword" (`item_group="SWORD"`), so the scripted path never takes this arm and §6.1's allow-list §B asserts its hit count is **0**. Tests: `CriticalProcEffectTest.PolearmStaffAndGreatswordTakeTheStumbleArm`, `BowTakesTheStunArm` |
| `NpcSkillList.getPostSpawnSkills` | Returns the entries whose `hasPostSpawnCondition()` is true (NpcSkillList.java:70-76) | `AION_PARTIAL("post-spawn npc skills are not cast yet (M5b-2)")`, returns an empty list | **Status (M5b-2): ported in part 2, held back until part 3 (P5-02b.md, "Held back until part 3"); the cast it feeds (`SkillEngine.getSkill`, `useWithoutPropSkill`) is ported (P5-02a.md). Closed by the part 3 Regate (m5b2-plan.md G-05, D7, D11): Java's filter, no partial; test `NpcSkillListTest.PostSpawnSkillsAreTheEntriesWithThePostSpawnConditionInListOrder`.** m5b-plan.md **D3**. The only caller, `SpawnEventHandler.onSpawn` (SpawnEventHandler.java:20-22), feeds each entry to `SkillEngine.getSkill(...).useWithoutPropSkill()`, and `SkillEngine::getSkill` is `AION_UNPORTED`; a non-empty answer would abort the spawn (`VisibleObjectSpawner.cpp:113` logs "Error during spawn:"). **185 npc ids carry `is_post_spawn="true"` across 13 of the 25 files under `data/static_data/npc_skills/`, none of them on Poeta or Ishalgen**, so the gate never reaches it and §6.1's allow-list §B asserts hit count **0** — but `gs.smoke.startup` spawns the whole world and would log 185 spawn errors without this partial. M5b-2 closes it. Test: `NpcSkillListTest.PostSpawnSkillsAreThePartialThatReturnsAnEmptyList` |
| `NpcSkillList.getRandomSkill` | `return Rnd.get(skills)` (Rnd.java:50-52: null when empty, `getFirst()` when the size is 1 — **no random draw** — else `get(nextInt(size))`) | The three arms written out against the `ArrayList` shim, not a call to `commons::utils::Rnd::get` | The shim is neither a sized nor a forward range, so the `Rnd::get` range overloads do not accept it; and that overload (`commons/src/aion/commons/utils/Rnd.h:211-216`) draws `randomIndex(size)` even for a single element, where Java draws none. Writing the body out keeps Java's draw count, which matters because the npc AI shares one seeded generator per thread. Test: `NpcSkillListTest.GetRandomSkillDrawsFromTheListAndIsNullWhenEmpty` |

### AION_PARTIAL sites this lane adds (for `tests/scenario/m5b_partial_allowlist.txt`, §B — the scripted path must not reach either)

- `skillengine/SkillEngine.cpp` `SkillEngine::createCriticalProcEffect` — "critical proc stumble/stun needs the effect engine (M5b-2)" -
  **closed in M5b-2 part 2** (P5-02a.md)
- `model/skill/NpcSkillList.cpp` `NpcSkillList::getPostSpawnSkills` — "post-spawn npc skills are not cast yet (M5b-2)" - held back until
  part 3 (P5-02b.md); **closed by the part 3 Regate**, and its allow-list rows are gone

The wave-5a sites (`SkillEngine::applyEffectDirectly`, `PlayerEffectController::addSavedEffect`) are unchanged and stay in §A / the M5a list.
(M5b-2 part 2: `applyEffectDirectly` is held back and stays; `addSavedEffect` is closed, see the wave-5a list above. Part 3's Regate closed
`applyEffectDirectly` as well.)

### Not a deviation

`NpcSkillList::isEmpty`, `getSkillOnPosition`, `getSkillsByPriority`, `getChainSkills` and `NpcSkillEntry::setLastTimeUsed` are statement-for-statement
ports of NpcSkillList.java:53-114 and NpcSkillEntry.java:31-37. Java's `Collections.emptyList()` short-circuits (NpcSkillList.java:84, :102) are kept
as an early `return {}`; they are not observable — falling through the loop would return an equally empty vector — but they keep the bodies readable
next to the Java. `getNpcSkills` and `getPriorities` were already ported in wave 5a.
