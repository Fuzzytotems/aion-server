Merged into [../DEVIATIONS.md](../DEVIATIONS.md), section "game-server / game objects" (phase 4 close-out, 2026-09-15).

## Wave 5a (world-engines lane, m5a-plan.md W-04)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `FlyRing`, `Road` constructors | `setOwner(this)`, then `template` and `plane` are assigned | The const members `template_` and `plane` are initialized before the constructor body calls `setOwner` | Const members; `setOwner` only binds the part, so the order is not observable. A null template or center throws NullPointerException before the object id is taken (C++ does not fix the evaluation order of the base initializer's arguments) |
| `Gatherable` constructor | Defined | Declared, not defined | It destroys its `std::unique_ptr<GatherableController>`, whose destructor needs `GatheringTask.h` (P5-02); see docs/deviations/P4-10.md `spawnGatherable` |
