# Intentional deviations from the Java server

This file lists behaviour that differs from the Java reference implementation on purpose. The code marks each one with a `Deviation:` comment.
Any other difference is a bug: code not listed here should behave like Java.

## General

| Area | Java | C++ | Reason |
|---|---|---|---|
| Scripting | Handlers are compiled from `data/handlers` at runtime and can be hot-reloaded | Compiled into the binary | No runtime compiler. See PORTING_PLAN.md › Future notes |
| `DeadLockDetector` | Detects JVM monitor deadlocks via `ThreadMXBean` | Not available | No C++ equivalent |
| Null strings | `String` may be `null` | `std::string` (empty) or `std::optional<std::string>` where null and empty differ | No null references |
| `ExitCode.ERROR` | `ERROR` | `ExitCode::ERROR_` | `<wingdi.h>` defines an `ERROR` macro |

## commons / utils

| Area | Java | C++ | Reason |
|---|---|---|---|
| `Rnd` | `L64X256MixRandom` split per thread. `get(List)` returns the element or null. Streams bound to the creating thread | xoshiro256++ split per thread (different sequences). `get(range)` returns a pointer (lvalue ranges) or `std::optional` (rvalue ranges). Ranges of `int32_t` return the value and throw when empty. Streams are lazy ranges | No null references. The exact Java sequence is not required |
| `NetworkUtils.checkIPMatching` | Octets and range bounds are parsed as signed bytes, so values above 127 throw | Parsed as 0-255. Malformed input returns false | Java bug |
| `NetworkUtils.toHex` | Text column of the last row only printed if `end == capacity` | Always printed | Java bug |
| `RunnableStatsManager` | Keyed by `Class`, Java names. Non-transitive string comparator | Keyed by `std::type_info`, C++ names (`network::aion::serverpackets::SM_X`). Strict weak ordering | `std::stable_sort` needs a strict weak ordering |
| `ExecuteWrapper` | `Runnable` executor | Template over callables / objects with `run()` | No `Runnable` |
| `UncaughtExceptionHandler` | Only the crashing thread dies. `OutOfMemoryError` exits with RESTART | An uncaught exception terminates the process: log, flush, then `quick_exit(RESTART)` for `bad_alloc`, otherwise `quick_exit(ERROR_)` (never `abort()`). Threads catching at their entry point keep Java's behaviour | C++ always terminates on uncaught exceptions; `abort()` can hang on a CRT dialog (Debug) or WER (Release) |
| `ByteBuffer` byte order | `allocate`/`wrap` default to big endian; `slice()` is always big endian | Little endian by default; `slice()` keeps the order. Big endian file readers (e.g. `GeoWorldLoader`) must call `order(BIG_ENDIAN_ORDER)` | Every Aion protocol is little endian |
| `StringUtils` case mapping | Full Unicode case mapping incl. final sigma | Simple case mapping for ASCII, Latin-1, Latin Extended-A, Greek, Cyrillic; special cases İ→i̇ and ß→SS; no final sigma | No ICU; covers the name scripts the servers allow |
| `StringUtils::toUtf8` | Unpaired surrogates encoded as `?` | U+FFFD | Safer for display |
| `parseInt`/`parseLong` (`Numbers.h`) | `Character.digit` accepts all Unicode digits | ASCII digits only | Negligible edge case |
| `InetSocketAddress` bytes | Resolved once at creation (config load) | `resolveAddressBytes()` resolves on each call (IPv4 first) | The host is kept unresolved |
| `PriorityThreadFactory` | Unstarted `Thread` in a `ThreadGroup` | Started `std::jthread`. Priority mapped like HotSpot on Windows | No unstarted threads or thread groups |
| `VersionInfo` | Reads jar manifests; "for Java 25" | Reads the CMake-generated `aion/BuildInfo.h`; "for C++23 (MSVC x.y.z)" | No jars |
| `SystemInfo` | JVM, heap max/allocated/used | Compiler, available CPUs (affinity), physical RAM, committed and working-set memory | No JVM heap |

## commons / logging

| Area | Java | C++ | Reason |
|---|---|---|---|
| Configuration | `config/logback.xml` read by logback | `Logging::init(Config)` builds the same appenders in code; the server passes webhook URLs and time zone from its properties | No logback |
| Console | Always ANSI colors; `consoleEncoding` | Colors only on terminals with VT support; Windows console output as UTF-16 | Clean redirected output |
| Exceptions in patterns | Throwable separate from the message (`%msg` vs `%ex`) | Same output: the Logger passes the exception position to layouts through a thread-local; plain spdlog sinks see the message followed by a newline and the exception | spdlog's `log_msg` has no throwable field |
| Log archive on startup | `ZipOutputStream` with zlib, `\` entry separators, archive time as entry time | Own DEFLATE encoder (slightly larger), `/` separators, file modification time | zlib not linked; `/` is what the ZIP spec requires |
| `DiscordChannelAppender` | Separate async appender; regex separator; NPE on null name/avatar; `Retry-After` read as ms; no HTTP timeout | One async sink; literal separator; missing fields left out; `Retry-After` in seconds (per Discord docs); 30 s timeout; non-finite/negative rate-limit headers count as absent, capped at one year | Fixes Java bugs; a hanging request cannot block the queue; no UB float→int conversion |

## commons / configuration

| Area | Java | C++ | Reason |
|---|---|---|---|
| Field binding | Reflection over `@Property`/`@Properties` fields (incl. superclasses); runtime transformer registry | Explicit `bind`/`bindPattern` calls per config class; compile-time `PropertyTransformer<T>` specializations | No reflection |
| Error/debug messages | "Error modifying field X of class Y" | "Error modifying field for property <key>" (same cause chain) | No field names without reflection |
| `bindPattern` | HashMap order decides duplicates; unmatched group gives a null key; java.util.regex | Sorted key order (last wins); unmatched group gives `""`; ECMAScript `std::regex` | Deterministic; no null |
| Empty enum/regex value | Field becomes null | `std::optional<E>` / `std::optional<std::regex>` → nullopt; plain type → `TransformationException` | Value types cannot be null |
| Regex syntax | java.util.regex on UTF-16 | ECMAScript `std::regex` (UTF-8 bytes) or `std::wregex` (UTF-16 code units, Java-like counting) | No java.util.regex |
| `ZoneId` | Any fixed offset | Region IDs, Z/UTC/GMT/UT, whole-hour offsets −12..+14 | `std::chrono::time_zone` has no arbitrary offsets |
| `File` | Separators normalized | `std::filesystem::path` kept as written | Cosmetic only |
| `InetSocketAddress` | Host resolved eagerly; any spelling of the unspecified address is `isAnyLocalAddress()` | Kept unresolved until bind/connect; unscoped spellings of the unspecified address are normalized to `::` / `0.0.0.0` | `utils::InetSocketAddress` design |
| Runtime config reloads | Plain static fields; reference stores are atomic, so reloading is memory safe | Fields rebound while other threads read them are `ConfigValue<T>` (atomic `shared_ptr` snapshot) or `std::atomic<T>`; `CommonsConfig::RUNNABLESTATS_ENABLE` is `std::atomic<bool>` | Assigning strings/containers while they are read is UB in C++ (confirmed by a crashing stress test) |
| Time zone database | Java ships its own tz data; `ZoneId.systemDefault()` never fails | `std::chrono` tzdb needs the Windows ICU (`icu.dll`: Windows 10 1903 / Server 2022+). Without it, or without a determinable system zone, config loading fails with a clear `IllegalStateException` | No zone object exists without the database; a silently wrong zone would shift all schedules |
| Number/boolean parsing | Unicode digits; Unicode case folding | ASCII only | Negligible edge cases |
| `Properties` | Synchronized Hashtable; mutable live defaults; lone surrogates kept; platform line separator in `store` | Unsynchronized; `shared_ptr<const Properties>` defaults; lone surrogates → U+FFFD; `\n` | Value semantics; UTF-8 strings |
| `PropertiesUtils.loadFromDirectory` | File system order; symlinked start dir not followed | Sorted by path; symlinks followed | Deterministic load order |
| `commons.script_compiler.caching.enable` | Config field | Validated but unused | No script compiler |

## commons / database

| Area | Java | C++ | Reason |
|---|---|---|---|
| Prepared statements | Connector/J emulates them client-side (text protocol) | Server-side prepared statements (binary protocol) | Natural Connector/C API; differences negligible for DAOs |
| NULL strings/bytes | `getString`/`getBytes` return null | Empty value + `wasNull()`, or `getObject<std::string>()` → `std::optional`; `getTimestamp`/`getDate` return `std::optional` | C++ convenience |
| ResultSet lifetime | Closed with its statement | Fully materialized; stays valid after statement/connection are gone | RAII, cheap scrolling |
| `ResultSet.relative` | Cursor not clamped | Clamped to before-first/after-last | Connector/J bug |
| Timestamp precision | Nanoseconds | Milliseconds | No DAO uses sub-millisecond values |
| Credentials | Empty HikariConfig user/password override the URL | Empty values count as unset (URL credentials apply) | Missing and empty keys are indistinguishable |
| `characterEncoding` | Any Java charset | Always utf8mb4 (other values warn) | All strings are UTF-8 |
| JDBC URL | Multi-host/load-balancing URLs | First host only | Not used |
| Host resolution | Java prefers IPv4 addresses | Resolved by the port, IPv4 first, then each address tried in order | Windows resolves `localhost` to `::1` first (2 s per connection against an IPv4-only server) |
| `Transaction` | Leaks statements/connection if not committed | Owns its pooled connection; uncommitted work is rolled back by the pool | RAII |
| Pool | HikariCP fills the pool and retires connections in background threads | Created on demand in the calling thread; maxLifetime checked on borrow/return | Simpler, no background thread |
| `Connection.isValid(timeout)` | Honours the timeout | Socket send/receive timeouts set to the timeout around `mysql_ping` (bounds each network wait, not the whole call); the pool validates with `isValid(5)` | No per-call timeout in Connector/C |
| `sslMode=REQUIRED` | Rejected before the authentication response is sent | Same `SQLException` (08001), but checked right after `mysql_real_connect`, i.e. after authentication | Connector/C has no hook between greeting and authentication |
| System time zone without tzdb | `TimeZone.getDefault()` always available | Without `icu.dll` the connection's system zone falls back to the Windows dynamic time zone rules (with a warning); determined once on first use | Falling back to UTC would shift all stored timestamps |
| Before `DatabaseFactory.init` | NullPointerException | `SQLException("DatabaseFactory is not initialized")` | No NPE |
| Not supported | CallableStatement OUT params, updatable result sets, a standalone `Statement` class (plain-SQL batches are covered by `PreparedStatement::addBatch(sql)`) | Not ported | Unused by the servers |

## commons / network

| Area | Java | C++ | Reason |
|---|---|---|---|
| Threads | 0 → one accept+IO dispatcher; N → N IO dispatchers + 1 accept dispatcher | `max(1, N)` Asio IO threads doing both | No separate accept thread needed; 0/1 stay single-threaded as the game server expects |
| `NioServer.shutdown` | Logs counts and returns; stragglers stay connected; threads keep running | Same log lines, then remaining connections are disconnected, IO threads joined, all `onDisconnect` callbacks awaited | C++ must stop its threads; guarantees `onDisconnect` for every connection |
| Oversized packets | Declared size > read buffer hangs the connection (selector spins) | Warning and disconnect | Java bug |
| `writeData` exceptions | Logged; half-written buffer corrupts the stream | Logged and disconnected | Java corrupts the stream |
| Pending close | Disconnect when the queue is empty; SO_LINGER(true, 10); read interest removed | Disconnect when the close packet was handed to the socket, a write failed, or after 2 s; shutdown + close without linger; received data is read and discarded meanwhile | Deterministic close packet delivery; draining avoids an RST that could discard the close packet |
| Disconnect executor | `connect(Executor)` required; `shutdown` does not wait; a rejecting executor loses `onDisconnect` | `connect(DisconnectExecutor)` or `connect()` with own threads (default 2); `shutdown` waits for callbacks; if the executor throws, `onDisconnect` runs on the IO thread | No `Executor`; C++ must join threads; every connection gets its `onDisconnect` |
| Outbound connections | `SocketChannel.open` + `new Connection` + `dispatcher.register` + `initialized()` | `openSocket(address)` + `make_shared<Connection>` + `registerConnection(con)` (registers, calls `initialized()`, starts IO) | Asio has no per-connection dispatcher; keeps Java's order |
| `sendPacket`/`close` before registration | Act on the selection key immediately | Deferred until IO starts after `initialized()`; a connection is never disconnected before it is registered | Lets a `ConnectionFactory` reject a client with `close(packet)` without racing registration |
| Write batching | One socket write per packet | Consecutive packets batched into one write (same bytes and order) | Fewer system calls |
| `initialized()` | May race with the first read; exceptions leave the connection registered | Always before the first read; exceptions close the connection | Deterministic |
| `PacketProcessor` | Linear scan of one global list; no shutdown; exceptions kill workers | Per-connection queues + ready list (same guarantees and checker rules); `shutdown()`; exceptions logged, workers continue | O(1) scheduling; C++ must join threads |
| `PacketProcessor` executor | `Executor` receiving a `Runnable` | `std::function<void(ClientPacketBase&)>` that calls `packet.run()` | No `Runnable` |
| `BaseServerPacket.buf` | Mutable field set during `write()` | No buffer member; static write helpers take the target buffer | One instance can be broadcast concurrently |
| `BasePacket.getPacketName` | final, `getSimpleName()` | virtual, simple name from `typeid` | Compiler names can be overridden |
| `ServerCfg.getIP` | Resolved address | Configured host string (resolved at bind/connect, IPv4 first) | Unresolved `InetSocketAddress` |
| Accept errors | Retried on next select | Logged, retried after 100 ms | No busy loop on e.g. EMFILE |
| Misc | `readB(-1)` → NegativeArraySizeException; `sendPacket(null)` → NPE later; `Object.toString()` | `IllegalArgumentException`; null packets ignored; `toString()` = "<SimpleClassName> <ip>" | No equivalents / better logs |

## login-server / encryption (`network/ncrypt`)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `CryptEngine.decrypt` checksum | Loops `i < length - 4` instead of `offset + length - 4`: packets not at read offset 2 are only partly verified | Always verifies the whole packet (identical to Java at offset 2, the first packet of a read) | Java bug; spans have no offset |
| `CryptEngine` threads | Cipher shared by read (decrypt) and write (encrypt/rekey) paths without synchronization | Internal mutex | Data race is UB |
| Invalid encrypt input / empty keys | AIOOBE after partial writes; broken cipher state | `IndexOutOfBounds`/`IllegalState`/`IllegalArgumentException` before anything is modified | Exception safety; unreachable with valid packets |
| `KeyGen` | NPE/null before `init()`; key pair array overwritten on re-init | `IllegalStateException` before `init()`; `shared_ptr<const EncryptedRSAKeyPair>` behind a mutex, handed-out pairs stay valid | No null; memory safety |
| Blowfish session key | JCE `KeyGenerator("Blowfish")` (128 bit) | 16 bytes from OpenSSL `RAND_bytes` | No JCE; equivalent CSPRNG key |
| RSA login data decryption | Inline `Cipher` per 128-byte block in `CM_LOGIN` | `EncryptedRSAKeyPair::decrypt(span)` → `optional` (nullopt where Java catches `GeneralSecurityException`) | Reusable API; same result |

Kept on purpose because it is on the wire: `AionServerPacket` passes `payload size - 2` to `encrypt`, so for payload sizes with `size % 8 == 5`
the checksum overwrites the last payload byte (first packet: `size % 8 == 1` loses it to the XOR key). The checksum check ignores the last
word, so client packets carry the checksum in the last-but-one word; the "unknown/random" trailing fields of `CM_*` packets are that padding.

## login-server / data layer

| Area | Java | C++ | Reason |
|---|---|---|---|
| Unknown config property warnings | Removes the keys referenced by the logback.xml in use | Always removes `Logging::getPropertyKeys("loginserver")` | No logback.xml |
| Logging settings | logback reads `logging.properties` and `myls.properties` itself | `Config::loadLoggingConfig()` reads the same files for `Logging::init` | Logging is configured in code |
| `Account` / `AccountTime` | Unsynchronized fields; `AccountTime` shared and changed in place | One mutex per account; getters return copies; `modifyAccountTime`/`modifyAndStoreAccountTime` do atomic read-modify(-store) | Data races are UB; stored copies must not overwrite newer state (e.g. a ban penalty) |
| `AccountDAO` name column | `static final` chosen at class init | Chosen from `Config::useExternalAuth()` per call | Config must be loaded first; same result |
| `AccountDAO.getLastIp` | `null` for NULL (CM_BAN then throws an NPE: no ban, kick or response) | `""` (CM_BAN bans the given IP, kicks and answers) | Java bug |
| `BannedHddDAO.load` | Zero date is stored as null, SM_HDDBAN_LIST later throws an NPE | Row skipped with a warning | No null timestamps |
| Nullables | `null` accounts, bans, strings, timestamps | `shared_ptr` (nullptr), `optional`, empty strings where the column is `NOT NULL` | No null references |

## login-server / protocols, controllers, startup

| Area | Java | C++ | Reason |
|---|---|---|---|
| Game server packet execution | Cached thread pool: packets of one game server run concurrently and unordered | `PacketProcessor<GsConnection>(4, 8, 50, 3)`: per game server in receive order; `CM_GS_PONG` runs directly on the IO thread | Memory safety and ordering; a pong behind a slow packet must not trigger the ping timeout |
| Executors and shutdown | Static executors, shut down in `onServerClose` | `NetConnector` owns packet processors, ping scheduler and 8 disconnect threads; joined on shutdown; restartable | C++ must join threads; in-process tests |
| `LoginConnection` session id | `Object.hashCode()` | Random int in [1, INT_MAX] | No identity hash |
| `LoginConnection.onDisconnect` | Removes the account id from `accountsOnLS` whichever connection is mapped | Only if it maps to this connection; logins that finish after a disconnect clean up after themselves (`AccountAttachScope`) | Java bugs: a kicked client could remove the new login; stale connections kept accounts "logged in" |
| `GameServerInfo` accounts | Added unconditionally; connection and accounts cleared separately on disconnect | Added only while that connection is active; connection + accounts cleared atomically | A packet finishing after a disconnect left the account "already logged in" until restart |
| `GameServerTable` | HashMap order; unsynchronized registration; NPE races on kick | Ordered by id; registration serialized; kick skipped if the game server just left | Determinism; data races |
| `CM_BAN` | Penalty only in the DB unless the account is on a game server (a later logout of the LS/reconnecting copy lifts it) | Penalty also set on the in-memory account found on the LS or in the reconnect state; `kickAccount` also drops pending fast reconnects | Java bug |
| `SM_ACCOUNT_AUTH_RESPONSE` | Looks the account up again while writing (NPE if it left) | Account time snapshot passed to the constructor | Race between packet processing and IO write |
| `SM_SERVER_LIST` | NPE without character counts; endless byte loop at id 127 | Missing counts = empty; int loop counter | Java bugs |
| `CM_PTRANSFER_CONTROL` | Service call in `readImpl` (IO thread) | In `runImpl` (packet processor) | No database work on IO threads |
| `CM_ACCOUNT_LIST` | Negative/huge counts → exceptions or OOM | Invalid count throws, packet not executed | Memory safety |
| Ban list packets | Iterate live unsynchronized maps while writing | Copy taken under the controller mutex | Data race |
| `ExternalAuth` | No timeout; fastjson2 | cpr with 30 s timeout, no redirects; nlohmann-json mimicking fastjson's lenient parsing | A hanging auth server must not block packet threads |
| Scheduled tasks / `PingPongTask` | Exceptions silently stop periodic tasks; NPE if the GS info is gone | Exceptions logged, task stops; "Gameserver #null connection died" | No exceptions escaping threads |
| NPE cases | `NullPointerException` | `IllegalState`/`IllegalArgumentException` with the same observable result (logged, no response) | No NPE |
| Startup failure | Main thread dies, started threads keep the JVM alive | Logged, started components shut down, exit code `ERROR_` | The process must not hang |
| Command line / shutdown hook | No arguments; JVM shutdown hook | `-Dkey=value` overrides config properties (C++ addition); `SetConsoleCtrlHandler` (Ctrl+C, close, logoff, shutdown) or SIGINT/SIGTERM | Run against scratch databases; no JVM hooks |

## game-server / runtime kernel

The free-threaded runtime (design D1) replaces JVM guarantees; its design-level deviations are listed in
[design/runtime-architecture.md](design/runtime-architecture.md) §18 and apply as they are implemented. Behaviour-visible deviations of the
kernel implemented so far:

| Area | Java | C++ | Reason |
|---|---|---|---|
| Object lifetime | Garbage collector | Atomic intrusive `Ref<T>`, borrows valid until the task ends, epoch Reclaimer frees later; C++-only cycle breakers; zombie breaker cuts edges of objects out of the world > 30 min with a warning (D7) | No GC |
| Executors after shutdown | `AionRejectedExecutionHandler` drops tasks silently | Submissions are cancelled and dropped (a `get()` on them does not block forever) | Deterministic shutdown |
| Periodic tasks | `RunnableWrapper` logs exceptions, the task keeps running | Same; runs more than 10 periods (and ≥ 2 s) behind are coalesced into one and realigned | Avoid catch-up storms (e.g. after a debugger pause) |
| Cancelled tasks | Captured objects stay referenced until the queue drops the task | Captures are released immediately on cancel | Earlier reclamation |
| `getDelay()` | Only on scheduled futures | Returns 0 for tasks that are not scheduled | `Future` is one type |
| Rejection policy (instant pool) | Caller thread priority (Java) | Recorded Java-style thread priority of kernel threads | Portable |
| LS/CS link packets | Unordered on the general pool | In order per link (`SerialExecutor`) | Ordering |
| Object IDs | Lowest free ID reused immediately (after GC for auto-release objects) | Monotone cursor wrapping at 2^27, released IDs quarantined ≥ 300 s (also across a wrap); a double release while quarantined warns | ABA safety for handlers holding IDs |
| Cron (Quartz subset) | Quartz: L/W/# supported; misfires within 60 s catch up one by one; searches up to +100 years | L/W/# rejected (unused); missed fire times always collapse into one run; nonexistent local times skipped, ambiguous ones fire once (earlier instant); stricter parser; searches stop after 2299; `findJobs` by exact type | Only the used subset; deterministic |
| Deadlock detection | `DeadLockDetector`: dump and exit RESTART | Watchdog: lock wait-graph cycle confirmed in two consecutive checks, dump and keep running (D5); STALL measured since the task's last `quiescentPoint`, all stalls of one check in one dump; long-running/startup/main kinds exempt. The Windows minidump is written from a process snapshot by a helper process (`--write-minidump`, terminated after 30 s; in-process writer thread otherwise), at most one per check, none within 60 s of the previous one, at most 20 per run (rows "Watchdog STALL dumps" and "Watchdog minidumps") | D5; avoid false positives; a dump must not hang the server |
| `synchronized` | JVM monitor, not fair; `wait`/`notify` | Reentrant `Monitor` with eventual fairness (starvation mode after 1 ms); no `wait`/`notify` (unused by the server) | Prevent starvation |
| `StampedLock` / fair `Semaphore` | Queue-based, writer preference / strict FIFO | No writer preference (avoids EffectController's nested read-lock deadlock); fair semaphore approximate | Simpler; unused features omitted |
| Plain collections in shared objects | Unsynchronized, `ConcurrentModificationException`, may corrupt | Internally synchronized shims, snapshot iteration, no CME; `HashMap` iteration in insertion order; modifying a collection from its own element's equals/hashCode/comparator throws `IllegalStateException`; `ArrayList.sort` with a throwing comparator leaves the list unchanged | Memory safety |
| `HashMap`/`TreeMap` compute callbacks | Any structural change → CME | Same-key recursive update throws `IllegalStateException("Recursive update")` (like CHM); other keys may change | Consistent with CHM |
| `ConcurrentHashMap` | Per-bin locks; `snapshot`-free weakly consistent iteration | 16 stripes with reentrant stripe Monitors for callbacks; lock-free weakly consistent reads and iteration; nested writes to another key of the same map inside a compute callback can deadlock across stripes, so such Java sites are ported without the nesting (design §21, lint L20) | Java's CHM contract forbids them |
| `ConcurrentLinkedQueue`/`Deque` | Lock-free | Monitor-guarded (`isEmpty`/`size` lock-free) | Interior `remove(Object)` with epoch reclamation |
| `String` fields | Nullable | `Field<std::string>`: null equals empty | No null strings |
| Watchdog STALL dumps | – (Java has no stall detection) | All tasks that stall in one watchdog check are reported in one STALL dump (summary "N stalled tasks:" and one line per task; `DumpReport::threadIds` names all of them); a single stall keeps the former summary | The first M4 run reported every element of a parallel load at once: one dump and one minidump per thread, and the process hung inside the 45th minidump |
| Watchdog minidumps | `DeadLockDetector` writes no dump | Written from a process snapshot (`PssCaptureSnapshot` with a virtual address clone), preferably by a helper process (`<executable> --write-minidump <pid> <file>`, the executable itself when its `main` calls `MinidumpWriter::runIfRequested`, as `aion_game_server` does) that is terminated after `minidumpTimeout` (30 s); otherwise on a dedicated in-process writer thread that is abandoned after the timeout. At most one minidump per check, none within `minidumpMinInterval` (60 s) after the previous attempt, at most `maxMinidumps` (20) per run; DEADLOCK minidumps ignore the interval and have their own budget of `maxMinidumps` (a cycle is dumped once and may be followed by the restart, so its minidump must not be lost to an earlier STALL). The text dump of all threads is always logged, before the minidump is written, followed by a second message naming the minidump or why none was written (also when the write throws); listeners get the report with both. Paths are UTF-8 (the configured directory and helper, the reported path) | An in-process `MiniDumpWriteDump` of the live process suspends every other thread while DbgHelp allocates and loads modules on the dumping thread: a suspended thread holding the loader or heap lock hangs the dump and the server (D5: dump and keep running). A snapshot suspends the threads only while the kernel clones the address space, so a killed or hanging writer cannot leave the server suspended; DbgHelp also no longer runs inside the server next to `std::stacktrace` symbolization |
| Watchdog minidump settings | – | `Watchdog::Config::minidumpHelperExecutable`, `minidumpTimeout`, `minidumpMinInterval`, `maxMinidumps` (defaults above; not bound to config keys yet, `RuntimeConfig::watchdogConfig` of P4-01 can add `gameserver.watchdog.*` keys) | – |
| `Reclaimer::awaitBacklogBelow(objects, timeout)` | – (garbage collector) | Backpressure for bulk producers of short-lived objects: waits in 1 ms steps inside a `BlockingRegion` while the backlog exceeds the limit; returns at once when the Reclaimer thread is not running or the caller has published an epoch | Parallel startup loops retire objects faster than the one Reclaimer thread destroys them (World creation: 11 million `Point3D` in about a second) |
| `QuiescentScope::active()`, `ForkJoinPool::isParallelFromCurrentThread()` | – | Queries for code that chooses between a Java-shaped parallel loop and a variant with quiescent points or per-element task scopes | World creation (below) |
| `QuiescentOptIn(purpose)`, `QuiescentOptIn::active(purpose)` | – | An explicit opt-in to quiescent points in code far below a `QuiescentScope`: active only under an active `QuiescentScope` with a guard of the same purpose opened at the current `TaskScope` and `QuiescentScope` depth (`QuiescentOptIn::WORLD_CREATION`) | `QuiescentScope::active()` alone would let `WorldMap` construction take quiescent points under any scope a later caller opened while holding borrows (review, wave 3b-2) |

## game-server / configs

| Area | Java | C++ | Reason |
|---|---|---|---|
| `Config.load` concurrency | Unsynchronized: event start/stop and `//reload config` can overlap | Serialized by a `runtime::Monitor`; the event provider and file reading run outside the lock | D6 race fix: data race on commons' plain `DatabaseConfig` fields and the `CLIENT_CONNECT_ADDRESS` read-modify-write (`ConfigLoadTest.ReadersStayValidDuringConcurrentReloads`) |
| Missing key without default | Field stays null (e.g. `DISABLE_RANGE_CHECK_MAPS.contains` throws an NPE) | Field keeps its initial value: empty collection, `nullptr` or `std::nullopt` | No null references; the shipped config has every such key |
| `Config.load(allowedConfigs)` error | `IllegalArgumentException` naming the class | "Config bind function is not an allowed config" | Bind function pointers instead of `Class` objects |
| Event config properties | Read from `EventService` | `Config::setEventConfigPropertiesProvider`; no event properties until `EventService` is ported and registers | Not ported yet |
| Command line (`aion_game_server`) | No arguments | `-Dkey=value` overrides (C++ addition, S0a `main.cpp`) are registered as the event-properties provider, so they are layered over `mygs.properties`, warned like other unknown keys and kept across later `Config::load` calls; other arguments are warned and ignored. `EventService` (P5-12b) must merge them into its provider | Run against scratch databases (`gs.smoke.startup`) |
| Unknown property warnings | Removes the keys referenced by logback.xml | Always removes `Logging::getPropertyKeys("gameserver")` | No logback.xml (as in the login server) |
| Logging settings | logback reads `gameserver.properties`, `logging.properties`, `mygs.properties` | `Config::loadLoggingConfig()` reads the same files for `Logging::init` | Logging is configured in code |
| `RuntimeConfig` | – | C++-only config class after Java's `Config.CONFIGS` classes; keys in design/runtime-architecture.md §10, all optional | Runtime kernel settings |
| `database.socket_timeout` | No such key (only the URL's `socketTimeout`) | C++-only optional key in milliseconds (commons `DatabaseConfig`). Precedence: key, then URL `socketTimeout`, then the server default; the game server uses a 60 s default and rejects 0 (`DatabaseFactory::gameServerOptions()`); the login server keeps Java behaviour | Database calls run inline on pool threads and must end (runtime-architecture.md §2.6) |

## game-server / geo math (`geoEngine/math`)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `FastMath.sin/cos/tan/exp/log/pow` | HotSpot `Math` intrinsics | C runtime; may differ by up to 1 ulp. `asin/acos/atan/atan2` are bit-exact through the fdlibm port `geoEngine/math/StrictMath` | Intrinsics are not bit-reproducible in Java either; only rarely used paths depend on them (fromAngleAxis, angleRotation, rotateAroundOrigin, spherical conversions) |
| Constant names | `FastMath.FLT_EPSILON`, `DBL_EPSILON`, `Vector3f.NAN` | `FLOAT_EPSILON`, `DOUBLE_EPSILON`, `NOT_A_NUMBER` | `<cfloat>`/`<cmath>` macros |
| `Ray(Vector3f, Vector3f)` | Keeps the caller's vector objects | Copies them; in-place mutation through `getOrigin()`/`getDirection()` still works | Value types; GeoMap, AbstractCollisionObserver and BIHNode do not observe the difference |
| Null arguments | Null store/result parameters allocate; `set(null)` gives identity; Vector2f warns | Overloads without the parameter; no null branches | References cannot be null |
| Not ported | `FastMath.rand/nextRandomFloat/nextRandomInt`, FloatBuffer methods, `Vector2f` externalization, `getClassTag`, `Vector3f.create`, `Matrix4f.fromFrustum`/`set(float[][])`, `equalIdentity` | – | JVM-specific or unused |

## game-server / geo engine (`geoEngine`, except `geoEngine/math`; P4-04)

The last four rows are performance rewrites of wave 3b-2 (runtime lane); their results are bit-identical, checked by the M4 oracles.

| Area | Java | C++ | Reason |
|---|---|---|---|
| Mesh collision trees | `GeoWorldLoader.load` preloads the BIH trees on the long-running pool after the maps are published; a collision check can meanwhile build the same tree lazily, since `Mesh.createCollisionData` is unsynchronized and `BIHTree.construct` sorts the mesh's triangles in place | `GeoWorldLoader::load` builds the tree of every mesh of the loaded maps on the ForkJoin pool before it returns; the lazy path stays for meshes outside the maps (`// java-race` on `Mesh::createCollisionData`) | Obvious race (D6, research/dao-geo.md): concurrent sorting while another thread traverses the triangles gives wrong collisions. Test `GeoWorldLoaderFilesTest.LoadsTerrainsMeshesAndPlacements` (every geometry of the map has its tree after `load`) |
| Terrain images | `loadTerrains` decodes the PNGs in a parallel stream and calls `setHeightmap`/`setMaterials` from those threads, so the size checks between a map's heightmap and materials read the other file's fields without synchronization | The images are decoded in parallel, then applied to the terrains on the calling thread in file name order | Obvious race (D6); the checks are symmetric, so the order does not change which images are accepted. Test `GeoWorldLoaderFilesTest.LoadsTerrainsMeshesAndPlacements` (a heightmap, a materials image and a heightmap shared by two maps) |
| `BoundingBox.containAABB` | Stores `min + max` into `center` and halves it in a second step | Computes the halved center first and stores it once | Obvious race (D6): the maps load in parallel and share meshes, so `Geometry.updateModelBound` of one thread can read a mesh bound another thread is recomputing and see the doubled center (a wrong world bound, chunk and zone name). Single-threaded results are identical (`BoundingBoxTest.ContainAabb`); the interleaving itself is not testable |
| `TempVars` after an exception | An exception between `TempVars.get()` and `release()` leaves the instance marked as used; after five such exceptions every geo call on that thread throws `ArrayIndexOutOfBoundsException` | `TempVars::ReleaseGuard` releases the instance when an exception leaves the scope (all Java `release()` calls stay where they are) | Java bug: e.g. `BIHNode.intersectWhere` throws `ArithmeticException` for a geometry with a singular world matrix (none in the data) while holding an instance. Tests `TempVarsTest.StackDiscipline` and `BoundingBoxTest.ContainAabb` (a failed `containAABB` leaves the stack usable) |
| `BIHNode.intersectWhere` traversal stack | Clears the thread-local `TempVars.bihStack` only on entry: an `onlyFirst` stop (every `canSee` and every first-hit query) or an exception leaves the pushed far-node entries in the `ThreadLocal` list until the thread's next BIH query | The stack is cleared before `release()` and by a scope guard on the exception path, so it is empty outside the call | The C++ entries hold `Ref<BIHNode>`, and runtime-architecture.md L15 allows no Ref in thread-local storage beyond a call (review of wave 3a-2). No observable behaviour change. Tests `BihTest.OnlyFirstStopsAtTheFirstCollision`, `BihTest.AnExceptionLeavesNoNodeInTheThreadLocalStack` |
| Game service lookups | `DespawnableNode.collideWith` calls `EventService` and `SiegeService`; `GeoWorldLoader.createZone` calls `ZoneName.createOrGet` and `ZoneService.createMaterialZoneTemplate` | `GeoCallbacks`: replaceable function pointers for the event theme id, the siege shield state and the material zone sink; without a callback the event theme and shield state come from `EventService`/`SiegeService` as in Java, and the material zone sink calls `ZoneService.createMaterialZoneTemplate` with `ZoneName::createOrGet` (the binding `GeoService.init` registers; a check-mode listener can observe it) | Handlers-and-porting-plan.md §2.6: geo can become a leaf library; the world and siege chunks register their bindings. Tests `DespawnableNodeTest.CollisionGates`, `GeoWorldLoaderFilesTest` |
| PNG decoding | `ImageIO.read` (any PNG); RGB samples stored as BGR, RGBA as ABGR | `utils::PngImage`, a self-written decoder; samples keep the PNG order; interlaced images with fewer than 8 bits per sample are rejected with `IOException`; chunk CRCs are not checked | No PNG library in the build (the plan's lodepng was not added: no new vcpkg packages). The loader accepts only one sample per pixel, and the data has 16-bit and 8-bit grayscale and one 8-bit palette image, none interlaced (`PngImageTest`) |
| Load errors | `GameServerError` (an `Error`) wraps `IOException`/`CloneNotSupportedException` of `loadMeshes` and every exception of `loadWorld`; a negative name length throws `NegativeArraySizeException` | `commons::utils::Exception` with the same message and the cause; a negative name length throws `IllegalArgumentException("NegativeArraySizeException: n")`; `CloneNotSupportedException` is `scene::CloneNotSupportedException` | CONVENTIONS: fatal startup errors are commons exceptions; `GameServerError.h` (P5-14) does not exist. Test `GeoWorldLoaderFilesTest.LoadErrors` |
| `Spatial.matches` | `Class.isInstance` (any subclass) and `String.matches` (java.util.regex); a null name never matches | The class filter is a `std::type_info*` tested with `dynamic_cast` for the scene classes and by exact type otherwise; the pattern is an ECMAScript `std::regex` over the whole name; an empty name (Java null) never matches | No reflection or java.util.regex; the Java code has no caller with a pattern (`Node::descendantMatches<T>` tests the class with the template argument) |
| Identity hash codes | `CollisionResult.hashCode`, `toString` and the index array records use `System.identityHashCode` of the geometry or array | A hash of the object address | Not reproducible; only used for hashing and debug output |
| `BIHTree.construct` | `createNode` recursion on the mesh with temporary `BoundingBox` objects | The same algorithm and float expressions on a copy of the triangles (a value box type with the expressions of `BoundingBox.setMinMax/getMin/getMax`); the resulting triangle order is written back with the mesh's swaps. The Java-shaped members stay as the reference | 3.1 million temporary RefCounted boxes per M4 load and a checked `Field` access per coordinate; the collision trees took 2.2 s of 4.5 s geo (RelWithDebInfo), now 0.4 s. A digest over all 25,437 trees (node axes, index ranges, plane bits) and every mesh's triangle order is equal before and after (`0f3c1f762e77dce4`) |
| `PngImage` | `ImageIO.read` | Whole-file read, inflate output reserved from the image size, match copies on the buffer, Adler-32 in 5552-byte blocks, one filter switch per scanline | Integer-exact; `PngImageTest`, `GeoRealDataTest` and the M4 `getZ` probes |
| `Terrain.setHeightmap/setMaterials`, `Mesh.setVertices`, `IndexByteArray/IndexShortArray.create`, `GeoWorldLoader.readFile` | array copies, `Files.readAllBytes`-like reads | One checked `Ref` dereference per array instead of one per element; whole-file reads | The terrain step spent most of its time in per-element checks, not in PNG decoding |

## game-server / static data runtime (`dataholders/loadingutils`)

Design-level deviations of the static data design are listed in [design/static-data.md](design/static-data.md) (DEVIATIONS entries and
amendments §8) and apply as they are implemented. Implemented in wave 1:

| Area | Java | C++ | Reason |
|---|---|---|---|
| Merged cache | XmlMerger writes `./cache/static_data.xml` with CRC metadata | No merged file; imports are resolved and bound directly | The cache exists only for JAXB |
| Directory import order | `Files.find` order of the file system (NTFS: uppercase ordinal; Linux: readdir) | Depth-first, uppercase-ordinal names on every platform | Deterministic; equals NTFS (checked for all 12 directories) |
| Strict mode (tests, CI) | JAXB ignores unknown attributes and unexpected text; repeated single elements and duplicate XmlIDs: last wins; XmlMerger merges different root tags of a directory import | Errors: unknown attributes, non-whitespace text in object elements, repeated single elements and wrappers, duplicate XmlIDs, mixed root tags, unknown `<import>` attributes. Lenient mode (server runtime) warns once and keeps the JAXB/XmlMerger behaviour | XSD parity; finds data mistakes |
| Required values | JAXB never enforces `required = true` (the startup XSD check catches most) | Enforced in both modes, except the 16 `xmlgen.toml [unenforced_required]` entries the data violates | No XSD validation at runtime |
| Unknown enum constants | JAXB gives null | Error, except `[lenient_enums]` (`SkillTemplate.counterSkill`: `std::nullopt` and a warning) | Stricter parsing proven unreachable by the census |
| Numbers | Float overflow gives Infinity/0; an empty number gives 0 (JAXB) | Errors | Malformed data fails loudly |
| `LocalDateTime` | Signed or longer years, nanoseconds | 4-digit unsigned years, millisecond precision (sub-millisecond digits must be zero) | `local_time<milliseconds>`; the data uses neither |
| File encoding and form | XmlMerger reads files with `FileReader` and copies inline holder elements of `static_data.xml` | UTF-8 only; inline holder elements, default-namespaced elements (`xmlns="uri"`) and directory imports without `.xml` files are rejected | Not used by the data |
| XSD-only attributes | Ignored by JAXB | Consumed without a member (`[ignore_attributes]`, 5 attributes) | Same result |
| Abstract `@XmlElements` choices | Cannot be instantiated | Left out of the factories (`<buf>` → `BufEffect`) | Same result, found at build time |
| Bound private nested classes | Private | Public | `XmlBinding` is specialized at namespace scope |
| `Loaded N npc templates` | `npcData.size()` logged while `NpcData.init` may still run asynchronously (can show a smaller number) | `NpcData::init` runs inline; always 63,287 (P4-09) | Deterministic |

## game-server / static data templates (P4-07a: item, npc, spawns, world, zone, stats, pet; P4-07b: the other `model.templates` packages)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `PetTemplate.getPetFunctions` | On the first call appends `PetFunction.CreateEmpty()` to the template's own list when no function is a player function (or creates a one-element list), caching the result in `hasPlayerFuncs`; unsynchronized, so concurrent first calls on the shared template can append twice or iterate a list being modified | Returns `std::vector<const PetFunction*>`: the bound functions followed by one shared immutable empty function under the same condition, computed on each call; the template is never modified. `getWarehouseFunction` reads the bound functions (the appended NONE function is never a warehouse, so the result is the same) | D6: obvious Java race on shared static data; templates are const after load. Test: `PetTemplateTest.ConcurrentPetFunctionReadsAreConsistent` |
| `ItemTemplate.afterUnmarshal` empty stats | `weaponStats`/`useLimits` without an element point at the shared statics `emptyWeaponStats`/`emptyUseLimits` | Each such template owns a default-constructed `WeaponStats`/`ItemUseLimits` (about 100k small objects) | The generated members are `std::unique_ptr`; the values are equal, only the identity differs (no Java code compares them by identity) |
| Exceptions of afterUnmarshal hooks (P4-07a classes) | `RandomItem`/`ResultedItem` throw `IllegalArgumentException`, `TemporarySpawn` throws `NumberFormatException`/`ArrayIndexOutOfBoundsException` for a malformed expression, `WorldMapTemplate` throws `NullPointerException` without `flags`; JAXB aborts the load | `LoadContext::fail` throws `StaticDataException` with the Java message (the parse error text for `TemporarySpawn`) and the file/line/element location | The loader's error type; startup fails the same way (none of these occur in the data) |
| `ResultedItem.afterUnmarshal` item check | `(staticData != null ? staticData.itemData : DataManager.ITEM_DATA).getItemTemplate(itemId)` | Looks the item up among the XmlIDs of the current load (`LoadContext::findXmlId<ItemTemplate>`), no fallback to the published `ITEM_DATA` | Written before `ItemData::getItemTemplate` existed (header requests templates-b-1/items-1 added it and kept the hook: in a full load the XmlIDs are the same templates); decomposable items always load together with the items, and //reload is deferred (D3). Switch to `ItemData::getItemTemplate` with //reload |
| Exceptions of afterUnmarshal hooks (P4-07b classes) | `SummonGroup`, `GlobalDropItem`, `ItemRaceEntry` and `StaticDoorWorld` throw `IllegalArgumentException`; `HouseAddress` throws `NullPointerException`/`ClassCastException` outside a `HousingLand`; `WalkerTemplate` throws `NumberFormatException` for malformed `rows` and `IndexOutOfBoundsException` without steps; `AssaultData` throws `NullPointerException` for an assaulter without `type` or `npc_ids`; JAXB aborts the load | `LoadContext::fail` throws `StaticDataException` with the Java message (the parse error text for `rows`) and the file/line/element location | The loader's error type; startup fails the same way (none of these occur in the data), as in P4-07a |
| `GlobalDropItem` and `ItemRaceEntry` item checks | `(staticData != null ? staticData.itemData : DataManager.ITEM_DATA).getItemTemplate(id)` | The item templates of the current load are found through their XmlIDs (`LoadContext::findXmlId<ItemTemplate>`), without the fallback to the published `ITEM_DATA` | Written before `ItemData::getItemTemplate` existed (header request templates-b-1 added it and kept the hooks: in a full load the XmlIDs are the same templates); item groups, global drop rules and events always load together with the items, and //reload is deferred (D3). Same rule as P4-07a's `ResultedItem` |
| `QuestDrop.questId` | `QuestEngine.init` calls `setQuestId(questId)` on the drops of the published quest templates | `QuestDrop::setQuestId` is a C++-only non-const setter for the holder (`QuestsData`, P4-09) to call while loading; `QuestEngine::init` only reads the id | Templates are const after publication; the value is the owning quest's id either way |
| Lazily created template lists | `CollectItems.getCollectItem`, `ExMaterials`/`Materials.getMaterial`, the `GlobalDrop*` list getters, `IGCategory.getSubCategories`, `InstanceBonusAttr.getPenaltyAttr`, `RouteParent.getRouteVersion`, `ShoutGroup.getShoutNpcs`, `ShoutList.getNpcShouts`/`getNpcIds`, `StreamLocations.getLocation`, `TradeListTemplate.getTradeTablist` and the `Tribe` relation getters create (or replace) the list on the first call, and `QuestKill.getNpcIds` moves `npc_ids` into a new list; unsynchronized writes to shared static data | The getters are const and return the bound list, or an empty list where Java stores a new one; no template is modified | D6: obvious race on shared static data (templates are const after load); the returned contents are Java's. Tests: `QuestTemplatesTest.ListsAndFlags` (repeated `getNpcIds`), `MiscWorldTemplatesTest.ShoutsPanelsRecipesRidesMaterials` |
| `TradeListTemplate.getCount` | `tradeTablist.size()`: `NullPointerException` for a template without `<tradelist>` whose `getTradeTablist()` was never called | 0 | Follows from the row above (the list always exists); every caller reads a template with lists |
| `Buff.afterUnmarshal` warning | Prints the `skill_ids` HashSet in its hash order | Prints the ids in ascending order | Log text only |
| `DoorRepairData.getRepairStones` | `HashMap.values()` order | Ascending static id order | Nothing calls it; the lookups (`getRepairStone`) are unchanged |
| Java overrides of frozen shells | `ItemRaceEntry.getCount`/`getChance`/`matchesQuest`/`matchesLevel` (overridden by the rewards classes), `BonusItemGroup.getItems` (abstract), `MailPart.getType`/`getParamValue` (overridden by `Sender`, `Title`, `Header`, `Body`, `Tail`) and `PlaceableHouseObject.getTypeId` (abstract) are virtual | New virtual functions would change the layout of frozen shells: the base functions select the concrete class through a C++-only private `entryClass`/`groupClass` tag (set only through the protected constructor each subclass calls) or the generated `javaClassName()` and call its non-virtual function of the same name; `BonusItemGroup::getItems()` returns `std::vector<const ItemRaceEntry*>`, the subclasses their typed lists | Same results through base and derived types (tests: `ItemGroupTemplatesTest.EntryClassesAndGroups`, `ItemGroupTemplatesTest.MatchesDispatchOnTheEntryClass`, `HousingTemplatesTest.PlaceableObjectsAndEnumCompanions`, `MailTemplatesTest.SystemMailsFormatting`); the shells do not list the Java interfaces `Chance` and `IMailFormatter` as bases, since `Chance::selectElement` is a template and custom mail formatters implement `IMailFormatter` |
| Custom mail formatters | `MailFormatter` passes anonymous `MailPart` subclasses overriding `getParamValue` | Custom formatters implement `mail::IMailFormatter`; `MailPart::getFormattedString(const IMailFormatter*)` and `MailTemplate::getFormatted*` take them (nullptr for Java null) | Static data parts cannot be subclassed at run time; the only method Java calls on a custom formatter is `getParamValue` |
| Template objects created at run time | `new HandlerSideDrop(...)` (stored in `QuestService.questDrop`), `new FlyRingTemplate(...)` (kept by `FlyRing`) are garbage collected | The classes keep Java's constructors; their callers (QuestEngine, instance handlers) allocate them once and never free them, like templates referenced as `const T*` | No GC; the objects are created at startup or per instance handler registration and are few |
| Index maps of templates | `HashMap`/`EnumMap` members filled by the hooks, bound lists set to null | C++-only `detail::FlatMap` (sorted vector) or ordinal-indexed arrays pointing into the bound storage, which stays; iteration orders that callers can observe (`StaticDoorWorld.getStaticDoors`, `TownSpawnMap.getTownSpawns`, `TownSpawn.getTownLevels`) are recomputed in Java's `HashMap<Integer>` order (`detail::javaIntegerHashMapValues`) | Bound templates are moved into reserved vectors only through noexcept moves, which the MSVC `std::map`/`std::unordered_map` lack; static-data.md §2.6 keeps the storage |
| Present but empty optional string attributes (`WalkerTemplate` `rows`, `Building` `parts_match`) | `null` when absent, `""` when present and empty: `WalkerTemplate` fails the load on `Integer.parseInt("")`, `Building.getPartsMatchTag` returns `""` | The generated member is a plain `std::string`, so an empty value counts as absent: `rows=""` makes the formation `POINT`, `parts_match=""` reads the holder's building (the binder logs a one-time warning for the empty value). `rows` with commas only gives an empty list like `String.split` | The binder cannot tell `""` from an absent attribute; neither form occurs in the data (the census finds only non-empty values). Test `WalkerTemplatesTest.StepsLoopTypesAndRows` |
| `NpcShoutData.afterUnmarshal` mutations of the shout templates | Copies the shouts per npc id, then `shoutList.getNpcIds().remove(j)`, `getNpcShouts().clear()`, `shoutList.makeNull()`, `group.getShoutNpcs().remove(i)` and `group.makeNull()` | `ShoutList`/`ShoutGroup` getters are const; `makeNull` is ported but destroys the bound `NpcShout` objects, so the holder (P4-09) builds its index from `const NpcShout*` into the bound lists in Java's visiting order and skips every remove, clear and makeNull | Templates are const after load and index maps point into the bound storage (static-data.md §2.6); the index content and order are Java's |
| Missing required lists in edge cases absent from the data | `ArcadeLevels.getMaxUpgradeLevel` and `StorageExpansionTemplate`'s getters throw `NullPointerException` when the (required) list was never bound; `MeshList.getZoneName` returns `null` for a mesh without `zone` | `ArcadeLevels` throws `IndexOutOfBoundsException`, `StorageExpansionTemplate` returns 0 / nullopt; `MeshList::getZoneName` returns `""` for a present mesh without `zone` | Bound lists are empty vectors, never null; the one `<levels>` element of arcadelist.xml has `<level>` children and every `<expansion_npc>` has `<expand>` children, and `MeshList` has no binder and no caller (Java does not use it either) |

## game-server / skill and quest static data (P4-08, the lease over the `skillengine` and `questEngine` shells)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `MotionTime.afterUnmarshal` indexes | Builds `HashMap<WeaponTypeWrapper, HashMap<Integer, Times>>` per race and gender and a `HashMap<Integer, Times> robotTimes`, then sets the five bound lists to null | `std::map` indexes keyed by the normalized (main hand, off hand) `ItemGroup` pair of Java's `WeaponTypeWrapper` constructor (`MotionTime::weaponTypeWrapper`), with `const Times*` values into the bound lists, which stay | Stable template addresses (static-data.md §2.6); `WeaponTypeWrapper` (P5-02) has no C++ header yet. The maps are only looked up, never iterated, so the ordered map is not observable. Tests `MotionTimeHookTest.*`, `SkillShellsRealDataTest.MotionTimesBindWithHooks` |
| `MotionTime.getTimesFor(Player, id)` | Reads `player.isInRobotMode()` before the loop and again in every iteration | Reads the robot mode, race, gender and weapon types once and delegates to the C++-only `getTimesFor(inRobotMode, race, gender, mainHand, offHand, id)` | The lookup can be tested without a Player; the result differs only if the robot mode changes while the loop runs |
| `Effects` type sets | `EnumSet<EffectType>` fields; the type is resolved from `getClass().getSimpleName()` | `std::set<EffectType>` members (ordinal iteration order); the type is resolved from `javaClassName()` with the same `replace("Effect", "").toUpperCase()` rule | No reflection (static-data.md §2.2). All 169 concrete effect classes resolve: `EffectsHookTest.EveryEffectChoiceResolvesItsEffectType` |
| `Motion.afterUnmarshal`, `Times.afterUnmarshal` | `String.intern()` of the motion name and the weapon, only to save memory; `Times` throws `NullPointerException` for a missing `weapon` | `Motion`: no-op. `Times`: an empty weapon (absent; the census finds no present-empty value) fails the load with `LoadContext::fail` | `std::string` has no interning; the loader's error type with the file location, the load fails as in Java (P4-07a does the same for its hooks). Test `TimesHookTest.WeaponIsRequiredByTheHook` |
| `Effects.afterUnmarshal` without effects | An `<effects>` element without children leaves the list null, and the hook throws `NullPointerException` | `LoadContext::fail` with the Java exception name | Same failure with the loader's error type; no such element in the data |
| Lazily created lists: `Actions.getActions`, `ActionModifiers.getActionModifiers`, `QuestEvent.getIds`, `OnKillEvent.getMonsters` | The getter stores a new empty `ArrayList` into the template when nothing was bound, unsynchronized, and returns the live list | Returns the bound list or an empty one (a shared immutable vector for `getIds`); the template is never modified | D6: an unsynchronized write to shared static data; templates are const after load and no Java caller adds to the returned list. Tests `QuestEventTest.IdsAndMonsters`. One Java branch depends on the null: `OnKillEvent.operate` returns false for `monster == null`, but `XmlQuest.register` (and `XmlQuestData.getAlternativeNpcs`) call `getMonsters()` at QuestEngine load, so at run time Java always sees a non-null list and runs `complite.operate(env)`; the P5-06 port of `operate` must therefore not return early for an empty `monster` (no `on_kill_event` exists in quest_script_data today) |
| `Condition implements StatCondition` | `Condition` implements the `StatCondition` interface (`validate(Stat2, IStatFunction)`) | `Condition` declares the virtual `validate(Stat2&, IStatFunction&) const` itself and has no `StatCondition` base; no C++ header for `StatCondition` exists | `Condition` is the only implementor and no Java code holds a `StatCondition` (`Conditions.validate(Stat2, IStatFunction)` calls it through `Condition`); header request shells-3 |

## game-server / static data holders (`dataholders`, P4-09)

| Area | Java | C++ | Reason |
|---|---|---|---|
| Holder index maps (all holders) | `afterUnmarshal` builds `HashMap`/`LinkedHashMap`/`EnumMap` indexes and sets the bound list to null (or clears it: `InstanceCooltimeData`, `PetBuffsData`, `PetFeedData`, `WalkerData`, `WalkerVersionsData`, `WorldRaidData`, the food lists of `ItemGroupsData`, the shout lists of `NpcShoutData`) | The indexes are `std::unordered_map`/`std::map`/`detail::LinkedMap` of template pointers into the bound storage, which stays; only `WalkerVersionsData` (copies strings) clears its list | Stable template addresses without copying the templates (static-data.md §2.6). Not observable: nothing but the holder reaches the bound lists |
| Iteration order of `Map.values()`/`keySet()` getters (`ItemData.getItemTemplates`, `NpcData.getNpcData`, `QuestsData.getQuestTemplates`, `RecipeData.getRecipeTemplates`, `MotionData.getMotionTimes`, `NpcFactionsData.getNpcFactionsData`, `NpcSkillData.getAllNpcSkillTemplates`, `XMLQuests.getAllQuests`, `PetData.getPetIds`, `PetFeedData.getPetFlavours`, `SkillData.getSkillTemplates`, `AutoGroupData.getRecruitableInstanceMaskIds()`) and first-match searches over them (`TeleporterData.getTeleporterTemplateByNpcId`, `ChallengeData.getTaskByQuestId`/`getQuestByQuestId`, `GuideHtmlData.getTemplateByTitle`, `HouseData.getAddresses`, `TownSpawnsData`) | `HashMap<Integer or String, V>` iteration | The same order, computed once by the hook with `detail::JavaHashMapOrder`, a model of `HashMap.putVal` (appends to the bucket, resizes after the insert that exceeds the threshold), `computeIfAbsent` (inserts at the head of the bucket, resizes at the start of a call while the size exceeds the threshold; `AutoGroupData`), `removeNode`, `resize` and `treeifyBin`'s resize of small tables: buckets in index order. A bucket that Java turns into a red-black tree (8 or more keys in a bucket of a table of 64 or more buckets) keeps the list order instead of the tree's; every such bucket is reported (a warning and `detail::javaTreeifiedBucketCount`). Maps keyed by enums (identity hash codes in Java) have no defined order in either language | Log wording (`SkillData.validateMotions`), client lists and first-match results equal Java's. `StaticDataRealDataTest` asserts that no index map of the real data reaches the tree case. Tests `JavaHashMapOrderTest.*` (put, computeIfAbsent head insertion and lazy resize, remove, treeify resize and report), `SkillDataTest.SkillTemplatesInJavaHashMapOrder`, `TreeifyBinResizesSmallTables`, `QuestsDataTest.*`, `ChallengeDataTest.*`, `HolderMapOrderTest.MapGettersIterateInJavaHashMapOrder` |
| Map-returning getters (`AtreianPassportData.getAll`, `ChallengeData.getTasks`, `TradeListData.getTradeListTemplate()`, `WorldRaidData.getLocations`; `GuideHtmlData.getTemplates`, `ZoneData.getZones`) | The live `HashMap`, iterated by `AtreianPassportService.onLogin` (new passports of the account), `ChallengeTaskService.buildTaskList` (the client's task list and new task rows), `LimitedItemTradeService.start` and the //worldraid admin command | The first four return a `const detail::LinkedMap<int32_t, const T*>&` filled by the hook in Java's `HashMap` iteration order (`JavaHashMapOrder`, put or putIfAbsent like Java); `GuideHtmlData.getTemplates` and `ZoneData.getZones` return a const reference to the unordered index map | Read-only after publication; packet, database and log order equal Java's. No Java caller iterates the two unordered maps where the order is observable (`getTemplates` has no caller, `ZoneService` copies `getZones` by map id). Test `HolderMapOrderTest.MapGettersIterateInJavaHashMapOrder` |
| `SpawnsData.getSpawnsByWorldId` order | `spawnGroupsByNpcId.values()` of the world's `HashMap<Integer, List<SpawnGroup>>` in bucket order (filled by `computeIfAbsent` and `remove`, changed at run time by Event start and stop), flattened | The npc id groups in the order the `RcHashMap` shim keeps: the order in which each npc id was (last) added to the world's map | The order only decides the order in which `SpawnEngine.spawnInstance` spawns the world's groups (object ids, rift and temporary spawn registration order), which no Java caller relies on; the run-time mutable map would need Java's table shape tracked through Event start and stop, and the instance object lookups that follow (`WorldMapInstance.getNpc`) iterate a `ConcurrentHashMap` whose order differs anyway. `getSpawnsForNpc` and the location indexes keep Java's list order |
| `LinkedHashMap` holders (`RiftData`, `VortexData`, `SiegeLocationData`, `WorldMapsData`, `WalkerData`, `InstanceCooltimeData`) | `LinkedHashMap` getters (`getRiftLocations`, `getSiegeLocations`, ...; `getInstanceCooltimes` copies it) | `detail::LinkedMap` (index plus insertion-ordered entries; a repeated key keeps its first position with the last value) or an ordered key vector; the rift, vortex and siege locations are `Ref`s owned by the published holder | Same order and values; read-only after publication |
| Load failures of hooks (`HouseBuildingData`, `HouseData`, `HouseNpcsData`, `StaticDoorData`, `EventData`) | Throw `IllegalArgumentException`; JAXB aborts the load | `LoadContext::fail` throws `StaticDataException` with the Java message and the file location | The loader's error type (as the P4-07a/b hooks); startup fails the same way. Tests `HouseBuildingDataTest.BuildingsByIdAndDuplicates`, `StaticDoorDataTest.DuplicateWorldFailsTheLoad` |
| Absent bound lists in hooks | A `for` over a null list (no element in the data) throws `NullPointerException` and aborts the load (except the hooks that check for null) | An absent list is the empty vector, the hook builds an empty index; `CubeExpandData`, `WarehouseExpandData` and `NpcSkillData` still throw `NullPointerException` for templates without npc ids | The count oracle rejects data on which Java would crash, so the real data never reaches the difference |
| `ItemRandomBonusData.bonusData` | `EnumMap` with an empty `HashMap` for every `StatBonusType`, filled by `afterUnmarshal` | A `std::map` keyed by the bonus types that have sets; a missing key reads as Java's empty map | Not observable: `getTemplate` returns null for an unknown set either way |
| TaskScope in hooks (`SpawnsData`, `ZoneData`, `RiftData`, `VortexData`, `SiegeLocationData`) | JAXB hooks run on the loading thread | The hooks that create RefCounted run-time objects or write collection shims open a nested `TaskScope` (STARTUP): a depth increment inside DataManager's scope, an own scope when a holder is bound alone | The runtime's read barrier (C2) needs a scope; template tests bind event templates (with their `<spawns>` child) outside one |
| `NpcData.init`, `ItemData.cleanup`, `GlobalDropData.processRules`, `GatherableData`, `SpawnsData` (mercenary zones) | Write into the templates | The writes go through `const_cast` on templates of the unpublished holder (their objects are not `const`) | Templates are `const` only after publication (static-data.md §3.6) |
| DataManager post-processing | Assigns the public static fields first, then runs `ITEM_DATA.cleanup()`, `processRules`, `validateBuyLists`, `validateMotions`, `DecomposeAction.validateRandomItemIds()`, which read `DataManager.X` | `DataManager::loadStaticData` (load, IDREFs, holders into `StaticData`, retired objects kept, counts logged), `postProcess` on the unpublished holders with the holders passed explicitly (`cleanup(itemCleanup)`, `validateMotions(motionData)`, ...), then `publish` (each holder once; an absent holder stays unpublished like Java's null field) | Publish-once immutable holders (static-data.md §3.3). The published data is the same |
| XML cache, XSD validation | `XmlDataLoader` logs "Preparing static data cache file..." and "Processing cache file...", writes `./cache/static_data.xml` and, when the merged data changed, validates it against the XSDs asynchronously; `waitForValidationToFinishAndShutdownOnFail` (GameServer.java:177) throws `GameServerError` for an invalid file, so the server does not start | No cache file and no XSD validation (the strict binder, static-data.md §3.4); the two messages are not logged (the loader logs "Loaded 92 static data import(s) from 664 file(s) in N ms"); `waitForValidationToFinishAndShutdownOnFail` returns at once. `DataManager::init` binds strictly: an unknown attribute or element, a missing required value or an unknown enum constant stops the startup | The design's deviations for the JAXB replacement. Strict binding at startup takes the place of Java's startup XSD check, which also refuses edited data with unknown attributes (the design note "warns at runtime" in docs/design/README.md predates that comparison) |
| Load time message | `String.format("%.1f", time / 1000f)` | `DataManager::formatSeconds`: HALF_UP of the shortest decimal form of the float widened to double, like `java.util.Formatter` | Same text; test `DataManagerTest.FormatSecondsLikeJavaFormatter` |
| `GlobalDropData.getAllowedNpcs` | Appends to the rule's own `GlobalDropNpcs` list and puts that list into the new `GlobalDropNpcs` object | Copies the existing npcs, appends, and moves the vector into the new object | Same final rule state; test `GlobalDropDataTest.*` |
| `SpawnsData` indexes | `allSpawnMaps` is a `ConcurrentHashMap` of plain `HashMap`s and `ArrayList`s written inside `compute` (Event start) and read without a lock (SpawnEngine, handlers); the other indexes are plain `HashMap`s | `allSpawnMaps` is the `ConcurrentHashMap` shim of `RcHashMap`/`RcArrayList` shims (Monitor-guarded), getters return snapshots (`std::vector<Ref<SpawnGroup>>`); the base, rift, siege, vortex, mercenary and Ahserion indexes are built only by the hook and stay plain maps | Java race (D6), fixed; test `SpawnsDataTest.RegularSpawnsChangeWhileOtherThreadsReadThem`. The event `<spawns>` child keeps its bound spawn maps like Java; the C++ storage of the top-level holder stays too (the spawn groups refer to the bound spawns) |
| Write-back and //reload | `SpawnsData.saveSpawn` (with `loadSpawnsFromTemplateFiles`, `getRelativePath`), `WalkerData.addTemplate`/`saveData`, `ZoneData.saveData` marshal XML with JAXB; `EventData.setEvents`, `NpcSkillData.setNpcSkillTemplates`, `XMLQuests.setData` replace data in place; `FlyRingData`/`RoadData`/`ShieldData.addAll` append (no caller) | `AION_UNPORTED` (write-back after the load path, static-data.md §3.6 item 7; //reload deferred, D3); `addAll` is not declared | Published holders are immutable |
| `EventData`, `QuestsData.getQuestsByNpcFaction`, `PetSkillData`, `TownSpawnsData.getSpawns`, `TribeRelationsData.getBaseTribe`, `UpgradeArcadeData`, `ItemGroupsData.bonusSize`, `PlayerInitialData.getItems` | Dereference nulls (`NullPointerException`) | The same situations throw `runtime::NullPointerException` explicitly | C++ has no implicit null check |
| `PlayerInitialData.data` | `HashMap<PlayerClass, PlayerCreationData>`, a null key for a player_data without class | `std::map<std::optional<PlayerClass>, ...>` | Same lookups |
| `PlayerInitialData.PlayerCreationData.ItemType.toString` | `template` printed by `Object.toString` (class name and identity hash) | Class name and the template address in hex | Identity hashes do not exist in C++ (as P4-11a) |
| `InstanceCooltimeData.calculateInstanceEntranceCooltime` | `ZonedDateTime.with(LocalTime)` and `plusDays` resolve a daylight saving overlap with the previous offset | The local date time is resolved by `ServerTime::of` (gaps forward, overlaps with the earlier offset); `minutes * 60 * 1000` wraps like Java's int arithmetic; a cooldown rate of 0 throws `ArithmeticException` | `ServerTime` has no `with`/`plusDays`; differs only inside a daylight saving overlap |
| `EmotionLearnAction.LEARNABLE_IDS` (P5-07, ported for the load path) | `ConcurrentHashMap.newKeySet()` | The Monitor-guarded `runtime::HashSet<int32_t>` shim | Item templates are also bound outside a TaskScope (tests); a set of ints needs no read barrier |
| `DecomposeAction.validateRandomItemIds` (P5-07) | A static method of `DecomposeAction` over its id arrays, reading `DataManager.ITEM_DATA` | `DecomposeAction::validateRandomItemIds(const dataholders::ItemData&)` (header request holders-2) over the Java arrays, file-local constants in `DecomposeAction.cpp`; `chunkEarth`/`chunkSand` are checked Asmodians first, then Elyos | `ITEM_DATA` is not published yet during C++ post-processing, so the holder is passed (static-data.md §3.3); Java's `HashMap<Race, int[]>` order can only change which invalid id the message names |
| `MotionData.calculateAnimationTimesAfterLastHit` | `skill instanceof ChargeSkill` | `AION_UNPORTED` until `skillengine/model/ChargeSkill.h` (P5-02) exists | The class has no header |
| Config JAXB roots (`InGameShopProperty`, `RiftSchedule`, `SiegeSchedules`, `WorldRaidSchedules`) `load()` | `JAXBUtil.deserialize(file, class)`: ignores unknown attributes, `RuntimeException("Failed to unmarshal class <FQN> from <file>")` | Strict binding (unknown attributes are errors, as for static data), `commons::utils::Exception` with the Java message and the binding error as cause, `std::unique_ptr` results; `getCategories()` never creates the list (it always exists) | The strict binder; tests `ConfigRootsRealDataTest.*` |

## game-server / network crypt and generated packet tables

| Area | Java | C++ | Reason |
|---|---|---|---|
| `Crypt.encrypt/decrypt` without a key | NullPointerException | `IllegalStateException` | No NPE |
| `EncryptionKeyPair.decrypt` of an empty packet | XORs the byte just past the packet | Returns false and touches nothing | Memory safety; the dispatcher never passes an empty packet |
| `SM_SYSTEM_MESSAGE` parameters | Formatted with `toString` in `writeImpl`; null strings allowed | Formatted when the packet is constructed; string parameters are `std::string_view` (no null) | Generated factories; no null references |
| `Object...` parameters of `SM_SYSTEM_MESSAGE`, `SM_QUESTION_WINDOW`, `SM_CLOSE_QUESTION_WINDOW` (S0c) | Formatted in `writeImpl`; a `null` element prints `null` | Formatted into strings at construction; a null parameter is written as an empty string (sysmsg.py contract) | No `Object` in C++; no caller passes null |
| Packet members iterated from hash containers (S0c headers: `SM_NEARBY_QUESTS`, `SM_MOTION.activeMotions`, `SM_TOWNS_LIST`, `SM_SIEGE_LOCATION_INFO`, `SM_RECIPE_COOLDOWN`, `SM_RECIPE_LIST`; `SM_ITEM_COOLDOWN`) | `HashMap`/`HashSet` iteration order | The frozen `std::unordered_map`/`unordered_set` members stay (header requests sm-b-1 and sm-b-2 rejected). As ported in wave 3b-2: `SM_NEARBY_QUESTS`, `SM_RECIPE_*` and `SM_TOWNS_LIST` approximate Java's bucket order with `JavaHashMapOrder` over ascending keys, `SM_SIEGE_LOCATION_INFO` reads the service's map at write time, `SM_ITEM_COOLDOWN` writes ascending ids (sections "server packets" below); `SM_RIFT_ANNOUNCE` and `SM_SHOW_BRAND` use `std::map`, which equals Java's order there | Layout from `fieldmap.json`; the client does not depend on these orders. An ordered container is a header request if one turns out to matter |
| `STR_MSG_MERCHANT_PET_GET_SELL_ITEM` | Returns `AionServerPacket` | Returns `SM_SYSTEM_MESSAGE` | Uniform generated factories |
| Reserved factory names | `_STR_MSG_Heal_TO_ME`, `STR_RESURRECT_DIALOG__SKILL/ITEM/BIND/5MIN/30MIN`, `STR_RESURRECTOTHER_DIALOG__5MIN`, `STR_ERROR_CHANGE_WEAPON_SKIN__*` (3), `STR_SKILL_CAN_NOT_*__WHILE_IN_CURRENT_STANCE` (2) | Leading `_` stripped, `__` collapsed, trailing `_` (`STR_MSG_Heal_TO_ME_`); only `CM_EMOTION` calls two of them | Reserved identifiers in C++ (CONVENTIONS) |
| `DialogAction` | Final class with `int` constants; `nameOf` returns null; constant `NULL` | Namespace of `inline constexpr int32_t`; `nameOf` returns `std::optional<std::string_view>`; `NULL_`; `entries()` added | `NULL` is a C macro |

## game-server / system messages (`SM_SYSTEM_MESSAGE`, P4-06)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `Object...` parameter types | Any object, formatted with `toString()` in `writeImpl` (`Boolean` "true", `Short`, `Character`, enums by name, objects by their `toString`) | `toJavaString` overloads for `bool`, `int16_t`, `char16_t`, generated enums (constant name) and objects with `toString()` (also through `Ptr`/`Ref`; a null object is written as a null string, the same bytes as ""); formatted at construction | No `Object` in C++ (S0B-035); `bool` would otherwise print "1" |
| `STR_SKILL_ABYSS_SKILL_IS_FIRED`, `STR_ABYSS_ORDER_RANKER_DIE` | `ChatUtil.l10n`, `Race.getL10n()`, `AbyssRankEnum.getRankL10n(player)` | Stand-ins for those three helpers and the `ChatType` ids in `network/detail/SystemMessageL10n.h` (P4-15), tested against the Java sources; the l10n string is WTF-8 like `ChatUtil::l10n` (header request pre-4) | The companions and `ChatUtil` (P4-05, P5-01) do not exist yet; replace when they land |

## game-server / network (`network/**`, P4-15)

| Area | Java | C++ | Reason |
|---|---|---|---|
| Server packet serialization | `AionServerPacket.write` runs `writeImpl` lazily on the NIO dispatcher thread when the packet is written | `serialize` runs `writeImpl` eagerly on the sending thread into a thread-local scratch buffer (nested serializations get their own buffer); the connection queue holds the serialized bodies ordered by serialization sequence; `writeData` on the IO strand prepends the length and encrypts. A `writeImpl` exception reaches the sender instead of disconnecting the client | runtime-architecture.md §8 (eager packets, D1); the IO strand never touches game objects |
| Oversized server packets | Warned in `writeData` after writing | Warned in `sendPacket` after serializing (same message) | The size is known after serialization |
| Packet name echo for membership-10 accounts | Sent from `writeData` for every written packet except `SM_MESSAGE` | Sent from `sendPacket` after the packet was queued | runtime-architecture.md §8.6: the IO strand does not send packets |
| `SM_KEY` | Queued by `initialized()`; `writeImpl` enables the key while it is written on the dispatcher | Serialized for the connection in `initialized()` before IO starts (the key is created there); the first `encrypt` on the strand still only enables the crypt. The packet is `serverpackets::SM_KEY` itself (ported by P4-16; the private stand-in subclass is gone, wave 3b-2) | runtime-architecture.md §8.5 |
| `ConnectionAliveChecker` creation | In the constructor | In `initialized()`; the scheduled task runs on its own copy of the `std::weak_ptr<AionConnection>` (the `task` member is initialized before the `aionConnection` member, so the task never reads that member) and does nothing once the connection is gone | `weak_from_this()` is empty during construction (runtime-architecture.md §11) |
| Packet flood filter map (`pffRequests`) | Created only with `PffConfig.PFF_MODE > 0` and a threshold map when the connection is created | Always created; `processData` checks `PFF_MODE > 0` (and a threshold for the opcode) for every packet | Frozen header layout (always-created member); only a config reload during a connection's lifetime behaves differently |
| `AionConnection.toString` | Plain field reads | Opens its own `TaskScope` for the field loads | Called from commons log statements outside any task |
| Client packet table | Reflective `PacketInfo` table built in the static initializer | Built on first use from `ClientPacketInfo.gen.inc` and the `AION_CLIENT_PACKET` registry; `setEntries` replaces the registry (startup, tests). A class without a registration (not ported yet) is logged once and the packet ignored | No reflection; partial port |
| Unknown opcode log | `String.format("...%n%s")` (platform line separator) | `\n` | Deterministic logs (as in commons) |
| LS/CS link packet execution | `ThreadPoolManager.execute(packet)`: unordered, concurrently | A per-link `runtime::SerialExecutor` on the instant pool runs the packets in receive order | runtime-architecture.md §1.4 |
| LS/CS link connect | `SocketChannel.open` + `Dispatcher.register` on the calling thread | `NioServer::openSocket` inside a `BlockingRegion`, then `registerConnection` (commons convention for outbound connections) | Asio NioServer |
| `NetFlusher` | Own daemon `java.util.Timer` thread; `RuntimeException`s printed to stderr | Fixed-rate task on the ThreadPoolManager scheduled pool; exceptions logged | Game code creates no threads |
| `CM_PTRANSFER_RESPONSE` wrong server id | Throws and catches an `Exception` to print its stack trace to stderr | Logs the exception with the class logger | No stderr stack traces |
| `CsClientPacket.clonePacket` | Shallow `Object.clone()` | Returns null (Java's `CloneNotSupportedException` result) | No polymorphic shallow copy; nothing calls it |
| `GameConnectionFactoryImpl.create` | `NetworkConfig.ENABLE_FLOOD_CONNECTIONS` switched on by a reload after the factory was created: NullPointerException | The flood check is skipped while the factory has no `FloodManager` | Java bug (NPE on the accept path) |
| `ConnectionFactory` | `GameConnectionFactoryImpl implements ConnectionFactory` | RefCounted factory plus `toConnectionFactory()` returning the commons `std::function` (it retains the factory) | commons `ConnectionFactory` is a `std::function` |
| `BannedMacManager` banned-until log | `Timestamp.toString()` (nanosecond fraction) | Same format with millisecond precision (the fraction without trailing zeros) | `Timestamp` has millisecond precision |
| Stand-ins for missing companions (temporary) | `ItemSlot`, `ItemGroup.getArmorType`, `PlumStatEnum`, `StatEnum.getItemStoneMask/getSign`, `ChatType.getId`, `ChatUtil.l10n`, `Race.getL10nId`, `AbyssRankEnum.getRankL10n`, `Race.getRaceId`, `PlayerClass.getClassId`, `Gender.getGenderId`, `BanAction.getId` | Private data tables in `network/detail/ItemData.h`, `network/detail/EnumIds.h`, `network/detail/SystemMessageL10n.h` and `SM_HDDBAN_CONTROL.cpp`, copied from the Java enums | The companion headers belong to P4-05/P4-07a/P4-13/P5-01 and do not exist yet; replace when they land |
| `GameServer.isShutdownScheduled/isShuttingDownSoon` | ShutdownHook state | Always false until P5-14 writes `GameServer.h` and the ShutdownHook (header request network-1 rejected) | The GameServer class belongs to P5-14; no shutdown can be scheduled before |
| LS/CS link send after a link drop (D6) | `LoginServer.authenticateClient/requestAuthReconnection/sendPacket` and `ChatServer.sendPlayer*` read `lsCon`/`csCon` in `isUp()` and again to send; `kickOnlineCharacters` reads `player.getClientConnection()` twice. A link drop (`disconnect()` nulls the field) or a logout between the reads throws a NullPointerException in the caller | The connection is read once (`upConnection()`, one `getClientConnection()`): the send goes to the connection that was checked, which ignores it once closed; `sendPacket` returns true as if the drop came after the send | Obvious Java race (design D6); a null `std::shared_ptr` dereference would crash the process. Tests: `LoginServerLinkTest.LinkDropWhileAPacketIsWritten`, `*.SendingRacesWithTheLinkDrop` |
| LS server packet serialization | `LsServerPacket.writeImpl` runs lazily on the NIO dispatcher in `writeData` with `guard` held (`SM_PTRANSFER_CONTROL` loads items from the database there, `SM_ACCOUNT_LIST`/`SM_LS_CONTROL` read game state) | `LoginServer` serializes every LS packet it sends on the sending thread (into a buffer of the link's write buffer size, the same overflow bound) and queues the bytes; `writeData` only copies them. A `writeImpl` exception reaches the sender instead of the dispatcher. Packets sent by the link itself (`SM_GS_AUTH`, `SM_LS_PONG`) stay lazy, they read no game state | runtime-architecture.md §1.1: IO threads frame and write serialized bytes and never block on the database while other senders wait on `guard`. Test: `LoginServerLinkTest.LinkDropWhileAPacketIsWritten` |
| `PvpInstanceScoreWriter.writeEmptyDataToBuffer` with more players than the table | `new byte[negative]`: NegativeArraySizeException | `IllegalArgumentException("Negative array size: n")`, as `PacketWriteHelper::skip` | The runtime has no NegativeArraySizeException type |

## game-server / server packets SM_A* to SM_K* (`network/aion/serverpackets/SM_[A-K]*`, P4-16)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `SM_ITEM_COOLDOWN` entry order | Iterates the cooldown `Map<Integer, ItemCooldown>` (the player's `ConcurrentHashMap`) in its hash order | Writes the entries in ascending item cooldown group id | The frozen member is an `unordered_map`, whose order is unspecified. Java's order is the `ConcurrentHashMap` bucket order (`id & (n - 1)`, 16 buckets until the map grows, insertion and resize history inside a bucket), so it differs from ascending ids for most real group ids (126 of the 136 `usedelayid` values in the item templates exceed 15, e.g. ids 5 and 20 are written 20, 5 by Java). The exact order cannot be rebuilt from the map's contents; ascending ids give a deterministic order, and the client matches the entries by id |
| Java `null` strings | `writeS(null)` for missing parameters (`SM_CLOSE_QUESTION_WINDOW`, `SM_CRAFT_UPDATE`, `SM_GATHER_UPDATE`, `SM_ALLIANCE_INFO`, `PlayerService.getPlayerName` in `SM_BROKER_SERVICE`) | `writeS("")` | `BaseServerPacket.writeS(null)` writes only the terminating 0 char, the same bytes as an empty string |
| Negative array sizes | `new byte[size]` with a negative size (`SM_GAMEGUARD`, `SM_CUSTOM_PACKET` element `b`): `NegativeArraySizeException` | `IllegalArgumentException("Negative array size: n")` | The runtime has no `NegativeArraySizeException` type (as P4-15) |
| PER_RECIPIENT packets serialized without a connection | `con.getActivePlayer()` on a null `con`: `NullPointerException` | `NullPointerException("SM_X::writeImpl without a connection")` | runtime-architecture.md §8.7; a raw null dereference would crash |
| `SM_GROUP_INFO`, `SM_ALLIANCE_INFO`, `SM_CHAT_WINDOW` team leader | `group.getLeader()` / `alliance.getLeader()` (the erased generic's cast to `PlayerGroupMember` / `PlayerAllianceMember`) | `GeneralTeam::getLeader()`, the same member before the cast | `PlayerGroupMember.h` and `PlayerAllianceMember.h` (P5-10) do not exist yet; the narrowing accessors cannot be called on incomplete types. Same object and same `getObjectId()`/`getName()` |
| Enum constructor data without companion headers (temporary) | `ItemDeleteType.getMask`, `ItemAddType.getMask`, `ItemUpdateType.getMask/isSendable`, `AttackStatus.getId/isCounterSkill`, `SkillTargetSlot.getId/FULLSLOTS`, `TeamType.getType/getSubType`, `LootRuleType.getId`, `GroupEvent.getId`, `PlayerAllianceEvent.getId`, `HouseDoorState.getId`, `ArtifactStatus.getValue`, `InstanceProgressionType.getId`, `EffectResult.getId`, `SpellStatus.getId`, `LegionRank.getRankId`, `EffectReserved.ResourceType.getValue`, `SM_BROKER_SERVICE.BrokerPacketType.getId` | Tables copied from the Java enums. The ones several packets use are in `serverpackets/detail/PacketSupport.h`, shared with P4-17 (`ItemDeleteType`, `ItemAddType`, `ItemUpdateType`, `AttackStatus`, `SkillTargetSlot` id and `FULLSLOTS`, `TeamType`, `LootRuleType`, `HouseDoorState`, `LegionRank`; `OpcodesAndSupportTest` checks every constant); the others stay local to their packet `.cpp` | The companions belong to P5-01, P5-02, P5-07, P5-10, P5-11, P5-12a and P5-13; replace the local tables when they land. The nested enums of this chunk got companions: `SM_ATTACK_STATUS_TYPEInfo.h`, `SM_ATTACK_STATUS_LOGInfo.h`, `SM_ENTER_WORLD_CHECK_MsgInfo.h`, `SM_CUSTOM_PACKET_PacketElementTypeInfo.h` |
| `SM_AUTO_GROUP` template lookup | `AutoGroupType.getAGTByMaskId(maskId)`: the enum's templates (`DataManager.AUTO_GROUP.getTemplateByInstanceMaskId` of each constant's instance mask id, read by the enum constructor at class loading) | The constructor walks the 33 instance mask ids of the enum constants in ordinal order and reads each template from the holder when the packet is created; a missing template before the match throws `NullPointerException` like Java's loop | The `AutoGroupType` companion (P5-10) does not exist; the result, including the exception, is Java's once the holder is loaded |
| `SM_INVENTORY_INFO` null items | The constructor removes the `null` elements from the caller's list (`items.removeAll(singletonList(null))`) | The packet copies the non-null elements; the caller's vector is unchanged | The C++ constructor takes the list by `const&` (frozen signature); no Java caller reads the list after creating the packet |
| `SM_GROUP_MEMBER_INFO`, `SM_ALLIANCE_MEMBER_INFO` event | `writeImpl` changes the packet's `event` field from `ENTER` to `ENTER_OFFLINE` for an offline player | Same, once per serialization of the packet | Per-recipient serialization (runtime-architecture.md §8.5); a packet object is not reused after the player comes online |
| Partly ported bodies | – | `SM_HOUSE_BIDS` reaches `AION_UNPORTED` for the first bid entry (`AuctionEndTask.getRemainingAuctionSeconds`, P5-11) and `SM_ALLIANCE_INFO` for an alliance in a league (`LeagueMember.getLeaguePosition`, P5-10); everything before is ported | The headers of those classes do not exist yet |

## game-server / server packets SM_L* to SM_Z* and the Abstract* bases (`network/aion/serverpackets/**` without `SM_[A-K]*`, P4-17)

| Area | Java | C++ | Reason |
|---|---|---|---|
| Service calls and stat reads of the packet bodies | The packets call the services (`LegionService`, `HousingService`, `SiegeService`, `PricesService`, `TownService`, `DropRegistrationService`, `RepurchaseService`, `LimitedItemTradeService`, `MultiClientingService`, `BrokerService`, `ConquerorAndProtectorService`, `LegionDominionService`, `EventService`), DAOs (`PlayerSettingsDAO`, `MailDAO`) and the stat containers (`getLifeStats().getHpPercentage()`, `getGameStats().getMovementSpeedFloat()`, `getAttackSpeed()`) directly | The same expressions behind `serverpackets/detail/PacketLookups.h` (one free function per Java expression). A test can install function pointers with `setPacketLookupsForTests` (the pattern of `model/items/detail/StaticDataLookups.h`); server code never installs them, so production behaviour is the Java expression | Most services and the stat calculation (P5-01) are not ported yet; the golden-byte tests of `SM_PLAYER_INFO`, `SM_NPC_INFO`, `SM_VERSION_CHECK`, `SM_LOOT_STATUS` and others need the values those calls return |
| `HashMap` / `HashSet` iteration (`SM_NEARBY_QUESTS`, `SM_RECIPE_COOLDOWN`, `SM_RECIPE_LIST`, `SM_TOWNS_LIST`) | Iterates the `HashMap<Integer, ...>` / `HashSet<Integer>` in its bucket order | The frozen members are `std::unordered_map` / `std::unordered_set`; `writeImpl` iterates them through `dataholders::detail::JavaHashMapOrder` (Java's bucket index, table size and resize) with the keys inserted in ascending order | An approximation. It is exact only when Java's table has the size a new map of the same keys gets (no removals after the map grew) and, for keys that share a bucket, Java inserted them in ascending order. Recipe and quest ids have 6 to 9 digits and a player has dozens to hundreds of them, so shared buckets are the norm (e.g. recipes 155000017 and 155000001 both land in bucket 12 of 16); inside a bucket Java keeps the insertion order (`RecipeList.addRecipe` appends in learning order), and a `HashMap`/`HashSet` never shrinks, so after `RecipeList.removeRecipe` Java's table can be larger than the one rebuilt for the remaining keys. The unordered containers of the frozen headers keep neither the insertion order nor the table history (header request sm-b-2, rejected). The client does not depend on the order of recipe ids, quest markers, craft cooldowns or towns |
| `SM_SIEGE_LOCATION_INFO()` (info type 0) | Stores the service's live `LinkedHashMap` (`SiegeService.getSiegeLocations()`) in the constructor and iterates it in `writeImpl` | The constructor leaves `locations` empty; `writeImpl` reads the service's map (`detail::getSiegeLocations()`, the map's own snapshot order) each time it is serialized | The frozen member is an unordered map, which would lose the XML order of the `LinkedHashMap`; reading the live map at write time is what Java's stored reference observes (header request sm-b-1, rejected). The XML order itself is not guaranteed yet: `SiegeService::locations` is a `runtime::HashMap` (`MapOrder::HASH`, unspecified order) where Java has `SiegeLocationData`'s `LinkedHashMap`; it iterates in insertion order today only by accident of the shim. The SiegeService owner (P5-12a) should switch the member to `runtime::LinkedHashMap` through a header request |
| `SM_LOOT_ITEMLIST` item order | Iterates the drop registration's `HashSet<DropItem>` (identity hash order) | Iterates the `unordered_set<Ptr<DropItem>>` given by the caller in its order | Identity hash order is arbitrary in Java too; the client matches loot by index within this packet only |
| `SM_MAIL_SERVICE.writeLetterDelete` | `private void writeLetterDelete(int, int, int, int, int... letterIds)`, called with the `int[]` field | A C++-only overload taking `std::span<const int32_t>` (the `initializer_list` overload delegates to it) | Java's varargs accepts the array; the frozen overload takes only an `initializer_list`, which cannot be built from the stored vector |
| `SM_VERSION_CHECK` server ratios and start time | `GameServer.getRatiosFor(race)`, `GameServer.getCountFor(race)`, `GameServer.START_TIME_SECONDS` | File-local stand-ins: ratio 0, count 0 (Java's initial values; only `GameServer.updateRatio` changes them, whose callers are not ported), start time from `SystemInfo::getProcessStartTime()` (the JVM start time equivalent). `TODO(P5-14)` | `GameServer.h` is a P5-14 file that does not exist yet |
| `SM_PLASTIC_SURGERY` ticket check | `CM_CHARACTER_EDIT.checkOrRemoveTicket(player, isGenderSwitch, false)` | A file-local copy of the check without removal (the same ticket ids, `getInventory().getItemCountByItemId(id) > 0`). `TODO(P5-15)` | The client packet (P5-15) is not ported yet; the non-removing branch only reads the inventory |
| `SM_TIME_CHECK` server uptime | `ManagementFactory.getRuntimeMXBean().getUptime()` | `system_clock::now() - SystemInfo::getProcessStartTime()` in milliseconds | No JVM runtime bean |
| `SM_SKILL_COOLDOWN` reset constructor | `resettableCooldownIds.stream().collect(Collectors.toMap(c -> c, _ -> 0L))` throws `IllegalStateException("Duplicate key X (attempted merging values 0 and 0)")` for a repeated id | The same exception type and message from a local loop; the entries are then sorted by `getDurationMillis` with a stable sort like Java's `List.sort` | – (kept for exactness) |
| Enum constructor data without companion headers (temporary) | `LegionEmblemType.getValue`, `LegionRank.getRankId`, `LegionHistoryAction.getId`, `HouseDoorState.getId`, `SiegeRace.getRaceId`, `QuestStatus.value`, `TransformType.getId`, `SummonMode.getId`, `AbyssRankEnum.getId`, `ItemAddType.getMask`, `ItemUpdateType.getMask/isSendable`, and for P4-16 too `ItemDeleteType.getMask`, `AttackStatus.getId/isCounterSkill`, `TeamType.getType/getSubType`, `LootRuleType.getId`, `SkillTargetSlot.getId/FULLSLOTS` | Tables in `serverpackets/detail/PacketSupport.h` with the Java constructor values (`OpcodesAndSupportTest` checks every constant) | The companion headers of those generated enums do not exist yet; the tables can be replaced by the companions once they do |
| PER_RECIPIENT packets serialized without a connection | `con.getActivePlayer()` on a null `con`: `NullPointerException` | `NullPointerException("SM_X::writeImpl without a connection")` (`detail::requireConnection`) | runtime-architecture.md §8.7 (as P4-16) |
| Java `null` for boxed values | Auto-unboxing a `null` `Integer`/`Long`/enum, or dereferencing a `null` `Timestamp` (`SM_RIFT_ANNOUNCE`, `SM_NPC_ASSEMBLER`, `AbstractHouseInfoPacket`, `SM_TOWNS_LIST`), throws `NullPointerException` | `detail::unbox(optional, what)` throws `NullPointerException(what)` | Same failure, with a message. `SM_TOWNS_LIST` never reaches it today: `Town::getLevelUpDate()` wraps a `const Timestamp` member that the `Town` constructor (P5-11, still `AION_UNPORTED`) initializes to the epoch instead of the DB value, so once that constructor is ported without a header change the packet would write level up date 0. The `Town` owner needs a header request for a mutable `Field<std::optional<Timestamp>>` (Java's `levelUp()` calls `levelUpDate.setTime(now)`) |
| Partly ported bodies | – | Bodies are complete; calls into other chunks that are still `AION_UNPORTED` (P5-01 stat containers for `SM_STATS_INFO`, `SM_SUMMON_PANEL`, `SM_SUMMON_UPDATE`; `Legion` construction, P5-10; `QuestVars.getQuestVars`, `QuestState.canRepeat`; `SkillEntry.getSkillTemplate`; `PetFeedProgress.getDataForPacket`; `House` getters; `Npc.canSell/canBuy/canPurchase`; `PlayerSkillEntry.isProfessionSkill`) throw `UnportedException` at run time until those chunks are ported | Those functions belong to other chunks |

## game-server / handler registry

| Area | Java | C++ | Reason |
|---|---|---|---|
| Handler discovery | Class listeners over the compiled `data/handlers` at startup | Marker macros; `aion_gs_regscan` writes the tables at build time | Handlers are compiled in |
| Duplicate keys | AI names and map ids: `put`, last wins; zone names, quest ids: warning | Build errors | Deterministic registration; the Java tree has no duplicates |
| QuestSpawnAnalyzer | Regex scan of `data/handlers/**/*.java` at startup | Same pattern over the compiled-in C++ handler sources at build time (`npcIdsSpawnedByHandlers()`) | No sources at runtime |
| Unported code | – | `AION_UNPORTED()` throws `UnportedException` (an `UnsupportedOperationException`) and logs the site once | Partial port links from day one |

## game-server / object model (planned member mapping, `fieldmap.toml`)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `Creature.ai` | `final` (replaced reflectively by `//ai set`) | `PartSlot<AbstractAI>` (RetireTo::OWNER); the retired AI stays alive with the creature | `//ai set` without reflection (runtime-architecture.md §18 item 9) |
| `WorldMapInstance.instanceHandler` | `final` | `Field<Ref<InstanceHandler>>`, detached in `destroyInstance` | Breaks the instance/handler cycle (§18 item 8) |

Declared in the spine headers (S0b/S0c) and effective when the owning chunk ports the body (design: runtime-architecture.md §5.3, §24):

| Area | Java | C++ | Reason |
|---|---|---|---|
| C++-only cycle breakers | Garbage collector frees unreachable cycles | Logout (`LogoutBreakers::run`, also after a throwing enter-world) clears, without notifications or packets: target (no `onTargetChanged`), kisk, the storage actors, stance and ride observers, IdianStone listeners of equipped items, observers and attack-calc observers. Deleting an object (`VisibleObjectController::onDelete`) clears its target; for Creatures the observers, the effect maps (without ending the effects) and the stat functions owned by effects; for Npcs the walker group. `Skill.removeObservers` also clears `firstTargetDieObserver`; `Effect.endEffect` also resets `designatedDispelEffect`; `GatheringTask` resets `gathererObserver`; `House.resetRegistry` clears the dropped registry's objects and decorations; `InstanceService.destroyInstance` detaches the handler and clears `startPos` and the registered team; `PlayerAllianceService.disband` clears the alliance's groups | No GC (D1); each is listed in `cycles.toml` |
| `HostileUpEffect.tempHate` | Template field written by `calculate` and read by `applyEffect` and its delayed task; concurrent casts of the same effect template overwrite each other's value | Per cast: `Effect::hostileUpTempHate` holds the value `calculate` computed for that effect | Static data is immutable; removes the cross-cast race |
| Run-time `BoundRadius` | `updateBoundingRadius` creates a new object per call | `BoundRadius::intern`: one immortal object per distinct radius, kept for the process lifetime | `PlayerCommonData` holds template pointers (`const BoundRadius*`) |
| `PlayerScript.LUA_SANDBOX_FIX` | Compressed script built by the static initializer (CompressUtil, zlib) | Null. CompressUtil is ported now (P4-05), so the house scripts owner (P5-11) can build it; `CompressUtil::decompress` still rejects `compress` output (open item, phase4-status.md) | A static initializer must not reach `AION_UNPORTED` |
| `hashCode` of `LegionHistoryEntry` / `PlayerScript` | Identity hash of the enum constant / of the byte array | Enum ordinal / address of the byte array | No identity hash in C++; only hash iteration order can differ |
| `EffectReserved.compareTo` ties | `hashCode() - o.hashCode()` of identity hashes (may overflow) | Equal positions ordered by object address | Consistent total order without overflow; only the order of distinct objects at the same position differs |
| `EffectController` passive map before the first passive effect | `Collections.emptyMap()`: a stray `put` throws `UnsupportedOperationException` | Null map: a stray `put` throws `NullPointerException` | One shared empty map would make every creature lock the same Monitor |
| `Collidable.collideWith` | Takes a `Collidable`; non-Ray arguments throw `UnsupportedCollisionException` | Takes `math::Ray&`; the branches do not exist | Every caller passes a Ray (a value type) |

## game-server / base (P4-05: utils, model direct classes and enums, `model.geometry`, audit, cache)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `PacketSendUtility.sendPacket` | `if (player.isOnline()) player.getClientConnection().sendPacket(packet)` loads the connection twice; a logout between the loads throws `NullPointerException` | The connection is loaded once and used if non-null | Obvious race (D6); runtime-architecture.md §8.3 already loads it once |
| `PositionUtil` sin/cos | `Math.sin`/`Math.cos` (HotSpot intrinsics) | C runtime `std::sin`/`std::cos`; `Math.atan2` is the bit-exact `StrictMath` port | The intrinsics are not bit-specified; results may differ in the last bit before the float casts |
| `CompressUtil.decompress` errors | `DataFormatException` with zlib's message for invalid data; `RuntimeException("Bad zip data, size: N")` for truncated data or a preset dictionary | `IllegalArgumentException` with the message of zlib's inflate (same checks in the same order); `IllegalStateException("Bad zip data, size: N")` | No checked exception types; the messages are kept. `compress` is a port of zlib's level 6 deflate and returns Java's bytes (zlib 1.3.1 vectors), because the game server does not link zlib |
| `HTMLCache` cache file | Serialized `HashMap` (`ObjectOutputStream`) | Own format: "AIONHTM1", then UTF-8 key and value with 4-byte little endian lengths; a Java cache file fails to load, is logged and rebuilt (Java's broken-file path) | C++ cannot read Java serialization |
| `HTMLCache` encoding | Files decoded with `HTMLConfig.HTML_ENCODING` | Files read as UTF-8 | No charset library; the configured default is UTF-8 |
| `CAPTCHAUtil.createImage` | `java.awt` renders the word with bold 25 pt Verdana into a 160x80 image | Unported (`AION_UNPORTED`), so `createCAPTCHA` throws; `getRandomWord` and `DDSConverter` are ported | No text rasterizer; needs a decision (font, bitmap glyphs) before the CAPTCHA feature can work |
| `Polygon2D` awt methods | `Polygon2D(Polygon)`, `getPolygon()`, `getPolyline2D()` (class `Polyline2D`), `addPoint(Point2D)`, `contains(Point)`, `contains(Point2D)`, `getBounds()`, `getPathIterator` | Not declared; `Rectangle2D` and the `GeneralPath` state are small value structs (`Polygon2D::Rectangle2D`, `Polygon2D::Path2D`) with Java 25's crossing and bounds rules | No java.awt types; none of them has a caller |
| `RectangleArea(ZoneName, int, Point, Point, Point, Point, int, int)` | Bounding box of four `java.awt.Point` via `Rectangle.add` | Not declared | No java.awt types; no caller |
| `XmlUtil`, `JAXBUtil`, `StringSchemaOutputResolver`, `XmlValidationHandler` | JAXB unmarshalling, DOM documents, XSD schemas and validation events | Only `XmlUtil.listFiles` (one overload taking `std::filesystem::path`); the rest has no C++ counterpart | The static data runtime (pugixml binders with strict binding, static-data.md) replaces JAXB and XSD validation. The Java callers outside static data (SpawnsData XML write-back with schema validation, the rift, siege and world raid schedule loaders, InGameShopProperty, DatabaseCleaningService) need binder entry points of their own owners (open issue) |
| `ChatUtil` link detection in `split` | `Character.isLowerCase` for every Unicode lowercase letter | Lowercase letters that commons `StringUtils` maps (ASCII, Latin-1, Latin Extended-A, Greek, Cyrillic) | No Unicode property tables; links start with ASCII names |
| `ChatUtil.l10n` | Two UTF-16 code units, possibly a lone surrogate, written to the packet unchanged by `writeS`; null for id 0 | The same units as WTF-8 (commons `StringUtils::toWtf8`, a lone surrogate as its own 3-byte sequence), which the server packet `writeS` helpers decode back to the same units (`wtf8ToUtf16`), so the wire bytes equal Java's; `split` decodes and encodes WTF-8 too. Id 0 returns "" instead of null. Such a string is not valid UTF-8: text that leaves the packet path (database, logs) would carry the 3-byte sequences | UTF-8 strings cannot hold lone surrogates (header request pre-4, docs/porting/header-requests.md). "" is the `L10n::getL10n` convention for null |
| `GMService.getOnlineStaffMembers` | Live `values()` view of the staff map | Snapshot `std::vector<Ptr<Player>>` | hub-headers.md §7.1 collection returns |
| `ServerTime` | `ZonedDateTime` with nanoseconds; `parse` accepts every `ISO_ZONED_DATE_TIME` form | `std::chrono::zoned_time` with milliseconds; `parse`/`parseLocal` accept `yyyy-MM-ddTHH:mm[:ss[.fraction]]` with `Z`/`±HH:MM[:SS]` and an optional `[Region/City]`; parse errors are `IllegalArgumentException` | The server and the DAO layer use milliseconds; the accepted forms are those the server's data and commands use |
| `Area.getClosestPoint` | Returns new `Point2D`/`Point3D` objects, null for `SphereArea` | The `Point3D` overloads return a new `Ref<Point3D>` (null for `SphereArea`); the `Point2D` overloads return the point by value as `std::optional<Point2D>` (`std::nullopt` for `SphereArea`); `AbstractArea` subclasses compute closest points with the C++-only `getClosestPoint2D` | `Point2D` is a value type without identity; header requests base-1 and base-2 (docs/porting/header-requests.md) |
| `Chance.selectElement` | `static <T extends Chance> T selectElement(Iterable<T>, boolean)` returning null | Member function template over a container of pointer-like elements; returns a value-initialized element for null | No null references; the elements are `const X*` templates or `Ptr`/`Ref` |
| `AutoBan.punishment` | `protected static`, reachable from the package (`AuditLogger`) | `private static` with `AuditLogger` as friend | C++ has no package access |
| `PlayerActions.setPlayerMode` | `Object obj` cast to `RideInfo` or `InRoll` | `const std::any&` holding `const RideInfo*` or `Ref<InRoll>`; a wrong type throws `std::bad_any_cast` | hub-headers.md §7.4 |
| Enum companions | Methods and constructor data of the Java enums (`ChatType.getId()`, `Race.getRaceByString`, ...) | Free functions in `<Enum>Info.h` (`getId(chatType)`); nullable results are `std::optional`; `TribeClass.isGuard` searches the name on each call instead of a field set by the constructor | Generated enums cannot have members (static-data.md §2.5) |

## game-server / world (P4-10: `world`, `spawnengine`, `GeoService`, respawn, weather and game time services, movement task managers)

The last three rows are the World creation changes of wave 3b-2 (runtime lane, performance only; the same maps, regions and zones).

| Area | Java | C++ | Reason |
|---|---|---|---|
| `KnownList` pair membership: `addPair` (the pair-add sites of `KnownList.findVisibleObjects`, both loops, and `FlagKnownList.update`), the two-sided removals of `clear` and `forgetObjectsOrUpdateVisibility`, and `FlagKnownList`'s removal | `if (b.getKnownList().add(a)) a.getKnownList().add(b);` and `del(b); b.getKnownList().del(owner);` as independent steps, with no coordination with `World.despawn` or with each other | Every membership change of a pair runs under the pair's striped lock (64 immortal Monitors, lock class `KnownList::pairLocks#stripe`, taken after the owner's `KnownList` monitor and before the `knownObjects` stripes, nothing else locked or called under it): `addPair` inserts both edges, then reads both spawn states (the Dekker handshake with `World.despawn`, which sets `isSpawned(false)` before its clear) and removes both inserts again if either object is despawned or the second insert fails (the owner not aware of the object); the removals take both edges out together. The controller notifications (see, notSee, notKnow) follow after the lock in Java's order; a rolled back insert sends no see, only the removal notifications of `del` | Java race (D6): a pair added concurrently with a despawn, a range removal or another pair add could leave a one-sided edge that no later clear finds (a spawned object keeps a despawned one). The first C++ handshake (unconditional rollback without a lock, runtime-architecture.md §5.3 RR-17) still lost a concurrent `addPair`'s fresh edge; a rollback that removes only its own inserts loses the pair when the other `addPair` relied on the rolled back edge. Tests: `KnownListTest.RollbackNeverRemovesTheEdgesOfAConcurrentAddPair` (the review's interleaving forced with controller hooks; the handshake without the lock fails it), `ConcurrentPairAddsRemovalsAndDespawnsKeepTheRelationTwoWay` (3 s of pair adds from both sides, range removals and despawn/respawn), `AddPairHandshakeWithConcurrentDespawn` |
| `FlagKnownList.update`, players of other map instances | `knownObjects.values().removeIf(...)` removes the player from the flag's list only, without notifications | Both edges are removed (`KnownList::delPair`, a protected static added to `KnownList`), under the pair lock and also without notifications | Java leaves the flag in the player's list and breaks the two-way relation (RR-17); no notSee/notKnow like `removeIf`, so neither the flag's controller nor the player's client sees a difference |
| `WorldMap.setWorldOption` / `removeWorldOption` | Unsynchronized `worldOptions \|= id` / `&= ~id` | Compare-and-set loop on the atomic member | Java race (D6): a concurrent update of another bit is lost |
| `WalkerFormationsCache.getInstanceFormations`, `WorldWalkerFormations.getInstanceFormations` | `get`, then `put` of a new formation when absent | `computeIfAbsent` | Java race (D6): two threads can create two formations, and the walker groups registered in the replaced one are lost |
| `PlayerContainer.updateCachedPlayerName` | `playersByName.compute(oldName, ...)`, whose callback does `playersByName.put(newName, player)` | `put(newName, player)`, then `remove(oldName, player)`. The result is the same: the old name stays only while it maps to another player. | The `ConcurrentHashMap` contract forbids writing the map from its own compute callback. In the shim this can deadlock two stripe Monitors (design 4.4, lint L20). Tests: `PlayerContainerTest.UpdateCachedPlayerNameMovesTheNameEntry`, `ConcurrentRenamesFinishAndKeepOnlyTheCurrentNames` |
| `ZoneService.getZoneInstancesByWorldId`, the whole-map zone | `new WorldZoneTemplate(worldSize, mapId)` on every call | One immortal `WorldZoneTemplate` per map, created on first use and kept in the C++-only `worldZoneTemplates` map | `ZoneInfo` holds templates as `const ZoneTemplate*` that are never freed. Every Java copy has the same values. |
| `ZoneService.zoneByMapIdMap` | The holder's own map (`DataManager.ZONE_DATA.getZones()`), which `createMaterialZoneTemplate` extends | A copy made by the constructor: a new list per map that shares the holder's `ZoneInfo` elements | Published holders are const. Only `ZoneService` reads that map in Java. |
| `ZoneService.createMaterialZoneTemplate` | `new MaterialZoneTemplate(...)`, reachable through its `ZoneInfo` | The template is never freed | Templates are immortal `const` pointers. Java never removes the `ZoneInfo` either. |
| `WeatherService.getOrCreateWeatherEntry` for a code that the zone's table does not define | `new WeatherEntry(zoneId, weatherCode)`, garbage collected | A `new WeatherEntry` that is never freed | Weather entries are immortal `const WeatherEntry*` in the arrays and in packets. Only admin weather changes reach this path. |
| `WeatherService.setNextWeather` | Writes into the `WeatherEntry[]` it receives | Writes into `worldZoneWeathers.get(mapId)` (the same array) | The frozen parameter is a read-only span; every caller of the private method passes that array, so the behaviour is the same (header request world-2 rejected) |
| `ZoneService.init` / `addZoneHandlerClass`, `InstanceEngine.init` / `getNewInstanceHandler` | Scan the handler directories and read the `@ZoneNameAnnotation` / `@InstanceID` of each class | Iterate the compiled registry (`AION_ZONE_HANDLER` / `AION_INSTANCE_HANDLER` markers, HandlerRegistry.h). Log lines are unchanged. | No run-time class loading in C++ |
| `AbstractPeriodicTaskManager` constructor log line | `getClass().getSimpleName() + " initialized."` | Subclasses pass their simple class name to a C++-only two-argument constructor. `AbstractFIFOPeriodicTaskManager` keeps it for its error and warning lines. Porting rule: every subclass uses the two-argument constructor (the one-argument constructors log "AbstractPeriodicTaskManager" and "AbstractFIFOPeriodicTaskManager"); done for `ZoneUpdateService`, `MovementNotifyTask`, `MoveTaskManager`, `PlayerMoveTaskManager` and `BrokerService.BrokerPeriodicTaskManager`, still to do for `ExpireTimerTask`, `LegionDominionIntruderUpdateTask`, `TemporaryTradeTimeTask`, `TeamMoveUpdater` and `TeamStatUpdater` | A C++ base constructor cannot see the dynamic type. Test: `TaskManagerTest.FifoRunProcessesEachTaskOnceAndLogsFailuresWithTheSubclassName` |
| `AbstractPeriodicTaskManager` constructor scheduling | `scheduleAtFixedRate(this::run, Rnd.get(500, 550), period)` in the base constructor | The same, pinning `this`; the object reaches the scheduler before the subclass constructor has run | Accepted risk, kept like Java: the first run comes 500-550 ms later, and the subclass constructors only initialize members. A constructor stalled that long (debugger) would make the first run call the pure virtual `run()`; a start step after `create` would need every subclass, including other chunks, to call it |
| `AbstractFIFOPeriodicTaskManager<T>` members that need the complete task class | Java generics | `run`, `taskClassOf`, `describeTask` and the destructor are defined below the class template; `ZoneUpdateService.h` and `MovementNotifyTask.h` declare `extern template class AbstractFIFOPeriodicTaskManager<Creature>` with `Creature` forward-declared, and `AbstractPeriodicTaskManager.cpp` holds the explicit instantiation | hub-headers.md §3.1: a subclass header does not include another hub's full header. Other task classes (BrokerService) are instantiated implicitly where they are complete |
| `AbstractFIFOPeriodicTaskManager.run` error line for a task without `toString` | `"... processing " + task` (`Object.toString`, class@hash) | The task's simple class name | No identity hash in C++; log text only |
| `TemporarySpawnEngine` `static synchronized` methods | Lock `TemporarySpawnEngine.class` | Lock one file-local Monitor of the class | Same exclusion. C++ has no class object. |
| `WorldMap3DInstance.initMapRegions` / `WorldMap2DInstance.initMapRegions` | Called from the constructor | Called by `create()` right after construction, before the instance is visible (spine S0B-042/118). 3D regions are still created on the ForkJoin common pool under the regions Monitor. | Virtual calls and `Ref` retains are not allowed during C++ construction |
| `World` / `WorldMapInstance` throwables used only for stack traces (`new Throwable()`, `new RuntimeException()`) | Java throwables | `runtime::Exception` with the Java class name | Log text only |
| `ZoneName::createOrGet` outside a task | Static `ZoneName.createOrGet` from any thread | Opens a STARTUP `TaskScope` when the calling thread has none (XML binding of `ZoneTemplate` outside a task: tests, parser threads) | The `ConcurrentHashMap` shim of the interned names needs a scope for its read barrier; no behaviour difference |
| `SpawnEngine.spawnInstance` without spawn groups | `getSpawnsByWorldId` returns `Collections.emptyList()`, so `if (worldSpawns != null)` always enters and `WalkerFormator.organizeAndSpawn` runs | The block is entered unconditionally, `organizeAndSpawn` runs for maps without spawn groups too | Same behaviour (the first port skipped it for an empty list) |
| `SiegeLocation.getType` (P5-12a, ported for the M4 path: `ShieldService.attachShield`) | Returns the template's type, null if the XML has none | Throws `NullPointerException` for a template without a type | The C++ return type is not nullable; every `siege_location` of the data has a type |
| `DuplicateAionObjectException` present object | `map.get(id)`, which can be null after a concurrent removal | `Ptr<AionObject>` (nullable). The message prints `null` like Java. | Same behaviour |
| `World` constructor | `DataManager.WORLD_MAPS_DATA.forEachParalllel(...)` (a parallel stream over all maps) | The same maps and puts, in a `PER_ELEMENT` fork-join job (every map in its own task scope with a `QuiescentScope` and `QuiescentOptIn::WORLD_CREATION`); when the caller opened a `QuiescentScope` with the World creation opt-in and can fork (`main.cpp`), the 3D map (Reshanta, `WorldMapInstanceFactory`) is created first on the calling thread, where its region loop forks by itself | The Reclaimer can free the temporaries of one map (the `Point3D` of `RectangleArea.getDistance3D` in the 3D region/zone tests, the replaced neighbour arrays) while the others are created: the M4 backlog high-water mark fell from 11,984,161 objects (867 MB) to about 500,000 (36 MB), the RelWithDebInfo peak working set from 3,570 MB to 1,670 MB. The creation order of the maps is not observable (Java's parallel stream has none) |
| `WorldMap3DInstance.initMapRegions` in World creation | `regionIds.parallelStream().forEach(createMapRegion + put)` | Inside World creation (`QuiescentOptIn::active(WORLD_CREATION)`): a `PER_ELEMENT` job (each region in its own task scope; an element waits while the Reclaimer backlog exceeds half the watchdog's dump threshold, and stops waiting after a 2 s wait that did not help), or, on a fork-join helper, in order with a quiescent point per region; the neighbour loop places a quiescent point per row. Elsewhere unchanged | As above; the regions and neighbours are the same |
| `WorldMap` constructor | creates the instances in a loop | a quiescent point between instances inside World creation (the opt-in) | As above |

## game-server / game objects (P4-11a: `model/gameobjects` without `player`, `model/assemblednpc`, `model/flyring`, `model/road`)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `Creature.setSkillCoolDown` first call | `if (skillCoolDowns == null) skillCoolDowns = new ConcurrentHashMap<>()` unguarded: two first calls on different threads can each create a map, and the cooldown written into the replaced map is lost | The new map is published with a compare-and-set; a caller that loses the race writes into the winner's map | Java race (D6); test `CreatureBodiesTest.ConcurrentFirstCooldownsAreNotLost` |
| `Item.getItemStones`, `getFusionStones`, `getCurrentModifiers` first calls | Same unguarded lazy creation: concurrent first calls can return different collections and lose added stones or modifiers | Published with a compare-and-set, every caller gets the same collection | Java race (D6); test `GameObjectsModelTest.LazilyCreatedItemCollectionsArePublishedOnce` |
| `Item` DAO constructor persistent state | `null` until `InventoryDAO` sets `UPDATED` | `NOACTION`: every `setPersistentState` transition and the `NEW`/`CHANGED`/`DELETED` predicates treat it like `null`; only a `getPersistentState()` read before the DAO's store differs | `Field<PersistentState>` has no null |
| `Item.toString` mana stone sets | `ManaStone@<identity hash>` per element | `com.aionemu.gameserver.model.items.ManaStone@<low 32 bits of the address>` | No identity hash in C++; debug text only |
| `Creature.canUseSkillInMove` | Looks the casting skill's template up in `DataManager.SKILL_DATA` | Reads the casting skill's own template (the same object: every Skill is created from `SKILL_DATA`) | Written before `SkillData::getSkillTemplate` existed (header request shells-1); no behaviour difference while a skill is casting, and the objects tests need no published `SKILL_DATA` |
| `Npc.getRating`, `getRank`, `getGroupDrop` for a template without the attribute | Return `null` | Throw `NullPointerException` | The frozen `Npc` accessors return the enum (no null); every `npc_template` of the static data has `rating`, `rank` and `group_drop` |
| `GroupRecruitment.getRace` for an object that is neither a Player nor a team | Returns `null` | Throws `NullPointerException` | The frozen accessor returns `Race`; `FindGroupService` registers only players and teams |
| `Letter` and `BrokerItem` (DAO constructor) with a null `settleTime`/`timeStamp` | Keep `null` | Keep the epoch | The frozen members are non-optional `Timestamp`s; the `mail.recieved_time` and `broker.settle_time` columns are `NOT NULL` and the services always pass the current time |
| `Kisk` of an npc template without `kisk_stats` | `new KiskStatsTemplate()` per Kisk | One immortal default `KiskStatsTemplate` shared by these Kisks | Keeps `const KiskStatsTemplate*` a static data pointer; the template is immutable, so the values are the same |
| `StaticDoor.states` / `getStates()` | `EnumSet<StaticDoorState>`, returned live | `TreeSet<StaticDoorState>` shim (ordinal order like EnumSet); `getStates()` returns a `std::set` snapshot | No EnumSet shim; the only caller formats the states |
| `Homing`, `Servant`, `SummonedHouseNpc` master name | The constructors store `""`, which `SummonedObject.getMasterName` returns because it is not null | C++-only `getMasterName()` overrides return the stored name or `""` | `Npc.masterName` is a `Field<std::string>` in which `""` reads as null; without the overrides the creator's name would be shown. `Trap` has the same override (header request objects-1) |
| `Servant.getNpcObjectType` before `setNpcObjectType` | `null` | `NORMAL` | The member keeps the fieldmap spelling `Field<NpcObjectType>`; `VisibleObjectSpawner` sets the type right after creation |
| `AssembledNpc` parts | Stores the caller's list | Copies the parts into its `ArrayList` shim, in order | The frozen member is the shim; `BalaurAssaultService` builds the list for this object only |
| `Item.toString` item creator | Concatenates the raw field: `null` for items made by the simple constructors, `""` only if an empty creator was stored | `null` whenever the creator is empty | `Npc.masterName`-style `Field<std::string>`, in which `""` reads as null; debug text only |

## game-server / controllers (P4-11b: `controllers`, `controllers/movement`, `controllers/observer`)

| Area | Java | C++ | Reason |
|---|---|---|---|
| Calls into classes without a C++ header (FollowStartService of P5-05; StatFunctions and AttackUtil of P5-01; ChargeSkill of P5-02; PlayerGroupService, PlayerAllianceService, PlayerTeamDistributionService, TeamMoveUpdater, TeamStatUpdater of P5-10; the RiftEnum companion of P5-12b; PlayerRestrictions and PvpMapService of P5-13; GameServer.updateRatio of P5-14) | Direct static or singleton calls | The bodies call same-named stand-ins in `controllers/ControllerStandIns.h`, whose out-of-line bodies are unported stubs (they throw `UnportedException` like any unported callee); the call sites switch to the real API when the header lands. The P4-10 providers (MovementNotifyTask, ZoneUpdateService, MoveTaskManager, PlayerMoveTaskManager, VisibleObjectSpawner, WalkerFormator) and the P4-09 lookups (`WalkerData::getWalkerTemplate`, `SkillChargeData::getChargedSkillEntry`) are called directly since wave 3b-1 | Header-less providers; no `__has_include` guards after the freeze. Behaviour equals an unported callee |
| `instanceHandler instanceof PvpMapHandler` (NpcController.doReward) | Type check | `standins::isPvpMapHandler` compares the simple class name of the dynamic type with `PvpMapHandler` | PvpMapHandler (P5-13) has no header; exactly one Java class has that name, so the check is exact |
| `DropRegistrationService.registerDrop(npc, player, level, null)` (NpcController.doReward) | `null` group members | An empty vector | The frozen signature takes `const std::vector<Ptr<Player>>&`; Java passes null only for a solo looter, whose group list is never read before `initDropNpc` checks the team (to confirm when P5-09 ports the service) |
| `AttackShieldObserver.healType` | `final HealType`, `null` unless ConvertHealEffect | `const std::optional<HealType>` (fieldmap spelling `const HealType`, `// fieldmap:` waiver) | Java stores null (hub-headers.md §6); the CONVERT branch reads it with `value()`, which throws like Java's switch on null |
| `ObserveController.clearWithoutNotify` (C++ only) | – | Clears the observer list through the shim's own Monitor (no extra `SYNCHRONIZED`) and the attack calc observers, without `onRemoved` | LogoutBreakers L7/D2; the shim's `clear` already takes the lock Java's `synchronized (observers)` blocks take |
| Anonymous Runnables of `AbstractCollisionObserver.moved` and `AbstractQuestZoneObserver.moved` | `execute(new Runnable() {...})` capturing `this` | `execute(this, [this] {...})`, a lambda pinned to the observer; the `finally` resetting `isRunning` is a scope guard | Same capture and lifetime as the fieldmap callback structs (`const Ref<...>` of the observer) |
| `AbstractMaterialSkillActor.MaterialSkillTask` | Inner Runnable scheduled at a fixed rate | K4 RefCounted class defined in the .cpp holding `const Ref<AbstractMaterialSkillActor>`; scheduled with an empty `Pin` and a `Ref` init-capture | The actor -> Future -> task -> actor cycle is cut by `abort()` (cycles.toml java-hook); cancelled tasks release their captures |
| Java's implicit exceptions on a null dereference, an unboxing or a list access: `WeatherService.findWeatherEntry` null (material SUNNY condition), `QUEST_DATA.getQuestById(questId)` null in `PlayerController.onDialogSelect` (client-chosen quest id) and `updateRepeatableQuests`, a null current fly path in `PlayerController.onFlyTeleportEnd`, an unknown sub-effect skill in `AttackShieldObserver.isPunchShield`, a null `currentStep` in NpcMoveController, the nullable Integer/Float getters of `RVController.onAccept`/`HouseController.moveOutside`/`AbstractQuestZoneObserver.moved`, `attackResult.getFirst()` and `List.get` in CreatureController/NpcMoveController, the templates of GatherableController | NullPointerException, NoSuchElementException, IndexOutOfBoundsException from the JVM | The same runtime exception types thrown explicitly through `controllers/ControllerSupport.h` (`nonNull`, `unbox`, `listGetFirst`, `listGet`); the message names the expression instead of the JVM's helpful NPE text | A null template pointer dereference is undefined behaviour in C++ (a client packet could end the process), and `std::optional::value()`/`vector::at()` would throw `std::bad_optional_access`/`std::out_of_range`. Test `PlayerControllerTest.DialogSelectQuestAcceptWithAnUnknownQuestIdThrowsNullPointerException`, `ControllerSupportTest.*` |
| `NpcMoveController.setWalkerTemplate(null, i)` | NullPointerException after assigning the field | Assigns the field, then throws `NullPointerException` | Same state as Java before the exception |
| `RVController` constructors | Assign the non-final-but-effectively-final fields in the body (`isMaster`, `isAccepting`, `slave`, ... only when `slave != null`) | The frozen `const` members are initialized in the member initializer list with the same conditions | Frozen member kinds; identical values |
| `RVController$1/$2` (anonymous RequestResponseHandlers) | Inner classes reading the controller's private fields | Callback structs `RVController_RequestResponseHandler(_2)` declared as friends of RVController | C++ access control; fieldmap spelling `const Ref<RVController>` |
| `PetController.onDelete` | `super.onDelete()` first | Same order, so the C++-only delete breakers (`LogoutBreakers::onDelete`, the last statement of `VisibleObjectController::onDelete`) run before the pet's saves | Java order kept; the breakers touch only the target of a pet |
| `GatherableController` | Controller with a `GatheringTask` field | The constructor and destructor stay undefined; `completeInteraction`, `cancelGathering`, `getGatheringPlayerId` and the task-creating tail of `startGathering` (after the material roll) are unported stubs; every other body is ported | The member's type GatheringTask (P5-02) has no header, and its `Ref` cannot be stored, read or released without the complete type. `StatEnum.getModifier` is a local stand-in in `ControllerSupport.h` (StatEnum companion of P5-01) |

## game-server / player model (P4-12: `model/gameobjects/player`, `model/account`)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `AbyssRank` initial persistent state | `null` until `doUpdate` or the DAO sets it; `setPersistentState(UPDATE_REQUIRED)` stores it because the state is not `NEW` | `NOACTION` before the constructor's `doUpdate`; the setter treats it like `null` (not `NEW`), so a transferred character (`CMT_CHARACTER_INFORMATION`, last update 0) becomes `UPDATE_REQUIRED` as in Java. Only `getPersistentState()` before any store differs (`NOACTION` instead of `null`, which Java callers would dereference) | `Field<PersistentState>` has no null; the frozen member keeps its type |
| `MotionList` with a motion id missing from `Motion.motionType` | `activeMotions.put(null, motion)` / `remove(null)` use a null key | `std::out_of_range` from the constant table | No null key for `int32_t`; only invalid client or DAO ids reach it |
| `Player.getAttackType` for a weapon template without an attack type | Returns `null` | Throws `std::bad_optional_access` | The frozen `Creature::getAttackType` returns `ItemAttackType` (no null); item templates of weapons define the attack type |
| Null `Timestamp`s read by `PassportsList.getPassport`/`hasPassportForDay`, `PetList.loadPets` and `Mailbox.getNewSystemLetters` | `NullPointerException` | `std::bad_optional_access` | Nullable timestamps are `std::optional` (hub-headers.md §6); both only report corrupt rows |
| `LogoutBreakers` step failures | No Java counterpart | Each step runs even if an earlier one threw: an `UnportedException` (a breaker helper of a chunk that is not ported yet) is skipped silently, any other exception is logged with the step id and the object id | `noexcept` breakers must not stop at the first failing step (runtime-architecture.md §5.3) |
| `LogoutBreakers::breakZombieEdges` | No Java counterpart | Cuts and reports target, known objects, walker group, kisk, the account warehouse actor while the zombie is that actor, ride observers, summon, pet and the observer lists (reported once as `observers`); resets the inventory and warehouse actors, the stance observer and the IdianStone listeners without reporting them | The inventory and warehouse actors are the owning player itself; one emptiness query covers both observer lists (header requests player-1 to player-3, docs/porting/header-requests.md) |
| `PetList` test seam | `PlayerPetsDAO.getPlayerPets` only | `PetList::setPlayerPetsLoaderForTests` replaces the DAO read while a test sets it; server code never sets it | `create<Player>` in tests before the DAO body (P4-14) exists; no behaviour change for the server |
| `MotionList.setActive(0, type)` without active motions | Sends `SM_MOTION(0, type)` to the owner, then broadcasts `SM_MOTION(ownerId, null)`, whose `writeImpl` throws a `NullPointerException` on each recipient's write thread, so no recipient gets it | Sends `SM_MOTION(0, type)` to the owner and skips the broadcast | The observable result without the write-thread exception; test `PlayerCreationTest.ReplaceableParts` |
| `Account` player iteration (`getPlayerAccDataList`, `iterator`, range-for; `SM_CHARACTER_LIST`, `LoginServer` account updates) | `HashMap<Integer, PlayerAccountData>` bucket order (`(h ^ h >>> 16) & (capacity - 1)`, then insertion order) | Ascending object id (`PartMap` order) | The frozen `PartMap` member iterates in key order; deterministic like `GameServerTable` (login server). Emulating the bucket order is possible if the client turns out to care |

## game-server / item model (P4-13: `model/items`, `model/trade`, `model/broker`, `model/drop`, `model/enchants`)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `ItemStorage` iteration (`getItems`, `getItemsById`, `getFirstItemById`, `getSlotIdByItemId`; so `Storage.decreaseByItemId` and `getFirstItemByItemId`) | `ConcurrentHashMap<Integer, Item>` bin order: which of several stacks of an item id is found first or decreased first follows the object ids' hash bins and the table size | The runtime `ConcurrentHashMap` shim's stripe and bucket order | Hash iteration order is unspecified in Java too (CONVENTIONS.md); counts and results are the same, only the choice among equal stacks may differ |
| `ManaStone.getModifiers` | The template's modifier list itself, `null` for a stone template without modifiers | A list of the template's modifiers (`const StatFunction*`), empty without modifiers | The frozen member is an `ArrayList`; the item equipment listener (P5-01) tests the list for emptiness where Java tests for `null` |
| `EnchantEffect.getItemSlot` of an effect without `PHYSICAL_ATTACK`/`MAGICAL_ATTACK` stats | `null` | `MAIN_HAND` | The frozen member is a plain `ItemSlot`; `CreatureGameStats` reads it only for attack stats, only the `//stat` admin command prints it for other effects |
| `Drop.afterUnmarshal` validation | `IllegalArgumentException`, which fails the unmarshalling | `LoadContext::fail`: `StaticDataException` with the same message and the XML location; the public constructor still throws `IllegalArgumentException` | The binder contract for hook errors (static-data.md); same message |
| `DropGroup.tryAddDropItems` candidate order | `new HashSet<>(drops)` of identity-hashed `Drop`s: an arbitrary order that changes between runs | The drops in XML order | Deterministic; the nearest-chance rule and the random choice among ties (`Rnd.get`) are unchanged |
| `NpcEquippedGear` initialization | Lazily in the first `getItemsMask()`/`iterator()` call (synchronized) | Once after IDREF resolution while the static data loads (`init(LoadContext&)`), so an equipment item without an equipment slot fails the load instead of the first use | static-data.md §2.4 (the binder cannot resolve the item IDREFs earlier); the real npc templates have no such item (scan of the 160,491 equipment entries) |
| `NpcEquippedGear.init` publication | Publishes the empty `TreeMap` first, then fills it and ORs `mask`; `getItemsMask()`/`iterator()` test `items == null` outside the monitor, so a reader can get a partial mask or map. Runtime gear (`Npc.overrideEquipmentList`, e.g. CustomInstanceBossAI) is initialized lazily by `SM_NPC_INFO` on the sending threads. An exception in the loop leaves the partial map published | Builds the map and the mask locally, stores the mask, then publishes the map last; an exception publishes nothing (the next call throws again) | Obvious race (D6, review of wave 3a-2). Test `NpcEquippedGearTest.AReaderThatSeesTheItemsSeesTheWholeMask` (a reader at every store of `init`) |
| `GodStone.tryActivate` cooldown | `now < cooldownExpireTimeMillis.get()` then `set(now + cooldown)`: two concurrent hits of the weapon holder (a skill and an auto attack on different tasks) both pass the cooldown and both roll | A compare-and-set loop on the `AtomicLong`: one hit stores the new expiry and rolls, the other sees it and returns false | Obvious race (D6, review of wave 3a-2) defeating `GODSTONE_EVALUATION_COOLDOWN_MILLIS`. Test `ItemStonesTest.GodStoneCooldownLetsOnlyOneOfTwoConcurrentHitsRoll` (a second hit between the read and the store) |
| `BrokerPlayerCache.removeFromCache` | `stream().filter(...).toList()`: an unmodifiable list | A new modifiable `RcArrayList` | No shim for unmodifiable lists; no caller modifies the cached list |
| `IdianStone`'s anonymous `ActionObserver` | Captures its `IdianStone` (`this$0`) | A non-retaining `IdianStone* const` (fieldmap.toml `[captures]`, header request items-7), read through `stone()`, which terminates checked builds (C4) when the stone's Item was destroyed while the observer is still referenced | `onUnEquip` removes the observer before every replacement of the stone (PolishAction, polish charge 0) and `LogoutBreakers` cuts it at logout; the check catches an item destroyed without unequipping (a replaced stone cannot be detected: parts have no liveness cookie and a destructor may not reach the observer, C8) |

## game-server / DAOs (`dao`, P4-14)

| Area | Java | C++ | Reason |
|---|---|---|---|
| `getUsedIDs()` of PlayerDAO, InventoryDAO, PlayerRegisteredItemsDAO, LegionDAO, MailDAO, GuideDAO, HousesDAO, PlayerPetsDAO on an SQL error | Logs and returns `null`; `IDFactory.lockIds(int...)` then throws a `NullPointerException` and the server start fails | Logs the same message and throws `NullPointerException` (`dao/detail/UsedIds.h`) | The frozen return type is `std::vector<int32_t>`; an empty vector would let the server start without the used ids locked (duplicate object ids). Same outcome as Java: the start fails. Test `ServerDataDaoTest.UsedIdsOfAMissingTableThrowLikeTheJavaNull` |
| `PlayerDAO.getPlayerOidsOnAccount` (both overloads) when `DB.select` fails | Returns `null`, which both callers iterate (`NullPointerException`) | Throws `NullPointerException` after `DB::select` logged the error | Frozen `std::vector` return; the callers' outcome is unchanged |
| Record `hashCode` of `AbyssRankDAO.RankingListPlayer` (race, player class, gender), `RankingListLegion` (race) and `PlayerDAO.PlayerAndLegionInfo` (legion rank) | `Enum.hashCode()` is the identity hash, different in every JVM run | The enum component hashes by its ordinal; String components use Java's `String.hashCode` over UTF-16 code units, floats `Float.floatToIntBits`, longs `Long.hashCode`, combined with `31 * h + c` in Java int arithmetic (`dao/detail/JavaHash.h`) | The identity hash cannot be reproduced and no caller depends on its value; the ordinal is deterministic. `Bookmark` equals compares floats like `Float.compare` (all NaNs equal, `0.0f != -0.0f`). Tests `RecordHashTest.*` |
| Nullable VARCHAR/TEXT columns read into frozen `std::string` members: `players.note` (`PlayerCommonData.note`), the legion name of `RankingListPlayer`, `inventory.item_creator` and `broker.item_creator`, `houses.sign_notice`, `legion_members.selfintro`, `player_punishments.reason` | `null` | `""`; `PlayerDAO.storePlayer` writes `''` back for a note that was NULL, `HousesDAO.insertNewHouse`/`updateHouse` for a sign notice that was NULL | `ResultSet::getString` returns an empty string for SQL NULL and the model members are not optional (CONVENTIONS.md, Database). The client shows both the same way |
| `ServerVariablesDAO.store(String, Object)` | `value.toString()` of any object | `std::any` holding `int8_t`..`int64_t`, `uint32_t`, `uint64_t`, `bool`, `std::string`, `std::string_view` or `const char*` (Java's `toString` text); an empty `std::any` throws `NullPointerException`, other types `IllegalArgumentException` | The Java callers store `Integer` and `Long` values (GameTimeService, PeriodicSaveService) |
| `EventDAO.serialize(Set<Integer>)` | Joins the `HashSet` in its bucket order | Emulates the order of a `HashSet<Integer>` filled in ascending order (`model/templates/detail/JavaHashMap.h`) | `std::unordered_set` has no Java order; for sets without two keys in one bucket inserted in descending order the text equals Java's. The stored order has no meaning (the text is parsed back into a set) |
| `AbyssRankDAO` `log.error(null, e)` | Logback prints the null message as `null` | Logs the message `"null"` | No null `std::string_view` message |
| `LegionDAO.saveAnnouncement` error message | Appends `Announcement[message=..., time=<Timestamp.toString()>]` | Appends `Announcement[message=..., time=<epoch millis>]` | Log text only |
| `LegionDAO.loadAnnouncement` with a NULL `date` (the column is NOT NULL) | Keeps a null `Timestamp` in the `Legion.Announcement` record; the first reader throws `NullPointerException` | Throws `NullPointerException` while loading (outside the `catch (SQLException)`) | The frozen record member is a `Timestamp`, not an optional; reachable only with a drifted schema |
| `PlayerQuestListDAO.deleteQuest(Connection, int, Collection, Set<Integer> questIds)` (D6 fix) | Iterates the player's live deleted-id set and clears the whole set at its end: an id the player deletes between the batch and the clear is dropped without its DELETE, and the row comes back on the next login | The frozen parameter is a `const` copy (a snapshot taken before the connection is acquired); `store` removes exactly the snapshotted ids from the player's set right after `deleteQuest`, under Java's condition, so an id added meanwhile stays for the next store | Java race fixed (README D6): the snapshot widened Java's window to the connection acquisition, and removing only the deleted ids changes nothing without a concurrent delete. Test `PlayerDaoTest.QuestIdDeletedDuringTheStoreStaysForTheNextStore` |
| `InventoryDAO`/`PlayerRegisteredItemsDAO` released object ids | `IDFactory.releaseObjectIds(Collection<AionObject>)` | `IDFactory::releaseObjectIds(ids, "Item")` (`"HouseObject"`, `"HouseDecoration"`) | The C++ API takes the ids and a class name for the checked-build reuse warning |
| `PlayerBindPointDAO.updateBindPoint` binds the player id with `setFloat` | Kept | Kept (`setFloat(6, static_cast<float>(objectId))`) | Not a deviation; noted because object ids above 2^24 without an exact float value miss their row in both servers |
| Java `catch (SQLException e)` / `catch (Exception e)` | Checked exception types | `catch (const commons::database::SQLException&)` / `catch (const std::exception&)`; `DB::select`/`DB::insertUpdate` catch `std::exception` like Java's `DB` catches `Exception` | An `UnportedException` derives from `UnsupportedOperationException`, so a `catch (Exception)` body logs an unported body of another chunk the way Java logs a runtime exception |
| Unported until other chunks | – | `CustomInstancePlayerModelEntryDAO` (no `PlayerModelEntry` header, P5-13) | The header requests dao-1 to dao-4 of wave 3b-1 are applied (docs/porting/header-requests.md) |

## game-server / startup (`src/main.cpp`: the M4 startup path of P5-14, check mode and minidump helper mode)

| Area | Java | C++ | Reason |
|---|---|---|---|
| Handler engines at startup | `Stream.of(QuestEngine, AIEngine, InstanceEngine, ChatProcessor, ZoneService, GeoService).parallel().forEach(GameEngine::init)` | Only `ZoneService.init` and then `GeoService.init`, sequentially in the stream's source order | Milestone M4 excludes the handler engines (handlers-and-porting-plan.md §2.7: their registries are empty; `QuestEngine.init`, `AIEngine.init` and `ChatProcessor` are unported, P5-05/06/14). The two engines are independent in Java too (`ZoneService.init` only registers zone handler classes, `GeoService.init` creates material zones through the synchronized `createMaterialZoneTemplate`), so the order does not change the result |
| Startup after `World` | `GameTimeService`, `DropRegistrationService`, sieges, spawns, ... `initNioServer`, LS/CS connect | The startup ends after `World.getInstance()` with "M4 startup sequence complete", then the runtime and the database shut down in order and the process exits with 0 | The rest of `GameServer.main` is not ported yet (P5) |
| Task scopes of the startup steps | – (no epochs) | `DataManager`, `ZoneService.init`, `GeoService.init` and `World` each run in their own STARTUP `TaskScope` | The Reclaimer can free what one step unlinked before the next step starts (the geo load retires more than a million objects); no step keeps a borrow of another |
| Command line | `-D` properties are JVM system properties | `-Dkey=value` overrides layered over `mygs.properties` (unchanged from S0a); C++-only check options `--check-static-data`, `--check-id-factory`, `--check-output=<dir>`, `--check-geo-probes=<file>` | The M4 gate (`gs.m4.check_static_data`, `cmake/RunM4Check.cmake`) runs the normal startup path and writes report files for the independent oracles: the `StaticData` logger's "Loaded N ..." lines through an additional file sink, the geo statistics, the material zone names (a `GeoCallbacks` listener), 200 `getZ` probes, the zone names of every map instance, the unported trace and the load time and peak working set. Without these options the startup is unchanged |
| `DatabaseCleaningService.deletePlayersOnInactiveAccounts` | Called when `gameserver.cleaning.enable` | Called the same way, but the body is still `AION_UNPORTED` (P5-14) | The default configuration disables cleaning; an enabled cleaning stops the startup at the unported function |
| `--write-minidump <pid> <file>` | – | As the only arguments: writes a snapshot minidump of process `<pid>` and exits before logging or configuration (the watchdog's helper process) | Watchdog above |
| World step | `World.getInstance()` | Called inside a `QuiescentScope` and `QuiescentOptIn::WORLD_CREATION` of the step's task scope (the frame holds no borrow) | Lets World creation unpublish the step while its maps are created |
| Check-mode reports | – | `m4_summary.txt` adds the duration of each startup step, the Reclaimer backlog at each step end and its high-water mark (sampled by a watchdog probe); `startup_timeline.txt` has the log lines of GameServer, DataManager, ZoneService, GeoWorldLoader and World with millisecond timestamps; one INFO line per step "M4 check: startup step '...' took N ms" | M4 measurements (phase4-status.md); informational, only with `--check-static-data`/`--check-id-factory` |
