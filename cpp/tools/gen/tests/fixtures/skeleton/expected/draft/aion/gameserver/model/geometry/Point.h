// DRAFT generated by cpp/tools/gen/skeleton.py --draft from cpp/tools/gen/tests/fixtures/skeleton/java/com/aionemu/gameserver/model/geometry/Point.java.
// Hand-owned after review (handlers-and-porting-plan.md §2.5); regenerating overwrites hand edits.
#pragma once

#include <cstdint>

#include "aion/gameserver/model/geometry/fwd.h"

namespace aion::gameserver::model::geometry {

// Java implements Comparable<Point>
class Point {
private:
	// TODO(fieldmap): no fieldmap.json entry for Point; declare these Java fields with the member types printed by
	// python cpp/tools/gen/fieldmap.py --class com.aionemu.gameserver.model.geometry.Point   (runtime-architecture.md §3.2)
	//   Point.java:3  record component float x
	//   Point.java:3  record component float y
public:
	Point(float x, float y); // canonical record constructor
	int32_t compareTo(const Point& o) const;
	float x() const; // record accessor
	float y() const; // record accessor
};

} // namespace aion::gameserver::model::geometry
