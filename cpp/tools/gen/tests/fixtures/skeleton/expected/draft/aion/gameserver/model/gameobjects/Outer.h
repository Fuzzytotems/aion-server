// DRAFT generated by cpp/tools/gen/skeleton.py --draft from cpp/tools/gen/tests/fixtures/skeleton/java/com/aionemu/gameserver/model/gameobjects/Outer.java.
// Hand-owned after review (handlers-and-porting-plan.md §2.5); regenerating overwrites hand edits.
#pragma once

#include <cstdint>

#include "aion/gameserver/runtime/lifetime/Ref.h"
#include "aion/gameserver/model/gameobjects/fwd.h"

namespace aion::gameserver::model::gameobjects {

class Outer {
private:
	class Base;
	class Derived;
public:
	class Inner;
private:
	class Base {
	public:
		explicit Base(int32_t value);
	};
	class Derived : public Outer::Base {
	public:
		Derived();
	};
	// TODO(fieldmap): Java fields of Outer without a usable fieldmap.json member:
	//   Outer.java:5  private final Inner first = new Inner();  [fieldmap type const Ref<Outer::Inner> names Outer::Inner (Ref<> of a class without a RefCounted/OwnedPart base in fieldmap.json)]
public:
	runtime::Ptr<Outer::Inner> getFirst() const; // trivial accessor of first: inline once the member exists
};

class Outer::Inner : public Outer {
public:
	int32_t size();
};

} // namespace aion::gameserver::model::gameobjects
