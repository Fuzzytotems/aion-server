#pragma once

namespace aion::gameserver::configs::main {

/** Existing C++ definition: the forward header must use 'struct'. */
struct GSConfig {
	static inline int PLAYER_MAX_LEVEL = 65;
};

} // namespace aion::gameserver::configs::main
