#pragma once

namespace aion::gameserver::model {

/** Existing C++ enum without a fixed underlying type: the forward declaration must not add one. */
enum class Gender { MALE, FEMALE };

} // namespace aion::gameserver::model
