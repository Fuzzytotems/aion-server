"""M5a scenario oracles (cpp/docs/design/m5a-plan.md F-05): the NPC and gatherable spots a player sees after entering the world, a region move
target, and the database rows of a new character. Written from the Java sources (SpawnEngine, SpawnsData, SpawnGroup, TemporarySpawn, GameTime,
PositionUtil, PlayerService.newPlayer, SkillLearnService, SkillTreeData, ItemFactory, ItemGroup, ItemSlot, PlayerStatCalculator, PlayerClass) and
the static data; nothing here reads the C++ tree.
"""
